
import Foundation

import UIKit

enum SocPackageShare {

    private struct SocRechargeNews: Codable {
var can_Seed: Bool? = false
var provider_padding: Double? = 0



        let name: String
        let avatarImageName: String?
        let message: String
        let profileVideoFileName: String?
    }

    private enum YCleanerQ {
        static let followingPrefix = "profile.social.following"
    }

    static var visibleFans: [SocKariCleaner] {
       var m_titlek: [Any]! = [String(cString: [115,101,116,102,105,101,108,100,0], encoding: .utf8)!, String(cString: [114,101,115,111,108,118,101,100,0], encoding: .utf8)!]
    _ = m_titlek
   if m_titlek.count >= 3 {
       var shakeW: [String: Any]! = [String(cString: [119,111,114,107,101,114,0], encoding: .utf8)!:79, String(cString: [120,98,105,110,0], encoding: .utf8)!:43]
         shakeW = ["\(shakeW.keys.count)": shakeW.keys.count & shakeW.keys.count]
         shakeW["\(shakeW.values.count)"] = shakeW.values.count
      repeat {
         shakeW = ["\(shakeW.values.count)": 3]
         if shakeW.count == 2584674 {
            break
         }
      } while (shakeW.count == 2584674) && (1 <= shakeW.values.count)
      m_titlek.append(m_titlek.count)
   }

            return fans.filter { !SocWilliamsEmpty.isBlocked(name: $0.name) }
    }

    static var visibleFollowing: [SocKariCleaner] {
       var emptyP: String! = String(cString: [97,110,99,104,111,114,105,110,103,0], encoding: .utf8)!
   repeat {
      emptyP = "\(emptyP.count | 3)"
      if (String(cString:[100,119,99,104,113,114,102,49,107,111,0], encoding: .utf8)!) == emptyP {
         break
      }
   } while ((String(cString:[100,119,99,104,113,114,102,49,107,111,0], encoding: .utf8)!) == emptyP) && (emptyP != emptyP)

            return following.filter { !SocWilliamsEmpty.isBlocked(name: $0.name) }
    }

    static func isFollowing(name: String) -> Bool {
       var replyS: Int = 1
   while (4 == (replyS - replyS) || (replyS - 4) == 5) {
      replyS ^= 2
      break
   }

return         following.contains { $0.name == name }
    }

    static func isMutualFollow(name: String) -> Bool {
       var release_da: String! = String(cString: [121,117,118,109,112,101,103,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      release_da = "\(release_da.count)"
   }

return         visibleFans.contains { $0.name == name } &&
            visibleFollowing.contains { $0.name == name }
    }

    static func follow(_ user: SocKariCleaner) {
       var testv: Float = 0.0
      testv /= Swift.max(1, (Float(Int(testv > 316067327.0 || testv < -316067327.0 ? 44.0 : testv))))

        var key = following.filter { $0.name != user.name }
        key.insert(user, at: 0)
        saveFollowing(key)
    }

    static func unfollow(name: String) {
       var faceM: String! = String(cString: [114,101,113,117,105,114,101,115,0], encoding: .utf8)!
       var leadingW: Double = 1.0
      if 3.98 >= (leadingW / (Swift.max(leadingW, 7))) {
         leadingW -= (Double(Int(leadingW > 46986631.0 || leadingW < -46986631.0 ? 39.0 : leadingW) & 2))
      }
      if 2.75 <= leadingW {
         leadingW -= (Double(Int(leadingW > 389049196.0 || leadingW < -389049196.0 ? 45.0 : leadingW)))
      }
         leadingW *= (Double(Int(leadingW > 9374437.0 || leadingW < -9374437.0 ? 34.0 : leadingW) >> (Swift.min(labs(Int(leadingW > 61631184.0 || leadingW < -61631184.0 ? 70.0 : leadingW)), 2))))
      faceM.append("\((faceM.count * Int(leadingW > 353134600.0 || leadingW < -353134600.0 ? 71.0 : leadingW)))")

        saveFollowing(following.filter { $0.name != name })
    }

    private static var following: [SocKariCleaner] {
       var options8: [String: Any]! = [String(cString: [115,116,97,116,105,111,110,97,114,121,0], encoding: .utf8)!:50, String(cString: [105,109,112,111,114,116,97,110,116,0], encoding: .utf8)!:0]
   repeat {
       var plays: Bool = false
       _ = plays
       var productO: String! = String(cString: [115,105,110,116,105,0], encoding: .utf8)!
      repeat {
          var areac: Int = 5
          var contentn: String! = String(cString: [102,117,108,108,121,0], encoding: .utf8)!
         productO = "\(2 - productO.count)"
         areac |= 2 / (Swift.max(5, contentn.count))
         contentn = "\(productO.count ^ areac)"
         if (String(cString:[54,57,101,111,0], encoding: .utf8)!) == productO {
            break
         }
      } while ((String(cString:[54,57,101,111,0], encoding: .utf8)!) == productO) && (3 < productO.count && plays)
       var iconP: [Any]! = [String(cString: [109,112,101,103,118,108,99,0], encoding: .utf8)!, String(cString: [112,97,103,101,110,111,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &iconP) { pointer in
    
      }
       var healingQ: [Any]! = [31, 81]
         productO = "\(2)"
      while ((iconP.count | healingQ.count) > 1 && 1 > (iconP.count | healingQ.count)) {
          var x_centerR: Double = 5.0
          var cache7: [Any]! = [89, 68]
          var window_seU: Float = 5.0
          var dimd: Double = 2.0
          var input7: String! = String(cString: [97,105,116,101,114,0], encoding: .utf8)!
          _ = input7
         healingQ.append(input7.count)
         x_centerR /= Swift.max(3, (Double(Int(window_seU > 298529569.0 || window_seU < -298529569.0 ? 33.0 : window_seU))))
         cache7 = [1 | iconP.count]
         dimd += Double(1 / (Swift.max(7, productO.count)))
         break
      }
         productO.append("\(2 ^ iconP.count)")
      repeat {
         healingQ = [(productO.count * (plays ? 3 : 4))]
         if healingQ.count == 4369888 {
            break
         }
      } while (3 <= (iconP.count / (Swift.max(6, healingQ.count)))) && (healingQ.count == 4369888)
      options8["\(plays)"] = (1 | (plays ? 2 : 5))
      if 4034712 == options8.count {
         break
      }
   } while (4034712 == options8.count) && ((5 - options8.values.count) >= 5 && 2 >= (5 - options8.count))

        guard let data = UserDefaults.standard.data(forKey: followingStorageKey),
              let key = try? JSONDecoder().decode([SocRechargeNews].self, from: data) else {
            return []
        }

        return key.map {
            SocKariCleaner(
                name: $0.name,
                avatarColor: UIColor(red: 0.76, green: 0.87, blue: 1.00, alpha: 1),
                avatarImageName: $0.avatarImageName,
                message: $0.message,
                profileVideoFileName: $0.profileVideoFileName
            )
        }
    }

    private static func saveFollowing(_ users: [SocKariCleaner]) {
       var insetsL: String! = String(cString: [118,101,114,98,111,115,101,0], encoding: .utf8)!
      insetsL = "\(((String(cString:[98,0], encoding: .utf8)!) == insetsL ? insetsL.count : insetsL.count))"

        let male = users.map {
            SocRechargeNews(
                name: $0.name,
                avatarImageName: $0.avatarImageName,
                message: $0.message,
                profileVideoFileName: $0.profileVideoFileName
            )
        }

        guard let data = try? JSONEncoder().encode(male) else { return }

        UserDefaults.standard.set(data, forKey: followingStorageKey)
    }

    private static var followingStorageKey: String {
       var register_vzb: [String: Any]! = [String(cString: [103,101,110,101,114,105,99,0], encoding: .utf8)!:String(cString: [105,100,119,116,0], encoding: .utf8)!, String(cString: [102,111,114,119,97,114,100,105,110,103,0], encoding: .utf8)!:String(cString: [99,97,116,99,104,0], encoding: .utf8)!, String(cString: [102,105,108,101,119,114,105,116,101,115,116,114,101,97,109,0], encoding: .utf8)!:String(cString: [109,101,109,100,101,98,117,103,0], encoding: .utf8)!]
    _ = register_vzb
    var visibleT: Double = 3.0
   for _ in 0 ..< 3 {
      visibleT /= Swift.max((Double(Int(visibleT > 284375255.0 || visibleT < -284375255.0 ? 9.0 : visibleT) * register_vzb.count)), 5)
   }

        let local_xb = SocLoginCache.currentEmail ?? "guest"
   while ((3 >> (Swift.min(3, register_vzb.count))) < 1 || (3 >> (Swift.min(1, register_vzb.count))) < 2) {
      visibleT /= Swift.max((Double(Int(visibleT > 266618520.0 || visibleT < -266618520.0 ? 96.0 : visibleT))), 5)
      break
   }
        return "\(YCleanerQ.followingPrefix).\(local_xb)"
    }

    private static var fans: [SocKariCleaner] {
       var d_viewu: Int = 0
      d_viewu ^= d_viewu

        guard isTestAccount else { return [] }

        return [
            SocKariCleaner(
                name: "Simo",
                avatarColor: UIColor(red: 0.76, green: 0.87, blue: 1.00, alpha: 1),
                avatarImageName: "EmotionSync/PostAvatars/emotion_post_avatar_simo",
                message: "Love is not a matter of counting the days. It's making the days count.",
                profileVideoFileName: "profile_male_video_simo"
            ),
            SocKariCleaner(
                name: "Arlan",
                avatarColor: UIColor(red: 0.76, green: 0.87, blue: 1.00, alpha: 1),
                avatarImageName: "EmotionSync/PostAvatars/emotion_post_avatar_arlan",
                message: "Don't be discouraged; it's often the last key in the bunch that opens the lock.",
                profileVideoFileName: "profile_female_video_arlan"
            ),
            SocKariCleaner(
                name: "Williams",
                avatarColor: UIColor(red: 0.76, green: 0.87, blue: 1.00, alpha: 1),
                avatarImageName: "EmotionSync/PostAvatars/emotion_post_avatar_williams",
                message: "Life is like a box of chocolates, you never know what you're gonna get.",
                profileVideoFileName: "profile_male_video_williams"
            )
        ]
    }

    private static var isTestAccount: Bool {
       var facer: String! = String(cString: [102,105,108,116,101,114,105,110,103,0], encoding: .utf8)!
    _ = facer
       var presentR: [String: Any]! = [String(cString: [105,110,116,101,114,112,111,108,97,116,101,112,111,108,121,100,101,99,0], encoding: .utf8)!:31, String(cString: [105,109,112,108,101,109,101,110,116,97,116,105,111,110,115,0], encoding: .utf8)!:68, String(cString: [115,101,116,98,105,116,0], encoding: .utf8)!:12]
      withUnsafeMutablePointer(to: &presentR) { pointer in
             _ = pointer.pointee
      }
       var followy: String! = String(cString: [109,112,101,103,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &followy) { pointer in
    
      }
       var q_heightN: Float = 5.0
         presentR = ["\(presentR.values.count)": 2]
      if 4 >= (presentR.values.count + followy.count) && (4 + followy.count) >= 3 {
         followy.append("\((Int(q_heightN > 356184453.0 || q_heightN < -356184453.0 ? 50.0 : q_heightN)))")
      }
          var cache3: Double = 2.0
          _ = cache3
         followy.append("\(presentR.count)")
         cache3 *= (Double(Int(cache3 > 358555260.0 || cache3 < -358555260.0 ? 60.0 : cache3) >> (Swift.min(labs(Int(q_heightN > 214939158.0 || q_heightN < -214939158.0 ? 64.0 : q_heightN)), 1))))
      repeat {
          var sheett: String! = String(cString: [110,111,108,111,99,107,0], encoding: .utf8)!
          _ = sheett
          var settingY: String! = String(cString: [97,109,114,119,98,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &settingY) { pointer in
    
         }
          var iconW: String! = String(cString: [116,117,112,108,101,0], encoding: .utf8)!
          var summariesB: String! = String(cString: [102,105,110,100,97,115,111,99,0], encoding: .utf8)!
          var switch_e3T: String! = String(cString: [115,101,112,97,114,97,116,101,115,0], encoding: .utf8)!
         followy.append("\(summariesB.count / (Swift.max(7, iconW.count)))")
         sheett = "\(((String(cString:[114,0], encoding: .utf8)!) == settingY ? settingY.count : followy.count))"
         switch_e3T.append("\(summariesB.count)")
         if followy == (String(cString:[97,57,121,108,109,0], encoding: .utf8)!) {
            break
         }
      } while (followy == (String(cString:[97,57,121,108,109,0], encoding: .utf8)!)) && (followy.count <= 3)
          var min_rq: Float = 1.0
          var indexP: Float = 5.0
          var cellI: String! = String(cString: [116,114,97,110,115,102,111,114,109,97,116,105,111,110,0], encoding: .utf8)!
         followy.append("\(1 - followy.count)")
         min_rq /= Swift.max(Float(3 - followy.count), 5)
         indexP -= (Float(Int(min_rq > 189205205.0 || min_rq < -189205205.0 ? 54.0 : min_rq) & Int(indexP > 271227694.0 || indexP < -271227694.0 ? 63.0 : indexP)))
         cellI = "\((1 - Int(indexP > 151060705.0 || indexP < -151060705.0 ? 53.0 : indexP)))"
       var datau: String! = String(cString: [114,101,118,97,108,105,100,97,116,105,111,110,0], encoding: .utf8)!
       var avatar6: String! = String(cString: [116,97,107,100,115,112,0], encoding: .utf8)!
       _ = avatar6
         avatar6.append("\(avatar6.count << (Swift.min(labs(3), 1)))")
         avatar6 = "\(3 * followy.count)"
          var hasH: Float = 0.0
          _ = hasH
          var tapV: Bool = true
          var launchC: Int = 1
         presentR["\(followy)"] = 3 * presentR.keys.count
         hasH -= (Float(Int(hasH > 169688789.0 || hasH < -169688789.0 ? 21.0 : hasH) | 1))
         tapV = (datau.count & avatar6.count) >= 68
         launchC %= Swift.max((Int(q_heightN > 95683313.0 || q_heightN < -95683313.0 ? 51.0 : q_heightN) / (Swift.max(followy.count, 5))), 5)
      facer = "\((Int(q_heightN > 104969999.0 || q_heightN < -104969999.0 ? 36.0 : q_heightN) - 1))"

            return SocLoginCache.currentEmail?.trimmingCharacters(in: .whitespacesAndNewlines).lowercased() == "sockes333@gmail.com"
    }

}
