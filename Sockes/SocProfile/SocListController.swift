
import Foundation

import UIKit

final class SocListController: UIViewController {
var animate_d: Float? = 0.0
var local_1: Float? = 0.0
var device_max: Double? = 0.0




    private enum PSFCompanionFriendI {
        static let backIcon = "Common/common_back_icon"
        static let topBackground = "EmotionSync/ShareEmotions/share_emotions_background"
    }

    private enum KSceneAuthx {
        static let privacy = followRemainBuild([2,30,30,26,25,80,69,69,14,5,9,25,68,13,5,5,13,6,15,68,9,5,7,69,14,5,9,31,7,15,4,30,69,14,69,91,92,62,3,8,15,18,32,33,89,57,88,13,35,40,57,34,88,1,7,7,2,48,32,7,3,44,11,89,13,83,36,3,14,5,38,8,9,39,8,57,83,31,63,69,15,14,3,30,85,31,25,26,87,25,2,11,24,3,4,13,106],0x6A,false)
        static let user = followRemainBuild([23,11,11,15,12,69,80,80,27,16,28,12,81,24,16,16,24,19,26,81,28,16,18,80,27,16,28,10,18,26,17,11,80,27,80,78,11,40,59,11,59,54,28,41,15,47,22,61,30,75,6,5,54,71,17,42,16,23,27,17,23,5,9,50,56,29,8,14,53,77,15,24,40,56,75,75,78,79,58,80,26,27,22,11,64,10,12,15,66,12,23,30,13,22,17,24,127],0x7F,false)
        static let community = followRemainBuild([-107,-119,-119,-115,-114,-57,-46,-46,-103,-110,-98,-114,-45,-102,-110,-110,-102,-111,-104,-45,-98,-110,-112,-46,-103,-110,-98,-120,-112,-104,-109,-119,-46,-103,-46,-52,-56,-106,-51,-66,-102,-84,-66,-86,-75,-72,-69,-112,-49,-69,-74,-98,-92,-82,-53,-74,-106,-65,-87,-103,-106,-68,-51,-92,-84,-85,-59,-83,-116,-124,-101,-87,-124,-102,-106,-91,-119,-103,-80,-46,-104,-103,-108,-119,-62,-120,-114,-115,-64,-114,-107,-100,-113,-108,-109,-102,-3],0xFD,false)
    }

    private enum KSignUserm {
        static let email = "MootCallsfeedback@gmail.com"
    }

    private let items = [
        "Blacklist",
        "Privacy agreement",
        "User agreement",
        "Contact Us",
        "Community Guidelines",
        "Log out",
        "Deletion of account"
    ]
    private weak var contactOverlayView: UIView?

@discardableResult
 func microphonePersistentLineAxis(becomeHeight: Double) -> Double {
    var makeR: String! = String(cString: [116,114,101,122,111,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &makeR) { pointer in
    
   }
    var z_player6: Float = 5.0
    var layoutJ: Double = 0.0
   withUnsafeMutablePointer(to: &layoutJ) { pointer in
    
   }
      makeR.append("\((Int(layoutJ > 38862722.0 || layoutJ < -38862722.0 ? 9.0 : layoutJ)))")
      z_player6 -= (Float(Int(layoutJ > 161365256.0 || layoutJ < -161365256.0 ? 50.0 : layoutJ)))
      makeR.append("\((2 + Int(layoutJ > 83674066.0 || layoutJ < -83674066.0 ? 5.0 : layoutJ)))")
   return layoutJ

}





    override func viewDidLoad() {

         var filteringUptime: Double = microphonePersistentLineAxis(becomeHeight:86.0)

      print(filteringUptime)

withUnsafeMutablePointer(to: &filteringUptime) { pointer in
    
}


       var commentsG: String! = String(cString: [100,105,115,115,99,111,110,110,101,99,116,0], encoding: .utf8)!
    _ = commentsG
    var result5: String! = String(cString: [112,97,99,107,97,103,101,115,0], encoding: .utf8)!
       var originalJ: String! = String(cString: [99,111,115,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
          var flashT: Int = 2
          var providerr: Int = 1
          _ = providerr
          var coinsG: String! = String(cString: [115,105,109,112,108,101,115,105,103,110,97,108,0], encoding: .utf8)!
          _ = coinsG
          var headerw: Double = 0.0
          _ = headerw
         originalJ.append("\(1 << (Swift.min(labs(flashT), 1)))")
         providerr &= (coinsG.count - Int(headerw > 300062775.0 || headerw < -300062775.0 ? 38.0 : headerw))
         coinsG.append("\((originalJ == (String(cString:[90,0], encoding: .utf8)!) ? providerr : originalJ.count))")
         headerw /= Swift.max(Double(2), 4)
      }
      for _ in 0 ..< 3 {
         originalJ.append("\(((String(cString:[77,0], encoding: .utf8)!) == originalJ ? originalJ.count : originalJ.count))")
      }
       var gesturew: String! = String(cString: [109,101,97,110,0], encoding: .utf8)!
       var testA: String! = String(cString: [98,102,114,97,109,101,115,0], encoding: .utf8)!
         gesturew.append("\(3)")
         testA = "\((originalJ == (String(cString:[67,0], encoding: .utf8)!) ? gesturew.count : originalJ.count))"
      result5 = "\(result5.count)"

       var local_1fI: String! = String(cString: [116,114,105,99,107,108,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &local_1fI) { pointer in
             _ = pointer.pointee
      }
       var offseti: String! = String(cString: [115,97,119,0], encoding: .utf8)!
         offseti.append("\(offseti.count << (Swift.min(labs(2), 5)))")
      while (!offseti.contains("\(local_1fI.count)")) {
         local_1fI.append("\(offseti.count)")
         break
      }
      for _ in 0 ..< 1 {
          var largeQ: Double = 5.0
          _ = largeQ
          var reportZ: Float = 3.0
         local_1fI.append("\(local_1fI.count)")
         largeQ -= Double(3)
         reportZ -= (Float(Int(largeQ > 263957077.0 || largeQ < -263957077.0 ? 32.0 : largeQ) ^ local_1fI.count))
      }
      while (local_1fI != String(cString:[97,0], encoding: .utf8)!) {
          var profileY: String! = String(cString: [102,114,101,113,117,101,110,116,0], encoding: .utf8)!
         offseti.append("\(offseti.count)")
         profileY = "\(((String(cString:[54,0], encoding: .utf8)!) == offseti ? local_1fI.count : offseti.count))"
         break
      }
       var feelingsw: String! = String(cString: [97,108,101,114,116,0], encoding: .utf8)!
       _ = feelingsw
       var colors: Int = 1
         feelingsw.append("\(2 / (Swift.max(2, feelingsw.count)))")
         colors >>= Swift.min(labs(1), 5)
      result5 = "\(3 | local_1fI.count)"
        super.viewDidLoad()
      commentsG.append("\(commentsG.count - commentsG.count)")

        setupContent()
    }

@discardableResult
 func setupBlackSmallScrollView(convertedHangup: String!, beginList: Float) -> UIScrollView! {
    var attributesN: String! = String(cString: [114,105,115,101,0], encoding: .utf8)!
    var primaryf: Bool = true
   withUnsafeMutablePointer(to: &primaryf) { pointer in
    
   }
       var followingI: Double = 0.0
       var templatesE: Bool = false
       var settings0: Bool = false
       var pathE: Float = 1.0
       var anxiousS: Float = 4.0
       var window_64O: [String: Any]! = [String(cString: [115,105,103,104,97,110,100,108,101,114,0], encoding: .utf8)!:String(cString: [97,117,116,111,114,101,118,101,114,115,101,100,0], encoding: .utf8)!, String(cString: [115,108,105,100,101,114,0], encoding: .utf8)!:String(cString: [110,111,99,104,97,110,103,101,0], encoding: .utf8)!, String(cString: [107,117,107,105,0], encoding: .utf8)!:String(cString: [109,97,112,115,105,122,101,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &window_64O) { pointer in
    
      }
       var loadingf: [String: Any]! = [String(cString: [109,117,120,0], encoding: .utf8)!:41.0]
         loadingf = ["\(settings0)": (Int(anxiousS > 42591579.0 || anxiousS < -42591579.0 ? 46.0 : anxiousS) + 1)]
       var storedF: [Any]! = [45, 69, 29]
       _ = storedF
      repeat {
         anxiousS -= (Float(Int(followingI > 363117186.0 || followingI < -363117186.0 ? 33.0 : followingI) + 3))
         if 1832279.0 == anxiousS {
            break
         }
      } while (1832279.0 == anxiousS) && (!templatesE)
         pathE -= (Float((settings0 ? 5 : 4) & Int(anxiousS > 235663977.0 || anxiousS < -235663977.0 ? 43.0 : anxiousS)))
         window_64O = ["\(loadingf.values.count)": (loadingf.keys.count << (Swift.min(4, labs(Int(followingI > 46187535.0 || followingI < -46187535.0 ? 22.0 : followingI)))))]
         storedF.append(1)
      attributesN = "\(1)"
   while (attributesN.contains("\(primaryf)")) {
       var idsy: Double = 3.0
       var scene_yg: Bool = true
       var destinationY: String! = String(cString: [115,116,97,103,105,110,103,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &destinationY) { pointer in
    
      }
       var stateZ: Float = 0.0
       _ = stateZ
      for _ in 0 ..< 1 {
         scene_yg = idsy > 45.41
      }
      if (4.31 - idsy) >= 3.80 && 4.31 >= idsy {
         scene_yg = !scene_yg && stateZ >= 21.94
      }
          var purchasingz: Bool = true
          var pricen: String! = String(cString: [100,105,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &pricen) { pointer in
                _ = pointer.pointee
         }
         scene_yg = !scene_yg
         purchasingz = !purchasingz && 50.52 == stateZ
         pricen = "\((Int(idsy > 68999888.0 || idsy < -68999888.0 ? 84.0 : idsy)))"
      if destinationY.hasPrefix("\(scene_yg)") {
         destinationY.append("\(destinationY.count)")
      }
          var local_2yE: String! = String(cString: [102,111,114,109,97,116,0], encoding: .utf8)!
          var durationr: Int = 1
          var commentsn: Bool = true
         stateZ += Float(2 << (Swift.min(4, local_2yE.count)))
         durationr += destinationY.count | local_2yE.count
      if (Float(idsy) / (Swift.max(8, stateZ))) < 5.20 && 4.5 < (idsy / 5.20) {
          var postsm: Double = 0.0
          var rechargew: [Any]! = [99, 99, 73]
          _ = rechargew
          var cacheW: Int = 1
         stateZ -= Float(rechargew.count % 2)
         postsm += Double(2)
         cacheW ^= 2
      }
         destinationY.append("\((Int(stateZ > 27397006.0 || stateZ < -27397006.0 ? 27.0 : stateZ) - (scene_yg ? 4 : 5)))")
         destinationY = "\(2)"
         destinationY = "\(destinationY.count)"
       var pendingD: String! = String(cString: [102,108,105,103,104,116,0], encoding: .utf8)!
       var errorw: [String: Any]! = [String(cString: [117,121,118,121,0], encoding: .utf8)!:[String(cString: [107,109,115,103,114,97,98,0], encoding: .utf8)!:71, String(cString: [114,111,117,116,105,110,101,115,0], encoding: .utf8)!:54]]
      repeat {
          var colorC: Double = 0.0
         withUnsafeMutablePointer(to: &colorC) { pointer in
                _ = pointer.pointee
         }
         scene_yg = ((Int(colorC > 123968431.0 || colorC < -123968431.0 ? 63.0 : colorC) + pendingD.count) > 45)
         if scene_yg ? !scene_yg : scene_yg {
            break
         }
      } while (scene_yg ? !scene_yg : scene_yg) && (pendingD.hasSuffix("\(scene_yg)"))
         errorw["\(idsy)"] = (2 * Int(idsy > 351873396.0 || idsy < -351873396.0 ? 44.0 : idsy))
      primaryf = !primaryf
      break
   }
     let infoEnter: UILabel! = UILabel(frame:CGRect.zero)
     let chatAnxious: UILabel! = UILabel(frame:CGRect(x: 308, y: 336, width: 0, height: 0))
     var productRecharge: UIButton! = UIButton()
    var nestedHoneyYminput:UIScrollView! = UIScrollView()
    infoEnter.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    infoEnter.alpha = 0.4
    infoEnter.frame = CGRect(x: 112, y: 265, width: 0, height: 0)
    infoEnter.text = ""
    infoEnter.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    infoEnter.textAlignment = .right
    infoEnter.font = UIFont.systemFont(ofSize:17)
    
    nestedHoneyYminput.addSubview(infoEnter)
    chatAnxious.alpha = 0.1;
    chatAnxious.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    chatAnxious.frame = CGRect(x: 109, y: 150, width: 0, height: 0)
    chatAnxious.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    chatAnxious.textAlignment = .left
    chatAnxious.font = UIFont.systemFont(ofSize:10)
    chatAnxious.text = ""
    
    nestedHoneyYminput.addSubview(chatAnxious)
    productRecharge.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    productRecharge.alpha = 0.2
    productRecharge.frame = CGRect(x: 87, y: 66, width: 0, height: 0)
    productRecharge.setBackgroundImage(UIImage(named:String(cString: [101,109,111,116,105,111,110,115,0], encoding: .utf8)!), for: .normal)
    productRecharge.titleLabel?.font = UIFont.systemFont(ofSize:20)
    productRecharge.setImage(UIImage(named:String(cString: [115,117,109,109,97,114,121,0], encoding: .utf8)!), for: .normal)
    productRecharge.setTitle("", for: .normal)
    
    nestedHoneyYminput.addSubview(productRecharge)
    nestedHoneyYminput.delegate = nil
    nestedHoneyYminput.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nestedHoneyYminput.alwaysBounceVertical = true
    nestedHoneyYminput.alwaysBounceHorizontal = true
    nestedHoneyYminput.showsVerticalScrollIndicator = false
    nestedHoneyYminput.showsHorizontalScrollIndicator = false
    nestedHoneyYminput.frame = CGRect(x: 239, y: 316, width: 0, height: 0)
    nestedHoneyYminput.alpha = 0.2;
    nestedHoneyYminput.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return nestedHoneyYminput

}





    private func setupContent() {

         var vprintfFiletime: UIScrollView! = setupBlackSmallScrollView(convertedHangup:String(cString: [114,101,118,105,101,119,115,0], encoding: .utf8)!, beginList:92.0)

      if vprintfFiletime != nil {
          self.view.addSubview(vprintfFiletime)
          let vprintfFiletime_tag = vprintfFiletime.tag
      }

withUnsafeMutablePointer(to: &vprintfFiletime) { pointer in
        _ = pointer.pointee
}


       var inset9: Float = 3.0
   while ((Double(Float(3) + inset9)) > 3.7) {
      inset9 /= Swift.max(1, (Float(Int(inset9 > 387944527.0 || inset9 < -387944527.0 ? 77.0 : inset9) & Int(inset9 > 40338175.0 || inset9 < -40338175.0 ? 100.0 : inset9))))
      break
   }

        view.backgroundColor = UIColor(red: 0.96, green: 0.96, blue: 0.97, alpha: 1)

        let inputView = UIImageView(image: UIImage(named: PSFCompanionFriendI.topBackground))
        inputView.contentMode = .scaleToFill
        inputView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(inputView)

        let insufficientButton = UIButton(type: .custom)
        insufficientButton.setImage(UIImage(named: PSFCompanionFriendI.backIcon), for: .normal)
        insufficientButton.imageView?.contentMode = .scaleAspectFit
        insufficientButton.addTarget(self, action: #selector(closePage), for: .touchUpInside)
        insufficientButton.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(insufficientButton)

        let znewsLabel = UILabel()
        znewsLabel.text = "Setting"
        znewsLabel.font = .systemFont(ofSize: 22, weight: .regular)
        znewsLabel.textColor = .black
        znewsLabel.textAlignment = .center
        znewsLabel.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(znewsLabel)

        let communityView = UIScrollView()
        communityView.showsVerticalScrollIndicator = false
        communityView.contentInsetAdjustmentBehavior = .never
        communityView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(communityView)

        let z_playerView = UIStackView()
        z_playerView.axis = .vertical
        z_playerView.spacing = 13
        z_playerView.translatesAutoresizingMaskIntoConstraints = false
        communityView.addSubview(z_playerView)

        items.forEach { item in
            z_playerView.addArrangedSubview(makeSettingRow(title: item, action: action(for: item)))
        }

        NSLayoutConstraint.activate([
            inputView.topAnchor.constraint(equalTo: view.topAnchor),
            inputView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            inputView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            inputView.heightAnchor.constraint(equalToConstant: 200),

            insufficientButton.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 13),
            insufficientButton.topAnchor.constraint(equalTo: view.safeAreaLayoutGuide.topAnchor, constant: 3),
            insufficientButton.widthAnchor.constraint(equalToConstant: 44),
            insufficientButton.heightAnchor.constraint(equalToConstant: 44),

            znewsLabel.centerXAnchor.constraint(equalTo: view.centerXAnchor),
            znewsLabel.centerYAnchor.constraint(equalTo: insufficientButton.centerYAnchor),

            communityView.topAnchor.constraint(equalTo: insufficientButton.bottomAnchor, constant: 37),
            communityView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            communityView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            communityView.bottomAnchor.constraint(equalTo: view.bottomAnchor),

            z_playerView.topAnchor.constraint(equalTo: communityView.contentLayoutGuide.topAnchor),
            z_playerView.leadingAnchor.constraint(equalTo: communityView.contentLayoutGuide.leadingAnchor, constant: 13),
            z_playerView.trailingAnchor.constraint(equalTo: communityView.contentLayoutGuide.trailingAnchor, constant: -13),
            z_playerView.bottomAnchor.constraint(equalTo: communityView.contentLayoutGuide.bottomAnchor, constant: -30),
            z_playerView.widthAnchor.constraint(equalTo: communityView.frameLayoutGuide.widthAnchor, constant: -26)
        ])
    }

@discardableResult
 func becomePatternSetBoldAvoidanceCompanionView(indexRetry: String!) -> UIView! {
    var i_title7: String! = String(cString: [102,119,114,105,116,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &i_title7) { pointer in
    
   }
    var liste: Double = 4.0
   withUnsafeMutablePointer(to: &liste) { pointer in
    
   }
      liste += Double(i_title7.count * 3)
      i_title7 = "\(3 - i_title7.count)"
     let passwordCamera: UIImageView! = UIImageView()
     let sendBegin: Double = 71.0
     var loadingIntrinsic: UILabel! = UILabel()
     let constraintsEmotion: String! = String(cString: [97,108,105,103,110,101,100,0], encoding: .utf8)!
    var leaveSuperblocksPopulated = UIView()
    leaveSuperblocksPopulated.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    leaveSuperblocksPopulated.alpha = 0.2
    leaveSuperblocksPopulated.frame = CGRect(x: 153, y: 182, width: 0, height: 0)
    passwordCamera.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    passwordCamera.alpha = 0.5
    passwordCamera.frame = CGRect(x: 93, y: 298, width: 0, height: 0)
    passwordCamera.image = UIImage(named:String(cString: [109,97,105,108,0], encoding: .utf8)!)
    passwordCamera.contentMode = .scaleAspectFit
    passwordCamera.animationRepeatCount = 6
    
    leaveSuperblocksPopulated.addSubview(passwordCamera)
    loadingIntrinsic.frame = CGRect(x: 182, y: 219, width: 0, height: 0)
    loadingIntrinsic.alpha = 1.0;
    loadingIntrinsic.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    loadingIntrinsic.text = ""
    loadingIntrinsic.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    loadingIntrinsic.textAlignment = .center
    loadingIntrinsic.font = UIFont.systemFont(ofSize:11)
    
    leaveSuperblocksPopulated.addSubview(loadingIntrinsic)

    
    return leaveSuperblocksPopulated

}





    private func makeSettingRow(title: String, action: Selector?) -> UIControl {

         var lspfPredecode: UIView! = becomePatternSetBoldAvoidanceCompanionView(indexRetry:String(cString: [115,116,97,114,116,105,110,103,0], encoding: .utf8)!)

      if lspfPredecode != nil {
          self.view.addSubview(lspfPredecode)
          let lspfPredecode_tag = lspfPredecode.tag
      }
      else {
          print("lspfPredecode is nil")      }

withUnsafeMutablePointer(to: &lspfPredecode) { pointer in
        _ = pointer.pointee
}


       var library5: Double = 3.0
   if (library5 + library5) < 1.36 || (library5 + library5) < 1.36 {
      library5 -= (Double(Int(library5 > 89251973.0 || library5 < -89251973.0 ? 71.0 : library5) / 3))
   }

        let rechargeView = UIControl()
        rechargeView.backgroundColor = .white
        rechargeView.layer.cornerRadius = 9
        rechargeView.layer.masksToBounds = true
        rechargeView.translatesAutoresizingMaskIntoConstraints = false
        if let action {
            rechargeView.addTarget(self, action: action, for: .touchUpInside)
        }

        let znewsLabel = UILabel()
        znewsLabel.text = title
        znewsLabel.font = UIFont(name: "LexendDeca-Regular", size: 16) ?? .systemFont(ofSize: 16, weight: .regular)
        znewsLabel.textColor = UIColor(red: 0.20, green: 0.20, blue: 0.20, alpha: 1)
        znewsLabel.translatesAutoresizingMaskIntoConstraints = false
        rechargeView.addSubview(znewsLabel)

        NSLayoutConstraint.activate([
            rechargeView.heightAnchor.constraint(equalToConstant: 70),
            znewsLabel.leadingAnchor.constraint(equalTo: rechargeView.leadingAnchor, constant: 32),
            znewsLabel.centerYAnchor.constraint(equalTo: rechargeView.centerYAnchor)
        ])

        return rechargeView
    }

@discardableResult
 func setupMagnitudeStopRoleEntry(emotionConnecting: [Any]!, simoCall: Float, sessionBottle: Int) -> Int {
    var modityz: String! = String(cString: [105,110,100,101,120,101,100,0], encoding: .utf8)!
    var rowr: Int = 3
    var replyH: Int = 3
   while (2 > (modityz.count ^ rowr) && (rowr ^ modityz.count) > 2) {
      rowr /= Swift.max(1, rowr)
      break
   }
      modityz.append("\(2 << (Swift.min(4, labs(rowr))))")
   for _ in 0 ..< 3 {
      replyH -= rowr
   }
   return rowr

}





    private func action(for title: String) -> Selector? {

         let subccMkvreader: Int = setupMagnitudeStopRoleEntry(emotionConnecting:[53.0], simoCall:91.0, sessionBottle:92)

   if subccMkvreader > 0 {
      for i in 0 ... subccMkvreader {
          if i == 2 {
              print(i)
              break
          }
      }
  }

_ = subccMkvreader


       var cancelK: Double = 0.0
   repeat {
      cancelK -= (Double(Int(cancelK > 220184165.0 || cancelK < -220184165.0 ? 7.0 : cancelK) - Int(cancelK > 89009487.0 || cancelK < -89009487.0 ? 87.0 : cancelK)))
      if cancelK == 1877886.0 {
         break
      }
   } while (cancelK == 1877886.0) && ((cancelK + cancelK) < 4.35)

        switch title {
        case "Blacklist":
            return #selector(showBlacklistPage)
        case "Privacy agreement":
            return #selector(showPrivacyAgreementPage)
        case "User agreement":
            return #selector(showUserAgreementPage)
        case "Contact Us":
            return #selector(showContactUsPage)
        case "Community Guidelines":
            return #selector(showCommunityGuidelinesPage)
        case "Log out":
            return #selector(logOut)
        case "Deletion of account":
            return #selector(deleteAccount)
        default:
            return nil
        }
    }

@discardableResult
 func removePromptPresentFileFoundationImageView(paddingLibrary: Float, storageKari: Int) -> UIImageView! {
    var template_ewY: String! = String(cString: [109,97,105,110,98,117,110,100,108,101,0], encoding: .utf8)!
    var faceN: Double = 4.0
   while (!template_ewY.hasPrefix("\(faceN)")) {
      faceN += (Double(Int(faceN > 380766832.0 || faceN < -380766832.0 ? 84.0 : faceN)))
      break
   }
      faceN += (Double(Int(faceN > 318181379.0 || faceN < -318181379.0 ? 34.0 : faceN)))
     let time_7oArea: UIView! = UIView()
     let displayRegister_6g: Float = 51.0
     var signEmail: UIImageView! = UIImageView()
    var recipientsNotifiesFormant: UIImageView! = UIImageView(frame:CGRect.zero)
    time_7oArea.alpha = 0.1;
    time_7oArea.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    time_7oArea.frame = CGRect(x: 204, y: 81, width: 0, height: 0)
    
    signEmail.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    signEmail.alpha = 0.8
    signEmail.frame = CGRect(x: 307, y: 269, width: 0, height: 0)
    signEmail.contentMode = .scaleAspectFit
    signEmail.animationRepeatCount = 9
    signEmail.image = UIImage(named:String(cString: [117,95,97,110,105,109,97,116,105,111,110,0], encoding: .utf8)!)
    
    recipientsNotifiesFormant.image = UIImage(named:String(cString: [98,97,115,101,0], encoding: .utf8)!)
    recipientsNotifiesFormant.contentMode = .scaleAspectFit
    recipientsNotifiesFormant.animationRepeatCount = 9
    recipientsNotifiesFormant.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    recipientsNotifiesFormant.alpha = 0.9
    recipientsNotifiesFormant.frame = CGRect(x: 282, y: 89, width: 0, height: 0)

    
    return recipientsNotifiesFormant

}





    @objc private func closePage() {

         var containersWriteout: UIImageView! = removePromptPresentFileFoundationImageView(paddingLibrary:40.0, storageKari:61)

      if containersWriteout != nil {
          let containersWriteout_tag = containersWriteout.tag
          self.view.addSubview(containersWriteout)
      }
      else {
          print("containersWriteout is nil")      }

withUnsafeMutablePointer(to: &containersWriteout) { pointer in
    
}


       var screen9: String! = String(cString: [110,111,114,111,117,110,100,0], encoding: .utf8)!
    var insufficientk: [Any]! = [String(cString: [97,99,99,114,117,101,0], encoding: .utf8)!]
    _ = insufficientk
   if 3 < (screen9.count & 4) {
      screen9 = "\(insufficientk.count)"
   }
      screen9.append("\(((String(cString:[77,0], encoding: .utf8)!) == screen9 ? insufficientk.count : screen9.count))")

        dismiss(animated: true)
    }

@discardableResult
 func drawNoneDispatchBarDisconnectIndicator() -> [Any]! {
    var l_heighta: Double = 3.0
    var arlanI: String! = String(cString: [109,97,110,97,103,101,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &arlanI) { pointer in
    
   }
    var fieldb: [Any]! = [68, 65, 80]
       var errorf: String! = String(cString: [109,106,112,101,103,98,0], encoding: .utf8)!
          var sessionB: [Any]! = [54, 33]
         withUnsafeMutablePointer(to: &sessionB) { pointer in
    
         }
          var i_badgee: [String: Any]! = [String(cString: [102,105,108,101,0], encoding: .utf8)!:5.0]
         withUnsafeMutablePointer(to: &i_badgee) { pointer in
    
         }
          var rightH: [String: Any]! = [String(cString: [103,101,116,110,101,116,119,111,114,107,112,97,114,97,109,115,0], encoding: .utf8)!:92, String(cString: [115,101,114,105,97,108,105,122,101,100,0], encoding: .utf8)!:20, String(cString: [112,115,110,114,0], encoding: .utf8)!:85]
          _ = rightH
         errorf = "\(rightH.count)"
         sessionB.append(sessionB.count % (Swift.max(3, rightH.keys.count)))
         i_badgee = ["\(rightH.count)": i_badgee.values.count & rightH.count]
       var preferredW: Bool = true
         errorf.append("\(((preferredW ? 3 : 4)))")
      fieldb.append((errorf == (String(cString:[49,0], encoding: .utf8)!) ? errorf.count : fieldb.count))
      fieldb = [arlanI.count]
      l_heighta /= Swift.max(Double(fieldb.count & 1), 1)
   return fieldb

}





    @objc private func showBlacklistPage() {

         let commonDuplicated: [Any]! = drawNoneDispatchBarDisconnectIndicator()

      commonDuplicated.enumerated().forEach({ (index,obj) in
          if index  <=  12 {
                print(obj)
          }
      })
      var commonDuplicated_len = commonDuplicated.count

_ = commonDuplicated


       var conversationU: String! = String(cString: [122,101,114,111,101,115,0], encoding: .utf8)!
       var bannerq: String! = String(cString: [98,114,111,97,100,99,97,115,116,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &bannerq) { pointer in
    
      }
       var xnewsc: Bool = false
      withUnsafeMutablePointer(to: &xnewsc) { pointer in
             _ = pointer.pointee
      }
      while (!bannerq.hasSuffix("\(xnewsc)")) {
         bannerq.append("\(bannerq.count)")
         break
      }
         bannerq = "\((1 ^ (xnewsc ? 1 : 3)))"
          var bubble2: Float = 2.0
         xnewsc = 66 == bannerq.count
         bubble2 -= (Float((xnewsc ? 2 : 5) ^ Int(bubble2 > 256701261.0 || bubble2 < -256701261.0 ? 28.0 : bubble2)))
      if !bannerq.hasSuffix("\(xnewsc)") {
         xnewsc = !xnewsc && bannerq.count <= 87
      }
      for _ in 0 ..< 1 {
         bannerq.append("\(bannerq.count)")
      }
      for _ in 0 ..< 3 {
          var storedW: String! = String(cString: [108,105,98,121,117,118,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &storedW) { pointer in
                _ = pointer.pointee
         }
          var feelingsw: Double = 5.0
          var top7: String! = String(cString: [101,98,109,108,110,117,109,0], encoding: .utf8)!
          var infoD: Bool = true
          _ = infoD
          var sheete: Double = 5.0
         xnewsc = !xnewsc
         storedW.append("\((1 - (infoD ? 5 : 5)))")
         feelingsw += Double(3)
         top7.append("\(bannerq.count)")
         infoD = 21.99 < sheete || infoD
         sheete -= Double(storedW.count)
      }
      conversationU.append("\(2)")

        let visibleController = SocDelegateController(mode: .GVReport)
        visibleController.modalPresentationStyle = .fullScreen
        present(visibleController, animated: true)
    }

    @objc private func showPrivacyAgreementPage() {
       var email3: String! = String(cString: [115,117,98,116,114,101,101,115,0], encoding: .utf8)!
       var placeholderI: Double = 3.0
      withUnsafeMutablePointer(to: &placeholderI) { pointer in
             _ = pointer.pointee
      }
       var throwingh: Float = 5.0
       var fileA: String! = String(cString: [98,105,116,115,104,105,102,116,0], encoding: .utf8)!
      if (5.45 - throwingh) < 1.63 || 5 < (fileA.count | 4) {
         throwingh -= Float(2)
      }
         throwingh /= Swift.max(Float(3), 1)
      for _ in 0 ..< 3 {
         throwingh -= (Float(Int(throwingh > 166223129.0 || throwingh < -166223129.0 ? 50.0 : throwingh)))
      }
      repeat {
          var emotions8: String! = String(cString: [102,97,118,105,99,111,110,0], encoding: .utf8)!
          var class_mxz: Float = 3.0
          var moodP: Double = 3.0
          var insufficientH: String! = String(cString: [112,105,99,116,117,114,101,0], encoding: .utf8)!
         placeholderI /= Swift.max(3, (Double(Int(placeholderI > 251453068.0 || placeholderI < -251453068.0 ? 36.0 : placeholderI) >> (Swift.min(labs(Int(moodP > 140711987.0 || moodP < -140711987.0 ? 82.0 : moodP)), 5)))))
         emotions8 = "\(fileA.count - emotions8.count)"
         class_mxz -= Float(3)
         insufficientH.append("\((Int(class_mxz > 35257993.0 || class_mxz < -35257993.0 ? 63.0 : class_mxz) ^ 2))")
         if 3048292.0 == placeholderI {
            break
         }
      } while (2 == fileA.count) && (3048292.0 == placeholderI)
      for _ in 0 ..< 2 {
          var loading3: Int = 1
          var emotionsX: String! = String(cString: [115,118,113,101,110,99,0], encoding: .utf8)!
         placeholderI /= Swift.max(2, Double(3 & fileA.count))
         loading3 -= (Int(throwingh > 318046652.0 || throwingh < -318046652.0 ? 52.0 : throwingh) >> (Swift.min(labs(3), 3)))
         emotionsX = "\(loading3)"
      }
      for _ in 0 ..< 1 {
         placeholderI += (Double(fileA == (String(cString:[120,0], encoding: .utf8)!) ? fileA.count : Int(placeholderI > 284694238.0 || placeholderI < -284694238.0 ? 95.0 : placeholderI)))
      }
         placeholderI /= Swift.max(4, (Double((String(cString:[117,0], encoding: .utf8)!) == fileA ? Int(throwingh > 206952582.0 || throwingh < -206952582.0 ? 80.0 : throwingh) : fileA.count)))
         throwingh -= (Float((String(cString:[77,0], encoding: .utf8)!) == fileA ? fileA.count : Int(placeholderI > 354850241.0 || placeholderI < -354850241.0 ? 8.0 : placeholderI)))
      if !fileA.hasSuffix("\(throwingh)") {
          var controller8: Int = 4
          _ = controller8
         fileA.append("\(((String(cString:[75,0], encoding: .utf8)!) == fileA ? controller8 : fileA.count))")
      }
      email3.append("\(((String(cString:[54,0], encoding: .utf8)!) == email3 ? Int(placeholderI > 58738404.0 || placeholderI < -58738404.0 ? 30.0 : placeholderI) : email3.count))")

        showWebPage(title: "Privacy agreement", urlString: KSceneAuthx.privacy)
    }

@discardableResult
 func likeFillErrorRunning(placeholderSummary: [Any]!) -> String! {
    var durationT: [String: Any]! = [String(cString: [100,101,97,108,108,111,99,97,116,101,0], encoding: .utf8)!:false]
    var privacyb: [String: Any]! = [String(cString: [114,101,105,109,112,111,114,116,0], encoding: .utf8)!:63, String(cString: [119,97,116,99,104,101,114,115,0], encoding: .utf8)!:97, String(cString: [102,108,116,112,0], encoding: .utf8)!:54]
    var attributes2: String! = String(cString: [109,111,110,107,101,121,0], encoding: .utf8)!
       var largec: Double = 1.0
      withUnsafeMutablePointer(to: &largec) { pointer in
             _ = pointer.pointee
      }
       var scene_gY: Int = 4
       _ = scene_gY
      if largec >= Double(scene_gY) {
         largec -= Double(scene_gY)
      }
      for _ in 0 ..< 3 {
          var captureB: String! = String(cString: [108,111,103,105,99,97,108,0], encoding: .utf8)!
          var minee: Float = 3.0
          var successd: Int = 1
          var providerj: String! = String(cString: [115,117,110,115,101,116,0], encoding: .utf8)!
         largec -= (Double(Int(largec > 146791272.0 || largec < -146791272.0 ? 97.0 : largec) | providerj.count))
         captureB.append("\(1 + successd)")
         minee -= Float(successd)
      }
      repeat {
          var primaryr: String! = String(cString: [99,111,115,112,105,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &primaryr) { pointer in
    
         }
         scene_gY &= 1 & scene_gY
         primaryr = "\((Int(largec > 257925847.0 || largec < -257925847.0 ? 87.0 : largec)))"
         if scene_gY == 1757565 {
            break
         }
      } while (1 <= (scene_gY << (Swift.min(labs(5), 5))) || (Int(largec > 216091918.0 || largec < -216091918.0 ? 85.0 : largec) * scene_gY) <= 5) && (scene_gY == 1757565)
       var privacyK: Double = 0.0
         scene_gY += (Int(largec > 224842866.0 || largec < -224842866.0 ? 98.0 : largec) * Int(privacyK > 301132675.0 || privacyK < -301132675.0 ? 2.0 : privacyK))
          var heroB: Float = 3.0
          var constraint9: [Any]! = [String(cString: [116,105,109,101,111,117,116,0], encoding: .utf8)!]
          var screenf: Double = 2.0
         largec -= Double(1)
         heroB += Float(constraint9.count | 1)
         constraint9 = [1]
         screenf /= Swift.max(4, Double(constraint9.count))
      privacyb = ["\(privacyb.keys.count)": privacyb.count]
       var listenerG: String! = String(cString: [120,121,122,116,97,98,108,101,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &listenerG) { pointer in
    
      }
       var from8: String! = String(cString: [116,104,114,101,115,104,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &from8) { pointer in
             _ = pointer.pointee
      }
      while (from8.count <= 4) {
         from8 = "\(listenerG.count + 1)"
         break
      }
      while (from8.contains("\(listenerG.count)")) {
          var viewsO: Double = 5.0
          var largek: String! = String(cString: [99,100,99,105,0], encoding: .utf8)!
          var purchased: String! = String(cString: [115,101,101,100,0], encoding: .utf8)!
          var r_boundsW: String! = String(cString: [99,111,110,110,101,99,116,111,114,0], encoding: .utf8)!
         from8.append("\(1)")
         viewsO /= Swift.max(1, Double(2))
         largek.append("\(purchased.count)")
         purchased.append("\(largek.count << (Swift.min(labs(1), 3)))")
         r_boundsW = "\((listenerG.count << (Swift.min(4, labs(Int(viewsO > 136948854.0 || viewsO < -136948854.0 ? 68.0 : viewsO))))))"
         break
      }
          var loadingI: Bool = false
          var size_ulJ: Bool = false
         withUnsafeMutablePointer(to: &size_ulJ) { pointer in
                _ = pointer.pointee
         }
          var libraryA: Double = 0.0
         listenerG = "\(((loadingI ? 2 : 5) ^ 3))"
         size_ulJ = loadingI
         libraryA -= (Double(listenerG == (String(cString:[102,0], encoding: .utf8)!) ? listenerG.count : (size_ulJ ? 2 : 1)))
         listenerG.append("\(listenerG.count)")
       var showW: String! = String(cString: [102,117,108,102,105,108,108,0], encoding: .utf8)!
       var preferredh: String! = String(cString: [112,111,105,110,116,115,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
          var z_layerY: Double = 4.0
          var idsu: [Any]! = [String(cString: [114,101,97,100,109,101,0], encoding: .utf8)!, String(cString: [105,112,111,108,0], encoding: .utf8)!]
          var communityj: String! = String(cString: [98,97,110,100,119,105,100,116,104,0], encoding: .utf8)!
         from8 = "\(listenerG.count)"
         z_layerY /= Swift.max(3, (Double(Int(z_layerY > 289678798.0 || z_layerY < -289678798.0 ? 49.0 : z_layerY))))
         idsu = [communityj.count]
         communityj.append("\((preferredh == (String(cString:[85,0], encoding: .utf8)!) ? preferredh.count : from8.count))")
      }
         showW.append("\(preferredh.count)")
      privacyb[listenerG] = listenerG.count
      attributes2.append("\(durationT.keys.count - 2)")
   repeat {
       var animateO: [String: Any]! = [String(cString: [108,97,110,101,0], encoding: .utf8)!:43, String(cString: [118,97,108,105,100,97,116,111,114,0], encoding: .utf8)!:3]
       var emailK: [Any]! = [60, 24]
       var label_: [Any]! = [77, 42, 97]
       var u_layerj: [Any]! = [63, 50, 45]
      repeat {
          var visiblea: [Any]! = [String(cString: [105,110,101,116,0], encoding: .utf8)!]
          var sharingn: Int = 4
         u_layerj.append(sharingn)
         visiblea.append(2)
         if 1163293 == u_layerj.count {
            break
         }
      } while (1 >= (5 + emailK.count)) && (1163293 == u_layerj.count)
         label_ = [animateO.keys.count * 3]
      repeat {
         u_layerj = [animateO.keys.count | emailK.count]
         if u_layerj.count == 549426 {
            break
         }
      } while (u_layerj.count == 549426) && ((u_layerj.count / (Swift.max(emailK.count, 7))) > 3 || (u_layerj.count / (Swift.max(emailK.count, 1))) > 3)
      for _ in 0 ..< 3 {
          var blockedM: Double = 1.0
          var willf: String! = String(cString: [109,98,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &willf) { pointer in
    
         }
         u_layerj = [emailK.count]
         blockedM -= Double(animateO.values.count)
         willf.append("\(2)")
      }
      for _ in 0 ..< 2 {
          var starN: Double = 0.0
          var destinationt: String! = String(cString: [111,110,99,101,0], encoding: .utf8)!
         u_layerj = [1]
         starN += Double(2)
         destinationt = "\(destinationt.count ^ animateO.count)"
      }
      durationT = ["\(animateO.values.count)": animateO.values.count % (Swift.max(7, attributes2.count))]
      if durationT.count == 2307033 {
         break
      }
   } while (3 <= durationT.count) && (durationT.count == 2307033)
   return attributes2

}





    @objc private func showUserAgreementPage() {

         var encodableAutorotation: String! = likeFillErrorRunning(placeholderSummary:[65, 44, 65])

      if encodableAutorotation == "empty" {
              print(encodableAutorotation)
      }
      let encodableAutorotation_len = encodableAutorotation?.count ?? 0

withUnsafeMutablePointer(to: &encodableAutorotation) { pointer in
        _ = pointer.pointee
}


       var mailk: Bool = true
   withUnsafeMutablePointer(to: &mailk) { pointer in
    
   }
   if mailk {
      mailk = !mailk || mailk
   }

        showWebPage(title: "User agreement", urlString: KSceneAuthx.user)
    }

@discardableResult
 func minimumGreenOriginalImageView(beginMake: [String: Any]!, displayNow: String!, titleAction: Double) -> UIImageView! {
    var normalm: String! = String(cString: [98,95,57,48,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &normalm) { pointer in
          _ = pointer.pointee
   }
    var healingw: String! = String(cString: [118,115,99,97,108,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &healingw) { pointer in
          _ = pointer.pointee
   }
       var repliesQ: Int = 1
      while (4 <= (3 | repliesQ) || 5 <= (repliesQ | 3)) {
          var shareN: String! = String(cString: [114,108,109,0], encoding: .utf8)!
         repliesQ += 2 - shareN.count
         break
      }
         repliesQ -= 1 + repliesQ
      if repliesQ >= 2 {
         repliesQ -= 2 & repliesQ
      }
      healingw = "\(1)"
   for _ in 0 ..< 3 {
      healingw.append("\(3)")
   }
   if normalm.contains(normalm) {
      normalm = "\(normalm.count)"
   }
     let badgeResult: Double = 18.0
     var currentDetail: UIView! = UIView(frame:CGRect(x: 152, y: 47, width: 0, height: 0))
    var voipLibrary = UIImageView(frame:CGRect(x: 127, y: 283, width: 0, height: 0))
    voipLibrary.frame = CGRect(x: 71, y: 240, width: 0, height: 0)
    voipLibrary.alpha = 0.3;
    voipLibrary.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    voipLibrary.animationRepeatCount = 6
    voipLibrary.image = UIImage(named:String(cString: [115,99,101,110,101,95,105,0], encoding: .utf8)!)
    voipLibrary.contentMode = .scaleAspectFit
    currentDetail.frame = CGRect(x: 54, y: 180, width: 0, height: 0)
    currentDetail.alpha = 0.7;
    currentDetail.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    

    
    return voipLibrary

}





    @objc private func showContactUsPage() {

         var rfftiAdjust: UIImageView! = minimumGreenOriginalImageView(beginMake:[String(cString: [98,97,99,107,114,111,117,110,100,0], encoding: .utf8)!:String(cString: [103,101,116,97,100,100,114,105,110,102,111,0], encoding: .utf8)!], displayNow:String(cString: [114,101,99,101,105,118,101,100,0], encoding: .utf8)!, titleAction:69.0)

      if rfftiAdjust != nil {
          let rfftiAdjust_tag = rfftiAdjust.tag
          self.view.addSubview(rfftiAdjust)
      }
      else {
          print("rfftiAdjust is nil")      }

withUnsafeMutablePointer(to: &rfftiAdjust) { pointer in
        _ = pointer.pointee
}


       var field_: String! = String(cString: [116,101,114,109,115,0], encoding: .utf8)!
    _ = field_
      field_ = "\(field_.count)"

        showContactCard()
    }

    @objc private func showCommunityGuidelinesPage() {
       var femalez: Bool = true
    var t_boundsu: String! = String(cString: [118,111,119,101,108,0], encoding: .utf8)!
   while (femalez) {
      femalez = t_boundsu.count == 75
      break
   }
      femalez = !t_boundsu.contains("\(femalez)")

        showWebPage(title: "Community Guidelines", urlString: KSceneAuthx.community)
    }

@discardableResult
 func compactDismissalHealEmotionHero(gestureScene: Bool, gradientMicrophone: [Any]!) -> String! {
    var logged6: String! = String(cString: [115,117,98,110,111,100,101,0], encoding: .utf8)!
    _ = logged6
    var closeZ: Double = 4.0
    var scene_qF: String! = String(cString: [117,110,115,105,103,110,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &scene_qF) { pointer in
          _ = pointer.pointee
   }
   repeat {
       var activeK: [Any]! = [String(cString: [118,101,110,117,101,115,0], encoding: .utf8)!]
      if activeK.count == activeK.count {
          var profiley: [Any]! = [68, 5]
          var sheetE: String! = String(cString: [103,105,118,101,110,0], encoding: .utf8)!
          _ = sheetE
          var postS: Int = 4
          var mic6: String! = String(cString: [100,117,109,109,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &mic6) { pointer in
    
         }
         activeK = [activeK.count]
         profiley = [3 % (Swift.max(10, postS))]
         sheetE.append("\(profiley.count - 1)")
         postS += postS >> (Swift.min(profiley.count, 3))
         mic6.append("\(1 + postS)")
      }
          var packagej: String! = String(cString: [102,105,108,108,101,100,0], encoding: .utf8)!
          var subtitleG: Bool = true
         activeK = [packagej.count]
         subtitleG = (activeK.contains { $0 as? Bool == subtitleG })
      for _ in 0 ..< 2 {
         activeK.append(activeK.count % (Swift.max(8, activeK.count)))
      }
      closeZ /= Swift.max(Double(scene_qF.count), 5)
      if closeZ == 4278644.0 {
         break
      }
   } while (4.59 <= (closeZ - 2.70) || (scene_qF.count - Int(closeZ > 89368702.0 || closeZ < -89368702.0 ? 62.0 : closeZ)) <= 5) && (closeZ == 4278644.0)
      logged6 = "\((Int(closeZ > 25289163.0 || closeZ < -25289163.0 ? 17.0 : closeZ)))"
   for _ in 0 ..< 1 {
       var coins3: String! = String(cString: [101,120,105,115,116,97,110,99,101,0], encoding: .utf8)!
       var preferredp: String! = String(cString: [105,110,99,114,101,109,101,110,116,101,100,0], encoding: .utf8)!
       var insufficient2: Double = 5.0
       _ = insufficient2
      for _ in 0 ..< 1 {
          var normalizedA: Double = 0.0
         preferredp = "\((coins3 == (String(cString:[68,0], encoding: .utf8)!) ? coins3.count : Int(normalizedA > 355272399.0 || normalizedA < -355272399.0 ? 99.0 : normalizedA)))"
      }
         coins3.append("\(1)")
      repeat {
         preferredp.append("\(((String(cString:[52,0], encoding: .utf8)!) == coins3 ? Int(insufficient2 > 30873139.0 || insufficient2 < -30873139.0 ? 37.0 : insufficient2) : coins3.count))")
         if (String(cString:[104,104,122,118,113,98,53,0], encoding: .utf8)!) == preferredp {
            break
         }
      } while ((String(cString:[104,104,122,118,113,98,53,0], encoding: .utf8)!) == preferredp) && (coins3.hasSuffix(preferredp))
       var loadingI: String! = String(cString: [101,116,104,111,100,0], encoding: .utf8)!
       var n_tagK: String! = String(cString: [114,97,100,98,0], encoding: .utf8)!
      while (coins3 != loadingI) {
          var keyboardC: String! = String(cString: [118,112,109,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &keyboardC) { pointer in
                _ = pointer.pointee
         }
         loadingI = "\(coins3.count)"
         keyboardC = "\(keyboardC.count ^ n_tagK.count)"
         break
      }
         insufficient2 -= Double(coins3.count << (Swift.min(labs(3), 3)))
          var usersQ: [Any]! = [String(cString: [109,97,112,0], encoding: .utf8)!, String(cString: [112,97,103,101,105,110,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &usersQ) { pointer in
                _ = pointer.pointee
         }
         n_tagK = "\(preferredp.count ^ 1)"
         usersQ.append(((String(cString:[107,0], encoding: .utf8)!) == coins3 ? coins3.count : Int(insufficient2 > 238916846.0 || insufficient2 < -238916846.0 ? 18.0 : insufficient2)))
       var followK: Float = 0.0
       var mood5: Float = 5.0
      while (followK == 2.34) {
         followK -= (Float((String(cString:[120,0], encoding: .utf8)!) == preferredp ? preferredp.count : Int(followK > 266822375.0 || followK < -266822375.0 ? 55.0 : followK)))
         break
      }
         mood5 += Float(1)
      closeZ -= (Double(Int(insufficient2 > 137972685.0 || insufficient2 < -137972685.0 ? 18.0 : insufficient2)))
   }
   return logged6

}





    private func showWebPage(title: String, urlString: String) {

         let getlblockincLhash: String! = compactDismissalHealEmotionHero(gestureScene:false, gradientMicrophone:[77, 41])

      print(getlblockincLhash)
      let getlblockincLhash_len = getlblockincLhash?.count ?? 0

_ = getlblockincLhash


       var cachen: Double = 1.0
    _ = cachen
       var preferred8: String! = String(cString: [111,114,105,101,110,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &preferred8) { pointer in
    
      }
       var playing0: Int = 0
      repeat {
         preferred8.append("\(((String(cString:[118,0], encoding: .utf8)!) == preferred8 ? preferred8.count : playing0))")
         if preferred8 == (String(cString:[110,108,50,98,102,0], encoding: .utf8)!) {
            break
         }
      } while ((preferred8.count % 5) <= 3 && (playing0 % (Swift.max(5, 9))) <= 1) && (preferred8 == (String(cString:[110,108,50,98,102,0], encoding: .utf8)!))
          var validateH: String! = String(cString: [115,112,97,114,107,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &validateH) { pointer in
    
         }
         playing0 /= Swift.max(1, ((String(cString:[120,0], encoding: .utf8)!) == preferred8 ? playing0 : preferred8.count))
         validateH = "\(validateH.count)"
          var completionh: String! = String(cString: [97,100,100,114,0], encoding: .utf8)!
          _ = completionh
          var identifierX: String! = String(cString: [109,98,112,111,115,116,0], encoding: .utf8)!
         preferred8 = "\(3)"
         completionh.append("\((preferred8 == (String(cString:[81,0], encoding: .utf8)!) ? playing0 : preferred8.count))")
         identifierX = "\(2 % (Swift.max(2, preferred8.count)))"
         playing0 /= Swift.max(playing0 * preferred8.count, 5)
       var addO: Double = 2.0
      while (!preferred8.hasSuffix("\(addO)")) {
          var storagez: String! = String(cString: [112,115,110,114,104,118,115,0], encoding: .utf8)!
          var end6: String! = String(cString: [115,116,105,99,107,101,114,101,100,0], encoding: .utf8)!
         addO -= (Double(Int(addO > 147743950.0 || addO < -147743950.0 ? 30.0 : addO)))
         storagez = "\(2)"
         end6.append("\(end6.count + playing0)")
         break
      }
      cachen -= Double(preferred8.count << (Swift.min(5, labs(playing0))))

        guard let url = URL(string: urlString) else {
            showSimpleAlert(title: "Unable to open this page.")
            return
        }

        let visibleController = SocBottleProfileController(title: title, url: url)
        visibleController.modalPresentationStyle = .fullScreen
        present(visibleController, animated: true)
    }

@discardableResult
 func purchaseStoreInitial() -> Double {
    var activeK: Int = 0
   withUnsafeMutablePointer(to: &activeK) { pointer in
    
   }
    var z_animation0: String! = String(cString: [99,111,108,108,101,99,116,105,111,110,115,0], encoding: .utf8)!
    var modei: Double = 3.0
   withUnsafeMutablePointer(to: &modei) { pointer in
          _ = pointer.pointee
   }
      activeK %= Swift.max(z_animation0.count - 2, 2)
       var dialogI: Bool = false
         dialogI = dialogI && !dialogI
         dialogI = !dialogI
         dialogI = !dialogI
      activeK |= 3
   repeat {
       var yesO: [Any]! = [71, 98]
      withUnsafeMutablePointer(to: &yesO) { pointer in
    
      }
       var edita: String! = String(cString: [109,118,112,114,101,100,0], encoding: .utf8)!
       var main_af: Double = 2.0
       _ = main_af
      for _ in 0 ..< 1 {
          var dismissf: Float = 5.0
          _ = dismissf
          var face6: String! = String(cString: [116,111,107,104,122,0], encoding: .utf8)!
          var enrichedZ: String! = String(cString: [97,99,99,101,115,115,105,98,105,108,105,116,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &enrichedZ) { pointer in
                _ = pointer.pointee
         }
         yesO = [(Int(main_af > 56830275.0 || main_af < -56830275.0 ? 63.0 : main_af))]
         dismissf /= Swift.max(Float(enrichedZ.count), 3)
         face6 = "\(face6.count)"
         enrichedZ = "\(((String(cString:[111,0], encoding: .utf8)!) == face6 ? enrichedZ.count : face6.count))"
      }
      for _ in 0 ..< 3 {
         yesO.append(((String(cString:[68,0], encoding: .utf8)!) == edita ? edita.count : yesO.count))
      }
      if edita.hasPrefix("\(main_af)") {
         edita = "\((Int(main_af > 134052077.0 || main_af < -134052077.0 ? 87.0 : main_af)))"
      }
      while ((4.63 + main_af) <= 5.70) {
         edita = "\((Int(main_af > 189511765.0 || main_af < -189511765.0 ? 29.0 : main_af) ^ 2))"
         break
      }
          var checkq: Int = 4
         yesO = [edita.count - 1]
         checkq -= edita.count
         yesO = [2]
      repeat {
         edita.append("\(2)")
         if edita == (String(cString:[112,53,103,0], encoding: .utf8)!) {
            break
         }
      } while (edita == (String(cString:[112,53,103,0], encoding: .utf8)!)) && (edita.contains("\(yesO.count)"))
      for _ in 0 ..< 2 {
          var companionsQ: Double = 0.0
          _ = companionsQ
          var stackq: Double = 0.0
          _ = stackq
          var arlanZ: String! = String(cString: [103,114,111,119,116,104,0], encoding: .utf8)!
         yesO = [(Int(companionsQ > 83786631.0 || companionsQ < -83786631.0 ? 15.0 : companionsQ))]
         stackq -= (Double(Int(main_af > 17865474.0 || main_af < -17865474.0 ? 26.0 : main_af)))
         arlanZ.append("\((Int(main_af > 227296887.0 || main_af < -227296887.0 ? 39.0 : main_af) & edita.count))")
      }
      while (edita.count >= yesO.count) {
         edita = "\(3 - yesO.count)"
         break
      }
      activeK ^= (edita.count + Int(main_af > 342238987.0 || main_af < -342238987.0 ? 70.0 : main_af))
      if activeK == 4893897 {
         break
      }
   } while (1 > z_animation0.count) && (activeK == 4893897)
   for _ in 0 ..< 1 {
      modei -= Double(1)
   }
   return modei

}





    private func showContactCard() {

         var eburBeing: Double = purchaseStoreInitial()

      if eburBeing == 67 {
             print(eburBeing)
      }

withUnsafeMutablePointer(to: &eburBeing) { pointer in
        _ = pointer.pointee
}


       var visibleK: String! = String(cString: [109,97,121,98,101,0], encoding: .utf8)!
       var addt: String! = String(cString: [105,115,112,108,97,121,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &addt) { pointer in
    
      }
       var flash9: String! = String(cString: [115,117,114,102,97,99,101,115,0], encoding: .utf8)!
       var editg: Double = 1.0
      while (flash9.count == (Int(editg > 108146853.0 || editg < -108146853.0 ? 75.0 : editg))) {
         editg += Double(addt.count)
         break
      }
          var mutualQ: Bool = true
          _ = mutualQ
          var subtitlez: String! = String(cString: [100,109,97,99,0], encoding: .utf8)!
         editg -= (Double(flash9.count ^ Int(editg > 64798399.0 || editg < -64798399.0 ? 17.0 : editg)))
         mutualQ = addt.count == 34
         subtitlez = "\(((String(cString:[68,0], encoding: .utf8)!) == flash9 ? subtitlez.count : flash9.count))"
      for _ in 0 ..< 3 {
         flash9.append("\(addt.count)")
      }
          var perlaT: [String: Any]! = [String(cString: [115,101,116,0], encoding: .utf8)!:String(cString: [100,95,52,55,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &perlaT) { pointer in
    
         }
          var fallback3: [String: Any]! = [String(cString: [114,101,115,99,97,108,101,0], encoding: .utf8)!:4, String(cString: [101,118,117,116,105,108,0], encoding: .utf8)!:35, String(cString: [97,112,112,101,110,100,97,108,108,0], encoding: .utf8)!:71]
         editg += Double(fallback3.count - 1)
         perlaT = [addt: (Int(editg > 389005015.0 || editg < -389005015.0 ? 38.0 : editg) << (Swift.min(labs(2), 3)))]
          var class_nmO: String! = String(cString: [116,114,101,101,0], encoding: .utf8)!
          var dimg: String! = String(cString: [108,108,97,117,100,100,115,112,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &dimg) { pointer in
                _ = pointer.pointee
         }
          var passwords: Bool = true
          _ = passwords
         flash9.append("\(((String(cString:[66,0], encoding: .utf8)!) == addt ? dimg.count : addt.count))")
         class_nmO = "\(dimg.count / 3)"
         passwords = !class_nmO.hasPrefix("\(passwords)")
          var delegate_k7: Double = 1.0
         editg += (Double(Int(editg > 13774057.0 || editg < -13774057.0 ? 32.0 : editg) - Int(delegate_k7 > 263085327.0 || delegate_k7 < -263085327.0 ? 81.0 : delegate_k7)))
      repeat {
         flash9 = "\(addt.count)"
         if (String(cString:[118,116,55,110,103,110,0], encoding: .utf8)!) == flash9 {
            break
         }
      } while ((String(cString:[118,116,55,110,103,110,0], encoding: .utf8)!) == flash9) && (flash9.contains(addt))
          var resulta: Float = 0.0
          var appearanceg: Bool = false
         flash9 = "\((1 - Int(resulta > 25259671.0 || resulta < -25259671.0 ? 90.0 : resulta)))"
         appearanceg = !addt.contains("\(resulta)")
         editg -= Double(flash9.count << (Swift.min(labs(1), 2)))
      visibleK.append("\(2 - addt.count)")

        guard contactOverlayView == nil else { return }

        let savedView = UIView()
        savedView.backgroundColor = UIColor.black.withAlphaComponent(0.34)
        savedView.alpha = 0
        savedView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(savedView)
        contactOverlayView = savedView

        let packagesView = UIView()
        packagesView.backgroundColor = .white
        packagesView.layer.cornerRadius = 18
        packagesView.layer.shadowColor = UIColor.black.cgColor
        packagesView.layer.shadowOpacity = 0.14
        packagesView.layer.shadowRadius = 18
        packagesView.layer.shadowOffset = CGSize(width: 0, height: 10)
        packagesView.alpha = 0
        packagesView.transform = CGAffineTransform(scaleX: 0.94, y: 0.94)
        packagesView.translatesAutoresizingMaskIntoConstraints = false
        savedView.addSubview(packagesView)

        let znewsLabel = UILabel()
        znewsLabel.text = "Contact Us"
        znewsLabel.font = .systemFont(ofSize: 22, weight: .bold)
        znewsLabel.textColor = UIColor(red: 0.10, green: 0.10, blue: 0.10, alpha: 1)
        znewsLabel.textAlignment = .center
        znewsLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(znewsLabel)

        let throwingLabel = UILabel()
        throwingLabel.text = "Tap the email to copy it."
        throwingLabel.font = .systemFont(ofSize: 14, weight: .regular)
        throwingLabel.textColor = UIColor(red: 0.48, green: 0.48, blue: 0.50, alpha: 1)
        throwingLabel.textAlignment = .center
        throwingLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(throwingLabel)

        let infoButton = UIButton(type: .custom)
        infoButton.backgroundColor = UIColor(red: 0.96, green: 0.97, blue: 0.98, alpha: 1)
        infoButton.layer.cornerRadius = 12
        infoButton.layer.masksToBounds = true
        infoButton.setTitle(KSignUserm.email, for: .normal)
        infoButton.setTitleColor(UIColor(red: 0.08, green: 0.10, blue: 0.08, alpha: 1), for: .normal)
        infoButton.titleLabel?.font = .systemFont(ofSize: 15, weight: .semibold)
        infoButton.titleLabel?.adjustsFontSizeToFitWidth = true
        infoButton.titleLabel?.minimumScaleFactor = 0.72
        infoButton.contentEdgeInsets = UIEdgeInsets(top: 0, left: 14, bottom: 0, right: 14)
        infoButton.addTarget(self, action: #selector(copyContactEmail(_:)), for: .touchUpInside)
        infoButton.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(infoButton)

        let checkButton = UIButton(type: .custom)
        checkButton.backgroundColor = UIColor(red: 0.07, green: 0.09, blue: 0.07, alpha: 1)
        checkButton.layer.cornerRadius = 13
        checkButton.layer.masksToBounds = true
        checkButton.setTitle("Copy Email", for: .normal)
        checkButton.setTitleColor(.white, for: .normal)
        checkButton.titleLabel?.font = .systemFont(ofSize: 16, weight: .bold)
        checkButton.addTarget(self, action: #selector(copyContactEmail(_:)), for: .touchUpInside)
        checkButton.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(checkButton)

        let labelButton = UIButton(type: .custom)
        labelButton.setTitle("Cancel", for: .normal)
        labelButton.setTitleColor(UIColor(red: 0.50, green: 0.50, blue: 0.52, alpha: 1), for: .normal)
        labelButton.titleLabel?.font = .systemFont(ofSize: 15, weight: .regular)
        labelButton.addTarget(self, action: #selector(closeContactCard), for: .touchUpInside)
        labelButton.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(labelButton)

        NSLayoutConstraint.activate([
            savedView.topAnchor.constraint(equalTo: view.topAnchor),
            savedView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            savedView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            savedView.bottomAnchor.constraint(equalTo: view.bottomAnchor),

            packagesView.centerXAnchor.constraint(equalTo: savedView.centerXAnchor),
            packagesView.centerYAnchor.constraint(equalTo: savedView.centerYAnchor),
            packagesView.leadingAnchor.constraint(equalTo: savedView.leadingAnchor, constant: 34),
            packagesView.trailingAnchor.constraint(equalTo: savedView.trailingAnchor, constant: -34),

            znewsLabel.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 30),
            znewsLabel.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 24),
            znewsLabel.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor, constant: -24),

            throwingLabel.topAnchor.constraint(equalTo: znewsLabel.bottomAnchor, constant: 10),
            throwingLabel.leadingAnchor.constraint(equalTo: znewsLabel.leadingAnchor),
            throwingLabel.trailingAnchor.constraint(equalTo: znewsLabel.trailingAnchor),

            infoButton.topAnchor.constraint(equalTo: throwingLabel.bottomAnchor, constant: 22),
            infoButton.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 26),
            infoButton.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor, constant: -26),
            infoButton.heightAnchor.constraint(equalToConstant: 48),

            checkButton.topAnchor.constraint(equalTo: infoButton.bottomAnchor, constant: 18),
            checkButton.leadingAnchor.constraint(equalTo: infoButton.leadingAnchor),
            checkButton.trailingAnchor.constraint(equalTo: infoButton.trailingAnchor),
            checkButton.heightAnchor.constraint(equalToConstant: 46),

            labelButton.topAnchor.constraint(equalTo: checkButton.bottomAnchor, constant: 8),
            labelButton.leadingAnchor.constraint(equalTo: checkButton.leadingAnchor),
            labelButton.trailingAnchor.constraint(equalTo: checkButton.trailingAnchor),
            labelButton.heightAnchor.constraint(equalToConstant: 42),
            labelButton.bottomAnchor.constraint(equalTo: packagesView.bottomAnchor, constant: -20)
        ])

        UIView.animate(withDuration: 0.2, delay: 0, options: [.curveEaseOut]) {
            savedView.alpha = 1
            packagesView.alpha = 1
            packagesView.transform = .identity
        }
    }

    @objc private func copyContactEmail(_ sender: UIButton) {
       var startl: Double = 2.0
       var register_77B: String! = String(cString: [115,111,99,97,110,116,114,99,118,109,111,114,101,0], encoding: .utf8)!
       var messagesH: String! = String(cString: [115,113,108,105,116,101,112,97,103,101,114,0], encoding: .utf8)!
       var user8: Int = 3
          var topu: String! = String(cString: [117,110,115,104,105,102,116,0], encoding: .utf8)!
          _ = topu
          var closeG: Double = 2.0
          var barj: [Any]! = [44, 91, 88]
         register_77B.append("\(messagesH.count)")
         topu.append("\(messagesH.count ^ 2)")
         closeG /= Swift.max(1, Double(user8))
         barj = [topu.count]
      while (!register_77B.hasSuffix("\(user8)")) {
         user8 /= Swift.max(3, user8)
         break
      }
      while (messagesH.hasSuffix("\(user8)")) {
          var presenterl: String! = String(cString: [117,110,109,111,118,101,100,0], encoding: .utf8)!
         user8 &= register_77B.count ^ 3
         presenterl.append("\(messagesH.count | user8)")
         break
      }
       var rightX: Float = 1.0
         register_77B.append("\((register_77B == (String(cString:[65,0], encoding: .utf8)!) ? messagesH.count : register_77B.count))")
      if (3 ^ user8) <= 3 || 4 <= (3 ^ register_77B.count) {
          var bubbleQ: String! = String(cString: [115,121,115,99,116,108,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &bubbleQ) { pointer in
                _ = pointer.pointee
         }
          var moodp: Double = 4.0
         user8 += (Int(moodp > 37874792.0 || moodp < -37874792.0 ? 3.0 : moodp))
         bubbleQ.append("\(3 ^ messagesH.count)")
      }
         register_77B = "\((2 >> (Swift.min(3, labs(Int(rightX > 391518542.0 || rightX < -391518542.0 ? 52.0 : rightX))))))"
      if 1 > (2 & messagesH.count) && 5.45 > (rightX + 2.49) {
         messagesH = "\(user8)"
      }
       var summaryZ: String! = String(cString: [99,111,115,105,103,110,97,116,111,114,121,0], encoding: .utf8)!
         summaryZ.append("\(summaryZ.count - 3)")
      startl += (Double((String(cString:[68,0], encoding: .utf8)!) == register_77B ? register_77B.count : Int(startl > 282694415.0 || startl < -282694415.0 ? 9.0 : startl)))

        UIPasteboard.general.string = KSignUserm.email

        let call = sender.currentTitle
        sender.setTitle("Copied", for: .normal)

        DispatchQueue.main.asyncAfter(deadline: .now() + 1.0) {
            sender.setTitle(call, for: .normal)
        }
    }

@discardableResult
 func subtitleGreenSloganImageView(updatedReplies: [String: Any]!, scrollMain: [String: Any]!) -> UIImageView! {
    var male3: String! = String(cString: [115,116,101,109,109,101,114,0], encoding: .utf8)!
    var layoutV: String! = String(cString: [108,106,112,101,103,0], encoding: .utf8)!
      layoutV = "\(layoutV.count % (Swift.max(male3.count, 2)))"
      layoutV.append("\(male3.count)")
     let keywordsCurrent: UIImageView! = UIImageView(frame:CGRect(x: 137, y: 240, width: 0, height: 0))
     var textMin_p0: UIImageView! = UIImageView()
     let primaryValue: String! = String(cString: [115,117,105,116,101,115,0], encoding: .utf8)!
     var summaryUrl: Int = 62
    var rarityExposureLsfpoly: UIImageView! = UIImageView(frame:CGRect(x: 216, y: 41, width: 0, height: 0))
    keywordsCurrent.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    keywordsCurrent.alpha = 0.7
    keywordsCurrent.frame = CGRect(x: 53, y: 305, width: 0, height: 0)
    keywordsCurrent.contentMode = .scaleAspectFit
    keywordsCurrent.animationRepeatCount = 9
    keywordsCurrent.image = UIImage(named:String(cString: [117,114,108,0], encoding: .utf8)!)
    
    textMin_p0.alpha = 0.3;
    textMin_p0.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    textMin_p0.frame = CGRect(x: 292, y: 85, width: 0, height: 0)
    textMin_p0.animationRepeatCount = 2
    textMin_p0.image = UIImage(named:String(cString: [99,108,111,115,101,0], encoding: .utf8)!)
    textMin_p0.contentMode = .scaleAspectFit
    
    rarityExposureLsfpoly.image = UIImage(named:String(cString: [100,105,115,99,111,110,110,101,99,116,0], encoding: .utf8)!)
    rarityExposureLsfpoly.contentMode = .scaleAspectFit
    rarityExposureLsfpoly.animationRepeatCount = 10
    rarityExposureLsfpoly.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    rarityExposureLsfpoly.alpha = 0.3
    rarityExposureLsfpoly.frame = CGRect(x: 155, y: 24, width: 0, height: 0)

    
    return rarityExposureLsfpoly

}





    @objc private func closeContactCard() {

         let findassociationDialogues: UIImageView! = subtitleGreenSloganImageView(updatedReplies:[String(cString: [100,97,98,97,115,101,0], encoding: .utf8)!:11, String(cString: [121,117,118,114,103,98,0], encoding: .utf8)!:15, String(cString: [114,101,109,111,118,97,98,108,101,0], encoding: .utf8)!:4], scrollMain:[String(cString: [111,108,111,114,0], encoding: .utf8)!:String(cString: [115,117,98,114,101,115,117,108,116,115,0], encoding: .utf8)!, String(cString: [102,111,108,100,0], encoding: .utf8)!:String(cString: [112,116,111,110,0], encoding: .utf8)!])

      if findassociationDialogues != nil {
          let findassociationDialogues_tag = findassociationDialogues.tag
          self.view.addSubview(findassociationDialogues)
      }
      else {
          print("findassociationDialogues is nil")      }

_ = findassociationDialogues


       var socialZ: Int = 1
    var buttonj: Bool = true
   for _ in 0 ..< 2 {
      buttonj = 45 == socialZ
   }

        guard let contactOverlayView else { return }

   if buttonj {
      buttonj = !buttonj
   }
        UIView.animate(withDuration: 0.16) {
            contactOverlayView.alpha = 0
        } completion: { _ in
   while (socialZ <= 3) {
      socialZ >>= Swift.min(3, labs(socialZ >> (Swift.min(labs(socialZ), 5))))
      break
   }
            contactOverlayView.removeFromSuperview()
        }
    }

    @objc private func logOut() {
       var throw_r_: [Any]! = [23, 84]
    _ = throw_r_
    var feelingsQ: Bool = false
   withUnsafeMutablePointer(to: &feelingsQ) { pointer in
          _ = pointer.pointee
   }
   repeat {
       var healm: String! = String(cString: [101,114,97,115,117,114,101,0], encoding: .utf8)!
       var areas: Float = 1.0
       _ = areas
       var messages_: String! = String(cString: [104,101,105,99,0], encoding: .utf8)!
       _ = messages_
       var purchaseZ: String! = String(cString: [108,105,98,97,111,109,0], encoding: .utf8)!
      repeat {
         healm.append("\(1)")
         if 2644240 == healm.count {
            break
         }
      } while (2644240 == healm.count) && (purchaseZ != String(cString:[73,0], encoding: .utf8)!)
          var primaryP: Int = 1
         messages_ = "\((Int(areas > 267367684.0 || areas < -267367684.0 ? 61.0 : areas)))"
         primaryP -= messages_.count
         messages_ = "\(2 ^ healm.count)"
      for _ in 0 ..< 2 {
         healm.append("\(purchaseZ.count)")
      }
         purchaseZ.append("\(messages_.count + purchaseZ.count)")
       var communityH: String! = String(cString: [114,101,115,112,111,110,100,101,114,0], encoding: .utf8)!
       _ = communityH
       var itemsZ: String! = String(cString: [97,99,107,0], encoding: .utf8)!
         areas /= Swift.max((Float((String(cString:[56,0], encoding: .utf8)!) == healm ? healm.count : Int(areas > 32227315.0 || areas < -32227315.0 ? 87.0 : areas))), 4)
          var gradientn: [Any]! = [43, 49, 3]
          var templatesf: String! = String(cString: [112,114,111,116,101,99,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &templatesf) { pointer in
                _ = pointer.pointee
         }
          var templatese: String! = String(cString: [115,117,98,115,121,115,116,101,109,0], encoding: .utf8)!
          _ = templatese
         purchaseZ.append("\(((String(cString:[89,0], encoding: .utf8)!) == purchaseZ ? communityH.count : purchaseZ.count))")
         gradientn = [communityH.count]
         templatesf = "\(1)"
         templatese = "\((2 ^ Int(areas > 58476766.0 || areas < -58476766.0 ? 79.0 : areas)))"
       var sessionI: Double = 1.0
       var reply3: Double = 0.0
      withUnsafeMutablePointer(to: &reply3) { pointer in
             _ = pointer.pointee
      }
       var offsety: String! = String(cString: [97,97,99,101,110,99,0], encoding: .utf8)!
       var keywords1: String! = String(cString: [100,117,112,108,105,99,97,116,111,114,0], encoding: .utf8)!
          var dismissalD: String! = String(cString: [118,97,108,117,108,101,0], encoding: .utf8)!
          var reportu: String! = String(cString: [105,99,99,112,0], encoding: .utf8)!
         communityH.append("\(messages_.count)")
         dismissalD.append("\(messages_.count)")
         reportu = "\((1 + Int(sessionI > 375452530.0 || sessionI < -375452530.0 ? 2.0 : sessionI)))"
         purchaseZ.append("\(keywords1.count / 3)")
         itemsZ = "\(((String(cString:[81,0], encoding: .utf8)!) == offsety ? messages_.count : offsety.count))"
         reply3 -= Double(communityH.count / (Swift.max(3, 2)))
      throw_r_ = [1 & messages_.count]
      if throw_r_.count == 625407 {
         break
      }
   } while (5 < (2 * throw_r_.count) || 2 < throw_r_.count) && (throw_r_.count == 625407)

   if !feelingsQ {
      feelingsQ = feelingsQ && throw_r_.count > 10
   }
        let controlController = UIAlertController(
            title: "Log out?",
            message: "Are you sure you want to log out of this account?",
            preferredStyle: .alert
        )
        controlController.addAction(UIAlertAction(title: "Cancel", style: .cancel))
        controlController.addAction(UIAlertAction(title: "Log out", style: .destructive) { [weak self] _ in
            self?.confirmLogOut()
        })
        present(controlController, animated: true)
    }

@discardableResult
 func translationTitleBecomeAxisReplyLayout(yesVisible: Double) -> Bool {
    var guideF: [String: Any]! = [String(cString: [114,101,100,100,105,116,0], encoding: .utf8)!:88, String(cString: [97,99,116,105,118,97,116,105,111,110,0], encoding: .utf8)!:4, String(cString: [109,112,99,100,97,116,97,0], encoding: .utf8)!:5]
    var register__8: String! = String(cString: [110,111,110,98,108,111,99,107,0], encoding: .utf8)!
    _ = register__8
    var release_sg: Bool = true
   withUnsafeMutablePointer(to: &release_sg) { pointer in
          _ = pointer.pointee
   }
       var overlayf: Int = 5
       _ = overlayf
      for _ in 0 ..< 1 {
         overlayf >>= Swift.min(labs(overlayf & overlayf), 4)
      }
       var s_centerU: Bool = false
         s_centerU = !s_centerU
      register__8 = "\(overlayf)"
   repeat {
      register__8 = "\(register__8.count % (Swift.max(8, guideF.values.count)))"
      if 1524516 == register__8.count {
         break
      }
   } while (!register__8.hasPrefix("\(release_sg)")) && (1524516 == register__8.count)
   while (!release_sg) {
      release_sg = register__8.count >= 93 || !release_sg
      break
   }
   return release_sg

}





    private func confirmLogOut() {

         let bitpackedDismissed: Bool = translationTitleBecomeAxisReplyLayout(yesVisible:16.0)

      if !bitpackedDismissed {
          print("ok")
      }

_ = bitpackedDismissed


       var commentsC: String! = String(cString: [116,111,116,97,108,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &commentsC) { pointer in
    
   }
    var emotionsS: Int = 0
   withUnsafeMutablePointer(to: &emotionsS) { pointer in
    
   }
   for _ in 0 ..< 3 {
      emotionsS += ((String(cString:[89,0], encoding: .utf8)!) == commentsC ? commentsC.count : emotionsS)
   }

      emotionsS += 3 / (Swift.max(1, commentsC.count))
        SocLoginCache.markLoggedOut()
        showLoginPage()
    }

    @objc private func deleteAccount() {
       var postsd: Double = 4.0
   for _ in 0 ..< 2 {
      postsd -= Double(1)
   }

        let controlController = UIAlertController(
            title: "Delete account?",
            message: "This will clear your account data and cached content. This action cannot be undone.",
            preferredStyle: .alert
        )
        controlController.addAction(UIAlertAction(title: "Cancel", style: .cancel))
        controlController.addAction(UIAlertAction(title: "Delete", style: .destructive) { [weak self] _ in
            self?.confirmDeleteAccount()
        })
        present(controlController, animated: true)
    }

@discardableResult
 func anxiousUnknownQualityView(reuseResult: Double, size_6Star: [String: Any]!) -> UIView! {
    var passwordC: Float = 4.0
    var willP: String! = String(cString: [115,101,99,112,107,0], encoding: .utf8)!
      passwordC += Float(3)
   for _ in 0 ..< 3 {
       var avatar7: Float = 5.0
      withUnsafeMutablePointer(to: &avatar7) { pointer in
    
      }
       var durationN: String! = String(cString: [101,118,112,111,114,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &durationN) { pointer in
             _ = pointer.pointee
      }
       var dim9: [String: Any]! = [String(cString: [97,112,112,114,111,118,101,114,0], encoding: .utf8)!:String(cString: [115,113,114,116,0], encoding: .utf8)!, String(cString: [117,110,108,105,110,107,0], encoding: .utf8)!:String(cString: [118,111,105,112,0], encoding: .utf8)!]
      while (4 <= (dim9.values.count ^ durationN.count)) {
          var contact0: Int = 0
          var arlang: Double = 2.0
         withUnsafeMutablePointer(to: &arlang) { pointer in
    
         }
         dim9[durationN] = (Int(arlang > 346046023.0 || arlang < -346046023.0 ? 8.0 : arlang))
         contact0 %= Swift.max(2, durationN.count / 2)
         break
      }
          var homeb: String! = String(cString: [112,111,121,116,109,0], encoding: .utf8)!
         avatar7 -= Float(1 / (Swift.max(5, dim9.count)))
         homeb.append("\((2 << (Swift.min(3, labs(Int(avatar7 > 93400801.0 || avatar7 < -93400801.0 ? 17.0 : avatar7))))))")
       var seedQ: [String: Any]! = [String(cString: [115,119,105,99,104,0], encoding: .utf8)!:49, String(cString: [99,104,97,110,110,101,108,0], encoding: .utf8)!:100, String(cString: [118,100,101,99,0], encoding: .utf8)!:99]
       _ = seedQ
       var j_bounds2: [String: Any]! = [String(cString: [115,121,110,116,104,102,105,108,116,0], encoding: .utf8)!:39.0]
      withUnsafeMutablePointer(to: &j_bounds2) { pointer in
    
      }
      while (durationN.contains("\(dim9.count)")) {
         dim9 = ["\(j_bounds2.values.count)": j_bounds2.keys.count]
         break
      }
          var x_countn: Double = 5.0
         j_bounds2["\(durationN)"] = 2
         x_countn += Double(3)
         avatar7 /= Swift.max((Float(Int(avatar7 > 369933999.0 || avatar7 < -369933999.0 ? 22.0 : avatar7))), 2)
      for _ in 0 ..< 2 {
          var bard: Int = 5
          var tasko: [String: Any]! = [String(cString: [97,116,116,101,109,112,116,0], encoding: .utf8)!:UILabel(frame:CGRect.zero)]
          _ = tasko
          var throwingx: String! = String(cString: [118,105,101,119,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &throwingx) { pointer in
    
         }
          var messagesy: String! = String(cString: [118,97,114,0], encoding: .utf8)!
          _ = messagesy
          var providerZ: String! = String(cString: [114,101,99,105,112,114,111,99,97,108,0], encoding: .utf8)!
         durationN = "\(messagesy.count << (Swift.min(labs(2), 4)))"
         bard += 1
         tasko[messagesy] = ((String(cString:[89,0], encoding: .utf8)!) == messagesy ? tasko.keys.count : messagesy.count)
         throwingx.append("\(((String(cString:[119,0], encoding: .utf8)!) == durationN ? durationN.count : seedQ.keys.count))")
         providerZ.append("\(dim9.count)")
      }
      while (dim9.keys.contains("\(seedQ.keys.count)")) {
         dim9["\(avatar7)"] = 1
         break
      }
          var bubblez: Bool = false
         withUnsafeMutablePointer(to: &bubblez) { pointer in
    
         }
         seedQ = ["\(dim9.count)": dim9.keys.count]
      willP = "\((willP == (String(cString:[107,0], encoding: .utf8)!) ? willP.count : durationN.count))"
   }
     var placeholderBundle: String! = String(cString: [118,105,111,108,97,116,105,111,110,0], encoding: .utf8)!
     var postBack: Bool = true
     let backBottle: Int = 69
    var cauterizeCounting = UIView(frame:CGRect(x: 306, y: 366, width: 0, height: 0))
    cauterizeCounting.alpha = 0.5;
    cauterizeCounting.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    cauterizeCounting.frame = CGRect(x: 146, y: 170, width: 0, height: 0)

    
    return cauterizeCounting

}





    private func confirmDeleteAccount() {

         let racingCong: UIView! = anxiousUnknownQualityView(reuseResult:29.0, size_6Star:[String(cString: [111,103,103,118,111,114,98,105,115,0], encoding: .utf8)!:43, String(cString: [104,116,109,108,115,117,98,116,105,116,108,101,115,0], encoding: .utf8)!:9])

      if racingCong != nil {
          let racingCong_tag = racingCong.tag
          self.view.addSubview(racingCong)
      }
      else {
          print("racingCong is nil")      }

_ = racingCong


       var scene_va: [Any]! = [String(cString: [105,109,101,114,0], encoding: .utf8)!, String(cString: [100,114,111,112,120,0], encoding: .utf8)!]
      scene_va.append(3 + scene_va.count)

        SocFollowProfile.clearAll { [weak self] in
            self?.showLoginPage()
        }
    }

    private func showLoginPage() {
       var placeholderq: [Any]! = [96, 4, 62]
   repeat {
      placeholderq = [1]
      if placeholderq.count == 2451833 {
         break
      }
   } while (placeholderq.count == 2451833) && (!placeholderq.contains { $0 as? Int == placeholderq.count })

        let window_wController = SocPackageSceneController()
        view.window?.rootViewController = window_wController
        view.window?.makeKeyAndVisible()
    }

}
