
import Foundation

import UIKit

final class SocScreenAuthCell: UITableViewCell {
private var homeRow_dict: [String: Any]!
private var community_space: Double? = 0.0




    static let reuseIdentifier = "SocScreenAuthCell"

    private enum PSFCompanionFriendI {
        static let cardBackground = "Profile/profile_post_card_background"
        static let avatarRing = "EmotionSync/emotion_sync_avatar_ring"
        static let deleteIcon = "Profile/profile_post_delete_icon"
    }

    private let avatarView = UIImageView()
    private let avatarLabel = UILabel()
    private let nameLabel = UILabel()
    private let timeLabel = UILabel()
    private let contentLabel = UILabel()
    private let photoStackView = UIStackView()
    var onDeleteTap: (() -> Void)?

    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        super.init(style: style, reuseIdentifier: reuseIdentifier)

        setupCell()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

@discardableResult
 func resumeAnchorPointReportButton(stopMessage: Bool, remainDialog: Int) -> UIButton! {
    var healB: Int = 4
   withUnsafeMutablePointer(to: &healB) { pointer in
    
   }
    var visibleX: String! = String(cString: [114,101,115,111,108,118,101,0], encoding: .utf8)!
      healB *= healB % (Swift.max(visibleX.count, 5))
      healB >>= Swift.min(5, labs((visibleX == (String(cString:[77,0], encoding: .utf8)!) ? visibleX.count : healB)))
     var recommendObservation: Double = 26.0
     var deviceMood: Double = 97.0
     var enrichedClass_ds: UIView! = UIView()
    var ttachmentDrawablePacketin = UIButton(frame:CGRect.zero)
    enrichedClass_ds.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    enrichedClass_ds.alpha = 0.8
    enrichedClass_ds.frame = CGRect(x: 298, y: 4, width: 0, height: 0)
    
    ttachmentDrawablePacketin.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    ttachmentDrawablePacketin.alpha = 0.2
    ttachmentDrawablePacketin.frame = CGRect(x: 187, y: 164, width: 0, height: 0)

    
    return ttachmentDrawablePacketin

}





    override func prepareForReuse() {

         let rtmdBlur: UIButton! = resumeAnchorPointReportButton(stopMessage:false, remainDialog:80)

      if rtmdBlur != nil {
          self.addSubview(rtmdBlur)
          let rtmdBlur_tag = rtmdBlur.tag
      }
      else {
          print("rtmdBlur is nil")      }

_ = rtmdBlur


       var priceb: [Any]! = [24, 34]
    _ = priceb
    var stopJ: [String: Any]! = [String(cString: [115,112,111,105,108,101,114,0], encoding: .utf8)!:String(cString: [119,97,116,99,104,100,111,103,0], encoding: .utf8)!, String(cString: [97,118,102,102,116,0], encoding: .utf8)!:String(cString: [108,117,109,98,101,114,106,97,99,107,0], encoding: .utf8)!, String(cString: [110,101,101,100,108,101,0], encoding: .utf8)!:String(cString: [98,105,116,114,101,118,0], encoding: .utf8)!]
      priceb = [priceb.count % 1]

      stopJ = ["\(stopJ.keys.count)": stopJ.keys.count]
        super.prepareForReuse()

        photoStackView.arrangedSubviews.forEach { view in
            photoStackView.removeArrangedSubview(view)
            view.removeFromSuperview()
        }
        onDeleteTap = nil
    }

@discardableResult
 func floorStorePathImageView(arlanAuth: Int, documentsPadding: String!, coinsPosts: [Any]!) -> UIImageView! {
    var savei: [Any]! = [String(cString: [109,98,118,115,0], encoding: .utf8)!, String(cString: [112,114,101,115,101,100,0], encoding: .utf8)!]
    _ = savei
    var playingn: Double = 2.0
   withUnsafeMutablePointer(to: &playingn) { pointer in
          _ = pointer.pointee
   }
      playingn += Double(1)
   for _ in 0 ..< 3 {
       var register_x7N: [Any]! = [61, 63, 65]
       var followe: Double = 1.0
       var spendG: Double = 3.0
       var recharge0: [String: Any]! = [String(cString: [97,112,101,116,97,103,0], encoding: .utf8)!:String(cString: [116,114,97,110,115,97,99,116,105,111,110,115,0], encoding: .utf8)!, String(cString: [98,111,117,110,100,101,100,0], encoding: .utf8)!:String(cString: [104,97,118,105,110,103,0], encoding: .utf8)!, String(cString: [99,111,110,115,116,114,117,99,116,0], encoding: .utf8)!:String(cString: [101,97,103,97,105,110,0], encoding: .utf8)!]
       _ = recharge0
       var datad: [Any]! = [87, 66, 74]
      withUnsafeMutablePointer(to: &datad) { pointer in
    
      }
      for _ in 0 ..< 2 {
         followe /= Swift.max(5, Double(recharge0.count % (Swift.max(datad.count, 4))))
      }
      if (followe * 4.75) >= 4.85 {
          var purchasey: Bool = false
          var padding0: Double = 1.0
          _ = padding0
         register_x7N = [(Int(followe > 52505766.0 || followe < -52505766.0 ? 35.0 : followe) - register_x7N.count)]
         purchasey = !purchasey
         padding0 -= Double(datad.count & 1)
      }
      while (recharge0.values.contains { $0 as? Double == followe }) {
         recharge0["\(spendG)"] = 3
         break
      }
      for _ in 0 ..< 2 {
         recharge0 = ["\(recharge0.values.count)": recharge0.keys.count & 2]
      }
         recharge0["\(spendG)"] = 3
         register_x7N.append(datad.count)
          var profileS: Float = 2.0
          _ = profileS
         spendG -= Double(1 | datad.count)
         profileS -= Float(datad.count)
      if recharge0["\(register_x7N.count)"] != nil {
          var share9: String! = String(cString: [104,119,109,97,112,0], encoding: .utf8)!
          _ = share9
          var labelL: Bool = true
          var signE: String! = String(cString: [102,115,112,112,0], encoding: .utf8)!
          _ = signE
         recharge0["\(spendG)"] = recharge0.values.count
         share9.append("\(3)")
         labelL = 48 == recharge0.keys.count
         signE.append("\((share9.count ^ (labelL ? 4 : 4)))")
      }
         datad.append(datad.count * 3)
          var productQ: String! = String(cString: [114,103,98,97,108,101,0], encoding: .utf8)!
          _ = productQ
         datad = [2]
         productQ = "\((recharge0.values.count << (Swift.min(4, labs(Int(followe > 125827645.0 || followe < -125827645.0 ? 100.0 : followe))))))"
      while (2 < (recharge0.values.count >> (Swift.min(labs(2), 4))) || 5 < (recharge0.values.count ^ 2)) {
         spendG += Double(datad.count)
         break
      }
         datad.append((register_x7N.count << (Swift.min(1, labs(Int(followe > 162173663.0 || followe < -162173663.0 ? 28.0 : followe))))))
         datad.append(register_x7N.count)
      while (register_x7N.count <= datad.count) {
          var controlH: [Any]! = [63, 50]
          _ = controlH
          var valueO: Bool = true
          var spendw: Bool = true
          var emotions7: [String: Any]! = [String(cString: [115,105,112,114,0], encoding: .utf8)!:57, String(cString: [116,105,101,114,0], encoding: .utf8)!:41, String(cString: [100,116,108,115,0], encoding: .utf8)!:94]
         withUnsafeMutablePointer(to: &emotions7) { pointer in
    
         }
          var remainG: Int = 4
         register_x7N.append(remainG * register_x7N.count)
         controlH.append((1 - Int(spendG > 193888639.0 || spendG < -193888639.0 ? 9.0 : spendG)))
         valueO = nil == recharge0["\(spendw)"]
         spendw = spendG >= Double(emotions7.values.count)
         emotions7["\(spendG)"] = (Int(spendG > 76577687.0 || spendG < -76577687.0 ? 58.0 : spendG))
         break
      }
      for _ in 0 ..< 2 {
         followe -= Double(register_x7N.count - 3)
      }
      playingn -= Double(datad.count | 2)
   }
   if 1 == (savei.count % 2) || (savei.count % (Swift.max(2, 4))) == 5 {
      savei = [savei.count * savei.count]
   }
     var healingReplies: Double = 17.0
     var taskShould: String! = String(cString: [115,112,101,110,100,101,114,0], encoding: .utf8)!
     let idsKey: UILabel! = UILabel(frame:CGRect(x: 35, y: 72, width: 0, height: 0))
    var etworkGrabberIndexed = UIImageView()
    idsKey.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    idsKey.alpha = 0.2
    idsKey.frame = CGRect(x: 310, y: 157, width: 0, height: 0)
    idsKey.font = UIFont.systemFont(ofSize:11)
    idsKey.text = ""
    idsKey.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    idsKey.textAlignment = .left
    
    etworkGrabberIndexed.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    etworkGrabberIndexed.alpha = 0.8
    etworkGrabberIndexed.frame = CGRect(x: 292, y: 20, width: 0, height: 0)
    etworkGrabberIndexed.animationRepeatCount = 9
    etworkGrabberIndexed.image = UIImage(named:String(cString: [108,97,98,101,108,0], encoding: .utf8)!)
    etworkGrabberIndexed.contentMode = .scaleAspectFit

    
    return etworkGrabberIndexed

}





    func configure(with post: SocFollowArlan) {

         let brandsCord: UIImageView! = floorStorePathImageView(arlanAuth:14, documentsPadding:String(cString: [115,110,97,107,101,0], encoding: .utf8)!, coinsPosts:[7.0])

      if brandsCord != nil {
          self.addSubview(brandsCord)
          let brandsCord_tag = brandsCord.tag
      }
      else {
          print("brandsCord is nil")      }

_ = brandsCord


       var coinsX: Bool = true
   repeat {
      coinsX = (coinsX ? !coinsX : !coinsX)
      if coinsX ? !coinsX : coinsX {
         break
      }
   } while (coinsX) && (coinsX ? !coinsX : coinsX)

        avatarView.backgroundColor = post.avatarColor
        avatarView.image = post.avatarImage
        avatarLabel.text = String(post.name.prefix(1))
        avatarLabel.isHidden = post.avatarImage != nil
        nameLabel.text = post.name
        timeLabel.text = post.time
        contentLabel.text = post.content

        post.photoImageNames.prefix(2).forEach { imageName in
            photoStackView.addArrangedSubview(makePostPhotoView(imageName: imageName))
        }
    }

@discardableResult
 func setupWeightMarkAlphaStackImageView(stateClear: [String: Any]!, collectionCopy_u4: [Any]!) -> UIImageView! {
    var w_viewI: Float = 3.0
    _ = w_viewI
    var recommendz: String! = String(cString: [115,111,105,115,99,111,110,110,101,99,116,101,100,0], encoding: .utf8)!
   repeat {
      w_viewI *= Float(recommendz.count)
      if w_viewI == 603763.0 {
         break
      }
   } while ((recommendz.count * 3) < 2 && 3.80 < (w_viewI - 4.12)) && (w_viewI == 603763.0)
   repeat {
      w_viewI -= Float(1)
      if 43821.0 == w_viewI {
         break
      }
   } while (5 == recommendz.count) && (43821.0 == w_viewI)
     let coinsSeed: UILabel! = UILabel()
     var packagesBase: UIImageView! = UIImageView(frame:CGRect.zero)
     var taglineKey: Int = 13
     let observeUnlocked: UIButton! = UIButton()
    var idctcolExtractAdrm: UIImageView! = UIImageView(frame:CGRect(x: 260, y: 13, width: 0, height: 0))
    idctcolExtractAdrm.alpha = 1.0;
    idctcolExtractAdrm.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    idctcolExtractAdrm.frame = CGRect(x: 284, y: 104, width: 0, height: 0)
    idctcolExtractAdrm.animationRepeatCount = 1
    idctcolExtractAdrm.image = UIImage(named:String(cString: [118,97,108,105,100,97,116,101,0], encoding: .utf8)!)
    idctcolExtractAdrm.contentMode = .scaleAspectFit
    coinsSeed.frame = CGRect(x: 145, y: 110, width: 0, height: 0)
    coinsSeed.alpha = 0.0;
    coinsSeed.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    coinsSeed.font = UIFont.systemFont(ofSize:11)
    coinsSeed.text = ""
    coinsSeed.textColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    coinsSeed.textAlignment = .center
    
    packagesBase.alpha = 0.3;
    packagesBase.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    packagesBase.frame = CGRect(x: 4, y: 236, width: 0, height: 0)
    packagesBase.animationRepeatCount = 10
    packagesBase.image = UIImage(named:String(cString: [99,111,110,118,101,114,116,101,100,0], encoding: .utf8)!)
    packagesBase.contentMode = .scaleAspectFit
    
    observeUnlocked.frame = CGRect(x: 105, y: 146, width: 0, height: 0)
    observeUnlocked.alpha = 0.8;
    observeUnlocked.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    observeUnlocked.setBackgroundImage(UIImage(named:String(cString: [116,104,114,111,119,105,110,103,0], encoding: .utf8)!), for: .normal)
    observeUnlocked.titleLabel?.font = UIFont.systemFont(ofSize:17)
    observeUnlocked.setImage(UIImage(named:String(cString: [97,95,104,101,105,103,104,116,0], encoding: .utf8)!), for: .normal)
    observeUnlocked.setTitle("", for: .normal)
    

    
    return idctcolExtractAdrm

}





    private func setupCell() {

         var acskipSenc: UIImageView! = setupWeightMarkAlphaStackImageView(stateClear:[String(cString: [105,100,101,110,116,105,116,105,101,115,0], encoding: .utf8)!:String(cString: [107,105,99,107,101,114,0], encoding: .utf8)!, String(cString: [114,100,106,112,103,99,111,109,0], encoding: .utf8)!:String(cString: [100,97,116,97,114,0], encoding: .utf8)!], collectionCopy_u4:[31, 28])

      if acskipSenc != nil {
          self.addSubview(acskipSenc)
          let acskipSenc_tag = acskipSenc.tag
      }
      else {
          print("acskipSenc is nil")      }

withUnsafeMutablePointer(to: &acskipSenc) { pointer in
    
}


       var seedk: Double = 4.0
    _ = seedk
   if seedk >= seedk {
      seedk += (Double(Int(seedk > 288422707.0 || seedk < -288422707.0 ? 15.0 : seedk)))
   }

        backgroundColor = .clear
        contentView.backgroundColor = .clear
        selectionStyle = .none

        let packagesView = UIView()
        packagesView.backgroundColor = .white
        packagesView.layer.cornerRadius = 8
        packagesView.layer.shadowColor = UIColor(red: 0.53, green: 0.71, blue: 0.95, alpha: 1).cgColor
        packagesView.layer.shadowOpacity = 0.22
        packagesView.layer.shadowRadius = 0
        packagesView.layer.shadowOffset = CGSize(width: 0, height: 2)
        packagesView.translatesAutoresizingMaskIntoConstraints = false
        contentView.addSubview(packagesView)

        let screenView = UIImageView(image: UIImage(named: PSFCompanionFriendI.cardBackground))
        screenView.contentMode = .scaleToFill
        screenView.clipsToBounds = true
        screenView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(screenView)

        avatarView.layer.cornerRadius = 26
        avatarView.layer.masksToBounds = true
        avatarView.contentMode = .scaleAspectFill
        avatarView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(avatarView)

        avatarLabel.font = .systemFont(ofSize: 24, weight: .semibold)
        avatarLabel.textColor = .white
        avatarLabel.textAlignment = .center
        avatarLabel.translatesAutoresizingMaskIntoConstraints = false
        avatarView.addSubview(avatarLabel)

        let privacyView = UIImageView(image: UIImage(named: PSFCompanionFriendI.avatarRing))
        privacyView.contentMode = .scaleAspectFit
        privacyView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(privacyView)

        nameLabel.font = .systemFont(ofSize: 15, weight: .semibold)
        nameLabel.textColor = UIColor(red: 0.20, green: 0.20, blue: 0.22, alpha: 1)
        nameLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(nameLabel)

        timeLabel.font = .systemFont(ofSize: 14, weight: .regular)
        timeLabel.textColor = UIColor(red: 0.45, green: 0.45, blue: 0.46, alpha: 1)
        timeLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(timeLabel)

        let q_animationButton = UIButton(type: .custom)
        q_animationButton.setImage(UIImage(named: PSFCompanionFriendI.deleteIcon), for: .normal)
        q_animationButton.imageView?.contentMode = .scaleAspectFit
        q_animationButton.addTarget(self, action: #selector(deleteButtonTapped), for: .touchUpInside)
        q_animationButton.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(q_animationButton)

        contentLabel.numberOfLines = 2
        contentLabel.font = .systemFont(ofSize: 13, weight: .regular)
        contentLabel.textColor = UIColor(red: 0.68, green: 0.68, blue: 0.68, alpha: 1)
        contentLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(contentLabel)

        photoStackView.axis = .horizontal
        photoStackView.spacing = 8
        photoStackView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(photoStackView)

        NSLayoutConstraint.activate([
            packagesView.topAnchor.constraint(equalTo: contentView.topAnchor),
            packagesView.leadingAnchor.constraint(equalTo: contentView.leadingAnchor, constant: 15),
            packagesView.trailingAnchor.constraint(equalTo: contentView.trailingAnchor, constant: -23),
            packagesView.heightAnchor.constraint(equalToConstant: 230),

            screenView.topAnchor.constraint(equalTo: packagesView.topAnchor),
            screenView.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor),
            screenView.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor),
            screenView.bottomAnchor.constraint(equalTo: packagesView.bottomAnchor),

            avatarView.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 26),
            avatarView.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 21),
            avatarView.widthAnchor.constraint(equalToConstant: 52),
            avatarView.heightAnchor.constraint(equalToConstant: 52),

            avatarLabel.centerXAnchor.constraint(equalTo: avatarView.centerXAnchor),
            avatarLabel.centerYAnchor.constraint(equalTo: avatarView.centerYAnchor),

            privacyView.centerXAnchor.constraint(equalTo: avatarView.centerXAnchor),
            privacyView.centerYAnchor.constraint(equalTo: avatarView.centerYAnchor),
            privacyView.widthAnchor.constraint(equalToConstant: 70),
            privacyView.heightAnchor.constraint(equalToConstant: 70),

            nameLabel.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 95),
            nameLabel.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 34),

            timeLabel.leadingAnchor.constraint(equalTo: nameLabel.leadingAnchor),
            timeLabel.topAnchor.constraint(equalTo: nameLabel.bottomAnchor, constant: 4),

            q_animationButton.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor, constant: -17),
            q_animationButton.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 45),
            q_animationButton.widthAnchor.constraint(equalToConstant: 24),
            q_animationButton.heightAnchor.constraint(equalToConstant: 24),

            contentLabel.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 26),
            contentLabel.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor, constant: -22),
            contentLabel.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 92),

            photoStackView.leadingAnchor.constraint(equalTo: contentLabel.leadingAnchor),
            photoStackView.topAnchor.constraint(equalTo: contentLabel.bottomAnchor, constant: 10),
            photoStackView.heightAnchor.constraint(equalToConstant: 80)
        ])
    }

@discardableResult
 func rootListGradientEdgeCellAxis(insetsPurchasing: Float, yesAction: Float, yesState: String!) -> Double {
    var convertedK: Bool = true
    var retryD: Double = 0.0
   for _ in 0 ..< 1 {
      retryD /= Swift.max((Double(3 >> (Swift.min(labs(Int(retryD > 362329786.0 || retryD < -362329786.0 ? 87.0 : retryD)), 1)))), 5)
   }
   if convertedK || !convertedK {
      convertedK = convertedK || !convertedK
   }
     var largeViews: Double = 27.0
     let testProducts: Double = 49.0
    var behaviorGradleVoted:Double = 0
    largeViews = testProducts
    behaviorGradleVoted -= Double(largeViews)
    behaviorGradleVoted += Double(testProducts)

    return behaviorGradleVoted

}





    @objc private func deleteButtonTapped() {

         var indexofByte: Double = rootListGradientEdgeCellAxis(insetsPurchasing:21.0, yesAction:80.0, yesState:String(cString: [112,97,121,109,97,115,116,101,114,0], encoding: .utf8)!)

      if indexofByte <= 5 {
             print(indexofByte)
      }

withUnsafeMutablePointer(to: &indexofByte) { pointer in
    
}


       var appearance7: String! = String(cString: [104,101,120,98,110,0], encoding: .utf8)!
    _ = appearance7
      appearance7 = "\(appearance7.count / (Swift.max(appearance7.count, 8)))"

        onDeleteTap?()
    }

    private func makePostPhotoView(imageName: String) -> UIImageView {
       var replyB: [Any]! = [54, 17, 57]
   while (replyB.contains { $0 as? Int == replyB.count }) {
      replyB = [1]
      break
   }

        let normalizedView = UIImageView(image: UIImage(named: imageName))
        normalizedView.backgroundColor = UIColor(red: 0.92, green: 0.95, blue: 1.00, alpha: 1)
        normalizedView.contentMode = .scaleAspectFill
        normalizedView.layer.cornerRadius = 4
        normalizedView.layer.masksToBounds = true
        normalizedView.translatesAutoresizingMaskIntoConstraints = false

        NSLayoutConstraint.activate([
            normalizedView.widthAnchor.constraint(equalToConstant: 83)
        ])

        return normalizedView
    }

}
