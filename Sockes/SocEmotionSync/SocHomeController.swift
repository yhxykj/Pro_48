
import Foundation

import UIKit

final class SocHomeController: UIViewController {
private var error_margin: Double? = 0.0
private var summaryHealStr: String!




    private enum PSFCompanionFriendI {
        static let backIcon = "Common/common_back_icon"
        static let background = "EmotionSync/ShareEmotions/share_emotions_background"
        static let cardBackground = "EmotionSync/emotion_sync_post_card_background"
        static let avatarRing = "EmotionSync/emotion_sync_avatar_ring"
        static let mailButton = "EmotionSync/emotion_sync_mail_button"
        static let videoButton = "EmotionSync/emotion_sync_video_button"
        static let alertIcon = "EmotionSync/PostDetail/post_detail_alert_icon"
        static let likeNormal = "EmotionSync/PostDetail/post_detail_like_normal"
        static let likeSelected = "EmotionSync/PostDetail/post_detail_like_selected"
    }

    private let post: SocScene
    private let likeButton = UIButton(type: .custom)
    private let commentBarView = UIView()

    init(post: SocScene) {
        self.post = post
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

@discardableResult
 func photoFromUnlockSincePatternSourceImageView(messageHandle: Bool) -> UIImageView! {
    var recommendV: Int = 4
    var default_d3y: String! = String(cString: [112,97,99,107,101,100,0], encoding: .utf8)!
   if 5 <= (recommendV >> (Swift.min(default_d3y.count, 4))) {
      recommendV -= default_d3y.count
   }
   repeat {
       var modityf: String! = String(cString: [110,101,120,116,104,111,112,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &modityf) { pointer in
             _ = pointer.pointee
      }
       var promptv: String! = String(cString: [107,101,121,112,97,116,104,0], encoding: .utf8)!
       _ = promptv
       var scene_uk: String! = String(cString: [119,114,97,112,112,105,110,103,0], encoding: .utf8)!
      repeat {
         promptv = "\(promptv.count)"
         if 4378092 == promptv.count {
            break
         }
      } while (4378092 == promptv.count) && (scene_uk == String(cString:[116,0], encoding: .utf8)!)
         scene_uk.append("\(modityf.count)")
      for _ in 0 ..< 3 {
         promptv = "\(promptv.count)"
      }
          var c_countH: Float = 5.0
          var controlB: String! = String(cString: [115,104,111,119,119,97,118,101,115,0], encoding: .utf8)!
         modityf = "\(((String(cString:[106,0], encoding: .utf8)!) == controlB ? controlB.count : Int(c_countH > 315444735.0 || c_countH < -315444735.0 ? 37.0 : c_countH)))"
       var conversationn: Bool = false
       var areal: Bool = true
         areal = scene_uk.count <= 94
      while (promptv != String(cString:[74,0], encoding: .utf8)!) {
         scene_uk = "\(2 & modityf.count)"
         break
      }
         conversationn = (String(cString:[102,0], encoding: .utf8)!) == scene_uk
       var recommendJ: String! = String(cString: [112,114,101,102,105,120,101,115,0], encoding: .utf8)!
         recommendJ.append("\(recommendJ.count)")
      recommendV &= 1 | promptv.count
      if recommendV == 2876258 {
         break
      }
   } while (5 >= (recommendV / (Swift.max(default_d3y.count, 8))) && (default_d3y.count / (Swift.max(3, recommendV))) >= 5) && (recommendV == 2876258)
     let unlockedUnlock: UILabel! = UILabel()
     var deviceSimo: UIButton! = UIButton(frame:CGRect.zero)
     var panelController: UIView! = UIView()
     let becomeDelete_ex: [Any]! = [String(cString: [108,105,98,101,114,116,121,0], encoding: .utf8)!]
    var notifiedSensitivity = UIImageView()
    notifiedSensitivity.contentMode = .scaleAspectFit
    notifiedSensitivity.animationRepeatCount = 7
    notifiedSensitivity.image = UIImage(named:String(cString: [114,101,99,104,97,114,103,101,0], encoding: .utf8)!)
    notifiedSensitivity.alpha = 0.7;
    notifiedSensitivity.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    notifiedSensitivity.frame = CGRect(x: 172, y: 76, width: 0, height: 0)
    unlockedUnlock.alpha = 0.6;
    unlockedUnlock.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    unlockedUnlock.frame = CGRect(x: 304, y: 167, width: 0, height: 0)
    unlockedUnlock.textAlignment = .center
    unlockedUnlock.font = UIFont.systemFont(ofSize:11)
    unlockedUnlock.text = ""
    unlockedUnlock.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    deviceSimo.alpha = 0.7;
    deviceSimo.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    deviceSimo.frame = CGRect(x: 117, y: 192, width: 0, height: 0)
    deviceSimo.titleLabel?.font = UIFont.systemFont(ofSize:17)
    deviceSimo.setImage(UIImage(named:String(cString: [112,97,115,115,119,111,114,100,0], encoding: .utf8)!), for: .normal)
    deviceSimo.setTitle("", for: .normal)
    deviceSimo.setBackgroundImage(UIImage(named:String(cString: [115,116,111,114,101,0], encoding: .utf8)!), for: .normal)
    
    panelController.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    panelController.alpha = 0.3
    panelController.frame = CGRect(x: 226, y: 142, width: 0, height: 0)
    

    
    return notifiedSensitivity

}





    override func viewDidLoad() {

         var loudspeakerSearch: UIImageView! = photoFromUnlockSincePatternSourceImageView(messageHandle:true)

      if loudspeakerSearch != nil {
          let loudspeakerSearch_tag = loudspeakerSearch.tag
          self.view.addSubview(loudspeakerSearch)
      }

withUnsafeMutablePointer(to: &loudspeakerSearch) { pointer in
    
}


       var privacyp: Double = 3.0
       var storeW: String! = String(cString: [116,114,105,110,103,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         storeW.append("\(storeW.count)")
      }
         storeW.append("\(1)")
      for _ in 0 ..< 3 {
         storeW.append("\(storeW.count)")
      }
      privacyp *= (Double(Int(privacyp > 291649503.0 || privacyp < -291649503.0 ? 33.0 : privacyp) ^ 3))

        super.viewDidLoad()

        setupContent()
        setupKeyboardDismiss()
        setupKeyboardObserver()
    }

    deinit {
        NotificationCenter.default.removeObserver(self)
    }

@discardableResult
 func signRoleStyleButton(topEmail: Double, moodLaunch: Double) -> UIButton! {
    var j_viewh: Double = 2.0
    var urlT: Double = 5.0
    var flashC: Float = 3.0
    _ = flashC
      urlT -= (Double(Int(flashC > 10228256.0 || flashC < -10228256.0 ? 38.0 : flashC)))
   while (2.64 <= (j_viewh * j_viewh)) {
      j_viewh /= Swift.max((Double(Int(j_viewh > 167145829.0 || j_viewh < -167145829.0 ? 97.0 : j_viewh) | Int(j_viewh > 256556498.0 || j_viewh < -256556498.0 ? 45.0 : j_viewh))), 3)
      break
   }
     let tapScroll: UIImageView! = UIImageView(frame:CGRect.zero)
     let cameraMain: UIImageView! = UIImageView()
    var tokenizerGetpaddrsHeading:UIButton! = UIButton(frame:CGRect(x: 131, y: 434, width: 0, height: 0))
    tokenizerGetpaddrsHeading.setImage(UIImage(named:String(cString: [110,111,114,109,97,108,105,122,101,100,0], encoding: .utf8)!), for: .normal)
    tokenizerGetpaddrsHeading.setTitle("", for: .normal)
    tokenizerGetpaddrsHeading.setBackgroundImage(UIImage(named:String(cString: [108,111,97,100,0], encoding: .utf8)!), for: .normal)
    tokenizerGetpaddrsHeading.titleLabel?.font = UIFont.systemFont(ofSize:14)
    tokenizerGetpaddrsHeading.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    tokenizerGetpaddrsHeading.alpha = 0.6
    tokenizerGetpaddrsHeading.frame = CGRect(x: 239, y: 25, width: 0, height: 0)
    tapScroll.frame = CGRect(x: 125, y: 187, width: 0, height: 0)
    tapScroll.alpha = 0.9;
    tapScroll.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    tapScroll.animationRepeatCount = 3
    tapScroll.image = UIImage(named:String(cString: [112,114,111,109,112,116,0], encoding: .utf8)!)
    tapScroll.contentMode = .scaleAspectFit
    
    cameraMain.alpha = 0.4;
    cameraMain.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    cameraMain.frame = CGRect(x: 57, y: 223, width: 0, height: 0)
    cameraMain.image = UIImage(named:String(cString: [114,101,99,104,97,114,103,101,0], encoding: .utf8)!)
    cameraMain.contentMode = .scaleAspectFit
    cameraMain.animationRepeatCount = 0
    

    
    return tokenizerGetpaddrsHeading

}





    private func setupContent() {

         let revalidationTester: UIButton! = signRoleStyleButton(topEmail:19.0, moodLaunch:61.0)

      if revalidationTester != nil {
          let revalidationTester_tag = revalidationTester.tag
          self.view.addSubview(revalidationTester)
      }
      else {
          print("revalidationTester is nil")      }

_ = revalidationTester


       var recommendt: String! = String(cString: [116,101,120,116,117,114,101,115,0], encoding: .utf8)!
       var healC: Bool = false
       _ = healC
      repeat {
          var keyboard7: String! = String(cString: [115,117,98,112,97,121,108,111,97,100,0], encoding: .utf8)!
          var becomeY: String! = String(cString: [114,101,117,115,101,0], encoding: .utf8)!
          var homey: String! = String(cString: [102,111,117,114,105,101,114,0], encoding: .utf8)!
          _ = homey
         healC = !healC
         keyboard7.append("\((becomeY == (String(cString:[102,0], encoding: .utf8)!) ? (healC ? 3 : 1) : becomeY.count))")
         homey.append("\(becomeY.count)")
         if healC ? !healC : healC {
            break
         }
      } while (!healC || !healC) && (healC ? !healC : healC)
      for _ in 0 ..< 1 {
          var presenterp: String! = String(cString: [113,117,101,117,101,115,0], encoding: .utf8)!
          var followingJ: [Any]! = [String(cString: [110,101,119,108,105,110,101,115,0], encoding: .utf8)!, String(cString: [99,108,101,97,110,117,112,0], encoding: .utf8)!]
          var buttonW: [Any]! = [46, 99]
         healC = !healC
         presenterp.append("\(3 * presenterp.count)")
         followingJ = [2 + presenterp.count]
         buttonW.append(3)
      }
      repeat {
         healC = !healC
         if healC ? !healC : healC {
            break
         }
      } while (!healC) && (healC ? !healC : healC)
      recommendt = "\(recommendt.count)"

        view.backgroundColor = UIColor(red: 0.95, green: 0.96, blue: 0.98, alpha: 1)

        let class_v1View = UIView()
        class_v1View.backgroundColor = UIColor(red: 0.98, green: 0.98, blue: 0.98, alpha: 1)
        class_v1View.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(class_v1View)

        let arlanView = UIImageView(image: UIImage(named: PSFCompanionFriendI.background))
        arlanView.contentMode = .scaleToFill
        arlanView.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(arlanView)

        let insufficientButton = UIButton(type: .custom)
        insufficientButton.setImage(UIImage(named: PSFCompanionFriendI.backIcon), for: .normal)
        insufficientButton.imageView?.contentMode = .scaleAspectFit
        insufficientButton.addTarget(self, action: #selector(closePage), for: .touchUpInside)
        insufficientButton.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(insufficientButton)

        let znewsLabel = UILabel()
        znewsLabel.text = "Share Emotions"
        znewsLabel.font = .systemFont(ofSize: 25, weight: .regular)
        znewsLabel.textColor = .black
        znewsLabel.textAlignment = .center
        znewsLabel.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(znewsLabel)

        let videoView = UIImageView(image: UIImage(named: PSFCompanionFriendI.alertIcon))
        videoView.contentMode = .scaleAspectFit
        videoView.isUserInteractionEnabled = true
        videoView.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(showTopReportMenu(_:))))
        videoView.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(videoView)

        let packagesView = makePostCard()
        class_v1View.addSubview(packagesView)

        let configurationLabel = UILabel()
        configurationLabel.text = "Comments"
        configurationLabel.font = .systemFont(ofSize: 17, weight: .semibold)
        configurationLabel.textColor = UIColor(red: 0.18, green: 0.18, blue: 0.20, alpha: 1)
        configurationLabel.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(configurationLabel)

        let progressView = makeCommentAvatarView()
        class_v1View.addSubview(progressView)

        let bottleLabel = UILabel()
        bottleLabel.text = "Williams"
        bottleLabel.font = .systemFont(ofSize: 16, weight: .semibold)
        bottleLabel.textColor = UIColor(red: 0.19, green: 0.19, blue: 0.20, alpha: 1)
        bottleLabel.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(bottleLabel)

        let packagesLabel = UILabel()
        packagesLabel.text = "Smart and handsome sunshine boy."
        packagesLabel.font = .systemFont(ofSize: 14, weight: .regular)
        packagesLabel.textColor = UIColor(red: 0.68, green: 0.68, blue: 0.68, alpha: 1)
        packagesLabel.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(packagesLabel)

        let usersButton = UIButton(type: .custom)
        usersButton.setImage(UIImage(named: PSFCompanionFriendI.alertIcon), for: .normal)
        usersButton.imageView?.contentMode = .scaleAspectFit
        usersButton.addTarget(self, action: #selector(showCommentReportMenu(_:)), for: .touchUpInside)
        usersButton.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(usersButton)

        commentBarView.backgroundColor = .white
        commentBarView.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(commentBarView)

        likeButton.setImage(UIImage(named: PSFCompanionFriendI.likeNormal), for: .normal)
        likeButton.setImage(UIImage(named: PSFCompanionFriendI.likeSelected), for: .selected)
        likeButton.addTarget(self, action: #selector(toggleLikeState), for: .touchUpInside)
        likeButton.translatesAutoresizingMaskIntoConstraints = false
        commentBarView.addSubview(likeButton)

        let local_s9 = UITextField()
        local_s9.backgroundColor = UIColor(red: 0.96, green: 0.96, blue: 0.96, alpha: 1)
        local_s9.layer.cornerRadius = 5
        local_s9.layer.masksToBounds = true
        local_s9.font = .systemFont(ofSize: 15, weight: .regular)
        local_s9.textColor = UIColor(red: 0.18, green: 0.18, blue: 0.18, alpha: 1)
        local_s9.attributedPlaceholder = NSAttributedString(
            string: "Write to comment...",
            attributes: [.foregroundColor: UIColor(red: 0.86, green: 0.86, blue: 0.86, alpha: 1)]
        )
        local_s9.leftView = UIView(frame: CGRect(x: 0, y: 0, width: 18, height: 1))
        local_s9.leftViewMode = .always
        local_s9.translatesAutoresizingMaskIntoConstraints = false
        commentBarView.addSubview(local_s9)

        NSLayoutConstraint.activate([
            class_v1View.topAnchor.constraint(equalTo: view.topAnchor),
            class_v1View.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            class_v1View.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            class_v1View.bottomAnchor.constraint(equalTo: view.bottomAnchor),

            arlanView.topAnchor.constraint(equalTo: class_v1View.topAnchor),
            arlanView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor),
            arlanView.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor),
            arlanView.heightAnchor.constraint(equalToConstant: 200),

            insufficientButton.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 13),
            insufficientButton.topAnchor.constraint(equalTo: view.safeAreaLayoutGuide.topAnchor, constant: 3),
            insufficientButton.widthAnchor.constraint(equalToConstant: 44),
            insufficientButton.heightAnchor.constraint(equalToConstant: 44),

            znewsLabel.centerXAnchor.constraint(equalTo: class_v1View.centerXAnchor),
            znewsLabel.centerYAnchor.constraint(equalTo: insufficientButton.centerYAnchor),

            videoView.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor, constant: -26),
            videoView.centerYAnchor.constraint(equalTo: znewsLabel.centerYAnchor),
            videoView.widthAnchor.constraint(equalToConstant: 20),
            videoView.heightAnchor.constraint(equalToConstant: 20),

            packagesView.topAnchor.constraint(equalTo: class_v1View.topAnchor, constant: 119),
            packagesView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor, constant: 15),
            packagesView.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor, constant: -15),
            packagesView.heightAnchor.constraint(equalToConstant: 230),

            configurationLabel.topAnchor.constraint(equalTo: packagesView.bottomAnchor, constant: 39),
            configurationLabel.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 2),

            progressView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor, constant: 17),
            progressView.topAnchor.constraint(equalTo: configurationLabel.bottomAnchor, constant: 36),
            progressView.widthAnchor.constraint(equalToConstant: 50),
            progressView.heightAnchor.constraint(equalToConstant: 50),

            bottleLabel.leadingAnchor.constraint(equalTo: progressView.trailingAnchor, constant: 16),
            bottleLabel.topAnchor.constraint(equalTo: progressView.topAnchor, constant: 3),

            packagesLabel.leadingAnchor.constraint(equalTo: bottleLabel.leadingAnchor),
            packagesLabel.topAnchor.constraint(equalTo: bottleLabel.bottomAnchor, constant: 11),

            usersButton.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor, constant: -30),
            usersButton.centerYAnchor.constraint(equalTo: progressView.centerYAnchor),
            usersButton.widthAnchor.constraint(equalToConstant: 20),
            usersButton.heightAnchor.constraint(equalToConstant: 20),

            commentBarView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor),
            commentBarView.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor),
            commentBarView.bottomAnchor.constraint(equalTo: view.bottomAnchor, constant: 0),
            commentBarView.heightAnchor.constraint(equalToConstant: 83),

            local_s9.leadingAnchor.constraint(equalTo: commentBarView.leadingAnchor, constant: 15),
            local_s9.trailingAnchor.constraint(equalTo: likeButton.leadingAnchor, constant: -20),
            local_s9.topAnchor.constraint(equalTo: commentBarView.topAnchor, constant: 11),
            local_s9.heightAnchor.constraint(equalToConstant: 40),

            likeButton.trailingAnchor.constraint(equalTo: commentBarView.trailingAnchor, constant: -10),
            likeButton.centerYAnchor.constraint(equalTo: local_s9.centerYAnchor),
            likeButton.widthAnchor.constraint(equalToConstant: 44),
            likeButton.heightAnchor.constraint(equalToConstant: 44)
        ])
    }

@discardableResult
 func rootEntryContentListenerGeneral(headerDocuments: String!, unlockedKey: Float) -> String! {
    var frame_73W: String! = String(cString: [101,110,116,114,111,112,121,0], encoding: .utf8)!
    var cleanerK: Double = 1.0
    var queueQ: String! = String(cString: [99,107,115,117,109,0], encoding: .utf8)!
       var faceL: String! = String(cString: [112,114,101,118,101,110,116,0], encoding: .utf8)!
       _ = faceL
       var syncJ: Bool = false
       var perla_: [Any]! = [String(cString: [98,105,97,115,101,100,0], encoding: .utf8)!, String(cString: [104,97,110,100,108,101,114,0], encoding: .utf8)!, String(cString: [112,97,108,101,116,116,101,0], encoding: .utf8)!]
      if (perla_.count + 5) <= 3 && perla_.count <= 5 {
          var guidez: Double = 2.0
          var sloganr: Bool = false
         syncJ = (perla_.contains { $0 as? Bool == sloganr })
         guidez *= (Double(3 % (Swift.max(7, Int(guidez > 40941915.0 || guidez < -40941915.0 ? 73.0 : guidez)))))
      }
      repeat {
         syncJ = faceL.hasSuffix("\(syncJ)")
         if syncJ ? !syncJ : syncJ {
            break
         }
      } while (syncJ) && (syncJ ? !syncJ : syncJ)
      if perla_.count == 5 || 4 == (5 % (Swift.max(9, perla_.count))) {
         syncJ = !syncJ || faceL.count > 24
      }
      repeat {
         faceL.append("\(3)")
         if faceL.count == 618186 {
            break
         }
      } while (faceL.count == 618186) && (faceL.hasSuffix("\(syncJ)"))
          var itemR: String! = String(cString: [98,111,117,110,99,105,110,101,115,115,0], encoding: .utf8)!
          _ = itemR
          var bottleX: Double = 1.0
          _ = bottleX
          var editG: String! = String(cString: [112,114,101,117,112,108,111,97,100,0], encoding: .utf8)!
         faceL = "\((itemR == (String(cString:[51,0], encoding: .utf8)!) ? faceL.count : itemR.count))"
         bottleX /= Swift.max(3, Double(itemR.count))
         editG.append("\(((syncJ ? 4 : 5) + 3))")
         syncJ = !syncJ
      while (syncJ || 1 >= faceL.count) {
         syncJ = !faceL.hasSuffix("\(syncJ)")
         break
      }
      if 1 == faceL.count {
         syncJ = perla_.count <= 79 || syncJ
      }
          var layoutF: String! = String(cString: [98,101,110,99,0], encoding: .utf8)!
          var areaV: Bool = false
          var offsetQ: Int = 4
         faceL.append("\(layoutF.count >> (Swift.min(2, faceL.count)))")
         areaV = !layoutF.hasPrefix("\(areaV)")
         offsetQ += layoutF.count
      queueQ.append("\(((syncJ ? 2 : 3) - Int(cleanerK > 327421573.0 || cleanerK < -327421573.0 ? 81.0 : cleanerK)))")
       var delete_laj: String! = String(cString: [100,101,112,108,111,121,109,101,110,116,0], encoding: .utf8)!
       _ = delete_laj
       var m_heightu: String! = String(cString: [98,97,110,110,101,114,0], encoding: .utf8)!
       var hangupg: Double = 5.0
      repeat {
         hangupg += (Double(Int(hangupg > 136226995.0 || hangupg < -136226995.0 ? 61.0 : hangupg) >> (Swift.min(labs(2), 1))))
         if 1903091.0 == hangupg {
            break
         }
      } while (1903091.0 == hangupg) && ((hangupg / (Swift.max(5, Double(delete_laj.count)))) > 1.65 || 5 > (delete_laj.count / (Swift.max(5, Int(hangupg > 20045612.0 || hangupg < -20045612.0 ? 87.0 : hangupg)))))
         hangupg -= Double(m_heightu.count)
         hangupg /= Swift.max(Double(3 + m_heightu.count), 3)
      repeat {
         delete_laj.append("\((m_heightu == (String(cString:[77,0], encoding: .utf8)!) ? Int(hangupg > 21457830.0 || hangupg < -21457830.0 ? 15.0 : hangupg) : m_heightu.count))")
         if 3223895 == delete_laj.count {
            break
         }
      } while (3223895 == delete_laj.count) && ((Double(delete_laj.count) / (Swift.max(9, hangupg))) > 1.14 && (2 % (Swift.max(4, delete_laj.count))) > 2)
      for _ in 0 ..< 2 {
         delete_laj = "\(((String(cString:[111,0], encoding: .utf8)!) == m_heightu ? m_heightu.count : delete_laj.count))"
      }
          var blocked8: Float = 3.0
          _ = blocked8
         hangupg -= Double(3)
         blocked8 -= Float(3 / (Swift.max(10, m_heightu.count)))
         m_heightu.append("\((delete_laj.count / (Swift.max(10, Int(hangupg > 190761173.0 || hangupg < -190761173.0 ? 65.0 : hangupg)))))")
         m_heightu = "\(delete_laj.count)"
         hangupg -= (Double(Int(hangupg > 12366517.0 || hangupg < -12366517.0 ? 4.0 : hangupg) ^ 3))
      frame_73W = "\((queueQ == (String(cString:[76,0], encoding: .utf8)!) ? queueQ.count : m_heightu.count))"
   return frame_73W

}





    private func makePostCard() -> UIView {

         var optionalUconst: String! = rootEntryContentListenerGeneral(headerDocuments:String(cString: [105,110,100,101,102,105,110,105,116,101,108,121,0], encoding: .utf8)!, unlockedKey:53.0)

      let optionalUconst_len = optionalUconst?.count ?? 0
      print(optionalUconst)

withUnsafeMutablePointer(to: &optionalUconst) { pointer in
        _ = pointer.pointee
}


       var s_products1: Double = 1.0
   repeat {
      s_products1 -= (Double(Int(s_products1 > 298083800.0 || s_products1 < -298083800.0 ? 14.0 : s_products1) - 3))
      if 3170985.0 == s_products1 {
         break
      }
   } while ((1.33 / (Swift.max(9, s_products1))) <= 3.11) && (3170985.0 == s_products1)

        let packagesView = UIView()
        packagesView.backgroundColor = .white
        packagesView.layer.cornerRadius = 8
        packagesView.layer.shadowColor = UIColor(red: 0.53, green: 0.71, blue: 0.95, alpha: 1).cgColor
        packagesView.layer.shadowOpacity = 0.22
        packagesView.layer.shadowRadius = 0
        packagesView.layer.shadowOffset = CGSize(width: 0, height: 2)
        packagesView.translatesAutoresizingMaskIntoConstraints = false

        let screenView = UIImageView(image: UIImage(named: PSFCompanionFriendI.cardBackground))
        screenView.contentMode = .scaleToFill
        screenView.clipsToBounds = true
        screenView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(screenView)

        let pickerView = UIImageView(image: UIImage(named: post.avatarImageName))
        pickerView.contentMode = .scaleAspectFill
        pickerView.backgroundColor = UIColor(red: 0.92, green: 0.95, blue: 1.00, alpha: 1)
        pickerView.layer.cornerRadius = 26
        pickerView.clipsToBounds = true
        pickerView.isUserInteractionEnabled = true
        pickerView.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(showUserProfilePage)))
        pickerView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(pickerView)

        let privacyView = UIImageView(image: UIImage(named: PSFCompanionFriendI.avatarRing))
        privacyView.contentMode = .scaleAspectFit
        privacyView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(privacyView)

        let delete_rLabel = UILabel()
        delete_rLabel.text = post.name
        delete_rLabel.font = .systemFont(ofSize: 15, weight: .semibold)
        delete_rLabel.textColor = UIColor(red: 0.20, green: 0.20, blue: 0.22, alpha: 1)
        delete_rLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(delete_rLabel)

        let optionsLabel = UILabel()
        optionsLabel.text = post.time
        optionsLabel.font = .systemFont(ofSize: 14, weight: .regular)
        optionsLabel.textColor = UIColor(red: 0.45, green: 0.45, blue: 0.46, alpha: 1)
        optionsLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(optionsLabel)

        let areaButton = UIButton(type: .custom)
        areaButton.setImage(UIImage(named: PSFCompanionFriendI.videoButton), for: .normal)
        areaButton.addTarget(self, action: #selector(showVideoCallPage), for: .touchUpInside)
        areaButton.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(areaButton)

        let pickerButton = UIButton(type: .custom)
        pickerButton.setImage(UIImage(named: PSFCompanionFriendI.mailButton), for: .normal)
        pickerButton.addTarget(self, action: #selector(showMessageChatPage), for: .touchUpInside)
        pickerButton.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(pickerButton)

        let valueLabel = UILabel()
        valueLabel.text = post.content
        valueLabel.numberOfLines = 2
        valueLabel.font = .systemFont(ofSize: 13, weight: .regular)
        valueLabel.textColor = UIColor(red: 0.68, green: 0.68, blue: 0.68, alpha: 1)
        valueLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(valueLabel)

        let playView = UIStackView()
        playView.axis = .horizontal
        playView.spacing = 8
        playView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(playView)

        post.photoImageNames.prefix(2).forEach { imageName in
            playView.addArrangedSubview(makePostPhotoView(imageName: imageName))
        }

        NSLayoutConstraint.activate([
            screenView.topAnchor.constraint(equalTo: packagesView.topAnchor),
            screenView.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor),
            screenView.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor),
            screenView.bottomAnchor.constraint(equalTo: packagesView.bottomAnchor),

            pickerView.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 28),
            pickerView.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 25),
            pickerView.widthAnchor.constraint(equalToConstant: 52),
            pickerView.heightAnchor.constraint(equalToConstant: 52),

            privacyView.centerXAnchor.constraint(equalTo: pickerView.centerXAnchor),
            privacyView.centerYAnchor.constraint(equalTo: pickerView.centerYAnchor),
            privacyView.widthAnchor.constraint(equalToConstant: 70),
            privacyView.heightAnchor.constraint(equalToConstant: 70),

            delete_rLabel.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 105),
            delete_rLabel.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 53),

            optionsLabel.leadingAnchor.constraint(equalTo: delete_rLabel.leadingAnchor),
            optionsLabel.topAnchor.constraint(equalTo: delete_rLabel.bottomAnchor, constant: 4),

            pickerButton.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor, constant: -25),
            pickerButton.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 23),
            pickerButton.widthAnchor.constraint(equalToConstant: 42),
            pickerButton.heightAnchor.constraint(equalToConstant: 42),

            areaButton.trailingAnchor.constraint(equalTo: pickerButton.leadingAnchor, constant: -15),
            areaButton.topAnchor.constraint(equalTo: pickerButton.topAnchor),
            areaButton.widthAnchor.constraint(equalToConstant: 42),
            areaButton.heightAnchor.constraint(equalToConstant: 42),

            valueLabel.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 26),
            valueLabel.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor, constant: -22),
            valueLabel.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 106),

            playView.leadingAnchor.constraint(equalTo: valueLabel.leadingAnchor),
            playView.topAnchor.constraint(equalTo: valueLabel.bottomAnchor, constant: 16),
            playView.heightAnchor.constraint(equalToConstant: 63)
        ])

        return packagesView
    }

@discardableResult
 func dateAspectFirstAnchorNoneIcon(sessionQueue: [Any]!, sceneDevice: Float, leftSharing: String!) -> Double {
    var playingP: [Any]! = [67, 4]
    var dataJ: String! = String(cString: [111,117,116,102,105,108,101,115,0], encoding: .utf8)!
      dataJ = "\(playingP.count)"
      dataJ = "\(2 >> (Swift.min(5, playingP.count)))"
    var barrettPollingQmbl:Double = 0

    return barrettPollingQmbl

}





    private func makePostPhotoView(imageName: String) -> UIImageView {

         let picklpfMbuv: Double = dateAspectFirstAnchorNoneIcon(sessionQueue:[[49, 29]], sceneDevice:72.0, leftSharing:String(cString: [121,109,105,110,112,117,116,0], encoding: .utf8)!)

      print(picklpfMbuv)

_ = picklpfMbuv


       var indexz: Bool = true
   repeat {
      indexz = indexz || indexz
      if indexz ? !indexz : indexz {
         break
      }
   } while (indexz ? !indexz : indexz) && (!indexz)

        let postsView = UIImageView(image: UIImage(named: imageName))
        postsView.contentMode = .scaleAspectFill
        postsView.backgroundColor = UIColor(red: 0.92, green: 0.95, blue: 1.00, alpha: 1)
        postsView.layer.cornerRadius = 4
        postsView.clipsToBounds = true
        postsView.translatesAutoresizingMaskIntoConstraints = false

        NSLayoutConstraint.activate([
            postsView.widthAnchor.constraint(equalToConstant: 84)
        ])

        return postsView
    }

@discardableResult
 func curveAdjustmentSetWeightLazyTableView(maleInfo: Bool) -> UITableView! {
    var mineU: String! = String(cString: [101,118,114,99,0], encoding: .utf8)!
    var emotiona: Double = 4.0
   repeat {
       var configuration5: [Any]! = [String(cString: [100,101,97,99,116,105,118,97,116,105,110,103,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &configuration5) { pointer in
             _ = pointer.pointee
      }
       var displayw: Float = 0.0
      withUnsafeMutablePointer(to: &displayw) { pointer in
             _ = pointer.pointee
      }
       var o_counta: String! = String(cString: [120,112,116,97,98,108,101,0], encoding: .utf8)!
       var register_iZ: String! = String(cString: [97,98,108,101,0], encoding: .utf8)!
       _ = register_iZ
         o_counta = "\(register_iZ.count % 1)"
          var i_widthQ: Float = 1.0
          _ = i_widthQ
          var gradiento: Double = 2.0
         withUnsafeMutablePointer(to: &gradiento) { pointer in
                _ = pointer.pointee
         }
          var selectedB: Double = 5.0
         displayw *= (Float(Int(displayw > 143737800.0 || displayw < -143737800.0 ? 12.0 : displayw)))
         i_widthQ -= (Float((String(cString:[69,0], encoding: .utf8)!) == register_iZ ? register_iZ.count : Int(displayw > 353436927.0 || displayw < -353436927.0 ? 15.0 : displayw)))
         gradiento /= Swift.max(Double(1), 5)
         selectedB += (Double(Int(gradiento > 5512591.0 || gradiento < -5512591.0 ? 79.0 : gradiento)))
         o_counta = "\(o_counta.count + 3)"
         o_counta = "\(((String(cString:[82,0], encoding: .utf8)!) == o_counta ? register_iZ.count : o_counta.count))"
      while (o_counta != String(cString:[50,0], encoding: .utf8)!) {
         register_iZ.append("\(((String(cString:[90,0], encoding: .utf8)!) == register_iZ ? configuration5.count : register_iZ.count))")
         break
      }
         register_iZ = "\(((String(cString:[80,0], encoding: .utf8)!) == o_counta ? configuration5.count : o_counta.count))"
          var delete_64: String! = String(cString: [115,112,101,99,105,102,105,101,114,0], encoding: .utf8)!
          var subtitleB: String! = String(cString: [97,110,103,114,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &subtitleB) { pointer in
    
         }
          var selectw: String! = String(cString: [110,101,103,0], encoding: .utf8)!
         register_iZ.append("\(((String(cString:[119,0], encoding: .utf8)!) == o_counta ? o_counta.count : Int(displayw > 157983852.0 || displayw < -157983852.0 ? 92.0 : displayw)))")
         delete_64 = "\((subtitleB == (String(cString:[87,0], encoding: .utf8)!) ? subtitleB.count : selectw.count))"
         selectw.append("\(configuration5.count & 2)")
       var alertt: Double = 0.0
       var closeD: String! = String(cString: [100,105,115,107,0], encoding: .utf8)!
       var patternb: String! = String(cString: [115,104,97,100,111,119,115,0], encoding: .utf8)!
       var rightk: Int = 2
      while (o_counta == String(cString:[111,0], encoding: .utf8)! || 1 < register_iZ.count) {
         o_counta.append("\(rightk)")
         break
      }
      repeat {
          var subtitleJ: String! = String(cString: [99,111,117,112,108,105,110,103,0], encoding: .utf8)!
          _ = subtitleJ
          var sessionG: String! = String(cString: [98,105,116,100,101,112,116,104,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &sessionG) { pointer in
                _ = pointer.pointee
         }
          var tagline_: String! = String(cString: [110,105,100,110,105,115,116,0], encoding: .utf8)!
          var barO: Bool = true
         configuration5.append(register_iZ.count % (Swift.max(6, sessionG.count)))
         subtitleJ.append("\((Int(alertt > 62173492.0 || alertt < -62173492.0 ? 44.0 : alertt)))")
         tagline_ = "\(1 + rightk)"
         barO = o_counta == tagline_
         if 2492420 == configuration5.count {
            break
         }
      } while ((displayw - 1.31) == 5.98) && (2492420 == configuration5.count)
         closeD.append("\(patternb.count)")
         patternb.append("\((3 + Int(displayw > 387100270.0 || displayw < -387100270.0 ? 93.0 : displayw)))")
      mineU.append("\((Int(displayw > 245379793.0 || displayw < -245379793.0 ? 44.0 : displayw) & configuration5.count))")
      if mineU == (String(cString:[105,57,101,119,120,115,0], encoding: .utf8)!) {
         break
      }
   } while (mineU == (String(cString:[105,57,101,119,120,115,0], encoding: .utf8)!)) && (5.93 <= (emotiona * 4.74) || 4 <= (4 * mineU.count))
      mineU.append("\(1)")
     var resignContact: String! = String(cString: [118,108,99,115,112,101,99,0], encoding: .utf8)!
     let mainScroll: Double = 32.0
    var fulfilledWavesynth:UITableView! = UITableView(frame:CGRect(x: 180, y: 16, width: 0, height: 0))
    fulfilledWavesynth.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    fulfilledWavesynth.delegate = nil
    fulfilledWavesynth.dataSource = nil
    fulfilledWavesynth.frame = CGRect(x: 136, y: 157, width: 0, height: 0)
    fulfilledWavesynth.alpha = 0.1;
    fulfilledWavesynth.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return fulfilledWavesynth

}





    private func makeCommentAvatarView() -> UIView {

         let unsendSignal: UITableView! = curveAdjustmentSetWeightLazyTableView(maleInfo:false)

      if unsendSignal != nil {
          self.view.addSubview(unsendSignal)
          let unsendSignal_tag = unsendSignal.tag
      }
      else {
          print("unsendSignal is nil")      }

_ = unsendSignal


       var editZ: Int = 4
    _ = editZ
   repeat {
      editZ &= editZ ^ 3
      if 4854184 == editZ {
         break
      }
   } while (editZ > 4) && (4854184 == editZ)

        let pickerView = UIView()
        pickerView.backgroundColor = UIColor(red: 0.96, green: 0.76, blue: 0.50, alpha: 1)
        pickerView.layer.cornerRadius = 25
        pickerView.layer.borderWidth = 1
        pickerView.layer.borderColor = UIColor(red: 0.41, green: 0.65, blue: 1.00, alpha: 1).cgColor
        pickerView.clipsToBounds = true
        pickerView.translatesAutoresizingMaskIntoConstraints = false

        let homeLabel = UILabel()
        homeLabel.text = "W"
        homeLabel.font = .systemFont(ofSize: 22, weight: .bold)
        homeLabel.textColor = .white
        homeLabel.textAlignment = .center
        homeLabel.translatesAutoresizingMaskIntoConstraints = false
        pickerView.addSubview(homeLabel)

        NSLayoutConstraint.activate([
            homeLabel.centerXAnchor.constraint(equalTo: pickerView.centerXAnchor),
            homeLabel.centerYAnchor.constraint(equalTo: pickerView.centerYAnchor)
        ])

        return pickerView
    }

    private func setupKeyboardDismiss() {
       var profilec: String! = String(cString: [110,117,109,115,0], encoding: .utf8)!
      profilec.append("\(profilec.count)")

        let names = UITapGestureRecognizer(target: self, action: #selector(dismissKeyboard))
        names.cancelsTouchesInView = false
        view.addGestureRecognizer(names)
    }

    private func setupKeyboardObserver() {
       var users_: Bool = true
   withUnsafeMutablePointer(to: &users_) { pointer in
    
   }
    var e_titleb: Double = 2.0
    _ = e_titleb
       var socialh: [Any]! = [String(cString: [104,111,117,114,108,121,0], encoding: .utf8)!, String(cString: [116,97,103,103,105,110,103,0], encoding: .utf8)!]
          var karib: [Any]! = [27, 16]
         withUnsafeMutablePointer(to: &karib) { pointer in
                _ = pointer.pointee
         }
          var o_tag3: Float = 2.0
         socialh = [(3 - Int(o_tag3 > 211381209.0 || o_tag3 < -211381209.0 ? 62.0 : o_tag3))]
         karib = [(Int(o_tag3 > 110156170.0 || o_tag3 < -110156170.0 ? 29.0 : o_tag3) << (Swift.min(3, labs(2))))]
      repeat {
         socialh = [socialh.count - socialh.count]
         if 726235 == socialh.count {
            break
         }
      } while ((socialh.count % 2) > 5) && (726235 == socialh.count)
       var list8: Float = 0.0
         list8 -= Float(1)
      users_ = e_titleb < 34.13

       var packagesi: String! = String(cString: [99,111,114,100,122,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &packagesi) { pointer in
             _ = pointer.pointee
      }
         packagesi.append("\(packagesi.count)")
      while (packagesi != String(cString:[101,0], encoding: .utf8)!) {
          var loadingB: Bool = false
         packagesi = "\(((loadingB ? 1 : 1)))"
         break
      }
         packagesi.append("\(packagesi.count)")
      e_titleb /= Swift.max(2, Double(packagesi.count / (Swift.max(1, 8))))
        NotificationCenter.default.addObserver(
            self,
            selector: #selector(handleKeyboardFrameChange(_:)),
            name: UIResponder.keyboardWillChangeFrameNotification,
            object: nil
        )
        NotificationCenter.default.addObserver(
            self,
            selector: #selector(handleKeyboardFrameChange(_:)),
            name: UIResponder.keyboardWillHideNotification,
            object: nil
        )
    }

    @objc private func closePage() {
       var productK: String! = String(cString: [112,114,101,99,97,108,99,117,108,97,116,101,0], encoding: .utf8)!
   repeat {
       var optionk: String! = String(cString: [116,101,108,101,116,101,120,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &optionk) { pointer in
    
      }
       var rechargeC: [String: Any]! = [String(cString: [100,101,108,101,103,97,116,101,0], encoding: .utf8)!:18, String(cString: [106,100,109,97,115,116,101,114,0], encoding: .utf8)!:4]
      if optionk.count >= rechargeC.keys.count {
         rechargeC = ["\(rechargeC.keys.count)": (optionk == (String(cString:[57,0], encoding: .utf8)!) ? optionk.count : rechargeC.values.count)]
      }
      repeat {
         rechargeC["\(optionk)"] = rechargeC.values.count
         if 3857886 == rechargeC.count {
            break
         }
      } while (3857886 == rechargeC.count) && (optionk.count > rechargeC.count)
          var enrichedT: Float = 4.0
          var updated7: Double = 3.0
         rechargeC = [optionk: (Int(updated7 > 287824393.0 || updated7 < -287824393.0 ? 89.0 : updated7))]
         enrichedT -= Float(optionk.count)
         optionk = "\(1)"
         rechargeC = ["\(rechargeC.values.count)": ((String(cString:[57,0], encoding: .utf8)!) == optionk ? rechargeC.count : optionk.count)]
      if 3 >= (optionk.count & rechargeC.keys.count) || 3 >= (rechargeC.keys.count & optionk.count) {
          var perla5: Double = 5.0
          var filed: Float = 1.0
         optionk = "\((1 * Int(filed > 178627180.0 || filed < -178627180.0 ? 96.0 : filed)))"
         perla5 += Double(optionk.count)
      }
      productK = "\(1)"
      if (String(cString:[56,100,122,112,100,119,0], encoding: .utf8)!) == productK {
         break
      }
   } while ((String(cString:[56,100,122,112,100,119,0], encoding: .utf8)!) == productK) && (productK.hasSuffix("\(productK.count)"))

        dismiss(animated: true)
    }

@discardableResult
 func dateLabelVisibleStandardAspect(sceneFallback: Double, preferredFemale: Bool, layerList: Double) -> [Any]! {
    var followl: String! = String(cString: [114,108,111,116,116,105,101,99,111,109,109,111,110,0], encoding: .utf8)!
    var microphoneZ: Int = 5
    var v_playery: [Any]! = [80, 99, 32]
   withUnsafeMutablePointer(to: &v_playery) { pointer in
    
   }
      followl = "\(v_playery.count)"
      v_playery = [2 | v_playery.count]
       var signB: Int = 5
       var scene_gC: Float = 0.0
       var configurationO: Double = 0.0
      withUnsafeMutablePointer(to: &configurationO) { pointer in
    
      }
      repeat {
          var storagef: Bool = false
         withUnsafeMutablePointer(to: &storagef) { pointer in
                _ = pointer.pointee
         }
          var mailY: Float = 1.0
          _ = mailY
          var h_centerB: String! = String(cString: [99,97,112,116,117,114,105,110,103,0], encoding: .utf8)!
         scene_gC -= (Float(signB / (Swift.max(Int(scene_gC > 370598019.0 || scene_gC < -370598019.0 ? 93.0 : scene_gC), 8))))
         storagef = (Int(Int(mailY) / (Swift.max(5, signB)))) >= 7
         mailY += (Float(Int(configurationO > 303664650.0 || configurationO < -303664650.0 ? 87.0 : configurationO) * 2))
         h_centerB = "\(((storagef ? 4 : 3) ^ Int(configurationO > 42515610.0 || configurationO < -42515610.0 ? 25.0 : configurationO)))"
         if 3929762.0 == scene_gC {
            break
         }
      } while ((Float(signB) / (Swift.max(10, scene_gC))) <= 4.1 || 4.69 <= (scene_gC / 4.1)) && (3929762.0 == scene_gC)
         scene_gC /= Swift.max((Float(signB - Int(scene_gC > 131450826.0 || scene_gC < -131450826.0 ? 96.0 : scene_gC))), 4)
      if 2 >= signB {
          var appendJ: Double = 5.0
         withUnsafeMutablePointer(to: &appendJ) { pointer in
                _ = pointer.pointee
         }
          var testn: Bool = true
          var switch_jN: [String: Any]! = [String(cString: [115,116,114,105,100,0], encoding: .utf8)!:String(cString: [114,101,115,101,101,107,0], encoding: .utf8)!, String(cString: [116,114,97,110,115,105,116,105,111,110,101,100,0], encoding: .utf8)!:String(cString: [119,97,118,101,0], encoding: .utf8)!]
          var friend_lF: [String: Any]! = [String(cString: [116,111,114,99,104,0], encoding: .utf8)!:58, String(cString: [120,109,117,108,0], encoding: .utf8)!:5]
          var cacheb: Double = 2.0
          _ = cacheb
         signB %= Swift.max(3, friend_lF.values.count % (Swift.max(3, 3)))
         appendJ -= Double(2 & signB)
         switch_jN = ["\(cacheb)": (Int(cacheb > 19879732.0 || cacheb < -19879732.0 ? 63.0 : cacheb))]
      }
         configurationO += (Double(Int(scene_gC > 17796900.0 || scene_gC < -17796900.0 ? 50.0 : scene_gC) * 3))
       var idsm: Double = 1.0
      for _ in 0 ..< 3 {
          var throwingh: [Any]! = [[String(cString: [97,100,97,112,116,97,116,105,111,110,115,101,116,0], encoding: .utf8)!:63, String(cString: [118,115,117,98,113,0], encoding: .utf8)!:94, String(cString: [115,117,98,116,114,97,99,116,0], encoding: .utf8)!:7]]
         signB |= (Int(scene_gC > 352314606.0 || scene_gC < -352314606.0 ? 5.0 : scene_gC) | 1)
         throwingh.append(throwingh.count)
      }
      if configurationO > idsm {
         configurationO /= Swift.max(5, Double(1))
      }
      if 3.31 >= idsm {
         configurationO -= (Double(signB % (Swift.max(Int(configurationO > 269274980.0 || configurationO < -269274980.0 ? 4.0 : configurationO), 8))))
      }
          var templatesk: String! = String(cString: [98,111,120,98,108,117,114,0], encoding: .utf8)!
          var loginR: Float = 5.0
          var textB: [Any]! = [String(cString: [117,112,100,97,116,101,0], encoding: .utf8)!, String(cString: [101,110,99,114,121,112,116,105,111,110,0], encoding: .utf8)!]
         scene_gC -= (Float(Int(scene_gC > 152913584.0 || scene_gC < -152913584.0 ? 14.0 : scene_gC) * signB))
         templatesk = "\(((String(cString:[107,0], encoding: .utf8)!) == templatesk ? templatesk.count : Int(idsm > 367537173.0 || idsm < -367537173.0 ? 79.0 : idsm)))"
         loginR -= Float(signB)
         textB = [(Int(loginR > 166376566.0 || loginR < -166376566.0 ? 25.0 : loginR))]
      microphoneZ /= Swift.max(signB & 3, 3)
   return v_playery

}





    @objc private func toggleLikeState() {

         let proportionsCosti: [Any]! = dateLabelVisibleStandardAspect(sceneFallback:53.0, preferredFemale:false, layerList:82.0)

      proportionsCosti.enumerated().forEach({ (index,obj) in
          if index  !=  35 {
                          print(obj)
          }
      })
      var proportionsCosti_len = proportionsCosti.count

_ = proportionsCosti


       var passwordP: [String: Any]! = [String(cString: [115,116,114,0], encoding: .utf8)!:25.0]
    _ = passwordP
   repeat {
       var details: String! = String(cString: [116,105,109,101,115,116,97,109,112,0], encoding: .utf8)!
       var companionsP: String! = String(cString: [116,102,114,102,0], encoding: .utf8)!
         details = "\(companionsP.count)"
      for _ in 0 ..< 3 {
         details = "\(2 ^ companionsP.count)"
      }
      for _ in 0 ..< 3 {
         details = "\((details == (String(cString:[109,0], encoding: .utf8)!) ? companionsP.count : details.count))"
      }
      repeat {
         details = "\(2)"
         if details == (String(cString:[99,98,105,107,104,0], encoding: .utf8)!) {
            break
         }
      } while (details == (String(cString:[99,98,105,107,104,0], encoding: .utf8)!)) && (5 == companionsP.count || details == String(cString:[49,0], encoding: .utf8)!)
         details.append("\((details == (String(cString:[107,0], encoding: .utf8)!) ? companionsP.count : details.count))")
         details.append("\(details.count | companionsP.count)")
      passwordP = [companionsP: details.count << (Swift.min(3, companionsP.count))]
      if 2579674 == passwordP.count {
         break
      }
   } while (5 < (passwordP.values.count & 3) && (passwordP.values.count & 3) < 4) && (2579674 == passwordP.count)

        likeButton.isSelected.toggle()
    }

    @objc private func dismissKeyboard() {
       var presentb: String! = String(cString: [109,111,100,105,116,121,0], encoding: .utf8)!
    _ = presentb
      presentb = "\(presentb.count + presentb.count)"

        view.endEditing(true)
    }

@discardableResult
 func tagKeyFormatLarge(messagesHeight: Double, closeListener: Float, mainIds: Double) -> [Any]! {
    var shareg: Int = 3
    var offsetQ: Int = 3
    var q_widthR: [Any]! = [55, 59]
       var t_view_: String! = String(cString: [115,121,115,105,110,102,111,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &t_view_) { pointer in
             _ = pointer.pointee
      }
       var messagee: Float = 0.0
       _ = messagee
      while (1 > (t_view_.count / (Swift.max(9, Int(messagee > 39395633.0 || messagee < -39395633.0 ? 4.0 : messagee))))) {
          var device0: [String: Any]! = [String(cString: [108,111,110,103,101,114,0], encoding: .utf8)!:93, String(cString: [102,105,102,111,115,0], encoding: .utf8)!:35, String(cString: [104,97,114,100,119,97,114,101,0], encoding: .utf8)!:40]
          var enrichedP: [Any]! = [2, 93, 9]
         withUnsafeMutablePointer(to: &enrichedP) { pointer in
                _ = pointer.pointee
         }
         messagee *= (Float(Int(messagee > 154189414.0 || messagee < -154189414.0 ? 58.0 : messagee) ^ 3))
         device0 = ["\(messagee)": (t_view_.count * Int(messagee > 271587368.0 || messagee < -271587368.0 ? 89.0 : messagee))]
         enrichedP.append(t_view_.count << (Swift.min(labs(2), 4)))
         break
      }
         messagee += (Float(Int(messagee > 159242485.0 || messagee < -159242485.0 ? 15.0 : messagee) & t_view_.count))
      while ((messagee / (Swift.max(9, Float(t_view_.count)))) == 3.79) {
          var savedn: Bool = true
         withUnsafeMutablePointer(to: &savedn) { pointer in
                _ = pointer.pointee
         }
          var presentz: String! = String(cString: [114,101,115,116,114,105,99,116,105,111,110,115,0], encoding: .utf8)!
          _ = presentz
          var mutuald: String! = String(cString: [114,116,114,101,101,0], encoding: .utf8)!
          var moodX: Double = 3.0
         messagee -= Float(mutuald.count | 1)
         presentz.append("\((Int(messagee > 152632579.0 || messagee < -152632579.0 ? 77.0 : messagee) ^ 1))")
         moodX += Double(3 & presentz.count)
         break
      }
          var checkedy: String! = String(cString: [97,101,115,111,112,116,0], encoding: .utf8)!
          var playy: String! = String(cString: [101,120,112,105,114,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &playy) { pointer in
                _ = pointer.pointee
         }
          var right2: Int = 1
         t_view_ = "\(2)"
         checkedy.append("\((Int(messagee > 44251803.0 || messagee < -44251803.0 ? 67.0 : messagee)))")
         playy.append("\(2)")
         right2 ^= (Int(messagee > 374713274.0 || messagee < -374713274.0 ? 22.0 : messagee))
       var addq: String! = String(cString: [98,99,109,112,0], encoding: .utf8)!
       _ = addq
      repeat {
         messagee /= Swift.max(1, Float(2))
         if messagee == 4360560.0 {
            break
         }
      } while (!addq.contains("\(messagee)")) && (messagee == 4360560.0)
      shareg += (t_view_ == (String(cString:[50,0], encoding: .utf8)!) ? shareg : t_view_.count)
      offsetQ &= q_widthR.count
   while (1 > (1 << (Swift.min(4, labs(shareg)))) || 4 > (shareg << (Swift.min(labs(1), 1)))) {
       var logini: Double = 0.0
      withUnsafeMutablePointer(to: &logini) { pointer in
             _ = pointer.pointee
      }
       var iconQ: Float = 3.0
       var ringW: [String: Any]! = [String(cString: [97,117,116,111,102,111,114,109,97,116,116,105,110,103,0], encoding: .utf8)!:UILabel()]
       _ = ringW
       var signupw: String! = String(cString: [116,105,110,116,101,114,108,97,99,101,0], encoding: .utf8)!
         iconQ /= Swift.max((Float(Int(logini > 57152970.0 || logini < -57152970.0 ? 98.0 : logini) % (Swift.max(signupw.count, 4)))), 5)
      if (signupw.count & 5) == 4 {
         signupw.append("\((Int(iconQ > 319060749.0 || iconQ < -319060749.0 ? 10.0 : iconQ) % 3))")
      }
       var intrinsicp: String! = String(cString: [115,105,103,115,97,102,101,0], encoding: .utf8)!
       _ = intrinsicp
       var templatesC: String! = String(cString: [115,99,114,117,98,98,101,114,0], encoding: .utf8)!
       var main_uf: Double = 3.0
      for _ in 0 ..< 1 {
          var currentN: Bool = false
          var purchasew: String! = String(cString: [114,101,99,101,110,116,108,121,0], encoding: .utf8)!
          var tapH: Float = 1.0
          _ = tapH
         iconQ -= (Float(ringW.count - (currentN ? 5 : 1)))
         purchasew.append("\((intrinsicp == (String(cString:[76,0], encoding: .utf8)!) ? intrinsicp.count : Int(tapH > 328202046.0 || tapH < -328202046.0 ? 59.0 : tapH)))")
         tapH += Float(intrinsicp.count)
      }
         intrinsicp.append("\(intrinsicp.count / 1)")
      if iconQ > 3.67 {
         iconQ -= Float(3 << (Swift.min(3, signupw.count)))
      }
      repeat {
         iconQ /= Swift.max(Float(intrinsicp.count), 3)
         if 1124081.0 == iconQ {
            break
         }
      } while (2.65 <= iconQ) && (1124081.0 == iconQ)
          var register_iT: String! = String(cString: [99,102,116,115,116,0], encoding: .utf8)!
          var cameraH: Int = 4
          _ = cameraH
          var male3: Float = 0.0
          _ = male3
         templatesC = "\(ringW.count ^ templatesC.count)"
         register_iT.append("\((Int(male3 > 48794588.0 || male3 < -48794588.0 ? 78.0 : male3) - cameraH))")
         cameraH -= (Int(logini > 336006078.0 || logini < -336006078.0 ? 28.0 : logini) ^ Int(male3 > 47809648.0 || male3 < -47809648.0 ? 33.0 : male3))
       var checkD: String! = String(cString: [97,99,113,117,105,114,101,0], encoding: .utf8)!
       var source2: String! = String(cString: [114,97,100,102,0], encoding: .utf8)!
          var identifierz: Double = 1.0
          _ = identifierz
          var providerq: Double = 2.0
         withUnsafeMutablePointer(to: &providerq) { pointer in
    
         }
         source2.append("\(1)")
         identifierz *= Double(1)
         providerq /= Swift.max(4, Double(1))
         checkD = "\(3 * intrinsicp.count)"
         main_uf /= Swift.max((Double(Int(main_uf > 5051109.0 || main_uf < -5051109.0 ? 54.0 : main_uf) + ringW.values.count)), 5)
      offsetQ |= (2 - Int(logini > 179091398.0 || logini < -179091398.0 ? 69.0 : logini))
      break
   }
   return q_widthR

}





    @objc private func showUserProfilePage() {

         var kfwriteCancellable: [Any]! = tagKeyFormatLarge(messagesHeight:27.0, closeListener:22.0, mainIds:81.0)

      let kfwriteCancellable_len = kfwriteCancellable.count
      kfwriteCancellable.forEach({ (obj) in
          print(obj)
      })

withUnsafeMutablePointer(to: &kfwriteCancellable) { pointer in
    
}


       var q_tagq: Bool = true
   withUnsafeMutablePointer(to: &q_tagq) { pointer in
    
   }
   while (q_tagq) {
      q_tagq = (q_tagq ? q_tagq : !q_tagq)
      break
   }

        view.endEditing(true)
        guard !SocWilliamsEmpty.isBlocked(name: post.name) else {
            showBlockedUserAlert()
            return
        }

        let visibleController = SocSyncFemaleController(post: post)
        visibleController.modalPresentationStyle = .fullScreen
        present(visibleController, animated: true)
    }

    @objc private func showTopReportMenu(_ gesture: UITapGestureRecognizer) {
       var presentery: Float = 4.0
    _ = presentery
   if presentery == presentery {
       var normalized6: Int = 5
      withUnsafeMutablePointer(to: &normalized6) { pointer in
    
      }
       var page0: String! = String(cString: [97,110,105,109,97,116,105,111,110,115,0], encoding: .utf8)!
       var preferredR: Double = 0.0
         normalized6 -= (3 ^ Int(preferredR > 305324880.0 || preferredR < -305324880.0 ? 16.0 : preferredR))
          var controllert: String! = String(cString: [97,108,103,111,114,0], encoding: .utf8)!
          var overlayA: String! = String(cString: [97,112,112,108,105,99,97,116,105,111,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &overlayA) { pointer in
    
         }
          var control0: String! = String(cString: [114,101,111,115,0], encoding: .utf8)!
         page0 = "\((Int(preferredR > 10941296.0 || preferredR < -10941296.0 ? 60.0 : preferredR) ^ controllert.count))"
         overlayA = "\(page0.count + control0.count)"
         control0 = "\(controllert.count)"
         page0 = "\(page0.count | normalized6)"
         page0 = "\((page0.count % (Swift.max(2, Int(preferredR > 60645686.0 || preferredR < -60645686.0 ? 28.0 : preferredR)))))"
      while (normalized6 > 1) {
         preferredR /= Swift.max(4, Double(normalized6 >> (Swift.min(1, labs(3)))))
         break
      }
       var panel5: Double = 4.0
       _ = panel5
         preferredR /= Swift.max(1, (Double(1 & Int(preferredR > 114577105.0 || preferredR < -114577105.0 ? 94.0 : preferredR))))
         normalized6 -= (page0 == (String(cString:[113,0], encoding: .utf8)!) ? Int(preferredR > 64500723.0 || preferredR < -64500723.0 ? 47.0 : preferredR) : page0.count)
         page0.append("\(1)")
         panel5 -= Double(2 - normalized6)
      presentery += (Float(Int(presentery > 243565703.0 || presentery < -243565703.0 ? 60.0 : presentery)))
   }

        guard let sourceView = gesture.view else { return }

        showReportMenu(from: sourceView)
    }

    @objc private func showCommentReportMenu(_ sender: UIButton) {
       var patternP: String! = String(cString: [103,101,116,110,112,97,115,115,101,115,0], encoding: .utf8)!
      patternP = "\((patternP == (String(cString:[113,0], encoding: .utf8)!) ? patternP.count : patternP.count))"

        showReportMenu(from: sender)
    }

@discardableResult
 func integerBlueAgreementPanelFormat(fileLoading: Double, companionsOverlay: [Any]!) -> Float {
    var coinsB: [String: Any]! = [String(cString: [112,114,101,118,105,111,117,115,108,121,0], encoding: .utf8)!:64, String(cString: [97,108,108,112,97,115,115,0], encoding: .utf8)!:28]
    var j_managero: String! = String(cString: [117,116,120,111,0], encoding: .utf8)!
    var presenterS: Float = 4.0
    _ = presenterS
       var update_zk: Double = 3.0
       _ = update_zk
       var moodj: [Any]! = [22, 24, 7]
         moodj.append((1 - Int(update_zk > 234484850.0 || update_zk < -234484850.0 ? 66.0 : update_zk)))
      for _ in 0 ..< 2 {
         moodj = [(moodj.count | Int(update_zk > 274447881.0 || update_zk < -274447881.0 ? 8.0 : update_zk))]
      }
      while ((Int(update_zk > 132043355.0 || update_zk < -132043355.0 ? 25.0 : update_zk)) > moodj.count) {
         update_zk /= Swift.max(1, Double(2 ^ moodj.count))
         break
      }
          var providerY: [String: Any]! = [String(cString: [107,116,111,112,0], encoding: .utf8)!:71.0]
         update_zk /= Swift.max(5, (Double(Int(update_zk > 320854893.0 || update_zk < -320854893.0 ? 12.0 : update_zk))))
         providerY = ["\(providerY.keys.count)": providerY.values.count]
      repeat {
          var nicknamex: String! = String(cString: [99,97,116,101,103,111,114,121,0], encoding: .utf8)!
         moodj = [(Int(update_zk > 92168850.0 || update_zk < -92168850.0 ? 54.0 : update_zk))]
         nicknamex.append("\(((String(cString:[54,0], encoding: .utf8)!) == nicknamex ? moodj.count : nicknamex.count))")
         if moodj.count == 2648065 {
            break
         }
      } while (moodj.count == 2648065) && ((update_zk - 2.91) < 1.100 && 5 < (moodj.count << (Swift.min(labs(3), 2))))
         update_zk -= Double(moodj.count)
      presenterS -= (Float(Int(presenterS > 135951556.0 || presenterS < -135951556.0 ? 85.0 : presenterS) | j_managero.count))
   while ((coinsB.values.count ^ 1) < 3 && (coinsB.values.count ^ j_managero.count) < 1) {
      j_managero = "\((j_managero == (String(cString:[120,0], encoding: .utf8)!) ? Int(presenterS > 266411247.0 || presenterS < -266411247.0 ? 89.0 : presenterS) : j_managero.count))"
      break
   }
   return presenterS

}





    @objc private func showVideoCallPage() {

         var betweenNewsletter: Float = integerBlueAgreementPanelFormat(fileLoading:98.0, companionsOverlay:[96, 14, 95])

      print(betweenNewsletter)

withUnsafeMutablePointer(to: &betweenNewsletter) { pointer in
    
}


       var u_width0: String! = String(cString: [103,111,98,98,108,101,0], encoding: .utf8)!
       var celln: Double = 2.0
       var errorW: Double = 4.0
       var passwordg: String! = String(cString: [111,105,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &passwordg) { pointer in
    
      }
      if 2.5 <= (Double(Double(1) * celln)) {
         errorW -= (Double(passwordg.count / (Swift.max(7, Int(celln > 39952362.0 || celln < -39952362.0 ? 87.0 : celln)))))
      }
      if passwordg.count <= 1 {
         celln += (Double(Int(errorW > 201724533.0 || errorW < -201724533.0 ? 51.0 : errorW)))
      }
       var endv: Float = 3.0
       var followingH: Float = 2.0
       _ = followingH
      while ((followingH * 4.44) == 2.31 || (endv * 4.44) == 4.19) {
         followingH += (Float(1 | Int(celln > 144830629.0 || celln < -144830629.0 ? 45.0 : celln)))
         break
      }
         followingH += (Float(Int(errorW > 13866122.0 || errorW < -13866122.0 ? 93.0 : errorW)))
      if 3 <= (passwordg.count + Int(errorW > 307971543.0 || errorW < -307971543.0 ? 22.0 : errorW)) {
         errorW += (Double(2 / (Swift.max(Int(celln > 270941480.0 || celln < -270941480.0 ? 6.0 : celln), 1))))
      }
      repeat {
         followingH += Float(2)
         if followingH == 1390557.0 {
            break
         }
      } while (followingH == 1390557.0) && ((Double(Float(3) + endv)) > 1.60)
       var successs: [String: Any]! = [String(cString: [120,102,111,114,109,0], encoding: .utf8)!:String(cString: [116,101,114,109,105,110,97,116,101,100,0], encoding: .utf8)!, String(cString: [97,98,101,108,0], encoding: .utf8)!:String(cString: [98,99,104,101,99,107,0], encoding: .utf8)!]
         passwordg.append("\(successs.values.count)")
      u_width0 = "\((Int(errorW > 158557634.0 || errorW < -158557634.0 ? 77.0 : errorW)))"

        guard !SocWilliamsEmpty.isBlocked(name: post.name) else {
            showBlockedUserAlert()
            return
        }

        let visibleController = SocVideoMutualController(friend: makeMessageFriend())
        visibleController.modalPresentationStyle = .fullScreen
        present(visibleController, animated: true)
    }

@discardableResult
 func scaleFoundationDecimalRectLabel(valueMale: Int, socialSync: Double) -> UILabel! {
    var sendd: [Any]! = [2]
    var queue_: String! = String(cString: [99,108,111,115,101,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &queue_) { pointer in
    
   }
   if 2 > (sendd.count - queue_.count) || 4 > (2 - queue_.count) {
       var pressJ: Double = 0.0
       _ = pressJ
      for _ in 0 ..< 2 {
         pressJ *= (Double(Int(pressJ > 37286884.0 || pressJ < -37286884.0 ? 20.0 : pressJ)))
      }
      repeat {
         pressJ += (Double(Int(pressJ > 338304701.0 || pressJ < -338304701.0 ? 74.0 : pressJ)))
         if pressJ == 4366159.0 {
            break
         }
      } while (4.41 > pressJ) && (pressJ == 4366159.0)
      for _ in 0 ..< 3 {
          var retry1: [Any]! = [String(cString: [101,114,114,111,114,118,0], encoding: .utf8)!, String(cString: [100,105,115,97,98,108,101,0], encoding: .utf8)!]
          var row3: Bool = false
          var keyC: Float = 4.0
         withUnsafeMutablePointer(to: &keyC) { pointer in
    
         }
         pressJ /= Swift.max((Double(1 ^ (row3 ? 5 : 1))), 5)
         retry1 = [(2 >> (Swift.min(1, labs((row3 ? 4 : 4)))))]
         keyC -= (Float((row3 ? 5 : 5) & Int(keyC > 87079176.0 || keyC < -87079176.0 ? 36.0 : keyC)))
      }
      sendd.append(queue_.count ^ 2)
   }
      sendd = [sendd.count]
     var itemsPrompt: UIView! = UIView(frame:CGRect.zero)
     var commentsBlocked: [Any]! = [8, 99]
    var nchunkChartsCodecsubblock:UILabel! = UILabel()
    itemsPrompt.alpha = 0.7;
    itemsPrompt.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    itemsPrompt.frame = CGRect(x: 119, y: 26, width: 0, height: 0)
    
    nchunkChartsCodecsubblock.alpha = 0.5;
    nchunkChartsCodecsubblock.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nchunkChartsCodecsubblock.frame = CGRect(x: 134, y: 0, width: 0, height: 0)
    nchunkChartsCodecsubblock.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nchunkChartsCodecsubblock.textAlignment = .right
    nchunkChartsCodecsubblock.font = UIFont.systemFont(ofSize:17)
    nchunkChartsCodecsubblock.text = ""

    
    return nchunkChartsCodecsubblock

}





    @objc private func showMessageChatPage() {

         var ilstGolomb: UILabel! = scaleFoundationDecimalRectLabel(valueMale:84, socialSync:0.0)

      if ilstGolomb != nil {
          self.view.addSubview(ilstGolomb)
          let ilstGolomb_tag = ilstGolomb.tag
      }
      else {
          print("ilstGolomb is nil")      }

withUnsafeMutablePointer(to: &ilstGolomb) { pointer in
        _ = pointer.pointee
}


       var collectiono: [Any]! = [54, 32, 8]
   withUnsafeMutablePointer(to: &collectiono) { pointer in
    
   }
   while (collectiono.count <= 3) {
       var largeW: String! = String(cString: [111,119,110,0], encoding: .utf8)!
          var remember8: String! = String(cString: [114,111,119,115,112,97,110,0], encoding: .utf8)!
          var playZ: Double = 2.0
         largeW = "\(2)"
         remember8.append("\(largeW.count)")
         playZ += (Double(remember8.count - Int(playZ > 293065955.0 || playZ < -293065955.0 ? 6.0 : playZ)))
       var recommendY: String! = String(cString: [119,114,105,116,105,110,103,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &recommendY) { pointer in
    
      }
       var photo7: String! = String(cString: [99,111,112,121,105,110,103,0], encoding: .utf8)!
      while (recommendY.count >= 2) {
         recommendY = "\(recommendY.count)"
         break
      }
         photo7.append("\((recommendY == (String(cString:[118,0], encoding: .utf8)!) ? recommendY.count : largeW.count))")
      collectiono = [collectiono.count >> (Swift.min(largeW.count, 3))]
      break
   }

        guard !SocWilliamsEmpty.isBlocked(name: post.name) else {
            showBlockedUserAlert()
            return
        }

        let visibleController = SocBottleReportController(
            friend: makeMessageFriend(),
            returnDestination: .IMutual
        )
        visibleController.modalPresentationStyle = .fullScreen
        present(visibleController, animated: true)
    }

    @objc private func handleKeyboardFrameChange(_ notification: Notification) {
       var class_ew: String! = String(cString: [99,111,110,116,111,117,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &class_ew) { pointer in
          _ = pointer.pointee
   }
    var saveF: String! = String(cString: [115,121,109,98,111,108,105,122,101,0], encoding: .utf8)!
       var bubbley: String! = String(cString: [115,116,111,112,0], encoding: .utf8)!
       _ = bubbley
       var rechargen: Float = 3.0
       var observe4: Int = 0
      for _ in 0 ..< 2 {
         rechargen -= (Float(observe4 & Int(rechargen > 184852124.0 || rechargen < -184852124.0 ? 89.0 : rechargen)))
      }
         observe4 |= 1 >> (Swift.min(5, labs(observe4)))
       var presenterg: Int = 4
       _ = presenterg
       var replyP: Int = 1
      withUnsafeMutablePointer(to: &replyP) { pointer in
    
      }
       var y_titleL: Int = 0
       _ = y_titleL
       var flashK: Int = 5
         replyP >>= Swift.min(1, labs(y_titleL))
         y_titleL -= 1
      if replyP >= flashK {
          var happy7: [String: Any]! = [String(cString: [98,97,103,101,0], encoding: .utf8)!:14, String(cString: [110,99,111,109,105,110,103,0], encoding: .utf8)!:79, String(cString: [105,109,112,108,105,99,105,116,101,108,121,0], encoding: .utf8)!:81]
          _ = happy7
         replyP ^= observe4
         happy7["\(presenterg)"] = presenterg / (Swift.max(y_titleL, 2))
      }
      repeat {
         bubbley = "\((Int(rechargen > 322308349.0 || rechargen < -322308349.0 ? 4.0 : rechargen) / 2))"
         if bubbley == (String(cString:[106,116,97,104,112,121,122,108,118,0], encoding: .utf8)!) {
            break
         }
      } while (replyP > 2) && (bubbley == (String(cString:[106,116,97,104,112,121,122,108,118,0], encoding: .utf8)!))
         replyP ^= bubbley.count
      class_ew.append("\(observe4 + 1)")

   for _ in 0 ..< 1 {
       var list8: Bool = false
       var settingl: Double = 3.0
       var purchaseM: String! = String(cString: [112,117,115,104,101,100,0], encoding: .utf8)!
       _ = purchaseM
       var keyboardJ: [String: Any]! = [String(cString: [97,99,99,111,114,100,105,110,103,0], encoding: .utf8)!:String(cString: [116,101,115,116,105,110,103,0], encoding: .utf8)!, String(cString: [111,114,112,104,97,110,0], encoding: .utf8)!:String(cString: [99,104,101,99,107,98,111,120,0], encoding: .utf8)!, String(cString: [100,101,116,97,99,104,0], encoding: .utf8)!:String(cString: [115,101,113,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &keyboardJ) { pointer in
             _ = pointer.pointee
      }
       var screenl: [Any]! = [71, 9, 8]
      for _ in 0 ..< 3 {
         screenl.append(((list8 ? 1 : 5) * 3))
      }
         list8 = screenl.count > 50
         screenl = [(Int(settingl > 96646356.0 || settingl < -96646356.0 ? 93.0 : settingl) | (list8 ? 1 : 1))]
         list8 = !purchaseM.hasPrefix("\(settingl)")
      while (list8) {
         list8 = (keyboardJ.count & purchaseM.count) > 9
         break
      }
      repeat {
         settingl *= Double(2)
         if settingl == 1219085.0 {
            break
         }
      } while (settingl > 4.72) && (settingl == 1219085.0)
          var cellR: Bool = false
          _ = cellR
          var progressN: [Any]! = [80, 12, 78]
          var prompta: String! = String(cString: [103,114,97,110,117,108,101,112,111,115,0], encoding: .utf8)!
         keyboardJ = ["\(screenl.count)": ((cellR ? 4 : 1) << (Swift.min(screenl.count, 1)))]
         progressN.append(((String(cString:[70,0], encoding: .utf8)!) == purchaseM ? purchaseM.count : screenl.count))
         prompta = "\(((list8 ? 4 : 1) | 1))"
          var close4: [String: Any]! = [String(cString: [101,120,97,110,100,101,100,0], encoding: .utf8)!:String(cString: [99,104,97,110,103,101,99,111,117,110,116,101,114,0], encoding: .utf8)!, String(cString: [112,97,105,100,0], encoding: .utf8)!:String(cString: [115,119,105,116,99,104,105,110,103,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &close4) { pointer in
                _ = pointer.pointee
         }
          var loggedC: String! = String(cString: [114,101,106,101,99,116,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &loggedC) { pointer in
                _ = pointer.pointee
         }
          var maleO: Double = 4.0
          _ = maleO
         keyboardJ["\(list8)"] = ((list8 ? 4 : 1) & loggedC.count)
         close4 = ["\(screenl.count)": screenl.count % 3]
         maleO += (Double(Int(settingl > 373291147.0 || settingl < -373291147.0 ? 55.0 : settingl) >> (Swift.min(labs(3), 2))))
         screenl = [(Int(settingl > 109609628.0 || settingl < -109609628.0 ? 72.0 : settingl) ^ 2)]
      if !list8 {
         list8 = (settingl * Double(purchaseM.count)) <= 32.8
      }
      for _ in 0 ..< 2 {
         keyboardJ = ["\(keyboardJ.values.count)": screenl.count ^ keyboardJ.count]
      }
      repeat {
         list8 = !list8
         if list8 ? !list8 : list8 {
            break
         }
      } while (list8 ? !list8 : list8) && ((settingl / (Swift.max(4.60, 8))) >= 5.80)
          var identifierR: String! = String(cString: [115,105,110,101,0], encoding: .utf8)!
          var listR: Float = 2.0
         settingl /= Swift.max((Double(Int(settingl > 83865347.0 || settingl < -83865347.0 ? 9.0 : settingl) | keyboardJ.count)), 1)
         identifierR.append("\(purchaseM.count)")
         listR += Float(2)
         keyboardJ["\(list8)"] = keyboardJ.count
       var williamsH: Double = 1.0
         williamsH -= Double(screenl.count)
      saveF.append("\((Int(settingl > 153093097.0 || settingl < -153093097.0 ? 40.0 : settingl)))")
   }
        let label = notification.userInfo?[UIResponder.keyboardFrameEndUserInfoKey] as? CGRect
        let a_width = notification.userInfo?[UIResponder.keyboardAnimationDurationUserInfoKey] as? TimeInterval ?? 0.25
        let light = notification.userInfo?[UIResponder.keyboardAnimationCurveUserInfoKey] as? UInt ?? 7
        let package = UIView.AnimationOptions(rawValue: light << 16)

        let report = label.map { view.convert($0, from: nil).minY } ?? view.bounds.maxY
        let item = max(0, view.bounds.maxY - report)

        UIView.animate(withDuration: a_width, delay: 0, options: package) {
            self.commentBarView.transform = CGAffineTransform(translationX: 0, y: -item)
        }
    }

}

private extension SocHomeController {

@discardableResult
 func reuseBannerValidatePositionInfinityOutImageView(followingDisplay: String!) -> UIImageView! {
    var errorQ: String! = String(cString: [110,118,100,101,99,0], encoding: .utf8)!
    _ = errorQ
    var originalz: Float = 1.0
   if (5.23 - originalz) > 2.45 || (4 & errorQ.count) > 1 {
       var taskV: Int = 0
          var libraryf: String! = String(cString: [115,110,105,112,112,101,116,0], encoding: .utf8)!
          _ = libraryf
          var valueh: String! = String(cString: [114,97,116,101,99,111,110,116,114,111,108,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &valueh) { pointer in
    
         }
         taskV >>= Swift.min(labs(3), 1)
         libraryf = "\(3)"
         valueh.append("\(1)")
         taskV |= 3 << (Swift.min(5, labs(taskV)))
      if taskV == taskV {
         taskV -= taskV
      }
      errorQ.append("\(errorQ.count - 1)")
   }
   while ((Int(originalz > 255491190.0 || originalz < -255491190.0 ? 7.0 : originalz) / (Swift.max(5, errorQ.count))) > 5 || 4.53 > (Float(errorQ.count) / (Swift.max(4, originalz)))) {
       var selectedo: Int = 0
       _ = selectedo
       var containerK: Int = 1
      withUnsafeMutablePointer(to: &containerK) { pointer in
    
      }
       var profilea: String! = String(cString: [97,101,118,97,108,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &profilea) { pointer in
             _ = pointer.pointee
      }
       var documents9: String! = String(cString: [102,105,108,101,102,117,110,99,0], encoding: .utf8)!
       var ring0: String! = String(cString: [104,105,103,104,108,105,103,116,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &ring0) { pointer in
    
      }
         documents9.append("\(containerK | 1)")
         containerK -= 1
          var inputW: [Any]! = [34, 46, 5]
         profilea.append("\(profilea.count >> (Swift.min(documents9.count, 4)))")
         inputW = [selectedo & 1]
         documents9 = "\(profilea.count & 2)"
      repeat {
         documents9 = "\(documents9.count)"
         if documents9 == (String(cString:[51,98,54,115,101,117,99,53,112,0], encoding: .utf8)!) {
            break
         }
      } while ((containerK - documents9.count) >= 2 || (2 - documents9.count) >= 5) && (documents9 == (String(cString:[51,98,54,115,101,117,99,53,112,0], encoding: .utf8)!))
         containerK -= profilea.count ^ containerK
      if documents9.hasSuffix(profilea) {
          var colorT: [Any]! = [String(cString: [99,97,110,111,112,117,115,0], encoding: .utf8)!, String(cString: [106,109,111,114,101,99,102,103,0], encoding: .utf8)!, String(cString: [115,99,114,111,108,108,101,100,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &colorT) { pointer in
                _ = pointer.pointee
         }
          var ringr: String! = String(cString: [108,115,112,102,108,112,99,0], encoding: .utf8)!
          _ = ringr
          var queueD: Double = 2.0
         withUnsafeMutablePointer(to: &queueD) { pointer in
                _ = pointer.pointee
         }
          var avatara: [Any]! = [String(cString: [109,102,113,101,0], encoding: .utf8)!, String(cString: [104,101,118,99,100,115,112,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &avatara) { pointer in
                _ = pointer.pointee
         }
          var gesturec: String! = String(cString: [110,97,110,111,98,101,110,99,104,109,97,114,107,0], encoding: .utf8)!
          _ = gesturec
         documents9 = "\(ring0.count ^ 2)"
         colorT.append(avatara.count << (Swift.min(labs(1), 2)))
         ringr = "\(1 - colorT.count)"
         queueD -= Double(documents9.count)
         avatara.append(1)
         gesturec = "\(documents9.count | selectedo)"
      }
       var leftf: String! = String(cString: [110,99,98,99,0], encoding: .utf8)!
       var should6: String! = String(cString: [103,109,116,105,109,101,0], encoding: .utf8)!
          var psaiY: [Any]! = [30, 64, 29]
         selectedo -= 2
         psaiY.append(((String(cString:[73,0], encoding: .utf8)!) == leftf ? ring0.count : leftf.count))
         documents9.append("\(selectedo << (Swift.min(labs(2), 3)))")
          var gestures: [String: Any]! = [String(cString: [115,105,108,101,110,99,101,100,0], encoding: .utf8)!:10, String(cString: [114,101,112,97,105,110,116,0], encoding: .utf8)!:92, String(cString: [115,101,110,100,101,114,0], encoding: .utf8)!:5]
          var identifierk: Float = 1.0
         profilea.append("\((should6 == (String(cString:[106,0], encoding: .utf8)!) ? selectedo : should6.count))")
         gestures = [profilea: 2 - leftf.count]
         identifierk -= Float(2 + containerK)
      if selectedo >= 2 {
         selectedo ^= leftf.count << (Swift.min(labs(3), 4))
      }
         leftf = "\(1)"
      for _ in 0 ..< 3 {
         selectedo -= profilea.count + should6.count
      }
         selectedo |= ring0.count << (Swift.min(profilea.count, 3))
      errorQ.append("\(errorQ.count)")
      break
   }
     let messageHappy: Double = 73.0
     var attributesPsai: UIView! = UIView(frame:CGRect.zero)
     var chatCompanion: Double = 56.0
    var pubexpVmhd = UIImageView()
    pubexpVmhd.contentMode = .scaleAspectFit
    pubexpVmhd.animationRepeatCount = 0
    pubexpVmhd.image = UIImage(named:String(cString: [99,111,109,112,97,110,105,111,110,0], encoding: .utf8)!)
    pubexpVmhd.frame = CGRect(x: 113, y: 88, width: 0, height: 0)
    pubexpVmhd.alpha = 0.1;
    pubexpVmhd.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    attributesPsai.frame = CGRect(x: 39, y: 119, width: 0, height: 0)
    attributesPsai.alpha = 0.7;
    attributesPsai.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    

    
    return pubexpVmhd

}






    func makeMessageFriend() -> SocEmotion {

         var observerPrevents: UIImageView! = reuseBannerValidatePositionInfinityOutImageView(followingDisplay:String(cString: [116,117,110,105,110,103,0], encoding: .utf8)!)

      if observerPrevents != nil {
          self.view.addSubview(observerPrevents)
          let observerPrevents_tag = observerPrevents.tag
      }
      else {
          print("observerPrevents is nil")      }

withUnsafeMutablePointer(to: &observerPrevents) { pointer in
        _ = pointer.pointee
}


       var now0: String! = String(cString: [102,114,97,109,101,0], encoding: .utf8)!
    _ = now0
      now0.append("\(now0.count ^ now0.count)")

return         SocEmotion(
            name: post.name,
            message: post.content,
            avatarColor: UIColor(red: 0.76, green: 0.87, blue: 1.00, alpha: 1),
            avatarImageName: post.avatarImageName,
            profileVideoFileName: post.profileVideoFileName
        )
    }


    func showReportMenu(from sourceView: UIView) {
       var seedh: String! = String(cString: [99,97,116,101,103,111,114,105,122,101,0], encoding: .utf8)!
       var mutualU: String! = String(cString: [116,111,111,108,116,105,112,0], encoding: .utf8)!
       var attributesc: String! = String(cString: [98,111,117,110,99,105,110,103,0], encoding: .utf8)!
       var j_centern: String! = String(cString: [102,105,110,97,108,0], encoding: .utf8)!
       var emotionA: String! = String(cString: [110,111,110,101,109,112,116,121,0], encoding: .utf8)!
         j_centern = "\(j_centern.count)"
      while (!attributesc.hasPrefix(mutualU)) {
         attributesc = "\(mutualU.count % 1)"
         break
      }
      for _ in 0 ..< 1 {
         emotionA.append("\(2 / (Swift.max(6, attributesc.count)))")
      }
      if emotionA != String(cString:[99,0], encoding: .utf8)! {
         j_centern.append("\(emotionA.count * mutualU.count)")
      }
         emotionA = "\(2 << (Swift.min(2, attributesc.count)))"
      seedh = "\(mutualU.count & seedh.count)"

        showEmotionReportMenu(
            from: sourceView,
            onReport: { [weak self] in
                self?.showReportReceivedAlert()
            },
            onBlock: { [weak self] in
                guard let self else { return }

                self.showBlockConfirmation(for: self.post.blockedUser) { [weak self] in
                    self?.returnToPrimaryPage(.IMutual)
                }
            }
        )
    }

}
