
import Foundation

import AVFoundation
import UIKit

final class SocSyncFemaleController: UIViewController {
var conversationBadgeString: String!
var recommendHomeNamesDictionary: [String: Any]!
private var reusePadding: Double? = 0.0




    private enum PSFCompanionFriendI {
        static let backIcon = "EmotionSync/UserProfile/user_profile_back_icon"
        static let alertIcon = "EmotionSync/UserProfile/user_profile_alert_icon"
        static let starNormal = "EmotionSync/UserProfile/user_profile_star_normal"
        static let starSelected = "EmotionSync/UserProfile/user_profile_star_selected"
        static let videoButton = "EmotionSync/UserProfile/user_profile_video_button"
        static let mailButton = "EmotionSync/UserProfile/user_profile_mail_button"
        static let cardBackground = "EmotionSync/emotion_sync_post_card_background"
        static let avatarRing = "EmotionSync/emotion_sync_avatar_ring"
    }

    private enum KQFSessiond {
        static let backIcon = "Common/common_back_icon"
        static let alertIcon = "EmotionSync/UserProfile/user_profile_play_button"
        static let videoButton = "EmotionSync/emotion_sync_video_button"
        static let mailButton = "EmotionSync/emotion_sync_mail_button"
    }

    private let post: SocScene
    private let headerVideoView = UIView()
    private let starButton = UIButton(type: .custom)
    private let playButton = UIButton(type: .custom)
    private var headerPlayer: AVPlayer?
    private var headerPlayerLayer: AVPlayerLayer?
    private var isHeaderVideoPlaying = false

    init(post: SocScene) {
        self.post = post
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

@discardableResult
 func signThanSymbolCompletionKeyboard(default_sFlash: Bool, followField: Int, optionsAppend: Int) -> Float {
    var colorA: String! = String(cString: [121,101,116,0], encoding: .utf8)!
    var q_view7: Float = 3.0
    var cellC: Float = 4.0
   for _ in 0 ..< 2 {
      colorA = "\((colorA.count / (Swift.max(4, Int(q_view7 > 56216039.0 || q_view7 < -56216039.0 ? 33.0 : q_view7)))))"
   }
   if (1.3 * q_view7) >= 5.95 || (q_view7 * cellC) >= 1.3 {
       var profile_: [String: Any]! = [String(cString: [115,108,102,0], encoding: .utf8)!:25, String(cString: [105,100,115,0], encoding: .utf8)!:6]
       var sharingm: [Any]! = [33, 20]
      withUnsafeMutablePointer(to: &sharingm) { pointer in
    
      }
       var rightW: String! = String(cString: [99,101,114,116,112,111,108,0], encoding: .utf8)!
       var constraintss: Double = 4.0
       var keyboardY: String! = String(cString: [105,100,99,105,110,0], encoding: .utf8)!
          var switch_tuV: Int = 3
         profile_ = [keyboardY: keyboardY.count]
         switch_tuV -= profile_.keys.count >> (Swift.min(labs(1), 3))
      while (5 < (rightW.count | 4)) {
          var summariesI: String! = String(cString: [98,111,111,108,101,97,110,0], encoding: .utf8)!
          var emotionsF: Bool = true
          _ = emotionsF
          var privacyb: Double = 0.0
         rightW = "\((Int(privacyb > 156328773.0 || privacyb < -156328773.0 ? 91.0 : privacyb) % 1))"
         summariesI.append("\(2)")
         emotionsF = (10 == ((emotionsF ? sharingm.count : 10) + sharingm.count))
         break
      }
         profile_["\(constraintss)"] = sharingm.count
         keyboardY.append("\((Int(constraintss > 188512304.0 || constraintss < -188512304.0 ? 35.0 : constraintss)))")
         rightW.append("\(sharingm.count)")
         profile_ = ["\(profile_.count)": (Int(constraintss > 360855514.0 || constraintss < -360855514.0 ? 94.0 : constraintss) & profile_.values.count)]
          var n_heightJ: String! = String(cString: [102,105,108,108,101,114,0], encoding: .utf8)!
          var paddingP: [String: Any]! = [String(cString: [119,101,98,112,97,103,101,115,0], encoding: .utf8)!:87, String(cString: [99,114,111,115,115,111,118,101,114,0], encoding: .utf8)!:73, String(cString: [115,114,112,0], encoding: .utf8)!:15]
         withUnsafeMutablePointer(to: &paddingP) { pointer in
                _ = pointer.pointee
         }
          var chatt: String! = String(cString: [99,121,99,108,105,99,114,101,102,114,101,115,104,0], encoding: .utf8)!
         constraintss += Double(2)
         n_heightJ = "\(1 << (Swift.min(5, n_heightJ.count)))"
         paddingP["\(keyboardY)"] = profile_.keys.count
         chatt.append("\(rightW.count)")
      while ((sharingm.count | profile_.values.count) == 1 || (sharingm.count | profile_.values.count) == 1) {
         sharingm.append(keyboardY.count)
         break
      }
       var femaleg: String! = String(cString: [109,115,103,0], encoding: .utf8)!
       var bannerX: String! = String(cString: [105,110,102,111,115,0], encoding: .utf8)!
      if 2.56 > (constraintss / (Swift.max(Double(profile_.count), 3))) {
         constraintss += (Double((String(cString:[105,0], encoding: .utf8)!) == bannerX ? bannerX.count : femaleg.count))
      }
         femaleg = "\(profile_.keys.count + sharingm.count)"
         rightW = "\(keyboardY.count ^ femaleg.count)"
         rightW.append("\(keyboardY.count)")
          var x_imageY: String! = String(cString: [116,105,110,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &x_imageY) { pointer in
    
         }
         femaleg.append("\((Int(constraintss > 108045495.0 || constraintss < -108045495.0 ? 99.0 : constraintss) << (Swift.min(x_imageY.count, 3))))")
      while ((sharingm.count * keyboardY.count) < 4) {
         sharingm = [femaleg.count ^ sharingm.count]
         break
      }
      q_view7 /= Swift.max(5, (Float(Int(cellC > 62705035.0 || cellC < -62705035.0 ? 13.0 : cellC))))
   }
      q_view7 -= (Float(1 >> (Swift.min(labs(Int(cellC > 215048297.0 || cellC < -215048297.0 ? 8.0 : cellC)), 1))))
   return q_view7

}





    override func viewDidLoad() {

         let rprobeDiscarded: Float = signThanSymbolCompletionKeyboard(default_sFlash:true, followField:78, optionsAppend:92)

      print(rprobeDiscarded)

_ = rprobeDiscarded


       var privacyd: String! = String(cString: [97,112,112,114,111,120,105,109,97,116,105,111,110,0], encoding: .utf8)!
    _ = privacyd
    var modey: Double = 4.0
   withUnsafeMutablePointer(to: &modey) { pointer in
    
   }
       var q_unlockd: Int = 3
       var follows: [Any]! = [String(cString: [110,111,116,104,105,110,103,0], encoding: .utf8)!, String(cString: [115,119,114,0], encoding: .utf8)!, String(cString: [101,109,117,101,100,103,101,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &follows) { pointer in
    
      }
       var results: Float = 4.0
      for _ in 0 ..< 1 {
         q_unlockd |= (Int(results > 330504620.0 || results < -330504620.0 ? 83.0 : results) * q_unlockd)
      }
          var nowP: String! = String(cString: [112,114,111,103,114,101,115,105,118,101,0], encoding: .utf8)!
         follows.append((Int(results > 381732790.0 || results < -381732790.0 ? 23.0 : results) - 2))
         nowP.append("\((Int(results > 225222479.0 || results < -225222479.0 ? 95.0 : results)))")
          var bottomu: Float = 0.0
          _ = bottomu
         follows.append(follows.count)
         bottomu += (Float(Int(bottomu > 176478658.0 || bottomu < -176478658.0 ? 45.0 : bottomu)))
          var simoS: [Any]! = [String(cString: [111,109,105,116,116,105,110,103,0], encoding: .utf8)!, String(cString: [115,109,97,108,108,0], encoding: .utf8)!]
         follows.append((1 << (Swift.min(labs(Int(results > 173279362.0 || results < -173279362.0 ? 2.0 : results)), 5))))
         simoS = [simoS.count]
      if q_unlockd == 4 {
          var synce: Float = 1.0
          var ynewsG: Float = 2.0
          var p_heightb: Double = 1.0
         results /= Swift.max(1, Float(follows.count))
         synce /= Swift.max((Float(Int(results > 43742470.0 || results < -43742470.0 ? 62.0 : results))), 3)
         ynewsG += (Float(q_unlockd - Int(p_heightb > 289632909.0 || p_heightb < -289632909.0 ? 27.0 : p_heightb)))
         p_heightb -= (Double(Int(ynewsG > 208692317.0 || ynewsG < -208692317.0 ? 38.0 : ynewsG) - 1))
      }
      if 1.91 >= (results * 2.30) && 2 >= (follows.count % (Swift.max(4, 2))) {
         results -= Float(3)
      }
      while (2 <= (q_unlockd - 3) && 1 <= (follows.count - 3)) {
          var message4: Double = 0.0
          _ = message4
          var release_njv: [String: Any]! = [String(cString: [120,105,110,103,0], encoding: .utf8)!:29, String(cString: [115,108,105,112,112,97,103,101,115,0], encoding: .utf8)!:29, String(cString: [100,97,116,97,98,97,115,101,115,0], encoding: .utf8)!:69]
          var psaiy: String! = String(cString: [97,108,112,104,97,98,101,116,115,0], encoding: .utf8)!
         follows.append(release_njv.keys.count)
         message4 += Double(psaiy.count)
         psaiy = "\(3)"
         break
      }
      repeat {
          var main_yy: String! = String(cString: [114,97,100,98,103,0], encoding: .utf8)!
          var size_ql8: String! = String(cString: [115,111,98,105,110,100,0], encoding: .utf8)!
         results += Float(3 + q_unlockd)
         main_yy = "\(size_ql8.count)"
         size_ql8.append("\(2)")
         if 2174597.0 == results {
            break
         }
      } while (2174597.0 == results) && ((results / 3.29) == 4.87 || 5 == (q_unlockd / 5))
      repeat {
          var cellT: String! = String(cString: [100,111,119,110,108,111,97,100,105,110,103,0], encoding: .utf8)!
          var angryE: String! = String(cString: [115,97,110,105,116,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &angryE) { pointer in
                _ = pointer.pointee
         }
          var insetq: Double = 0.0
         results -= Float(angryE.count ^ cellT.count)
         insetq -= Double(q_unlockd)
         if 3130654.0 == results {
            break
         }
      } while (follows.contains { $0 as? Float == results }) && (3130654.0 == results)
      privacyd = "\(privacyd.count % 1)"

      privacyd.append("\((privacyd.count - Int(modey > 152172495.0 || modey < -152172495.0 ? 80.0 : modey)))")
        super.viewDidLoad()
   while (4.63 <= (2.81 + modey)) {
       var update_gD: [Any]! = [String(cString: [112,114,105,109,0], encoding: .utf8)!, String(cString: [109,111,110,116,104,115,0], encoding: .utf8)!, String(cString: [103,108,107,0], encoding: .utf8)!]
       var feelingsO: Int = 0
       _ = feelingsO
       var messages_: Bool = false
         messages_ = feelingsO < 32
          var constraintsz: [Any]! = [String(cString: [99,104,114,111,109,97,104,111,108,100,0], encoding: .utf8)!, String(cString: [97,117,116,111,102,105,108,108,0], encoding: .utf8)!, String(cString: [105,110,118,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &constraintsz) { pointer in
    
         }
          var loadingT: Float = 5.0
          _ = loadingT
         feelingsO -= ((messages_ ? 5 : 2) >> (Swift.min(labs(feelingsO), 4)))
         constraintsz.append(1)
         loadingT /= Swift.max(Float(3), 1)
      if !messages_ {
         messages_ = (update_gD.contains { $0 as? Int == feelingsO })
      }
      while (messages_) {
          var leadingX: String! = String(cString: [108,111,103,115,0], encoding: .utf8)!
         messages_ = update_gD.count <= 37
         leadingX.append("\(feelingsO)")
         break
      }
         update_gD.append(1 - update_gD.count)
      if !update_gD.contains { $0 as? Int == feelingsO } {
          var identifier9: [Any]! = [19, 23, 44]
         withUnsafeMutablePointer(to: &identifier9) { pointer in
                _ = pointer.pointee
         }
          var companionn: String! = String(cString: [116,101,108,0], encoding: .utf8)!
          var return_5l: String! = String(cString: [120,108,97,98,101,108,119,105,100,116,104,0], encoding: .utf8)!
          var post5: Double = 5.0
          var enriched4: String! = String(cString: [99,112,117,102,108,97,103,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &enriched4) { pointer in
                _ = pointer.pointee
         }
         update_gD = [enriched4.count / 3]
         identifier9.append(update_gD.count - 3)
         companionn.append("\(update_gD.count)")
         return_5l.append("\(((messages_ ? 1 : 1) + Int(post5 > 250716712.0 || post5 < -250716712.0 ? 1.0 : post5)))")
         post5 /= Swift.max(2, Double(feelingsO % (Swift.max(update_gD.count, 10))))
      }
      repeat {
          var bannerg: String! = String(cString: [114,111,119,115,0], encoding: .utf8)!
          _ = bannerg
          var connectingS: Int = 1
          var signy: Float = 2.0
         feelingsO -= feelingsO
         bannerg.append("\((1 | Int(signy > 20607260.0 || signy < -20607260.0 ? 74.0 : signy)))")
         connectingS ^= bannerg.count - 3
         signy -= Float(3)
         if 279890 == feelingsO {
            break
         }
      } while (279890 == feelingsO) && (3 == (feelingsO >> (Swift.min(update_gD.count, 5))))
      while ((3 ^ feelingsO) == 4 || messages_) {
         messages_ = (feelingsO ^ update_gD.count) <= 10
         break
      }
      repeat {
          var normalizedw: String! = String(cString: [103,101,110,101,114,97,116,111,114,115,0], encoding: .utf8)!
          var loggeda: String! = String(cString: [115,101,116,116,105,110,103,0], encoding: .utf8)!
          var sharingJ: String! = String(cString: [115,117,98,113,117,101,114,121,0], encoding: .utf8)!
          var libraryE: Float = 3.0
          var kariW: String! = String(cString: [115,117,98,115,116,114,0], encoding: .utf8)!
         feelingsO -= feelingsO
         normalizedw.append("\(((String(cString:[53,0], encoding: .utf8)!) == loggeda ? loggeda.count : normalizedw.count))")
         sharingJ = "\((update_gD.count & (messages_ ? 3 : 2)))"
         libraryE -= Float(kariW.count)
         kariW.append("\(((messages_ ? 4 : 1) | feelingsO))")
         if feelingsO == 4550237 {
            break
         }
      } while (feelingsO == 4550237) && ((feelingsO + update_gD.count) == 3 || 1 == (update_gD.count + 3))
      modey -= (Double((messages_ ? 1 : 4) / (Swift.max(1, feelingsO))))
      break
   }

        setupContent()
    }

@discardableResult
 func setupDetailHandleCacheDismissalNavigationView() -> UIView! {
    var mailN: String! = String(cString: [99,114,101,97,116,111,114,0], encoding: .utf8)!
    var userK: Bool = false
   withUnsafeMutablePointer(to: &userK) { pointer in
    
   }
   repeat {
      userK = !mailN.contains("\(userK)")
      if userK ? !userK : userK {
         break
      }
   } while (userK ? !userK : userK) && (3 < mailN.count)
      userK = !userK || mailN.count < 80
     var insetsNickname: Bool = false
     let dataChat: UIView! = UIView(frame:CGRect(x: 3, y: 167, width: 0, height: 0))
     var clearKeywords: Float = 60.0
    var myrndOrdering: UIView! = UIView(frame:CGRect.zero)
    myrndOrdering.frame = CGRect(x: 23, y: 42, width: 0, height: 0)
    myrndOrdering.alpha = 0.7;
    myrndOrdering.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    dataChat.frame = CGRect(x: 138, y: 9, width: 0, height: 0)
    dataChat.alpha = 0.1;
    dataChat.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    myrndOrdering.addSubview(dataChat)

    
    return myrndOrdering

}





    override func viewDidAppear(_ animated: Bool) {

         var dxtaLamp: UIView! = setupDetailHandleCacheDismissalNavigationView()

      if dxtaLamp != nil {
          self.view.addSubview(dxtaLamp)
          let dxtaLamp_tag = dxtaLamp.tag
      }
      else {
          print("dxtaLamp is nil")      }

withUnsafeMutablePointer(to: &dxtaLamp) { pointer in
        _ = pointer.pointee
}


       var observationf: Bool = true
   for _ in 0 ..< 3 {
      observationf = (observationf ? !observationf : observationf)
   }

        super.viewDidAppear(animated)

        playHeaderVideo()
    }

    override func viewDidLayoutSubviews() {
       var homeR: String! = String(cString: [116,117,110,110,101,108,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &homeR) { pointer in
          _ = pointer.pointee
   }
   if homeR.count >= 5 {
      homeR = "\(homeR.count | homeR.count)"
   }

        super.viewDidLayoutSubviews()

        headerPlayerLayer?.frame = headerVideoView.bounds
    }

@discardableResult
 func settingItemHealingComponentLight() -> Double {
    var f_animationZ: String! = String(cString: [115,101,99,116,105,111,110,0], encoding: .utf8)!
    var controlr: String! = String(cString: [100,101,99,114,101,109,101,110,116,0], encoding: .utf8)!
   while (3 == f_animationZ.count) {
      f_animationZ.append("\(f_animationZ.count | 1)")
      break
   }
       var headerC: String! = String(cString: [97,114,114,111,119,0], encoding: .utf8)!
       var visibleF: String! = String(cString: [102,104,116,120,0], encoding: .utf8)!
         headerC.append("\(headerC.count)")
       var postsA: Bool = false
       var simoS: Bool = true
       var connectingH: Float = 1.0
      withUnsafeMutablePointer(to: &connectingH) { pointer in
             _ = pointer.pointee
      }
      repeat {
         visibleF.append("\((3 >> (Swift.min(labs(Int(connectingH > 62116797.0 || connectingH < -62116797.0 ? 40.0 : connectingH)), 4))))")
         if 2775411 == visibleF.count {
            break
         }
      } while (visibleF.count > 4) && (2775411 == visibleF.count)
      for _ in 0 ..< 2 {
          var signupp: Int = 2
          var source8: String! = String(cString: [100,101,112,114,101,99,105,97,116,101,100,104,0], encoding: .utf8)!
         connectingH += Float(3)
         signupp -= 1
         source8 = "\(headerC.count | 3)"
      }
      for _ in 0 ..< 3 {
         connectingH -= Float(visibleF.count % (Swift.max(headerC.count, 6)))
      }
      controlr.append("\(controlr.count / 2)")
     var preferredHero: String! = String(cString: [115,108,97,115,104,101,115,0], encoding: .utf8)!
    var basepointCorrespondents:Double = 0

    return basepointCorrespondents

}





    override func viewWillDisappear(_ animated: Bool) {

         var neterrnoElision: Double = settingItemHealingComponentLight()

      print(neterrnoElision)

withUnsafeMutablePointer(to: &neterrnoElision) { pointer in
        _ = pointer.pointee
}


       var observationT: String! = String(cString: [107,98,105,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &observationT) { pointer in
    
   }
      observationT.append("\((observationT == (String(cString:[51,0], encoding: .utf8)!) ? observationT.count : observationT.count))")

        super.viewWillDisappear(animated)

        pauseHeaderVideo()
    }

    deinit {
        NotificationCenter.default.removeObserver(self)
    }

@discardableResult
 func trackPrioritySlogan(rawFace: Float, captureFriend_cj: Float) -> Bool {
    var logint: Bool = false
    var bottomL: Double = 1.0
    _ = bottomL
    var intrinsicR: Bool = false
   while (!intrinsicR && !logint) {
      logint = 53.54 < bottomL
      break
   }
   while (intrinsicR) {
      logint = !intrinsicR
      break
   }
      intrinsicR = !intrinsicR
   return logint

}





    private func setupContent() {

         let medianCooldown: Bool = trackPrioritySlogan(rawFace:4.0, captureFriend_cj:23.0)

      if medianCooldown {
          print("now")
      }

_ = medianCooldown


       var adds: [Any]! = [String(cString: [100,101,115,104,97,107,101,0], encoding: .utf8)!, String(cString: [117,116,99,116,105,109,101,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &adds) { pointer in
          _ = pointer.pointee
   }
    var finish2: Int = 0
      adds = [2 + adds.count]

   while (5 <= (adds.count / (Swift.max(3, 9))) || 1 <= (3 / (Swift.max(3, finish2)))) {
      adds = [adds.count * 2]
      break
   }
        view.backgroundColor = UIColor(red: 0.95, green: 0.96, blue: 0.98, alpha: 1)
      finish2 ^= finish2

        let communityView = UIScrollView()
        communityView.backgroundColor = .clear
        communityView.showsVerticalScrollIndicator = false
        communityView.contentInsetAdjustmentBehavior = .never
        communityView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(communityView)

        let class_v1View = UIView()
        class_v1View.backgroundColor = UIColor(red: 0.96, green: 0.98, blue: 1.00, alpha: 1)
        class_v1View.translatesAutoresizingMaskIntoConstraints = false
        communityView.addSubview(class_v1View)

        configureHeaderVideoView()
        class_v1View.addSubview(headerVideoView)

        let insufficientButton = UIButton(type: .custom)
        insufficientButton.setImage(loadImage(named: PSFCompanionFriendI.backIcon, fallbackName: KQFSessiond.backIcon), for: .normal)
        insufficientButton.imageView?.contentMode = .scaleAspectFit
        insufficientButton.addTarget(self, action: #selector(closePage), for: .touchUpInside)
        insufficientButton.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(insufficientButton)

        let fileButton = UIButton(type: .custom)
        fileButton.setImage(loadImage(named: PSFCompanionFriendI.alertIcon, fallbackName: KQFSessiond.alertIcon), for: .normal)
        fileButton.imageView?.contentMode = .scaleAspectFit
        fileButton.addTarget(self, action: #selector(showReportMenu(_:)), for: .touchUpInside)
        fileButton.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(fileButton)

        configurePlayButton()
        class_v1View.addSubview(playButton)
        
        let arlanView = UIImageView(image: UIImage(named: "EmotionSync/ShareEmotions/share_emotions_background"))
        arlanView.contentMode = .scaleToFill
        arlanView.clipsToBounds = true
        arlanView.layer.cornerRadius = 10
        arlanView.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(arlanView)

        let screenView = UIView()
        screenView.backgroundColor = .clear
        screenView.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(screenView)

        let pickerView = makeProfileAvatarView(cornerRadius: 33)
        class_v1View.addSubview(pickerView)

        let delete_rLabel = UILabel()
        delete_rLabel.text = post.name
        delete_rLabel.font = .systemFont(ofSize: 17, weight: .semibold)
        delete_rLabel.textColor = UIColor(red: 0.19, green: 0.19, blue: 0.20, alpha: 1)
        delete_rLabel.translatesAutoresizingMaskIntoConstraints = false
        screenView.addSubview(delete_rLabel)

        configureStarButton()
        screenView.addSubview(starButton)

        let areaButton = UIButton(type: .custom)
        areaButton.setImage(loadImage(named: PSFCompanionFriendI.videoButton, fallbackName: KQFSessiond.videoButton), for: .normal)
        areaButton.addTarget(self, action: #selector(showVideoCallPage), for: .touchUpInside)
        areaButton.translatesAutoresizingMaskIntoConstraints = false
        screenView.addSubview(areaButton)

        let pickerButton = UIButton(type: .custom)
        pickerButton.setImage(loadImage(named: PSFCompanionFriendI.mailButton, fallbackName: KQFSessiond.mailButton), for: .normal)
        pickerButton.addTarget(self, action: #selector(showMessageChatPage), for: .touchUpInside)
        pickerButton.translatesAutoresizingMaskIntoConstraints = false
        screenView.addSubview(pickerButton)

        let local_ewLabel = UILabel()
        local_ewLabel.text = "Post"
        local_ewLabel.font = .systemFont(ofSize: 21, weight: .bold)
        local_ewLabel.textColor = .black
        local_ewLabel.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(local_ewLabel)

        let f_playerView = makeProfilePostCard()
        class_v1View.addSubview(f_playerView)

        NSLayoutConstraint.activate([
            communityView.topAnchor.constraint(equalTo: view.topAnchor),
            communityView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            communityView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            communityView.bottomAnchor.constraint(equalTo: view.bottomAnchor),

            class_v1View.topAnchor.constraint(equalTo: communityView.contentLayoutGuide.topAnchor),
            class_v1View.leadingAnchor.constraint(equalTo: communityView.contentLayoutGuide.leadingAnchor),
            class_v1View.trailingAnchor.constraint(equalTo: communityView.contentLayoutGuide.trailingAnchor),
            class_v1View.bottomAnchor.constraint(equalTo: communityView.contentLayoutGuide.bottomAnchor),
            class_v1View.widthAnchor.constraint(equalTo: communityView.frameLayoutGuide.widthAnchor),
            class_v1View.heightAnchor.constraint(greaterThanOrEqualTo: view.heightAnchor),

            headerVideoView.topAnchor.constraint(equalTo: class_v1View.topAnchor),
            headerVideoView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor),
            headerVideoView.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor),
            headerVideoView.heightAnchor.constraint(equalToConstant: 598),

            insufficientButton.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 13),
            insufficientButton.topAnchor.constraint(equalTo: view.safeAreaLayoutGuide.topAnchor, constant: 3),
            insufficientButton.widthAnchor.constraint(equalToConstant: 44),
            insufficientButton.heightAnchor.constraint(equalToConstant: 44),

            fileButton.trailingAnchor.constraint(equalTo: view.trailingAnchor, constant: -13),
            fileButton.topAnchor.constraint(equalTo: view.safeAreaLayoutGuide.topAnchor, constant: 3),
            fileButton.widthAnchor.constraint(equalToConstant: 44),
            fileButton.heightAnchor.constraint(equalToConstant: 44),

            playButton.centerXAnchor.constraint(equalTo: class_v1View.centerXAnchor),
            playButton.topAnchor.constraint(equalTo: class_v1View.topAnchor, constant: 340),
            playButton.widthAnchor.constraint(equalToConstant: 48),
            playButton.heightAnchor.constraint(equalToConstant: 48),
            
            arlanView.topAnchor.constraint(equalTo: headerVideoView.bottomAnchor, constant: -46),
            arlanView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor),
            arlanView.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor),
            arlanView.heightAnchor.constraint(equalToConstant: 200),

            screenView.topAnchor.constraint(equalTo: headerVideoView.bottomAnchor, constant: -46),
            screenView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor),
            screenView.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor),
            screenView.heightAnchor.constraint(equalToConstant: 68),

            pickerView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor, constant: 20),
            pickerView.centerYAnchor.constraint(equalTo: screenView.topAnchor, constant: 18),
            pickerView.widthAnchor.constraint(equalToConstant: 66),
            pickerView.heightAnchor.constraint(equalToConstant: 66),

            delete_rLabel.leadingAnchor.constraint(equalTo: pickerView.trailingAnchor, constant: 16),
            delete_rLabel.topAnchor.constraint(equalTo: screenView.topAnchor, constant: 20),

            pickerButton.trailingAnchor.constraint(equalTo: screenView.trailingAnchor, constant: -22),
            pickerButton.topAnchor.constraint(equalTo: screenView.topAnchor, constant: 13),
            pickerButton.widthAnchor.constraint(equalToConstant: 36),
            pickerButton.heightAnchor.constraint(equalToConstant: 36),

            areaButton.trailingAnchor.constraint(equalTo: pickerButton.leadingAnchor, constant: -22),
            areaButton.topAnchor.constraint(equalTo: pickerButton.topAnchor),
            areaButton.widthAnchor.constraint(equalToConstant: 36),
            areaButton.heightAnchor.constraint(equalToConstant: 36),

            starButton.trailingAnchor.constraint(equalTo: areaButton.leadingAnchor, constant: -22),
            starButton.centerYAnchor.constraint(equalTo: areaButton.centerYAnchor),
            starButton.widthAnchor.constraint(equalToConstant: 36),
            starButton.heightAnchor.constraint(equalToConstant: 36),

            local_ewLabel.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor, constant: 20),
            local_ewLabel.topAnchor.constraint(equalTo: screenView.bottomAnchor, constant: 5),

            f_playerView.topAnchor.constraint(equalTo: local_ewLabel.bottomAnchor, constant: 18),
            f_playerView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor, constant: 15),
            f_playerView.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor, constant: -15),
            f_playerView.heightAnchor.constraint(equalToConstant: 230),
            f_playerView.bottomAnchor.constraint(lessThanOrEqualTo: class_v1View.bottomAnchor, constant: -24)
        ])
    }

@discardableResult
 func playAccessLine(xcopy_wuAvoidance: String!, providerInitial: [Any]!, shareShare: Double) -> [String: Any]! {
    var backp: Double = 5.0
    var micC: String! = String(cString: [109,98,97,102,102,0], encoding: .utf8)!
    var handlez: [String: Any]! = [String(cString: [110,101,105,103,104,98,111,114,105,110,103,0], encoding: .utf8)!:37, String(cString: [114,101,109,117,120,0], encoding: .utf8)!:41]
       var register_cpA: Double = 4.0
       _ = register_cpA
       var alertU: Float = 5.0
       var providera: String! = String(cString: [104,117,102,102,0], encoding: .utf8)!
          var observerl: String! = String(cString: [111,112,97,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &observerl) { pointer in
    
         }
          var messagesq: Float = 1.0
          _ = messagesq
          var displayT: Double = 4.0
         register_cpA -= Double(3)
         observerl = "\((Int(displayT > 324584189.0 || displayT < -324584189.0 ? 13.0 : displayT) % (Swift.max(Int(messagesq > 247119382.0 || messagesq < -247119382.0 ? 52.0 : messagesq), 2))))"
         messagesq += (Float(Int(displayT > 134180254.0 || displayT < -134180254.0 ? 22.0 : displayT) ^ 2))
         alertU *= (Float(Int(register_cpA > 4882552.0 || register_cpA < -4882552.0 ? 4.0 : register_cpA)))
          var summaryx: String! = String(cString: [116,104,101,116,97,0], encoding: .utf8)!
         alertU -= (Float(Int(register_cpA > 267378252.0 || register_cpA < -267378252.0 ? 23.0 : register_cpA) & 2))
         summaryx = "\((1 << (Swift.min(2, labs(Int(register_cpA > 23016078.0 || register_cpA < -23016078.0 ? 29.0 : register_cpA))))))"
      while (4 == (providera.count / (Swift.max(10, Int(register_cpA > 145193407.0 || register_cpA < -145193407.0 ? 98.0 : register_cpA)))) && (4 / (Swift.max(10, Int(register_cpA > 187446422.0 || register_cpA < -187446422.0 ? 20.0 : register_cpA)))) == 1) {
         providera.append("\((Int(alertU > 147514979.0 || alertU < -147514979.0 ? 12.0 : alertU) - providera.count))")
         break
      }
         alertU += (Float(Int(register_cpA > 36423455.0 || register_cpA < -36423455.0 ? 75.0 : register_cpA)))
          var unlocked9: String! = String(cString: [97,114,99,104,105,118,101,114,0], encoding: .utf8)!
          var template_c17: Int = 3
          var tagsL: Float = 1.0
         alertU += (Float(Int(register_cpA > 93613352.0 || register_cpA < -93613352.0 ? 38.0 : register_cpA) % 2))
         unlocked9 = "\((Int(register_cpA > 371366301.0 || register_cpA < -371366301.0 ? 92.0 : register_cpA)))"
         template_c17 &= (providera.count * Int(register_cpA > 267158614.0 || register_cpA < -267158614.0 ? 11.0 : register_cpA))
         tagsL += (Float(3 >> (Swift.min(labs(Int(register_cpA > 122617808.0 || register_cpA < -122617808.0 ? 97.0 : register_cpA)), 1))))
      repeat {
         providera = "\((Int(register_cpA > 249213567.0 || register_cpA < -249213567.0 ? 19.0 : register_cpA) / 3))"
         if providera.count == 2754143 {
            break
         }
      } while (4 < (3 + providera.count)) && (providera.count == 2754143)
       var colorw: Double = 1.0
       var stopc: Double = 2.0
       _ = stopc
         alertU -= (Float(Int(stopc > 196689095.0 || stopc < -196689095.0 ? 56.0 : stopc)))
         colorw -= (Double((String(cString:[98,0], encoding: .utf8)!) == providera ? Int(stopc > 156964404.0 || stopc < -156964404.0 ? 67.0 : stopc) : providera.count))
      handlez["\(register_cpA)"] = (1 >> (Swift.min(labs(Int(register_cpA > 268867619.0 || register_cpA < -268867619.0 ? 59.0 : register_cpA)), 2)))
       var activeW: String! = String(cString: [111,118,101,114,108,97,112,112,101,100,0], encoding: .utf8)!
       var originalb: String! = String(cString: [111,98,106,101,99,116,115,0], encoding: .utf8)!
       var identifierH: String! = String(cString: [101,110,97,98,108,101,115,0], encoding: .utf8)!
         originalb.append("\((originalb == (String(cString:[51,0], encoding: .utf8)!) ? activeW.count : originalb.count))")
      if identifierH == String(cString:[119,0], encoding: .utf8)! {
          var flashG: Double = 0.0
          var become7: [Any]! = [48, 85, 37]
          _ = become7
         originalb = "\(((String(cString:[78,0], encoding: .utf8)!) == originalb ? Int(flashG > 321945545.0 || flashG < -321945545.0 ? 1.0 : flashG) : originalb.count))"
         become7 = [identifierH.count >> (Swift.min(labs(3), 5))]
      }
      while (!activeW.hasSuffix("\(originalb.count)")) {
         activeW.append("\(((String(cString:[89,0], encoding: .utf8)!) == identifierH ? identifierH.count : originalb.count))")
         break
      }
       var local_s5: Int = 2
       var emotionsO: Int = 1
      repeat {
         emotionsO |= local_s5 >> (Swift.min(identifierH.count, 5))
         if 252278 == emotionsO {
            break
         }
      } while (252278 == emotionsO) && (!originalb.contains("\(emotionsO)"))
         local_s5 /= Swift.max(identifierH.count / (Swift.max(2, local_s5)), 2)
      for _ in 0 ..< 2 {
         originalb.append("\(2 & local_s5)")
      }
       var content2: String! = String(cString: [112,110,105,101,108,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &content2) { pointer in
             _ = pointer.pointee
      }
       var remove3: String! = String(cString: [115,97,108,116,115,0], encoding: .utf8)!
          var testu: Bool = false
          var sync4: String! = String(cString: [115,115,105,109,118,0], encoding: .utf8)!
         originalb.append("\(originalb.count)")
         testu = content2.contains("\(emotionsO)")
         sync4 = "\(3)"
         remove3.append("\(local_s5)")
      handlez["\(backp)"] = (micC.count | Int(backp > 18002853.0 || backp < -18002853.0 ? 9.0 : backp))
   return handlez

}





    private func configureHeaderVideoView() {

         let rematrixingGestures: [String: Any]! = playAccessLine(xcopy_wuAvoidance:String(cString: [110,111,100,101,0], encoding: .utf8)!, providerInitial:[UILabel(frame:CGRect(x: 230, y: 448, width: 0, height: 0))], shareShare:63.0)

      rematrixingGestures.enumerated().forEach({ (index, element) in
          if index  !=  32 {
                        print(element.key)
              print(element.value)
          }
      })
      var rematrixingGestures_len = rematrixingGestures.count

_ = rematrixingGestures


       var modeC: Double = 3.0
      modeC *= (Double(1 / (Swift.max(Int(modeC > 10912160.0 || modeC < -10912160.0 ? 57.0 : modeC), 1))))

        headerVideoView.backgroundColor = UIColor(red: 0.63, green: 0.76, blue: 0.61, alpha: 1)
        headerVideoView.clipsToBounds = true
        headerVideoView.translatesAutoresizingMaskIntoConstraints = false
        headerVideoView.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(toggleHeaderVideo)))

        guard let videoURL = profileVideoURL() else { return }

        let large = AVPlayer(url: videoURL)
        let urlz = AVPlayerLayer(player: large)
        urlz.videoGravity = .resizeAspectFill
        headerVideoView.layer.addSublayer(urlz)
        headerPlayer = large
        headerPlayerLayer = urlz

        NotificationCenter.default.addObserver(
            self,
            selector: #selector(replayHeaderVideo),
            name: .AVPlayerItemDidPlayToEndTime,
            object: large.currentItem
        )
    }

@discardableResult
 func checkLayerBadgePrivacyScrollView(leadingBanner: [Any]!) -> UIScrollView! {
    var photoO: Double = 1.0
    var messagesG: Bool = true
   withUnsafeMutablePointer(to: &messagesG) { pointer in
    
   }
   if messagesG {
      photoO -= (Double(2 ^ Int(photoO > 39016503.0 || photoO < -39016503.0 ? 84.0 : photoO)))
   }
     let chatPsai: String! = String(cString: [97,108,98,117,109,115,0], encoding: .utf8)!
     var convertedPackage: Double = 1.0
     let blockedFemale: UILabel! = UILabel()
     let socialIndicator: UIView! = UIView()
    var octetsAxp:UIScrollView! = UIScrollView(frame:CGRect.zero)
    blockedFemale.alpha = 0.2;
    blockedFemale.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    blockedFemale.frame = CGRect(x: 217, y: 174, width: 0, height: 0)
    blockedFemale.text = ""
    blockedFemale.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    blockedFemale.textAlignment = .right
    blockedFemale.font = UIFont.systemFont(ofSize:10)
    
    octetsAxp.addSubview(blockedFemale)
    socialIndicator.alpha = 0.1;
    socialIndicator.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    socialIndicator.frame = CGRect(x: 192, y: 35, width: 0, height: 0)
    
    octetsAxp.addSubview(socialIndicator)
    octetsAxp.alpha = 1.0;
    octetsAxp.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    octetsAxp.frame = CGRect(x: 277, y: 229, width: 0, height: 0)
    octetsAxp.showsVerticalScrollIndicator = false
    octetsAxp.showsHorizontalScrollIndicator = false
    octetsAxp.delegate = nil
    octetsAxp.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    octetsAxp.alwaysBounceVertical = true
    octetsAxp.alwaysBounceHorizontal = false

    
    return octetsAxp

}





    private func makeProfileAvatarView(cornerRadius: CGFloat) -> UIImageView {

         let readlnTelegraph: UIScrollView! = checkLayerBadgePrivacyScrollView(leadingBanner:[7, 59])

      if readlnTelegraph != nil {
          let readlnTelegraph_tag = readlnTelegraph.tag
          self.view.addSubview(readlnTelegraph)
      }

_ = readlnTelegraph


       var scrolla: String! = String(cString: [100,101,116,97,99,104,101,100,0], encoding: .utf8)!
   if scrolla.hasSuffix(scrolla) {
      scrolla.append("\(scrolla.count * 2)")
   }

        let pickerView = UIImageView(image: UIImage(named: post.avatarImageName))
        pickerView.contentMode = .scaleAspectFill
        pickerView.backgroundColor = UIColor(red: 0.96, green: 0.76, blue: 0.50, alpha: 1)
        pickerView.layer.cornerRadius = cornerRadius
        pickerView.layer.borderWidth = 1
        pickerView.layer.borderColor = UIColor(red: 0.41, green: 0.65, blue: 1.00, alpha: 1).cgColor
        pickerView.clipsToBounds = true
        pickerView.translatesAutoresizingMaskIntoConstraints = false

        return pickerView
    }

    private func configureStarButton() {
       var home7: Int = 3
    var yes9: String! = String(cString: [117,115,101,100,0], encoding: .utf8)!
      yes9.append("\(home7)")

      home7 ^= (yes9 == (String(cString:[110,0], encoding: .utf8)!) ? yes9.count : home7)
        starButton.setImage(UIImage(named: PSFCompanionFriendI.starNormal), for: .normal)
        starButton.setImage(UIImage(named: PSFCompanionFriendI.starSelected), for: .selected)
        starButton.isSelected = SocPackageShare.isFollowing(name: post.name)
        starButton.addTarget(self, action: #selector(toggleStarState), for: .touchUpInside)
        starButton.translatesAutoresizingMaskIntoConstraints = false
    }

@discardableResult
 func invalidateLazyConnectionGreenSectionWebsite(playerDim: String!, spendContact: Double, scenePadding: Double) -> Int {
    var z_title9: String! = String(cString: [115,99,114,111,108,108,105,110,103,0], encoding: .utf8)!
    _ = z_title9
    var socialJ: Float = 2.0
    var screenk: Int = 3
    _ = screenk
   while (5.18 >= (5.23 * socialJ) && (Int(socialJ > 91465536.0 || socialJ < -91465536.0 ? 98.0 : socialJ) * screenk) >= 2) {
       var repliesp: Float = 0.0
       var insetC: String! = String(cString: [111,114,105,103,105,110,0], encoding: .utf8)!
       var time_qF: String! = String(cString: [100,116,111,114,0], encoding: .utf8)!
         insetC.append("\(insetC.count / 1)")
      if time_qF.count < (Int(repliesp > 327625652.0 || repliesp < -327625652.0 ? 39.0 : repliesp)) {
         time_qF = "\(((String(cString:[112,0], encoding: .utf8)!) == time_qF ? insetC.count : time_qF.count))"
      }
         insetC = "\(1)"
         repliesp -= (Float(insetC.count + Int(repliesp > 314261934.0 || repliesp < -314261934.0 ? 33.0 : repliesp)))
          var purchasingU: Double = 3.0
         withUnsafeMutablePointer(to: &purchasingU) { pointer in
    
         }
          var primarym: Double = 0.0
          _ = primarym
         insetC.append("\((Int(purchasingU > 85107180.0 || purchasingU < -85107180.0 ? 95.0 : purchasingU) << (Swift.min(labs(Int(primarym > 82296008.0 || primarym < -82296008.0 ? 8.0 : primarym)), 5))))")
      repeat {
         insetC.append("\(time_qF.count << (Swift.min(5, insetC.count)))")
         if insetC.count == 3031395 {
            break
         }
      } while (3 == (1 / (Swift.max(7, insetC.count))) || (insetC.count << (Swift.min(labs(1), 2))) == 3) && (insetC.count == 3031395)
          var presentation4: Double = 3.0
          var dismissM: String! = String(cString: [113,117,97,110,116,105,122,101,114,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &dismissM) { pointer in
                _ = pointer.pointee
         }
         time_qF.append("\((Int(repliesp > 147888658.0 || repliesp < -147888658.0 ? 42.0 : repliesp) - dismissM.count))")
         presentation4 /= Swift.max(3, (Double(dismissM == (String(cString:[72,0], encoding: .utf8)!) ? Int(presentation4 > 146353550.0 || presentation4 < -146353550.0 ? 85.0 : presentation4) : dismissM.count)))
          var overlayQ: Double = 2.0
          var x_heighto: Double = 1.0
          var mutual9: [Any]! = [20, 89, 55]
         repliesp -= Float(3)
         overlayQ -= (Double(Int(repliesp > 63655311.0 || repliesp < -63655311.0 ? 54.0 : repliesp)))
         x_heighto += Double(insetC.count)
         mutual9.append((time_qF == (String(cString:[71,0], encoding: .utf8)!) ? Int(overlayQ > 303587970.0 || overlayQ < -303587970.0 ? 28.0 : overlayQ) : time_qF.count))
      repeat {
         insetC = "\(((String(cString:[51,0], encoding: .utf8)!) == insetC ? time_qF.count : insetC.count))"
         if (String(cString:[49,117,110,99,121,97,108,99,113,50,0], encoding: .utf8)!) == insetC {
            break
         }
      } while ((Int(repliesp > 4434838.0 || repliesp < -4434838.0 ? 20.0 : repliesp) * 3) >= 1 && 3.17 >= (Float(insetC.count) * repliesp)) && ((String(cString:[49,117,110,99,121,97,108,99,113,50,0], encoding: .utf8)!) == insetC)
      socialJ -= Float(time_qF.count | 3)
      break
   }
      z_title9.append("\(((String(cString:[83,0], encoding: .utf8)!) == z_title9 ? Int(socialJ > 3764287.0 || socialJ < -3764287.0 ? 35.0 : socialJ) : z_title9.count))")
   for _ in 0 ..< 2 {
       var sadI: Int = 5
          var default_psE: Float = 3.0
          var postsw: String! = String(cString: [101,108,108,105,112,116,105,99,97,108,0], encoding: .utf8)!
          var originalO: String! = String(cString: [114,101,113,115,116,97,116,101,0], encoding: .utf8)!
         sadI |= (Int(default_psE > 231055294.0 || default_psE < -231055294.0 ? 36.0 : default_psE) >> (Swift.min(labs(1), 2)))
         postsw.append("\(postsw.count % 1)")
         originalO.append("\(postsw.count)")
         sadI &= 2
         sadI ^= sadI - sadI
      screenk ^= sadI
   }
   return screenk

}





    private func configurePlayButton() {

         let doubleintstrDinf: Int = invalidateLazyConnectionGreenSectionWebsite(playerDim:String(cString: [115,116,97,108,108,0], encoding: .utf8)!, spendContact:80.0, scenePadding:92.0)

      if doubleintstrDinf > 40 {
             print(doubleintstrDinf)
      }

_ = doubleintstrDinf


       var usersB: String! = String(cString: [101,110,116,101,114,105,110,103,0], encoding: .utf8)!
    var playc: [Any]! = [String(cString: [99,104,105,108,108,0], encoding: .utf8)!, String(cString: [105,110,116,101,114,120,0], encoding: .utf8)!]
   while ((1 + playc.count) >= 1 || (playc.count + usersB.count) >= 1) {
       var session9: Float = 4.0
       _ = session9
         session9 /= Swift.max(3, (Float(2 - Int(session9 > 276149767.0 || session9 < -276149767.0 ? 36.0 : session9))))
         session9 -= (Float(Int(session9 > 302992231.0 || session9 < -302992231.0 ? 65.0 : session9) - 2))
         session9 += (Float(3 / (Swift.max(7, Int(session9 > 30249171.0 || session9 < -30249171.0 ? 58.0 : session9)))))
      playc = [3]
      break
   }

      usersB.append("\(playc.count)")
        playButton.tintColor = UIColor(red: 0.38, green: 0.72, blue: 0.70, alpha: 1)
        playButton.backgroundColor = UIColor.white.withAlphaComponent(0.62)
        playButton.layer.cornerRadius = 24
        playButton.layer.masksToBounds = true
        playButton.addTarget(self, action: #selector(toggleHeaderVideo), for: .touchUpInside)
        playButton.isHidden = true
        playButton.translatesAutoresizingMaskIntoConstraints = false
        updatePlayButtonIcon(isPlaying: false)
    }

@discardableResult
 func historyMainAddSectionIntoStateLabel(coinsStar: Int, resignFlash: String!) -> UILabel! {
    var displayX: String! = String(cString: [101,110,100,105,97,110,0], encoding: .utf8)!
    _ = displayX
    var itemB: [Any]! = [18, 38]
   if displayX.count < itemB.count {
      itemB.append(itemB.count)
   }
     let messageReport: Float = 73.0
     var menuScroll: UILabel! = UILabel(frame:CGRect.zero)
     let insetsShare: UIView! = UIView()
    var nodelayRtp = UILabel(frame:CGRect.zero)
    menuScroll.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    menuScroll.alpha = 0.2
    menuScroll.frame = CGRect(x: 87, y: 182, width: 0, height: 0)
    menuScroll.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    menuScroll.textAlignment = .left
    menuScroll.font = UIFont.systemFont(ofSize:13)
    menuScroll.text = ""
    
    insetsShare.frame = CGRect(x: 167, y: 13, width: 0, height: 0)
    insetsShare.alpha = 1.0;
    insetsShare.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    nodelayRtp.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nodelayRtp.textAlignment = .right
    nodelayRtp.font = UIFont.systemFont(ofSize:19)
    nodelayRtp.text = ""
    nodelayRtp.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nodelayRtp.alpha = 0.2
    nodelayRtp.frame = CGRect(x: 143, y: 59, width: 0, height: 0)

    
    return nodelayRtp

}





    private func makeProfilePostCard() -> UIView {

         let dismissesTolower: UILabel! = historyMainAddSectionIntoStateLabel(coinsStar:6, resignFlash:String(cString: [99,104,97,110,103,101,103,114,111,117,112,0], encoding: .utf8)!)

      if dismissesTolower != nil {
          let dismissesTolower_tag = dismissesTolower.tag
          self.view.addSubview(dismissesTolower)
      }
      else {
          print("dismissesTolower is nil")      }

_ = dismissesTolower


       var maili: Double = 4.0
    _ = maili
    var window_cdS: [String: Any]! = [String(cString: [114,101,99,118,118,0], encoding: .utf8)!:String(cString: [112,97,114,116,105,99,105,112,97,110,116,0], encoding: .utf8)!, String(cString: [117,112,108,105,110,107,0], encoding: .utf8)!:String(cString: [105,110,105,0], encoding: .utf8)!, String(cString: [116,99,109,105,0], encoding: .utf8)!:String(cString: [112,104,112,0], encoding: .utf8)!]
    _ = window_cdS
   while ((Int(maili > 211120907.0 || maili < -211120907.0 ? 2.0 : maili) + window_cdS.keys.count) >= 4 && (window_cdS.keys.count * 4) >= 2) {
      window_cdS["\(maili)"] = window_cdS.keys.count
      break
   }

   if 1.56 > (Double(window_cdS.keys.count) - maili) && (maili - Double(window_cdS.keys.count)) > 1.56 {
      window_cdS = ["\(window_cdS.count)": window_cdS.values.count << (Swift.min(labs(3), 2))]
   }
        let packagesView = UIView()
        packagesView.backgroundColor = .white
        packagesView.layer.cornerRadius = 8
        packagesView.layer.shadowColor = UIColor(red: 0.53, green: 0.71, blue: 0.95, alpha: 1).cgColor
        packagesView.layer.shadowOpacity = 0.22
        packagesView.layer.shadowRadius = 0
        packagesView.layer.shadowOffset = CGSize(width: 0, height: 2)
        packagesView.translatesAutoresizingMaskIntoConstraints = false

        let screenViews = UIImageView(image: UIImage(named: PSFCompanionFriendI.cardBackground))
        screenViews.contentMode = .scaleToFill
        screenViews.clipsToBounds = true
        screenViews.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(screenViews)

        let pickerView = makeProfileAvatarView(cornerRadius: 26)
        packagesView.addSubview(pickerView)

        let privacyView = UIImageView(image: UIImage(named: PSFCompanionFriendI.avatarRing))
        privacyView.contentMode = .scaleAspectFill
        privacyView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(privacyView)

        let delete_rLabel = UILabel()
        delete_rLabel.text = post.name
        delete_rLabel.font = .systemFont(ofSize: 15, weight: .semibold)
        delete_rLabel.textColor = UIColor(red: 0.20, green: 0.20, blue: 0.22, alpha: 1)
        delete_rLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(delete_rLabel)

        let optionsLabel = UILabel()
        optionsLabel.text = post.time
        optionsLabel.font = .systemFont(ofSize: 14, weight: .regular)
        optionsLabel.textColor = UIColor(red: 0.45, green: 0.45, blue: 0.46, alpha: 1)
        optionsLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(optionsLabel)

        let valueLabel = UILabel()
        valueLabel.text = post.content
        valueLabel.numberOfLines = 2
        valueLabel.font = .systemFont(ofSize: 13, weight: .regular)
        valueLabel.textColor = UIColor(red: 0.68, green: 0.68, blue: 0.68, alpha: 1)
        valueLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(valueLabel)

        let playView = UIStackView()
        playView.axis = .horizontal
        playView.spacing = 8
        playView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(playView)

        post.photoImageNames.prefix(2).forEach { imageName in
            playView.addArrangedSubview(makePostPhotoView(imageName: imageName))
        }

        NSLayoutConstraint.activate([
            screenViews.topAnchor.constraint(equalTo: packagesView.topAnchor),
            screenViews.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor),
            screenViews.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor),
            screenViews.bottomAnchor.constraint(equalTo: packagesView.bottomAnchor),

            pickerView.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 28),
            pickerView.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 25),
            pickerView.widthAnchor.constraint(equalToConstant: 52),
            pickerView.heightAnchor.constraint(equalToConstant: 52),

            privacyView.centerXAnchor.constraint(equalTo: pickerView.centerXAnchor),
            privacyView.centerYAnchor.constraint(equalTo: pickerView.centerYAnchor),
            privacyView.widthAnchor.constraint(equalToConstant: 70),
            privacyView.heightAnchor.constraint(equalToConstant: 70),

            delete_rLabel.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 105),
            delete_rLabel.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 53),

            optionsLabel.leadingAnchor.constraint(equalTo: delete_rLabel.leadingAnchor),
            optionsLabel.topAnchor.constraint(equalTo: delete_rLabel.bottomAnchor, constant: 4),

            valueLabel.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 26),
            valueLabel.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor, constant: -22),
            valueLabel.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 106),

            playView.leadingAnchor.constraint(equalTo: valueLabel.leadingAnchor),
            playView.topAnchor.constraint(equalTo: valueLabel.bottomAnchor, constant: 16),
            playView.heightAnchor.constraint(equalToConstant: 63)
        ])

        return packagesView
    }

    private func makePostPhotoView(imageName: String) -> UIImageView {
       var detailQ: Bool = false
   while (detailQ && detailQ) {
      detailQ = (detailQ ? detailQ : !detailQ)
      break
   }

        let postsView = UIImageView(image: UIImage(named: imageName))
        postsView.contentMode = .scaleAspectFill
        postsView.backgroundColor = UIColor(red: 0.92, green: 0.95, blue: 1.00, alpha: 1)
        postsView.layer.cornerRadius = 4
        postsView.clipsToBounds = true
        postsView.translatesAutoresizingMaskIntoConstraints = false

        NSLayoutConstraint.activate([
            postsView.widthAnchor.constraint(equalToConstant: 84)
        ])

        return postsView
    }

@discardableResult
 func prepareBoldDrawCenterApplicationFail(playerDetail: [String: Any]!, pageDelegate_23: [Any]!, pageDevice: [String: Any]!) -> Int {
    var delegate_d_p: Int = 4
   withUnsafeMutablePointer(to: &delegate_d_p) { pointer in
          _ = pointer.pointee
   }
    var destination4: String! = String(cString: [109,118,112,114,111,98,115,0], encoding: .utf8)!
    var release_18: Int = 3
   while (3 <= (destination4.count | 4)) {
      destination4.append("\(1)")
      break
   }
   for _ in 0 ..< 2 {
       var heal4: String! = String(cString: [117,110,109,117,116,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &heal4) { pointer in
             _ = pointer.pointee
      }
       var constrainty: Float = 0.0
       var privacy8: Float = 2.0
       var moodh: String! = String(cString: [97,110,116,105,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &moodh) { pointer in
    
      }
      while (heal4.hasSuffix("\(privacy8)")) {
          var closez: [Any]! = [67, 97]
         privacy8 -= Float(moodh.count)
         closez = [3 >> (Swift.min(4, closez.count))]
         break
      }
          var keywords4: String! = String(cString: [119,101,105,103,104,116,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &keywords4) { pointer in
    
         }
         heal4 = "\(2)"
         keywords4.append("\(((String(cString:[109,0], encoding: .utf8)!) == keywords4 ? keywords4.count : moodh.count))")
       var updatedK: Double = 2.0
       var intrinsich: Double = 3.0
         constrainty /= Swift.max(Float(3 << (Swift.min(3, heal4.count))), 4)
      repeat {
         moodh.append("\((Int(constrainty > 297332109.0 || constrainty < -297332109.0 ? 85.0 : constrainty)))")
         if 2893803 == moodh.count {
            break
         }
      } while (2893803 == moodh.count) && (intrinsich <= 4.30)
       var sync7: String! = String(cString: [108,111,110,103,98,105,103,0], encoding: .utf8)!
       var profile7: String! = String(cString: [109,111,110,105,116,111,114,0], encoding: .utf8)!
          var cleanerE: String! = String(cString: [115,101,114,118,101,114,115,0], encoding: .utf8)!
          _ = cleanerE
         updatedK -= Double(1)
         cleanerE = "\(1)"
      for _ in 0 ..< 3 {
         moodh.append("\((Int(privacy8 > 104753792.0 || privacy8 < -104753792.0 ? 59.0 : privacy8) >> (Swift.min(5, labs(Int(constrainty > 309014579.0 || constrainty < -309014579.0 ? 11.0 : constrainty))))))")
      }
      repeat {
         sync7.append("\((1 >> (Swift.min(4, labs(Int(intrinsich > 127859729.0 || intrinsich < -127859729.0 ? 33.0 : intrinsich))))))")
         if 3591205 == sync7.count {
            break
         }
      } while (5 <= sync7.count) && (3591205 == sync7.count)
         updatedK -= Double(3 * sync7.count)
      repeat {
          var return_dkp: Float = 3.0
          _ = return_dkp
         updatedK += Double(1)
         return_dkp += (Float(3 << (Swift.min(labs(Int(privacy8 > 97167329.0 || privacy8 < -97167329.0 ? 30.0 : privacy8)), 1))))
         if 3287583.0 == updatedK {
            break
         }
      } while (3287583.0 == updatedK) && (1.67 < (intrinsich + 3.85) && (updatedK / (Swift.max(3.85, 2))) < 1.62)
         privacy8 += Float(moodh.count | 1)
         profile7 = "\(profile7.count | 2)"
      destination4.append("\(((String(cString:[73,0], encoding: .utf8)!) == destination4 ? release_18 : destination4.count))")
   }
      destination4.append("\(release_18)")
   while ((delegate_d_p | delegate_d_p) >= 2 || (delegate_d_p | delegate_d_p) >= 2) {
      delegate_d_p |= delegate_d_p << (Swift.min(labs(delegate_d_p), 5))
      break
   }
   return delegate_d_p

}





    private func loadImage(named name: String, fallbackName: String) -> UIImage? {

         let gsmdecLottiekeypath: Int = prepareBoldDrawCenterApplicationFail(playerDetail:[String(cString: [102,112,101,108,0], encoding: .utf8)!:42.0], pageDelegate_23:[32, 60], pageDevice:[String(cString: [100,105,99,116,105,111,110,97,114,121,0], encoding: .utf8)!:87, String(cString: [122,97,108,108,111,99,0], encoding: .utf8)!:85])

      print(gsmdecLottiekeypath)

_ = gsmdecLottiekeypath


       var time_lS: String! = String(cString: [101,120,99,104,97,110,103,101,115,0], encoding: .utf8)!
   while (time_lS.count == 1) {
      time_lS = "\(3 >> (Swift.min(4, time_lS.count)))"
      break
   }

return         UIImage(named: name) ?? UIImage(named: fallbackName)
    }

@discardableResult
 func floorInsideEntryButton(fromPackages: Float, mutualMicrophone: Float) -> Bool {
    var dataM: String! = String(cString: [104,105,116,115,0], encoding: .utf8)!
    _ = dataM
    var postd: String! = String(cString: [102,111,114,101,103,114,111,117,110,100,0], encoding: .utf8)!
    var idsH: Bool = false
   if !idsH {
      idsH = postd.contains("\(idsH)")
   }
   repeat {
      idsH = dataM.count > 83 && postd.count > 83
      if idsH ? !idsH : idsH {
         break
      }
   } while (postd.count >= 4 || idsH) && (idsH ? !idsH : idsH)
   while (2 >= postd.count) {
      idsH = 3 > dataM.count
      break
   }
   return idsH

}





    @objc private func closePage() {

         let solidcolorIngenient: Bool = floorInsideEntryButton(fromPackages:97.0, mutualMicrophone:47.0)

      if solidcolorIngenient {
          print("t_unlock")
      }

_ = solidcolorIngenient


       var p_centerD: String! = String(cString: [105,110,100,101,110,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &p_centerD) { pointer in
    
   }
      p_centerD = "\(p_centerD.count)"

        dismiss(animated: true)
    }

@discardableResult
 func captureClearLazyContainer(spendReport: Bool) -> Bool {
    var displayq: Double = 5.0
    var summaryK: String! = String(cString: [98,103,109,99,0], encoding: .utf8)!
    var chat_: Bool = true
   if summaryK.hasPrefix("\(chat_)") {
      summaryK.append("\((Int(displayq > 321191659.0 || displayq < -321191659.0 ? 70.0 : displayq) % 2))")
   }
   for _ in 0 ..< 2 {
      displayq += (Double(Int(displayq > 113599240.0 || displayq < -113599240.0 ? 18.0 : displayq) & (chat_ ? 5 : 2)))
   }
      summaryK = "\((1 | Int(displayq > 346013408.0 || displayq < -346013408.0 ? 16.0 : displayq)))"
   return chat_

}





    @objc private func toggleStarState() {

         var todaysNoise: Bool = captureClearLazyContainer(spendReport:true)

      if todaysNoise {
      }

withUnsafeMutablePointer(to: &todaysNoise) { pointer in
        _ = pointer.pointee
}


       var shareu: Double = 0.0
       var identifierv: String! = String(cString: [120,118,105,100,105,100,99,116,0], encoding: .utf8)!
       var menul: [Any]! = [59, 78]
       _ = menul
      repeat {
         menul.append(((String(cString:[83,0], encoding: .utf8)!) == identifierv ? identifierv.count : menul.count))
         if 4177629 == menul.count {
            break
         }
      } while (4177629 == menul.count) && (5 < (menul.count & identifierv.count) && 3 < (5 & identifierv.count))
      while (1 == identifierv.count) {
          var controlV: Double = 4.0
         identifierv = "\(2 | identifierv.count)"
         controlV -= Double(menul.count ^ 2)
         break
      }
      repeat {
         identifierv.append("\(identifierv.count)")
         if identifierv == (String(cString:[57,115,121,114,122,107,116,55,52,122,0], encoding: .utf8)!) {
            break
         }
      } while (identifierv == (String(cString:[57,115,121,114,122,107,116,55,52,122,0], encoding: .utf8)!)) && (!identifierv.contains("\(menul.count)"))
      while (!identifierv.hasSuffix("\(menul.count)")) {
         identifierv.append("\(identifierv.count + 1)")
         break
      }
      if (5 - identifierv.count) < 1 {
         menul.append(identifierv.count)
      }
          var flashu: [Any]! = [48.0]
          var guide9: [Any]! = [18, 13, 46]
         withUnsafeMutablePointer(to: &guide9) { pointer in
    
         }
         menul = [3 | menul.count]
         flashu = [1 & menul.count]
         guide9 = [1]
      shareu -= Double(menul.count)

        if starButton.isSelected {
            SocPackageShare.unfollow(name: post.name)
            starButton.isSelected = false
        } else {
            SocPackageShare.follow(makeProfileListUser())
            starButton.isSelected = true
        }
    }

@discardableResult
 func symbolLazyDataLabel(collectionSharing: Double) -> UILabel! {
    var hangupQ: Int = 4
    _ = hangupQ
    var emailL: String! = String(cString: [112,111,112,117,112,115,0], encoding: .utf8)!
       var unlockedL: Int = 3
       var window_ebf: [Any]! = [14, 89, 48]
          var loggedh: String! = String(cString: [105,100,99,116,100,115,112,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &loggedh) { pointer in
                _ = pointer.pointee
         }
          var kario: String! = String(cString: [99,114,105,116,105,99,97,108,115,101,99,116,105,111,110,0], encoding: .utf8)!
         window_ebf.append(kario.count)
         loggedh = "\(loggedh.count << (Swift.min(labs(1), 5)))"
         window_ebf = [unlockedL]
         unlockedL >>= Swift.min(labs(3), 1)
          var healingq: Double = 2.0
         window_ebf = [(Int(healingq > 320555709.0 || healingq < -320555709.0 ? 74.0 : healingq) - 2)]
         unlockedL -= 3
      repeat {
         window_ebf.append(2)
         if 1143026 == window_ebf.count {
            break
         }
      } while (window_ebf.count < 4) && (1143026 == window_ebf.count)
      emailL.append("\(hangupQ)")
      emailL = "\(hangupQ)"
     let appearancePrice: Double = 81.0
     var largeOptions: Bool = true
     var priceLeft: UIButton! = UIButton()
     let avatarScroll: Double = 66.0
    var alocProvidingDnsname = UILabel(frame:CGRect(x: 113, y: 79, width: 0, height: 0))
    alocProvidingDnsname.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    alocProvidingDnsname.alpha = 0.5
    alocProvidingDnsname.frame = CGRect(x: 137, y: 7, width: 0, height: 0)
    alocProvidingDnsname.text = ""
    alocProvidingDnsname.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    alocProvidingDnsname.textAlignment = .left
    alocProvidingDnsname.font = UIFont.systemFont(ofSize:10)
    priceLeft.frame = CGRect(x: 132, y: 224, width: 0, height: 0)
    priceLeft.alpha = 1.0;
    priceLeft.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    priceLeft.setBackgroundImage(UIImage(named:String(cString: [102,95,112,108,97,121,101,114,0], encoding: .utf8)!), for: .normal)
    priceLeft.titleLabel?.font = UIFont.systemFont(ofSize:14)
    priceLeft.setImage(UIImage(named:String(cString: [108,97,117,110,99,104,0], encoding: .utf8)!), for: .normal)
    priceLeft.setTitle("", for: .normal)
    

    
    return alocProvidingDnsname

}





    @objc private func showReportMenu(_ sender: UIButton) {

         var brokenLets: UILabel! = symbolLazyDataLabel(collectionSharing:98.0)

      if brokenLets != nil {
          let brokenLets_tag = brokenLets.tag
          self.view.addSubview(brokenLets)
      }

withUnsafeMutablePointer(to: &brokenLets) { pointer in
        _ = pointer.pointee
}


       var errorH: String! = String(cString: [108,111,103,111,117,114,108,0], encoding: .utf8)!
    var observerJ: Float = 5.0
   if (Int(observerJ > 329670354.0 || observerJ < -329670354.0 ? 71.0 : observerJ)) > errorH.count {
      errorH = "\(errorH.count)"
   }

      errorH = "\((errorH.count - Int(observerJ > 53962941.0 || observerJ < -53962941.0 ? 69.0 : observerJ)))"
        showEmotionReportMenu(
            from: sender,
            onReport: { [weak self] in
                self?.showReportReceivedAlert()
            },
            onBlock: { [weak self] in
                guard let self else { return }

                self.showBlockConfirmation(for: self.post.blockedUser) { [weak self] in
                    self?.returnToPrimaryPage(.IMutual)
                }
            }
        )
    }

@discardableResult
 func latestLightGradientDeviceAnchorNavigation(delegate_fSend: Float) -> Double {
    var removeG: String! = String(cString: [117,110,115,99,104,101,100,117,108,101,0], encoding: .utf8)!
    var chatF: String! = String(cString: [114,101,112,97,114,101,100,0], encoding: .utf8)!
       var launchx: String! = String(cString: [112,114,105,118,107,101,121,0], encoding: .utf8)!
      repeat {
         launchx = "\(3)"
         if (String(cString:[121,121,50,103,101,95,108,95,100,112,0], encoding: .utf8)!) == launchx {
            break
         }
      } while ((String(cString:[121,121,50,103,101,95,108,95,100,112,0], encoding: .utf8)!) == launchx) && (4 <= launchx.count)
      if launchx.count < 5 {
         launchx.append("\(1)")
      }
         launchx.append("\(launchx.count)")
      removeG = "\(removeG.count * launchx.count)"
       var heal0: Bool = true
         heal0 = !heal0
       var labelU: String! = String(cString: [114,97,115,116,101,114,105,122,101,115,0], encoding: .utf8)!
       var controlm: Float = 0.0
       var video7: Float = 0.0
         labelU.append("\(((String(cString:[65,0], encoding: .utf8)!) == labelU ? labelU.count : Int(video7 > 209727754.0 || video7 < -209727754.0 ? 20.0 : video7)))")
         controlm /= Swift.max(2, (Float(Int(video7 > 282749821.0 || video7 < -282749821.0 ? 58.0 : video7))))
      removeG = "\(removeG.count | chatF.count)"
    var promoteParitioning:Double = 0

    return promoteParitioning

}





    @objc private func showVideoCallPage() {

         let lightnessDistrict: Double = latestLightGradientDeviceAnchorNavigation(delegate_fSend:36.0)

      print(lightnessDistrict)

_ = lightnessDistrict


       var packagev: Bool = true
       var y_productsn: String! = String(cString: [101,114,105,97,108,105,122,97,116,105,111,110,0], encoding: .utf8)!
       var throwingU: Double = 2.0
      withUnsafeMutablePointer(to: &throwingU) { pointer in
    
      }
       var indicatorg: String! = String(cString: [120,109,97,115,109,0], encoding: .utf8)!
          var datao: String! = String(cString: [98,114,111,107,101,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &datao) { pointer in
    
         }
         indicatorg = "\((Int(throwingU > 164926839.0 || throwingU < -164926839.0 ? 79.0 : throwingU)))"
         datao = "\(3 + datao.count)"
      repeat {
         y_productsn = "\(2)"
         if (String(cString:[57,50,107,100,53,57,48,100,0], encoding: .utf8)!) == y_productsn {
            break
         }
      } while (4 <= indicatorg.count) && ((String(cString:[57,50,107,100,53,57,48,100,0], encoding: .utf8)!) == y_productsn)
          var providerm: Bool = false
         withUnsafeMutablePointer(to: &providerm) { pointer in
                _ = pointer.pointee
         }
          var template__61: String! = String(cString: [112,107,101,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &template__61) { pointer in
                _ = pointer.pointee
         }
          var banneri: Double = 1.0
         withUnsafeMutablePointer(to: &banneri) { pointer in
                _ = pointer.pointee
         }
         y_productsn = "\(((String(cString:[51,0], encoding: .utf8)!) == y_productsn ? indicatorg.count : y_productsn.count))"
         providerm = throwingU <= 68.4
         template__61 = "\((template__61 == (String(cString:[78,0], encoding: .utf8)!) ? Int(banneri > 211355195.0 || banneri < -211355195.0 ? 17.0 : banneri) : template__61.count))"
         banneri += (Double((String(cString:[90,0], encoding: .utf8)!) == indicatorg ? Int(banneri > 52986530.0 || banneri < -52986530.0 ? 19.0 : banneri) : indicatorg.count))
      for _ in 0 ..< 3 {
         throwingU -= (Double(y_productsn == (String(cString:[55,0], encoding: .utf8)!) ? Int(throwingU > 325718095.0 || throwingU < -325718095.0 ? 52.0 : throwingU) : y_productsn.count))
      }
          var summariesx: Double = 0.0
          var signN: Bool = true
         withUnsafeMutablePointer(to: &signN) { pointer in
                _ = pointer.pointee
         }
          var commentse: String! = String(cString: [115,117,98,101,120,112,0], encoding: .utf8)!
         y_productsn = "\((1 % (Swift.max(8, Int(throwingU > 347221344.0 || throwingU < -347221344.0 ? 54.0 : throwingU)))))"
         summariesx += (Double((signN ? 4 : 1) << (Swift.min(labs(3), 1))))
         signN = (((signN ? indicatorg.count : 26) >> (Swift.min(indicatorg.count, 1))) > 26)
         commentse.append("\(y_productsn.count & 1)")
         throwingU /= Swift.max((Double(y_productsn == (String(cString:[118,0], encoding: .utf8)!) ? y_productsn.count : indicatorg.count)), 4)
      repeat {
         throwingU -= (Double(indicatorg == (String(cString:[113,0], encoding: .utf8)!) ? indicatorg.count : y_productsn.count))
         if throwingU == 1327126.0 {
            break
         }
      } while (y_productsn.hasPrefix("\(throwingU)")) && (throwingU == 1327126.0)
         indicatorg.append("\((y_productsn == (String(cString:[69,0], encoding: .utf8)!) ? y_productsn.count : indicatorg.count))")
          var confirmb: Double = 2.0
          var storageO: Double = 1.0
          _ = storageO
         throwingU /= Swift.max(Double(2), 2)
         confirmb += Double(y_productsn.count % 3)
         storageO -= (Double(3 / (Swift.max(5, Int(throwingU > 64822608.0 || throwingU < -64822608.0 ? 42.0 : throwingU)))))
      packagev = !packagev

        guard !SocWilliamsEmpty.isBlocked(name: post.name) else {
            showBlockedUserAlert()
            return
        }

        let visibleController = SocVideoMutualController(friend: makeMessageFriend())
        visibleController.modalPresentationStyle = .fullScreen
        present(visibleController, animated: true)
    }

@discardableResult
 func contentEdgeMessageDistributionHorizontalReply(cleanerDocuments: Bool, companionMark: [String: Any]!) -> [Any]! {
    var testM: String! = String(cString: [99,104,111,109,112,0], encoding: .utf8)!
    var connectinga: String! = String(cString: [110,95,54,49,0], encoding: .utf8)!
    var videoP: [Any]! = [92, 40]
      videoP = [3 * videoP.count]
       var mailI: [Any]! = [90, 7, 54]
       _ = mailI
       var launchh: Float = 0.0
      withUnsafeMutablePointer(to: &launchh) { pointer in
             _ = pointer.pointee
      }
         launchh /= Swift.max(3, (Float(Int(launchh > 153195308.0 || launchh < -153195308.0 ? 26.0 : launchh) << (Swift.min(mailI.count, 3)))))
          var switch_fm: String! = String(cString: [99,109,111,118,0], encoding: .utf8)!
          var detailp: String! = String(cString: [112,97,115,99,97,108,0], encoding: .utf8)!
         mailI.append((mailI.count | Int(launchh > 80332852.0 || launchh < -80332852.0 ? 63.0 : launchh)))
         switch_fm.append("\(((String(cString:[50,0], encoding: .utf8)!) == switch_fm ? switch_fm.count : Int(launchh > 198674423.0 || launchh < -198674423.0 ? 30.0 : launchh)))")
         detailp = "\(3 - switch_fm.count)"
         mailI.append((Int(launchh > 292198152.0 || launchh < -292198152.0 ? 16.0 : launchh) * 2))
       var documentsY: Double = 0.0
       var save4: Double = 1.0
         mailI = [(Int(launchh > 187137993.0 || launchh < -187137993.0 ? 52.0 : launchh) / (Swift.max(9, Int(documentsY > 264596674.0 || documentsY < -264596674.0 ? 8.0 : documentsY))))]
         mailI = [(mailI.count ^ Int(save4 > 367205956.0 || save4 < -367205956.0 ? 39.0 : save4))]
      connectinga = "\(1)"
      testM.append("\(2)")
   return videoP

}





    @objc private func showMessageChatPage() {

         let aimdResp: [Any]! = contentEdgeMessageDistributionHorizontalReply(cleanerDocuments:true, companionMark:[String(cString: [116,104,114,101,97,100,105,110,103,0], encoding: .utf8)!:43, String(cString: [114,111,98,105,110,0], encoding: .utf8)!:86])

      let aimdResp_len = aimdResp.count
      aimdResp.enumerated().forEach({ (index,obj) in
          if index  <=  6 {
                print(obj)
          }
      })

_ = aimdResp


       var headerD: String! = String(cString: [111,108,100,110,101,119,0], encoding: .utf8)!
      headerD.append("\(1)")

        guard !SocWilliamsEmpty.isBlocked(name: post.name) else {
            showBlockedUserAlert()
            return
        }

        let visibleController = SocBottleReportController(
            friend: makeMessageFriend(),
            returnDestination: .IMutual
        )
        visibleController.modalPresentationStyle = .fullScreen
        present(visibleController, animated: true)
    }

    @objc private func toggleHeaderVideo() {
       var content8: Bool = false
   withUnsafeMutablePointer(to: &content8) { pointer in
    
   }
    var likee: String! = String(cString: [99,111,112,121,102,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &likee) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 3 {
      content8 = !likee.hasPrefix("\(content8)")
   }
   for _ in 0 ..< 3 {
      likee.append("\(likee.count - 2)")
   }

        isHeaderVideoPlaying ? pauseHeaderVideo() : playHeaderVideo()
    }

    @objc private func replayHeaderVideo() {
       var bar7: [String: Any]! = [String(cString: [102,116,118,99,108,0], encoding: .utf8)!:50, String(cString: [115,117,98,116,105,116,108,101,0], encoding: .utf8)!:48, String(cString: [116,104,101,114,101,0], encoding: .utf8)!:68]
    var save6: Int = 4
   withUnsafeMutablePointer(to: &save6) { pointer in
    
   }
   for _ in 0 ..< 2 {
      bar7 = ["\(bar7.values.count)": bar7.keys.count | save6]
   }

      save6 |= 2 % (Swift.max(10, save6))
        headerPlayer?.seek(to: .zero)
        playHeaderVideo()
    }

@discardableResult
 func productAlignmentSadEachNormalCleaner(stopData: String!) -> Float {
    var main_uo: Double = 0.0
   withUnsafeMutablePointer(to: &main_uo) { pointer in
    
   }
    var purchasingk: String! = String(cString: [97,116,99,104,0], encoding: .utf8)!
    _ = purchasingk
    var cachesx: Float = 5.0
    var return_6u: Double = 2.0
   while ((Double(cachesx) / (Swift.max(main_uo, 6))) <= 2.79) {
      main_uo /= Swift.max((Double(Int(cachesx > 139713674.0 || cachesx < -139713674.0 ? 79.0 : cachesx))), 2)
      break
   }
   while ((5 % (Swift.max(5, purchasingk.count))) >= 1 && (Double(purchasingk.count) / (Swift.max(3, main_uo))) >= 3.13) {
       var commentsJ: Double = 3.0
      withUnsafeMutablePointer(to: &commentsJ) { pointer in
    
      }
       var signupX: String! = String(cString: [99,117,98,101,100,0], encoding: .utf8)!
       var closeT: Bool = true
          var backL: Bool = false
          var summariesQ: [Any]! = [String(cString: [99,107,112,116,0], encoding: .utf8)!, String(cString: [114,105,103,104,116,0], encoding: .utf8)!, String(cString: [99,117,114,115,111,114,0], encoding: .utf8)!]
          _ = summariesQ
          var replyB: String! = String(cString: [104,95,56,49,95,117,100,116,97,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &replyB) { pointer in
                _ = pointer.pointee
         }
         signupX = "\(replyB.count ^ 2)"
         backL = !backL || replyB.count >= 15
         summariesQ = [signupX.count]
      repeat {
         signupX = "\(((closeT ? 4 : 1) >> (Swift.min(labs(Int(commentsJ > 336930225.0 || commentsJ < -336930225.0 ? 68.0 : commentsJ)), 5))))"
         if signupX == (String(cString:[104,100,109,113,109,108,57,103,98,0], encoding: .utf8)!) {
            break
         }
      } while (signupX == (String(cString:[104,100,109,113,109,108,57,103,98,0], encoding: .utf8)!)) && (!closeT)
         signupX.append("\((2 * Int(commentsJ > 283420869.0 || commentsJ < -283420869.0 ? 80.0 : commentsJ)))")
         closeT = signupX.hasSuffix("\(commentsJ)")
      repeat {
         closeT = !signupX.contains("\(closeT)")
         if closeT ? !closeT : closeT {
            break
         }
      } while ((1.12 + commentsJ) < 5.27 && commentsJ < 1.12) && (closeT ? !closeT : closeT)
         closeT = signupX.contains("\(closeT)")
          var launchX: [String: Any]! = [String(cString: [104,119,97,99,99,101,108,0], encoding: .utf8)!:String(cString: [118,109,115,108,0], encoding: .utf8)!, String(cString: [112,101,114,115,111,110,115,0], encoding: .utf8)!:String(cString: [99,105,114,99,108,101,115,0], encoding: .utf8)!, String(cString: [97,118,97,115,115,101,114,116,0], encoding: .utf8)!:String(cString: [100,114,105,102,116,105,110,103,0], encoding: .utf8)!]
          var e_tagL: Double = 2.0
         withUnsafeMutablePointer(to: &e_tagL) { pointer in
                _ = pointer.pointee
         }
          var send0: String! = String(cString: [118,97,114,105,110,102,111,0], encoding: .utf8)!
         signupX = "\((Int(commentsJ > 88417222.0 || commentsJ < -88417222.0 ? 37.0 : commentsJ) & 2))"
         launchX["\(closeT)"] = (Int(e_tagL > 295334889.0 || e_tagL < -295334889.0 ? 26.0 : e_tagL) >> (Swift.min(1, labs(2))))
         e_tagL += (Double(signupX == (String(cString:[101,0], encoding: .utf8)!) ? signupX.count : launchX.keys.count))
         send0.append("\(2 & launchX.keys.count)")
      repeat {
          var mutualD: String! = String(cString: [98,105,116,114,101,97,100,101,114,0], encoding: .utf8)!
          var fansW: [String: Any]! = [String(cString: [98,97,115,105,99,97,108,108,121,0], encoding: .utf8)!:[String(cString: [99,108,111,117,100,102,108,97,114,101,0], encoding: .utf8)!:32, String(cString: [104,102,121,117,0], encoding: .utf8)!:3]]
         withUnsafeMutablePointer(to: &fansW) { pointer in
                _ = pointer.pointee
         }
          var posts8: String! = String(cString: [100,101,99,0], encoding: .utf8)!
          var postsj: [Any]! = [96, 79, 33]
          _ = postsj
          var startI: String! = String(cString: [120,118,97,103,0], encoding: .utf8)!
         signupX.append("\(fansW.keys.count)")
         mutualD = "\(signupX.count % (Swift.max(2, mutualD.count)))"
         posts8.append("\(((String(cString:[105,0], encoding: .utf8)!) == startI ? (closeT ? 4 : 4) : startI.count))")
         postsj = [(Int(commentsJ > 252615879.0 || commentsJ < -252615879.0 ? 6.0 : commentsJ))]
         if signupX == (String(cString:[52,117,106,108,98,55,50,0], encoding: .utf8)!) {
            break
         }
      } while (signupX.contains("\(closeT)")) && (signupX == (String(cString:[52,117,106,108,98,55,50,0], encoding: .utf8)!))
          var idsY: [Any]! = [String(cString: [102,112,99,0], encoding: .utf8)!, String(cString: [110,117,108,108,115,0], encoding: .utf8)!]
         commentsJ /= Swift.max((Double((closeT ? 4 : 3))), 1)
         idsY = [(Int(commentsJ > 93008980.0 || commentsJ < -93008980.0 ? 67.0 : commentsJ))]
      purchasingk = "\((1 >> (Swift.min(labs(Int(main_uo > 298712483.0 || main_uo < -298712483.0 ? 42.0 : main_uo)), 2))))"
      break
   }
      return_6u -= (Double(Int(main_uo > 239354345.0 || main_uo < -239354345.0 ? 29.0 : main_uo)))
   return cachesx

}





    private func playHeaderVideo() {

         let foreverIntercept: Float = productAlignmentSadEachNormalCleaner(stopData:String(cString: [99,111,109,112,111,117,110,100,0], encoding: .utf8)!)

      print(foreverIntercept)

_ = foreverIntercept


       var promptq: Float = 4.0
      promptq += (Float(Int(promptq > 112704228.0 || promptq < -112704228.0 ? 24.0 : promptq)))

        guard let headerPlayer else { return }

        headerPlayer.play()
        isHeaderVideoPlaying = true
        updatePlayButtonIcon(isPlaying: true)
        playButton.isHidden = true
    }

@discardableResult
 func numberEaseDismissPrivacyContainer(screenHealing: String!, observationNews: Float, fansSelected: Double) -> Float {
    var rightG: Double = 4.0
   withUnsafeMutablePointer(to: &rightG) { pointer in
          _ = pointer.pointee
   }
    var qnewsP: Bool = true
    var statek: Float = 1.0
   withUnsafeMutablePointer(to: &statek) { pointer in
          _ = pointer.pointee
   }
   while (statek > Float(rightG)) {
       var cameray: String! = String(cString: [114,100,111,112,116,0], encoding: .utf8)!
       var messageV: Float = 2.0
       _ = messageV
       var actionL: Double = 4.0
      withUnsafeMutablePointer(to: &actionL) { pointer in
    
      }
       var presenterq: [Any]! = [51, 42]
         presenterq = [presenterq.count]
      repeat {
          var coinsp: String! = String(cString: [112,117,116,115,0], encoding: .utf8)!
         presenterq.append((2 & Int(actionL > 74299224.0 || actionL < -74299224.0 ? 62.0 : actionL)))
         coinsp.append("\((2 << (Swift.min(3, labs(Int(messageV > 304412288.0 || messageV < -304412288.0 ? 14.0 : messageV))))))")
         if presenterq.count == 634989 {
            break
         }
      } while ((messageV + Float(presenterq.count)) == 5.97 || 1 == (presenterq.count + 2)) && (presenterq.count == 634989)
      for _ in 0 ..< 2 {
         cameray = "\((Int(actionL > 126998577.0 || actionL < -126998577.0 ? 21.0 : actionL)))"
      }
      while (cameray.count <= presenterq.count) {
          var baseL: String! = String(cString: [99,108,97,105,109,101,100,0], encoding: .utf8)!
          var color4: String! = String(cString: [103,101,111,99,111,100,101,115,0], encoding: .utf8)!
          var observerx: [Any]! = [84, 83]
          var homeg: String! = String(cString: [100,97,109,112,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &homeg) { pointer in
                _ = pointer.pointee
         }
         presenterq.append(observerx.count / 1)
         baseL.append("\((Int(messageV > 212670125.0 || messageV < -212670125.0 ? 82.0 : messageV) << (Swift.min(presenterq.count, 5))))")
         color4.append("\(observerx.count)")
         homeg.append("\(3)")
         break
      }
      if (Int(messageV > 167939825.0 || messageV < -167939825.0 ? 35.0 : messageV) / (Swift.max(3, 4))) <= 1 || 4.47 <= (messageV / (Swift.max(Float(cameray.count), 3))) {
          var now9: Int = 0
         cameray = "\((cameray == (String(cString:[83,0], encoding: .utf8)!) ? Int(actionL > 23735613.0 || actionL < -23735613.0 ? 53.0 : actionL) : cameray.count))"
         now9 >>= Swift.min(5, labs((Int(actionL > 270872412.0 || actionL < -270872412.0 ? 74.0 : actionL) & Int(messageV > 307073423.0 || messageV < -307073423.0 ? 83.0 : messageV))))
      }
          var simoy: String! = String(cString: [105,115,111,109,0], encoding: .utf8)!
         messageV += Float(presenterq.count / (Swift.max(simoy.count, 9)))
      repeat {
         messageV /= Swift.max(2, Float(1))
         if messageV == 382474.0 {
            break
         }
      } while (Double(messageV) > actionL) && (messageV == 382474.0)
      while (actionL == 3.11) {
         actionL -= Double(presenterq.count)
         break
      }
          var p_centerQ: Double = 3.0
         messageV -= Float(2 >> (Swift.min(2, presenterq.count)))
         p_centerQ += Double(cameray.count)
      repeat {
          var repliesq: Double = 2.0
          var repliesB: String! = String(cString: [109,112,115,117,98,0], encoding: .utf8)!
          var modityC: String! = String(cString: [101,110,100,105,110,103,0], encoding: .utf8)!
          var durationS: String! = String(cString: [110,97,109,101,0], encoding: .utf8)!
          _ = durationS
          var likeC: Bool = true
         withUnsafeMutablePointer(to: &likeC) { pointer in
                _ = pointer.pointee
         }
         messageV += Float(2)
         repliesq += (Double(1 % (Swift.max(10, Int(actionL > 182303101.0 || actionL < -182303101.0 ? 31.0 : actionL)))))
         repliesB.append("\(1)")
         modityC = "\(modityC.count + cameray.count)"
         durationS.append("\((Int(actionL > 255124950.0 || actionL < -255124950.0 ? 69.0 : actionL)))")
         likeC = !modityC.hasPrefix("\(repliesq)")
         if messageV == 2698143.0 {
            break
         }
      } while ((messageV + 2.37) >= 4.47 || (Int(messageV > 301342583.0 || messageV < -301342583.0 ? 7.0 : messageV) + cameray.count) >= 5) && (messageV == 2698143.0)
       var purchaseD: String! = String(cString: [115,117,109,100,105,102,102,0], encoding: .utf8)!
       var inputS: String! = String(cString: [100,101,116,101,99,116,101,100,0], encoding: .utf8)!
      repeat {
         purchaseD = "\(3 & inputS.count)"
         if purchaseD.count == 3199470 {
            break
         }
      } while (purchaseD.count == 3199470) && (cameray.count < purchaseD.count)
      rightG += (Double(Int(actionL > 227793495.0 || actionL < -227793495.0 ? 46.0 : actionL)))
      break
   }
      qnewsP = !qnewsP || 37.72 <= statek
      rightG -= (Double((qnewsP ? 3 : 2) % (Swift.max(Int(rightG > 24129074.0 || rightG < -24129074.0 ? 31.0 : rightG), 2))))
   return statek

}





    private func pauseHeaderVideo() {

         let requestPinned: Float = numberEaseDismissPrivacyContainer(screenHealing:String(cString: [100,105,115,99,97,114,100,0], encoding: .utf8)!, observationNews:13.0, fansSelected:16.0)

      if requestPinned >= 2 {
             print(requestPinned)
      }

_ = requestPinned


       var indicatorL: Double = 1.0
   withUnsafeMutablePointer(to: &indicatorL) { pointer in
    
   }
    var friend_frj: [String: Any]! = [String(cString: [97,108,116,101,114,0], encoding: .utf8)!:47, String(cString: [100,115,116,0], encoding: .utf8)!:54]
   repeat {
      friend_frj["\(indicatorL)"] = (Int(indicatorL > 306979159.0 || indicatorL < -306979159.0 ? 26.0 : indicatorL) << (Swift.min(labs(3), 1)))
      if 1005153 == friend_frj.count {
         break
      }
   } while (1005153 == friend_frj.count) && (4.64 > (3.24 * indicatorL) && 3.24 > (indicatorL * Double(friend_frj.count)))

        headerPlayer?.pause()
        isHeaderVideoPlaying = false
        updatePlayButtonIcon(isPlaying: false)
        playButton.isHidden = false
    }

@discardableResult
 func starWebsiteLatestContactSuccess() -> Int {
    var packagesI: Bool = false
    var color1: String! = String(cString: [107,101,121,115,116,111,114,101,0], encoding: .utf8)!
    var insets6: Int = 1
   withUnsafeMutablePointer(to: &insets6) { pointer in
    
   }
   while (!packagesI) {
       var promptX: Float = 3.0
       var privacyO: Double = 2.0
       _ = privacyO
      while (2.41 >= (privacyO / (Swift.max(4.64, 7))) && 3.54 >= (privacyO / 4.64)) {
         privacyO += Double(3)
         break
      }
         privacyO += (Double(Int(promptX > 76580474.0 || promptX < -76580474.0 ? 38.0 : promptX) % (Swift.max(Int(privacyO > 219582490.0 || privacyO < -219582490.0 ? 55.0 : privacyO), 9))))
       var providerL: [String: Any]! = [String(cString: [108,105,98,114,97,114,105,101,115,0], encoding: .utf8)!:15, String(cString: [109,98,115,101,103,109,101,110,116,0], encoding: .utf8)!:63]
       var queue5: [String: Any]! = [String(cString: [104,97,115,104,101,114,0], encoding: .utf8)!:43, String(cString: [114,101,103,114,101,115,115,0], encoding: .utf8)!:38]
       var video8: String! = String(cString: [103,95,54,51,0], encoding: .utf8)!
      while (3 < (queue5.keys.count >> (Swift.min(labs(5), 2))) || (promptX / (Swift.max(Float(queue5.keys.count), 1))) < 4.87) {
          var mailc: Double = 5.0
         promptX /= Swift.max(4, Float(queue5.keys.count))
         mailc /= Swift.max((Double(Int(mailc > 287726347.0 || mailc < -287726347.0 ? 86.0 : mailc))), 5)
         break
      }
       var gestureT: Bool = true
         providerL = ["\(promptX)": (Int(privacyO > 346094955.0 || privacyO < -346094955.0 ? 76.0 : privacyO))]
         video8.append("\(video8.count + 2)")
         gestureT = ((Int(promptX > 364154921.0 || promptX < -364154921.0 ? 58.0 : promptX)) == providerL.values.count)
      packagesI = 64 > insets6
      break
   }
   for _ in 0 ..< 2 {
       var dismisst: String! = String(cString: [104,101,99,107,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &dismisst) { pointer in
    
      }
         dismisst = "\(3 >> (Swift.min(5, dismisst.count)))"
          var emailF: [Any]! = [String(cString: [110,111,114,109,0], encoding: .utf8)!, String(cString: [101,116,104,101,114,110,101,116,0], encoding: .utf8)!]
          var dismissal0: Double = 2.0
         withUnsafeMutablePointer(to: &dismissal0) { pointer in
                _ = pointer.pointee
         }
          var yes4: String! = String(cString: [116,114,101,101,114,101,97,100,101,114,0], encoding: .utf8)!
         dismisst = "\(3)"
         emailF.append(yes4.count / 3)
         dismissal0 += (Double(Int(dismissal0 > 42783836.0 || dismissal0 < -42783836.0 ? 48.0 : dismissal0) ^ yes4.count))
         dismisst = "\(3)"
      packagesI = color1.count >= 88
   }
   return insets6

}





    private func updatePlayButtonIcon(isPlaying: Bool) {

         var underlyingBin: Int = starWebsiteLatestContactSuccess()

      if underlyingBin > 16 {
             print(underlyingBin)
      }

withUnsafeMutablePointer(to: &underlyingBin) { pointer in
        _ = pointer.pointee
}


       var providerk: Float = 2.0
   while ((providerk - 2.84) >= 4.92 || (providerk - providerk) >= 2.84) {
      providerk += (Float(Int(providerk > 201726803.0 || providerk < -201726803.0 ? 33.0 : providerk) % 2))
      break
   }

        let settings = UIImage.SymbolConfiguration(pointSize: 20, weight: .semibold)
        let endImage = UIImage(systemName: "play.fill", withConfiguration: settings)
        playButton.setImage(endImage, for: .normal)
    }

    private func makeMessageFriend() -> SocEmotion {
       var dismissA: String! = String(cString: [100,101,108,105,118,101,114,121,0], encoding: .utf8)!
    var controllerj: String! = String(cString: [119,104,97,116,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &controllerj) { pointer in
          _ = pointer.pointee
   }
   repeat {
       var packagey: Double = 2.0
       var dialogv: String! = String(cString: [102,100,99,116,100,115,112,0], encoding: .utf8)!
       var perla6: String! = String(cString: [102,114,101,113,117,101,110,99,121,0], encoding: .utf8)!
       var sheetj: String! = String(cString: [100,97,116,97,100,105,114,0], encoding: .utf8)!
       var local_yf6: String! = String(cString: [105,110,99,114,101,97,115,105,110,103,0], encoding: .utf8)!
       _ = local_yf6
      if sheetj == String(cString:[67,0], encoding: .utf8)! || dialogv.count > 1 {
         dialogv = "\(dialogv.count)"
      }
         perla6.append("\(3 << (Swift.min(5, sheetj.count)))")
      for _ in 0 ..< 1 {
         perla6.append("\((local_yf6 == (String(cString:[72,0], encoding: .utf8)!) ? local_yf6.count : dialogv.count))")
      }
         perla6.append("\(2)")
         packagey -= (Double(Int(packagey > 107115868.0 || packagey < -107115868.0 ? 22.0 : packagey)))
      repeat {
         local_yf6 = "\(local_yf6.count)"
         if local_yf6 == (String(cString:[118,52,97,117,115,103,110,119,104,0], encoding: .utf8)!) {
            break
         }
      } while (!local_yf6.hasPrefix("\(dialogv.count)")) && (local_yf6 == (String(cString:[118,52,97,117,115,103,110,119,104,0], encoding: .utf8)!))
         perla6.append("\(perla6.count)")
      repeat {
         packagey -= (Double(Int(packagey > 162354651.0 || packagey < -162354651.0 ? 44.0 : packagey) + sheetj.count))
         if packagey == 2484978.0 {
            break
         }
      } while (packagey == 2484978.0) && (5.64 == (packagey * 3.69))
      if sheetj == String(cString:[115,0], encoding: .utf8)! && local_yf6 == String(cString:[72,0], encoding: .utf8)! {
         sheetj.append("\(3)")
      }
       var labelh: String! = String(cString: [100,98,115,105,122,101,0], encoding: .utf8)!
       var share3: Double = 4.0
       var guide7: Double = 4.0
          var guides: String! = String(cString: [114,116,99,115,116,97,116,115,0], encoding: .utf8)!
          var happyp: [Any]! = [0, 90, 98]
         share3 -= Double(guides.count >> (Swift.min(labs(2), 2)))
         happyp.append((3 << (Swift.min(3, labs(Int(packagey > 188272371.0 || packagey < -188272371.0 ? 96.0 : packagey))))))
         guide7 += (Double(Int(packagey > 177855200.0 || packagey < -177855200.0 ? 2.0 : packagey) + perla6.count))
      if (5 - sheetj.count) < 1 {
         share3 -= Double(2)
      }
         share3 += Double(3)
         labelh = "\((dialogv == (String(cString:[106,0], encoding: .utf8)!) ? labelh.count : dialogv.count))"
      dismissA.append("\(dialogv.count)")
      if dismissA == (String(cString:[122,116,111,112,105,0], encoding: .utf8)!) {
         break
      }
   } while (dismissA == (String(cString:[122,116,111,112,105,0], encoding: .utf8)!)) && (controllerj != String(cString:[108,0], encoding: .utf8)!)

   repeat {
       var conversationU: Double = 2.0
       var flashd: Int = 4
      withUnsafeMutablePointer(to: &flashd) { pointer in
    
      }
       var taglined: String! = String(cString: [115,116,114,109,97,116,99,104,0], encoding: .utf8)!
       var subtitleq: String! = String(cString: [99,111,101,114,99,101,0], encoding: .utf8)!
       var herow: Float = 0.0
       _ = herow
       var size_vdO: Int = 1
      if 5.35 == (1.83 - herow) {
         herow -= Float(taglined.count >> (Swift.min(3, labs(size_vdO))))
      }
       var nowx: Bool = false
       var screend: Bool = true
      for _ in 0 ..< 3 {
         size_vdO -= (taglined.count | Int(conversationU > 152641939.0 || conversationU < -152641939.0 ? 88.0 : conversationU))
      }
      for _ in 0 ..< 2 {
         nowx = herow == 66.40
      }
       var showE: [Any]! = [[82, 19, 26]]
       var companionY: [Any]! = [10, 70, 24]
      if (flashd << (Swift.min(labs(2), 3))) == 2 {
         subtitleq = "\(showE.count)"
      }
      for _ in 0 ..< 1 {
         showE = [1]
      }
      repeat {
         companionY.append(1)
         if 4472559 == companionY.count {
            break
         }
      } while (4472559 == companionY.count) && (3 > taglined.count)
          var contact7: String! = String(cString: [112,101,114,105,111,100,115,0], encoding: .utf8)!
          var tapZ: String! = String(cString: [97,110,97,108,121,115,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &tapZ) { pointer in
    
         }
         screend = subtitleq.count > 80 || flashd > 80
         contact7.append("\(subtitleq.count | 3)")
         tapZ = "\(contact7.count)"
       var companionb: Double = 5.0
          var likeO: Double = 3.0
         withUnsafeMutablePointer(to: &likeO) { pointer in
    
         }
          var testJ: String! = String(cString: [102,119,97,108,115,104,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &testJ) { pointer in
    
         }
          var fromL: Float = 3.0
         nowx = subtitleq == (String(cString:[100,0], encoding: .utf8)!)
         likeO -= (Double(2 - Int(conversationU > 138313608.0 || conversationU < -138313608.0 ? 19.0 : conversationU)))
         testJ.append("\(showE.count << (Swift.min(labs(3), 2)))")
         fromL -= (Float(2 << (Swift.min(4, labs(Int(likeO > 375165347.0 || likeO < -375165347.0 ? 77.0 : likeO))))))
         screend = companionY.count == showE.count
          var tab8: Double = 1.0
          var prefix__U: Double = 1.0
         withUnsafeMutablePointer(to: &prefix__U) { pointer in
    
         }
          var photo_: [Any]! = [UILabel(frame:CGRect(x: 223, y: 376, width: 0, height: 0))]
         conversationU -= Double(2)
         tab8 += (Double(Int(companionb > 365476662.0 || companionb < -365476662.0 ? 24.0 : companionb)))
         prefix__U /= Swift.max(2, (Double(photo_.count | (screend ? 4 : 4))))
         photo_ = [(Int(herow > 319002341.0 || herow < -319002341.0 ? 89.0 : herow) << (Swift.min(4, labs(1))))]
      controllerj.append("\(flashd * 2)")
      if 4880731 == controllerj.count {
         break
      }
   } while (4880731 == controllerj.count) && (controllerj.count < 5)
return         SocEmotion(
            name: post.name,
            message: post.content,
            avatarColor: UIColor(red: 0.76, green: 0.87, blue: 1.00, alpha: 1),
            avatarImageName: post.avatarImageName,
            profileVideoFileName: post.profileVideoFileName
        )
    }

    private func makeProfileListUser() -> SocKariCleaner {
       var indicatorq: Double = 4.0
    var rectI: String! = String(cString: [119,114,105,116,101,116,114,117,110,99,0], encoding: .utf8)!
      rectI = "\((Int(indicatorq > 210196241.0 || indicatorq < -210196241.0 ? 77.0 : indicatorq) << (Swift.min(labs(3), 1))))"

      rectI.append("\(((String(cString:[73,0], encoding: .utf8)!) == rectI ? Int(indicatorq > 219787729.0 || indicatorq < -219787729.0 ? 8.0 : indicatorq) : rectI.count))")
return         SocKariCleaner(
            name: post.name,
            avatarColor: UIColor(red: 0.76, green: 0.87, blue: 1.00, alpha: 1),
            avatarImageName: post.avatarImageName,
            message: post.content,
            profileVideoFileName: post.profileVideoFileName
        )
    }

    private func profileVideoURL() -> URL? {
       var healinge: [String: Any]! = [String(cString: [114,101,102,99,111,117,110,116,0], encoding: .utf8)!:8, String(cString: [109,117,116,101,120,0], encoding: .utf8)!:37, String(cString: [97,100,100,105,116,105,111,110,97,108,0], encoding: .utf8)!:11]
    _ = healinge
   while (1 <= (healinge.values.count % 3) || (healinge.values.count % (Swift.max(healinge.count, 8))) <= 3) {
      healinge = ["\(healinge.count)": 1 / (Swift.max(7, healinge.values.count))]
      break
   }

return         Bundle.main.url(
            forResource: post.profileVideoFileName,
            withExtension: "mp4",
            subdirectory: "Video"
        ) ?? Bundle.main.url(
            forResource: post.profileVideoFileName,
            withExtension: "mp4"
        )
    }

}
