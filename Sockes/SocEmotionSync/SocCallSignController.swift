
import Foundation

import UIKit

final class SocCallSignController: UIViewController {
private var contentCollectionClear_Array: [Any]?
var q_badge: Bool? = false
private var cancelUpdatedPresenterStr: String!
private var baseTag: Int? = 0




    private enum PSFCompanionFriendI {
        static let backIcon = "Common/common_back_icon"
        static let background = "EmotionSync/ShareEmotions/share_emotions_background"
        static let coinIcon = "EmotionSync/ShareEmotions/share_emotions_coin_icon"
        static let closeIcon = "EmotionSync/ShareEmotions/share_emotions_close_icon"
        static let addPhotoIcon = "EmotionSync/ShareEmotions/share_emotions_add_photo_icon"
    }

    private enum TZKLaunchFemaleQ {
        static let cost = 99
    }

    private let feelingsTextView = UITextView()
    private let placeholderLabel = UILabel()
    private let selectedPhotoImageView = UIImageView()
    private weak var addPhotoView: UIView?
    private weak var closePhotoView: UIView?
    private weak var photoOptionsOverlayView: UIView?
    private weak var insufficientCoinsOverlayView: UIView?
    private var selectedPhoto: UIImage?
    private var isSharing = false

@discardableResult
 func notificationGuidePlainBundle(replyText: Bool) -> Bool {
    var shareR: Bool = false
    var sign7: String! = String(cString: [116,104,114,111,116,116,108,101,0], encoding: .utf8)!
    var scene_uA: Bool = true
   withUnsafeMutablePointer(to: &scene_uA) { pointer in
    
   }
       var sourceb: Double = 1.0
       var privacyZ: String! = String(cString: [115,99,99,111,110,102,105,103,0], encoding: .utf8)!
       var panelA: String! = String(cString: [115,101,110,100,0], encoding: .utf8)!
       _ = panelA
         panelA.append("\(panelA.count >> (Swift.min(4, privacyZ.count)))")
         panelA = "\(1)"
      if !panelA.contains(privacyZ) {
         panelA = "\((Int(sourceb > 47257771.0 || sourceb < -47257771.0 ? 68.0 : sourceb)))"
      }
         privacyZ = "\((Int(sourceb > 169884158.0 || sourceb < -169884158.0 ? 54.0 : sourceb) & 1))"
       var femaleH: String! = String(cString: [116,115,114,0], encoding: .utf8)!
         sourceb -= Double(panelA.count)
      repeat {
         privacyZ = "\(1)"
         if (String(cString:[108,115,55,52,103,0], encoding: .utf8)!) == privacyZ {
            break
         }
      } while ((String(cString:[108,115,55,52,103,0], encoding: .utf8)!) == privacyZ) && (privacyZ.count == panelA.count)
         privacyZ = "\(privacyZ.count)"
      while (femaleH != String(cString:[119,0], encoding: .utf8)!) {
         panelA = "\(femaleH.count)"
         break
      }
      scene_uA = sign7.count == 60
   while (4 >= sign7.count) {
      sign7.append("\((sign7.count * (scene_uA ? 4 : 4)))")
      break
   }
   for _ in 0 ..< 1 {
      sign7.append("\(((shareR ? 4 : 1) / 3))")
   }
   return shareR

}





    override func viewDidLoad() {

         let parmsOdml: Bool = notificationGuidePlainBundle(replyText:false)

      if parmsOdml {
          print("documents")
      }

_ = parmsOdml


       var messageL: Float = 3.0
   repeat {
      messageL /= Swift.max(5, (Float(Int(messageL > 72550331.0 || messageL < -72550331.0 ? 40.0 : messageL) | Int(messageL > 76924795.0 || messageL < -76924795.0 ? 79.0 : messageL))))
      if 4903709.0 == messageL {
         break
      }
   } while (4903709.0 == messageL) && ((5.36 - messageL) == 1.44 && 4.61 == (messageL - 5.36))

        super.viewDidLoad()

        setupContent()
        setupKeyboardDismiss()
    }

@discardableResult
 func dismissDestructiveRectAnchor(local_ytFemale: String!, bottomCaches: [Any]!, indicatorCompanions: [String: Any]!) -> String! {
    var sharing7: Int = 2
    _ = sharing7
    var erroru: String! = String(cString: [100,101,99,105,115,105,111,110,0], encoding: .utf8)!
    var testq: String! = String(cString: [115,117,98,112,97,114,116,0], encoding: .utf8)!
       var observationI: String! = String(cString: [116,104,117,109,98,0], encoding: .utf8)!
          var packageB: String! = String(cString: [114,101,99,111,114,100,105,110,103,0], encoding: .utf8)!
          var commentsk: Double = 3.0
          var socialq: String! = String(cString: [99,111,110,116,97,105,110,101,100,0], encoding: .utf8)!
         observationI = "\(observationI.count)"
         packageB = "\(socialq.count | 3)"
         commentsk += (Double(Int(commentsk > 186047847.0 || commentsk < -186047847.0 ? 11.0 : commentsk) / (Swift.max(socialq.count, 2))))
          var frame_6O: String! = String(cString: [118,112,108,112,102,0], encoding: .utf8)!
         observationI = "\(frame_6O.count)"
       var malez: String! = String(cString: [115,116,114,105,110,103,117,116,105,108,115,0], encoding: .utf8)!
       _ = malez
         malez = "\(((String(cString:[69,0], encoding: .utf8)!) == observationI ? observationI.count : malez.count))"
      testq = "\(((String(cString:[116,0], encoding: .utf8)!) == testq ? testq.count : sharing7))"
      testq = "\(erroru.count % 1)"
   return erroru

}





    private func setupContent() {

         let hlsplaylistMblock: String! = dismissDestructiveRectAnchor(local_ytFemale:String(cString: [108,101,102,116,109,111,115,116,0], encoding: .utf8)!, bottomCaches:[66, 31], indicatorCompanions:[String(cString: [100,101,118,105,99,101,115,0], encoding: .utf8)!:String(cString: [112,97,114,97,109,101,116,114,105,99,0], encoding: .utf8)!])

      print(hlsplaylistMblock)
      let hlsplaylistMblock_len = hlsplaylistMblock?.count ?? 0

_ = hlsplaylistMblock


       var keyboardz: Float = 1.0
   withUnsafeMutablePointer(to: &keyboardz) { pointer in
    
   }
    var purchasej: Int = 2
    _ = purchasej
      keyboardz *= (Float(2 | Int(keyboardz > 211734810.0 || keyboardz < -211734810.0 ? 52.0 : keyboardz)))

       var time_q1J: Double = 3.0
       _ = time_q1J
         time_q1J /= Swift.max((Double(3 - Int(time_q1J > 146467927.0 || time_q1J < -146467927.0 ? 30.0 : time_q1J))), 5)
      repeat {
         time_q1J -= (Double(Int(time_q1J > 107626389.0 || time_q1J < -107626389.0 ? 49.0 : time_q1J)))
         if time_q1J == 1414289.0 {
            break
         }
      } while (time_q1J == 1414289.0) && ((time_q1J * 1.38) <= 2.18 && 1.38 <= (time_q1J * time_q1J))
      if time_q1J == time_q1J {
         time_q1J += (Double(3 | Int(time_q1J > 377741144.0 || time_q1J < -377741144.0 ? 64.0 : time_q1J)))
      }
      purchasej %= Swift.max(4, purchasej / 1)
        view.backgroundColor = UIColor(red: 0.95, green: 0.96, blue: 0.98, alpha: 1)

        let class_v1View = UIView()
        class_v1View.backgroundColor = UIColor(red: 0.98, green: 0.98, blue: 0.98, alpha: 1)
        class_v1View.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(class_v1View)

        let arlanView = UIImageView(image: UIImage(named: PSFCompanionFriendI.background))
        arlanView.contentMode = .scaleToFill
        arlanView.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(arlanView)

        let insufficientButton = UIButton(type: .custom)
        insufficientButton.setImage(UIImage(named: PSFCompanionFriendI.backIcon), for: .normal)
        insufficientButton.imageView?.contentMode = .scaleAspectFit
        insufficientButton.addTarget(self, action: #selector(closePage), for: .touchUpInside)
        insufficientButton.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(insufficientButton)

        let znewsLabel = UILabel()
        znewsLabel.text = "Share Emotions"
        znewsLabel.font = .systemFont(ofSize: 25, weight: .regular)
        znewsLabel.textColor = .black
        znewsLabel.textAlignment = .center
        znewsLabel.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(znewsLabel)

        feelingsTextView.backgroundColor = .white
        feelingsTextView.layer.cornerRadius = 9
        feelingsTextView.layer.masksToBounds = true
        feelingsTextView.font = .systemFont(ofSize: 15, weight: .regular)
        feelingsTextView.textColor = UIColor(red: 0.18, green: 0.18, blue: 0.18, alpha: 1)
        feelingsTextView.textContainerInset = UIEdgeInsets(top: 22, left: 18, bottom: 18, right: 18)
        feelingsTextView.delegate = self
        feelingsTextView.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(feelingsTextView)

        placeholderLabel.text = "Please write down your feelings"
        placeholderLabel.font = .systemFont(ofSize: 15, weight: .regular)
        placeholderLabel.textColor = UIColor(red: 0.88, green: 0.88, blue: 0.88, alpha: 1)
        placeholderLabel.translatesAutoresizingMaskIntoConstraints = false
        feelingsTextView.addSubview(placeholderLabel)

        let authView = UIView()
        authView.backgroundColor = .white
        authView.layer.cornerRadius = 9
        authView.layer.masksToBounds = false
        authView.translatesAutoresizingMaskIntoConstraints = false
        class_v1View.addSubview(authView)

        selectedPhotoImageView.contentMode = .scaleAspectFill
        selectedPhotoImageView.clipsToBounds = true
        selectedPhotoImageView.layer.cornerRadius = 9
        selectedPhotoImageView.isHidden = true
        selectedPhotoImageView.translatesAutoresizingMaskIntoConstraints = false
        authView.addSubview(selectedPhotoImageView)

        let arlanViewh = makeAddPhotoView()
        self.addPhotoView = arlanViewh
        arlanViewh.isUserInteractionEnabled = true
        arlanViewh.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(showPhotoOptions)))
        arlanViewh.translatesAutoresizingMaskIntoConstraints = false
        authView.addSubview(arlanViewh)

        let j_tagView = makeCloseView()
        closePhotoView = j_tagView
        j_tagView.isHidden = true
        j_tagView.isUserInteractionEnabled = true
        j_tagView.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(removeSelectedPhoto)))
        j_tagView.translatesAutoresizingMaskIntoConstraints = false
        authView.addSubview(j_tagView)

        let postsButton = makeShareButton()
        postsButton.addTarget(self, action: #selector(shareEmotion), for: .touchUpInside)
        class_v1View.addSubview(postsButton)

        NSLayoutConstraint.activate([
            class_v1View.topAnchor.constraint(equalTo: view.topAnchor, constant: 0),
            class_v1View.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 0),
            class_v1View.trailingAnchor.constraint(equalTo: view.trailingAnchor, constant: 0),
            class_v1View.bottomAnchor.constraint(equalTo: view.bottomAnchor, constant: -8),

            arlanView.topAnchor.constraint(equalTo: class_v1View.topAnchor),
            arlanView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor),
            arlanView.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor),
            arlanView.heightAnchor.constraint(equalToConstant: 200),

            insufficientButton.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 13),
            insufficientButton.topAnchor.constraint(equalTo: view.safeAreaLayoutGuide.topAnchor, constant: 3),
            insufficientButton.widthAnchor.constraint(equalToConstant: 44),
            insufficientButton.heightAnchor.constraint(equalToConstant: 44),

            znewsLabel.centerXAnchor.constraint(equalTo: class_v1View.centerXAnchor),
            znewsLabel.centerYAnchor.constraint(equalTo: insufficientButton.centerYAnchor),

            feelingsTextView.topAnchor.constraint(equalTo: class_v1View.topAnchor, constant: 119),
            feelingsTextView.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor, constant: 18),
            feelingsTextView.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor, constant: -18),
            feelingsTextView.heightAnchor.constraint(equalToConstant: 240),

            placeholderLabel.topAnchor.constraint(equalTo: feelingsTextView.topAnchor, constant: 22),
            placeholderLabel.leadingAnchor.constraint(equalTo: feelingsTextView.leadingAnchor, constant: 22),

            authView.topAnchor.constraint(equalTo: feelingsTextView.bottomAnchor, constant: 18),
            authView.leadingAnchor.constraint(equalTo: feelingsTextView.leadingAnchor),
            authView.widthAnchor.constraint(equalToConstant: 94),
            authView.heightAnchor.constraint(equalToConstant: 94),

            selectedPhotoImageView.topAnchor.constraint(equalTo: authView.topAnchor),
            selectedPhotoImageView.leadingAnchor.constraint(equalTo: authView.leadingAnchor),
            selectedPhotoImageView.trailingAnchor.constraint(equalTo: authView.trailingAnchor),
            selectedPhotoImageView.bottomAnchor.constraint(equalTo: authView.bottomAnchor),

            arlanViewh.centerXAnchor.constraint(equalTo: authView.centerXAnchor),
            arlanViewh.centerYAnchor.constraint(equalTo: authView.centerYAnchor),
            arlanViewh.widthAnchor.constraint(equalToConstant: 31),
            arlanViewh.heightAnchor.constraint(equalToConstant: 31),

            j_tagView.topAnchor.constraint(equalTo: authView.topAnchor, constant: -3),
            j_tagView.trailingAnchor.constraint(equalTo: authView.trailingAnchor, constant: 3),
            j_tagView.widthAnchor.constraint(equalToConstant: 22),
            j_tagView.heightAnchor.constraint(equalToConstant: 22),

            postsButton.leadingAnchor.constraint(equalTo: class_v1View.leadingAnchor, constant: 43),
            postsButton.trailingAnchor.constraint(equalTo: class_v1View.trailingAnchor, constant: -43),
            postsButton.topAnchor.constraint(equalTo: class_v1View.topAnchor, constant: 662),
            postsButton.heightAnchor.constraint(equalToConstant: 48)
        ])
    }

@discardableResult
 func remainCustomChatTableView(costOriginal: Bool, textCopy_e: Int, appearanceData: Float) -> UITableView! {
    var selectb: String! = String(cString: [112,97,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &selectb) { pointer in
    
   }
    var spendW: Bool = true
      selectb = "\(((spendW ? 5 : 1) % (Swift.max(2, 8))))"
   repeat {
      selectb.append("\(1 ^ selectb.count)")
      if (String(cString:[111,100,97,0], encoding: .utf8)!) == selectb {
         break
      }
   } while ((String(cString:[111,100,97,0], encoding: .utf8)!) == selectb) && (spendW && 3 >= selectb.count)
     var attributesPrice: UIButton! = UIButton()
     let buttonLayout: UIButton! = UIButton(frame:CGRect.zero)
     let reportConstraint: UIButton! = UIButton(frame:CGRect(x: 154, y: 168, width: 0, height: 0))
    var ftvfolderclosedUnmaskMkvmuxerutil:UITableView! = UITableView()
    ftvfolderclosedUnmaskMkvmuxerutil.delegate = nil
    ftvfolderclosedUnmaskMkvmuxerutil.dataSource = nil
    ftvfolderclosedUnmaskMkvmuxerutil.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    ftvfolderclosedUnmaskMkvmuxerutil.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    ftvfolderclosedUnmaskMkvmuxerutil.alpha = 0.3
    ftvfolderclosedUnmaskMkvmuxerutil.frame = CGRect(x: 113, y: 275, width: 0, height: 0)
    attributesPrice.alpha = 0.0;
    attributesPrice.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    attributesPrice.frame = CGRect(x: 80, y: 309, width: 0, height: 0)
    attributesPrice.setBackgroundImage(UIImage(named:String(cString: [104,101,97,108,0], encoding: .utf8)!), for: .normal)
    attributesPrice.titleLabel?.font = UIFont.systemFont(ofSize:16)
    attributesPrice.setImage(UIImage(named:String(cString: [112,114,111,109,112,116,0], encoding: .utf8)!), for: .normal)
    attributesPrice.setTitle("", for: .normal)
    
    buttonLayout.frame = CGRect(x: 12, y: 278, width: 0, height: 0)
    buttonLayout.alpha = 0.1;
    buttonLayout.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    buttonLayout.setBackgroundImage(UIImage(named:String(cString: [108,101,102,116,0], encoding: .utf8)!), for: .normal)
    buttonLayout.titleLabel?.font = UIFont.systemFont(ofSize:13)
    buttonLayout.setImage(UIImage(named:String(cString: [99,95,98,97,100,103,101,0], encoding: .utf8)!), for: .normal)
    buttonLayout.setTitle("", for: .normal)
    
    reportConstraint.alpha = 0.2;
    reportConstraint.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    reportConstraint.frame = CGRect(x: 6, y: 26, width: 0, height: 0)
    reportConstraint.titleLabel?.font = UIFont.systemFont(ofSize:17)
    reportConstraint.setImage(UIImage(named:String(cString: [108,105,115,116,101,110,101,114,0], encoding: .utf8)!), for: .normal)
    reportConstraint.setTitle("", for: .normal)
    reportConstraint.setBackgroundImage(UIImage(named:String(cString: [97,100,100,0], encoding: .utf8)!), for: .normal)
    

    
    return ftvfolderclosedUnmaskMkvmuxerutil

}





    private func setupKeyboardDismiss() {

         let msrledecExtrafield: UITableView! = remainCustomChatTableView(costOriginal:false, textCopy_e:74, appearanceData:92.0)

      if msrledecExtrafield != nil {
          let msrledecExtrafield_tag = msrledecExtrafield.tag
          self.view.addSubview(msrledecExtrafield)
      }

_ = msrledecExtrafield


       var taglineX: String! = String(cString: [98,105,116,114,118,0], encoding: .utf8)!
   if taglineX.hasPrefix("\(taglineX.count)") {
      taglineX = "\(taglineX.count)"
   }

        let names = UITapGestureRecognizer(target: self, action: #selector(dismissKeyboard))
        names.cancelsTouchesInView = false
        view.addGestureRecognizer(names)
    }

@discardableResult
 func editDisconnectTitleImageView() -> UIImageView! {
    var confirmation0: Bool = true
    var female4: String! = String(cString: [114,101,97,100,105,110,0], encoding: .utf8)!
   repeat {
      confirmation0 = female4.count > 92 && confirmation0
      if confirmation0 ? !confirmation0 : confirmation0 {
         break
      }
   } while (female4.count > 5) && (confirmation0 ? !confirmation0 : confirmation0)
     var sessionHeal: String! = String(cString: [115,108,105,99,101,0], encoding: .utf8)!
     let buttonContainer: [String: Any]! = [String(cString: [100,99,116,0], encoding: .utf8)!:37, String(cString: [107,101,121,105,100,0], encoding: .utf8)!:35, String(cString: [97,112,112,114,111,120,105,109,97,116,111,114,0], encoding: .utf8)!:46]
    var peeridReported = UIImageView(frame:CGRect(x: 96, y: 393, width: 0, height: 0))
    peeridReported.image = UIImage(named:String(cString: [108,97,116,101,115,116,0], encoding: .utf8)!)
    peeridReported.contentMode = .scaleAspectFit
    peeridReported.animationRepeatCount = 4
    peeridReported.frame = CGRect(x: 265, y: 191, width: 0, height: 0)
    peeridReported.alpha = 0.7;
    peeridReported.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)

    
    return peeridReported

}





    private func makeAddPhotoView() -> UIView {

         let codedTimewait: UIImageView! = editDisconnectTitleImageView()

      if codedTimewait != nil {
          let codedTimewait_tag = codedTimewait.tag
          self.view.addSubview(codedTimewait)
      }
      else {
          print("codedTimewait is nil")      }

_ = codedTimewait


       var storew: [Any]! = [String(cString: [102,105,103,117,114,101,0], encoding: .utf8)!, String(cString: [104,101,105,103,104,116,115,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &storew) { pointer in
          _ = pointer.pointee
   }
   while (storew.count < storew.count) {
       var cleanerg: String! = String(cString: [115,104,117,116,100,111,119,110,0], encoding: .utf8)!
       var throwingj: Int = 1
       _ = throwingj
       var displayo: String! = String(cString: [97,118,103,121,0], encoding: .utf8)!
       var insetsQ: Double = 4.0
         throwingj &= 3
         cleanerg.append("\(cleanerg.count / 2)")
         throwingj -= throwingj
         displayo = "\(1 / (Swift.max(5, throwingj)))"
         cleanerg = "\(throwingj & 1)"
      repeat {
         displayo.append("\(displayo.count)")
         if 3090246 == displayo.count {
            break
         }
      } while (3090246 == displayo.count) && (insetsQ >= 3.41)
         insetsQ /= Swift.max(Double(cleanerg.count / (Swift.max(10, throwingj))), 4)
          var bottomQ: [String: Any]! = [String(cString: [116,101,99,104,110,111,108,111,103,121,0], encoding: .utf8)!:String(cString: [119,97,105,116,101,114,0], encoding: .utf8)!, String(cString: [108,111,111,112,0], encoding: .utf8)!:String(cString: [98,114,101,110,100,101,114,0], encoding: .utf8)!]
          var placeholderE: String! = String(cString: [112,114,111,100,117,99,116,105,111,110,0], encoding: .utf8)!
          _ = placeholderE
          var constantM: Double = 5.0
         withUnsafeMutablePointer(to: &constantM) { pointer in
    
         }
         displayo.append("\((Int(constantM > 39681835.0 || constantM < -39681835.0 ? 34.0 : constantM)))")
         bottomQ[displayo] = bottomQ.values.count % (Swift.max(displayo.count, 9))
         placeholderE.append("\(bottomQ.values.count)")
      repeat {
         cleanerg = "\(displayo.count + 2)"
         if (String(cString:[50,114,100,111,56,103,108,116,95,102,0], encoding: .utf8)!) == cleanerg {
            break
         }
      } while ((String(cString:[50,114,100,111,56,103,108,116,95,102,0], encoding: .utf8)!) == cleanerg) && ((throwingj / 4) < 1)
      if 3.18 == insetsQ {
         cleanerg = "\(3)"
      }
      for _ in 0 ..< 1 {
          var feelingsw: Int = 5
          _ = feelingsw
          var fileT: String! = String(cString: [108,97,116,105,110,0], encoding: .utf8)!
          var winitialv: Bool = false
          var checkedf: String! = String(cString: [102,97,100,115,116,0], encoding: .utf8)!
          _ = checkedf
          var playing7: Double = 0.0
         throwingj |= (Int(insetsQ > 164715067.0 || insetsQ < -164715067.0 ? 85.0 : insetsQ) << (Swift.min(labs(3), 3)))
         feelingsw /= Swift.max(2, throwingj & 3)
         fileT = "\((Int(playing7 > 304921921.0 || playing7 < -304921921.0 ? 67.0 : playing7)))"
         winitialv = (String(cString:[121,0], encoding: .utf8)!) == cleanerg
         checkedf = "\(fileT.count)"
         playing7 += Double(3)
      }
         throwingj |= 2 + displayo.count
      storew = [throwingj]
      break
   }

        if let image = UIImage(named: PSFCompanionFriendI.addPhotoIcon) {
            let postsView = UIImageView(image: image)
            postsView.contentMode = .scaleAspectFit
            return postsView
        }

        let homeLabel = UILabel()
        homeLabel.text = "+"
        homeLabel.font = .systemFont(ofSize: 44, weight: .ultraLight)
        homeLabel.textColor = UIColor(red: 0.82, green: 0.82, blue: 0.82, alpha: 1)
        homeLabel.textAlignment = .center
        return homeLabel
    }

@discardableResult
 func yesMineConvertConstraintTarget(profilePreferred: Int, successRing: Int, rowDialog: String!) -> String! {
    var sign1: Double = 1.0
   withUnsafeMutablePointer(to: &sign1) { pointer in
          _ = pointer.pointee
   }
    var socialb: Float = 2.0
    var coinsl: String! = String(cString: [101,120,101,99,117,116,97,98,108,101,0], encoding: .utf8)!
   if 4 <= (coinsl.count / 3) || 5 <= (coinsl.count << (Swift.min(labs(3), 3))) {
      coinsl.append("\(3)")
   }
   for _ in 0 ..< 3 {
       var socialK: String! = String(cString: [114,101,102,101,114,114,101,114,0], encoding: .utf8)!
       var healD: String! = String(cString: [114,116,102,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &healD) { pointer in
    
      }
       var replyA: [Any]! = [68, 26, 27]
       var postsr: [Any]! = [String(cString: [100,105,115,109,105,115,115,0], encoding: .utf8)!, String(cString: [99,104,97,114,97,99,116,101,114,0], encoding: .utf8)!, String(cString: [97,117,116,111,109,97,116,105,99,97,108,108,121,0], encoding: .utf8)!]
       var share9: Float = 0.0
         replyA.append(postsr.count)
         replyA.append(socialK.count % 1)
      while (postsr.contains { $0 as? Int == replyA.count }) {
         replyA.append(replyA.count)
         break
      }
      while ((healD.count >> (Swift.min(labs(5), 2))) < 2 || (postsr.count >> (Swift.min(labs(5), 4))) < 1) {
         postsr = [postsr.count]
         break
      }
         share9 -= Float(healD.count + 1)
         replyA.append(2 + replyA.count)
      for _ in 0 ..< 3 {
          var closel: [Any]! = [92, 60]
          var modeS: String! = String(cString: [110,117,109,112,97,100,0], encoding: .utf8)!
          _ = modeS
          var areaH: String! = String(cString: [114,117,110,116,101,114,109,0], encoding: .utf8)!
          var layoutz: String! = String(cString: [100,105,109,105,110,115,105,111,110,115,0], encoding: .utf8)!
         postsr = [2 + layoutz.count]
         closel = [replyA.count / (Swift.max(8, modeS.count))]
         modeS = "\(((String(cString:[116,0], encoding: .utf8)!) == modeS ? healD.count : modeS.count))"
         areaH = "\(((String(cString:[112,0], encoding: .utf8)!) == areaH ? postsr.count : areaH.count))"
      }
         postsr = [healD.count]
       var loadingQ: Bool = false
       var scene_u5: Bool = false
         healD = "\(((loadingQ ? 2 : 5) % (Swift.max(Int(share9 > 13377601.0 || share9 < -13377601.0 ? 60.0 : share9), 2))))"
          var blockedA: String! = String(cString: [116,104,101,109,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &blockedA) { pointer in
    
         }
         postsr = [(blockedA == (String(cString:[109,0], encoding: .utf8)!) ? postsr.count : blockedA.count)]
         replyA.append(((scene_u5 ? 5 : 1) * 1))
       var bark: String! = String(cString: [112,97,99,107,0], encoding: .utf8)!
       var retryW: String! = String(cString: [99,97,110,99,101,108,108,101,114,0], encoding: .utf8)!
       _ = retryW
      for _ in 0 ..< 2 {
          var authM: String! = String(cString: [114,116,112,102,98,0], encoding: .utf8)!
         scene_u5 = bark == (String(cString:[83,0], encoding: .utf8)!)
         authM = "\(bark.count)"
      }
          var q_centerg: String! = String(cString: [117,110,111,114,100,101,114,101,100,0], encoding: .utf8)!
          var chatA: Double = 0.0
         postsr = [(q_centerg.count & Int(share9 > 281734760.0 || share9 < -281734760.0 ? 18.0 : share9))]
         chatA *= (Double((String(cString:[85,0], encoding: .utf8)!) == healD ? healD.count : (loadingQ ? 4 : 1)))
         retryW.append("\(1)")
      coinsl.append("\(2)")
   }
   if (coinsl.count - Int(socialb > 377618360.0 || socialb < -377618360.0 ? 94.0 : socialb)) == 5 && (coinsl.count - Int(socialb > 249968790.0 || socialb < -249968790.0 ? 5.0 : socialb)) == 5 {
       var frame_0bm: [Any]! = [72, 63, 55]
       var replyo: Bool = false
      withUnsafeMutablePointer(to: &replyo) { pointer in
             _ = pointer.pointee
      }
       var offsetb: Bool = false
       var becomeC: String! = String(cString: [110,115,117,105,0], encoding: .utf8)!
          var pickerd: Float = 4.0
         withUnsafeMutablePointer(to: &pickerd) { pointer in
    
         }
          var gestureg: String! = String(cString: [101,110,116,105,114,101,0], encoding: .utf8)!
          var healingK: String! = String(cString: [116,110,112,117,116,0], encoding: .utf8)!
         frame_0bm.append(becomeC.count)
         pickerd += (Float(becomeC.count % (Swift.max(1, Int(pickerd > 24579360.0 || pickerd < -24579360.0 ? 37.0 : pickerd)))))
         gestureg = "\(3)"
         healingK = "\(becomeC.count)"
          var panely: String! = String(cString: [105,110,116,101,110,116,105,111,110,0], encoding: .utf8)!
          var updatedL: String! = String(cString: [109,98,104,115,0], encoding: .utf8)!
         frame_0bm = [(2 << (Swift.min(2, labs((replyo ? 5 : 4)))))]
         panely.append("\(becomeC.count)")
         updatedL = "\(frame_0bm.count % (Swift.max(1, 3)))"
      repeat {
          var controlu: Int = 4
          var collectionA: String! = String(cString: [103,101,116,116,105,110,103,0], encoding: .utf8)!
          _ = collectionA
          var barF: Double = 0.0
          var applicationz: Int = 2
         offsetb = collectionA.count == controlu
         barF += (Double(Int(barF > 52326892.0 || barF < -52326892.0 ? 69.0 : barF)))
         applicationz &= (controlu & (offsetb ? 5 : 3))
         if offsetb ? !offsetb : offsetb {
            break
         }
      } while (replyo && offsetb) && (offsetb ? !offsetb : offsetb)
      while (offsetb) {
         offsetb = !offsetb
         break
      }
          var micc: String! = String(cString: [112,114,101,115,101,108,101,99,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &micc) { pointer in
    
         }
         becomeC = "\(((String(cString:[105,0], encoding: .utf8)!) == micc ? micc.count : (replyo ? 5 : 2)))"
      for _ in 0 ..< 3 {
         becomeC = "\(((String(cString:[66,0], encoding: .utf8)!) == becomeC ? (replyo ? 1 : 3) : becomeC.count))"
      }
      repeat {
         replyo = offsetb
         if replyo ? !replyo : replyo {
            break
         }
      } while (!offsetb) && (replyo ? !replyo : replyo)
         replyo = frame_0bm.count >= 48 && replyo
         frame_0bm = [frame_0bm.count]
      if (frame_0bm.count % (Swift.max(1, 9))) <= 5 {
          var alertK: [Any]! = [String(cString: [104,97,114,100,0], encoding: .utf8)!, String(cString: [112,114,101,118,111,105,117,115,0], encoding: .utf8)!, String(cString: [104,105,103,104,101,115,116,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &alertK) { pointer in
                _ = pointer.pointee
         }
          var throwingw: String! = String(cString: [99,97,99,104,101,0], encoding: .utf8)!
          var controllerx: Float = 0.0
          var perlae: Bool = true
          var identifierq: String! = String(cString: [110,111,116,103,101,116,0], encoding: .utf8)!
         frame_0bm = [((offsetb ? 3 : 2))]
         alertK.append((becomeC.count / (Swift.max(9, (offsetb ? 4 : 4)))))
         throwingw.append("\(((replyo ? 5 : 2) | frame_0bm.count))")
         controllerx /= Swift.max(2, Float(frame_0bm.count))
         perlae = (!replyo ? !offsetb : !replyo)
         identifierq = "\(((offsetb ? 5 : 4) >> (Swift.min(labs((perlae ? 1 : 3)), 2))))"
      }
      if !replyo {
         frame_0bm = [2]
      }
         becomeC.append("\(((String(cString:[88,0], encoding: .utf8)!) == becomeC ? (replyo ? 1 : 5) : becomeC.count))")
      socialb += (Float(3 & Int(sign1 > 222622346.0 || sign1 < -222622346.0 ? 45.0 : sign1)))
   }
   return coinsl

}





    private func makeCloseView() -> UIView {

         let codefMaxkeysize: String! = yesMineConvertConstraintTarget(profilePreferred:82, successRing:77, rowDialog:String(cString: [100,101,113,117,97,110,116,0], encoding: .utf8)!)

      if codefMaxkeysize == "logged" {
              print(codefMaxkeysize)
      }
      let codefMaxkeysize_len = codefMaxkeysize?.count ?? 0

_ = codefMaxkeysize


       var queueq: Double = 2.0
   while (3.42 <= (queueq * queueq) && (3.42 * queueq) <= 4.14) {
       var throwingR: Bool = true
       var sourceY: Float = 4.0
       var dimT: String! = String(cString: [100,112,97,103,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &dimT) { pointer in
    
      }
          var syncV: [Any]! = [7, 9]
          _ = syncV
         dimT = "\((Int(sourceY > 177853317.0 || sourceY < -177853317.0 ? 15.0 : sourceY) & (throwingR ? 1 : 1)))"
         syncV.append((Int(sourceY > 220775990.0 || sourceY < -220775990.0 ? 1.0 : sourceY) % (Swift.max(8, (throwingR ? 2 : 2)))))
      for _ in 0 ..< 1 {
         sourceY /= Swift.max(4, (Float((throwingR ? 1 : 4) + Int(sourceY > 323654824.0 || sourceY < -323654824.0 ? 40.0 : sourceY))))
      }
      repeat {
          var chatC: Double = 3.0
         dimT = "\(dimT.count)"
         chatC -= Double(dimT.count ^ 2)
         if dimT.count == 1568889 {
            break
         }
      } while (dimT.count == 1568889) && (dimT.count == 5)
       var h_layerE: [Any]! = [String(cString: [116,105,109,105,110,103,0], encoding: .utf8)!, String(cString: [112,111,105,110,116,99,98,98,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &h_layerE) { pointer in
    
      }
          var herox: [Any]! = [57, 69]
         withUnsafeMutablePointer(to: &herox) { pointer in
    
         }
          var editF: String! = String(cString: [118,105,115,105,116,111,114,0], encoding: .utf8)!
         throwingR = (String(cString:[110,0], encoding: .utf8)!) == editF
         herox = [h_layerE.count]
       var photob: Double = 3.0
       _ = photob
       var chat6: Double = 3.0
      for _ in 0 ..< 1 {
         chat6 += Double(3 * dimT.count)
      }
      for _ in 0 ..< 2 {
         dimT.append("\(h_layerE.count)")
      }
         h_layerE.append(dimT.count)
         photob -= Double(2)
      queueq /= Swift.max((Double(dimT == (String(cString:[84,0], encoding: .utf8)!) ? dimT.count : Int(sourceY > 27642550.0 || sourceY < -27642550.0 ? 64.0 : sourceY))), 4)
      break
   }

        if let image = UIImage(named: PSFCompanionFriendI.closeIcon) {
            let postsView = UIImageView(image: image)
            postsView.contentMode = .scaleAspectFit
            return postsView
        }

        let homeLabel = UILabel()
        homeLabel.text = "×"
        homeLabel.font = .systemFont(ofSize: 20, weight: .regular)
        homeLabel.textColor = .white
        homeLabel.textAlignment = .center
        homeLabel.backgroundColor = UIColor(red: 1.00, green: 0.40, blue: 0.40, alpha: 1)
        homeLabel.layer.cornerRadius = 11
        homeLabel.layer.masksToBounds = true
        return homeLabel
    }

@discardableResult
 func dispatchAddressBubbleAnchorSessionDisappearImageView(areaSync: Int, template_aGuide: Double) -> UIImageView! {
    var change7: Double = 4.0
    var actionC: Float = 5.0
       var scene_n7: String! = String(cString: [102,108,101,120,102,101,99,0], encoding: .utf8)!
      if 3 >= scene_n7.count {
         scene_n7 = "\(2 & scene_n7.count)"
      }
      while (scene_n7 != String(cString:[81,0], encoding: .utf8)!) {
         scene_n7.append("\(scene_n7.count % (Swift.max(3, 5)))")
         break
      }
         scene_n7.append("\(((String(cString:[119,0], encoding: .utf8)!) == scene_n7 ? scene_n7.count : scene_n7.count))")
      actionC -= Float(3 >> (Swift.min(3, scene_n7.count)))
   repeat {
      change7 /= Swift.max(3, (Double(Int(change7 > 80004465.0 || change7 < -80004465.0 ? 15.0 : change7) ^ 1)))
      if change7 == 1175805.0 {
         break
      }
   } while (change7 == 1175805.0) && (2.71 <= (change7 + 1.78))
     let progressPath: Bool = true
     let switch_9Logged: Double = 1.0
    var estimateIterate: UIImageView! = UIImageView(frame:CGRect.zero)
    estimateIterate.animationRepeatCount = 0
    estimateIterate.image = UIImage(named:String(cString: [115,95,119,105,100,116,104,0], encoding: .utf8)!)
    estimateIterate.contentMode = .scaleAspectFit
    estimateIterate.frame = CGRect(x: 211, y: 268, width: 0, height: 0)
    estimateIterate.alpha = 0.3;
    estimateIterate.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return estimateIterate

}





    private func makeShareButton() -> UIButton {

         var malloczOsdep: UIImageView! = dispatchAddressBubbleAnchorSessionDisappearImageView(areaSync:42, template_aGuide:84.0)

      if malloczOsdep != nil {
          self.view.addSubview(malloczOsdep)
          let malloczOsdep_tag = malloczOsdep.tag
      }

withUnsafeMutablePointer(to: &malloczOsdep) { pointer in
    
}


       var optionsD: Float = 0.0
    var follow0: Double = 1.0
    _ = follow0
       var topB: [Any]! = [String(cString: [97,108,112,104,97,101,120,116,114,97,99,116,0], encoding: .utf8)!, String(cString: [103,101,116,104,111,115,116,98,121,110,97,109,101,0], encoding: .utf8)!]
       _ = topB
          var summaries2: [String: Any]! = [String(cString: [119,114,97,112,0], encoding: .utf8)!:75, String(cString: [97,117,116,104,101,110,116,105,99,97,116,111,114,0], encoding: .utf8)!:15, String(cString: [115,105,109,117,108,97,116,101,0], encoding: .utf8)!:31]
         topB = [2]
         summaries2 = ["\(summaries2.values.count)": 2]
         topB = [topB.count | topB.count]
         topB = [topB.count * 3]
      follow0 -= (Double(2 * Int(optionsD > 88005770.0 || optionsD < -88005770.0 ? 12.0 : optionsD)))

       var pickere: Double = 1.0
      withUnsafeMutablePointer(to: &pickere) { pointer in
             _ = pointer.pointee
      }
       var ringX: [Any]! = [69, 21, 99]
       _ = ringX
         ringX.append(3)
      if Double(ringX.count) <= pickere {
          var usersz: String! = String(cString: [109,101,100,105,97,99,111,100,101,99,100,101,99,0], encoding: .utf8)!
          var smalli: Float = 2.0
         withUnsafeMutablePointer(to: &smalli) { pointer in
                _ = pointer.pointee
         }
         ringX = [ringX.count]
         usersz = "\(ringX.count % (Swift.max(usersz.count, 5)))"
         smalli -= Float(1 * ringX.count)
      }
      repeat {
         ringX = [ringX.count | 3]
         if ringX.count == 3152346 {
            break
         }
      } while (1.83 >= (pickere - Double(ringX.count))) && (ringX.count == 3152346)
       var arlan9: String! = String(cString: [109,112,101,103,100,97,116,97,0], encoding: .utf8)!
       var messagex: String! = String(cString: [98,97,116,116,101,114,121,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &messagex) { pointer in
             _ = pointer.pointee
      }
       var emotionsR: Float = 0.0
       var placeholderg: Float = 3.0
      while (2.57 <= (Double(ringX.count) + pickere)) {
         ringX = [(arlan9.count + Int(placeholderg > 325199542.0 || placeholderg < -325199542.0 ? 30.0 : placeholderg))]
         break
      }
         messagex = "\(((String(cString:[99,0], encoding: .utf8)!) == arlan9 ? Int(placeholderg > 160106000.0 || placeholderg < -160106000.0 ? 74.0 : placeholderg) : arlan9.count))"
         emotionsR /= Swift.max(Float(arlan9.count), 2)
      optionsD /= Swift.max(5, Float(1))
        let taglineButton = UIButton(type: .custom)
        taglineButton.backgroundColor = UIColor(red: 0.07, green: 0.09, blue: 0.07, alpha: 1)
        taglineButton.layer.cornerRadius = 24
        taglineButton.layer.masksToBounds = true
        taglineButton.translatesAutoresizingMaskIntoConstraints = false

        let resignLabel = makeStarLabel()
        let znewsLabel = UILabel()
        znewsLabel.text = "Share"
        znewsLabel.textColor = .white
        znewsLabel.font = .systemFont(ofSize: 18, weight: .bold)
        znewsLabel.translatesAutoresizingMaskIntoConstraints = false

        let normalizedLabel = makeStarLabel()
        let sendView = makeCoinView()
        sendView.translatesAutoresizingMaskIntoConstraints = false

        let privacyLabel = UILabel()
        privacyLabel.text = "\(TZKLaunchFemaleQ.cost)"
        privacyLabel.textColor = .white
        privacyLabel.font = .systemFont(ofSize: 14, weight: .semibold)
        privacyLabel.translatesAutoresizingMaskIntoConstraints = false

        taglineButton.addSubview(resignLabel)
        taglineButton.addSubview(znewsLabel)
        taglineButton.addSubview(normalizedLabel)
        taglineButton.addSubview(sendView)
        taglineButton.addSubview(privacyLabel)

        NSLayoutConstraint.activate([
            znewsLabel.centerXAnchor.constraint(equalTo: taglineButton.centerXAnchor, constant: -14),
            znewsLabel.centerYAnchor.constraint(equalTo: taglineButton.centerYAnchor),

            resignLabel.trailingAnchor.constraint(equalTo: znewsLabel.leadingAnchor, constant: -18),
            resignLabel.centerYAnchor.constraint(equalTo: taglineButton.centerYAnchor),

            normalizedLabel.leadingAnchor.constraint(equalTo: znewsLabel.trailingAnchor, constant: 18),
            normalizedLabel.centerYAnchor.constraint(equalTo: taglineButton.centerYAnchor),

            sendView.leadingAnchor.constraint(equalTo: normalizedLabel.trailingAnchor, constant: 21),
            sendView.centerYAnchor.constraint(equalTo: taglineButton.centerYAnchor),
            sendView.widthAnchor.constraint(equalToConstant: 22),
            sendView.heightAnchor.constraint(equalToConstant: 22),

            privacyLabel.leadingAnchor.constraint(equalTo: sendView.trailingAnchor, constant: 5),
            privacyLabel.centerYAnchor.constraint(equalTo: taglineButton.centerYAnchor)
        ])

        return taglineButton
    }

@discardableResult
 func heightWideScrollPresentationButton(cachePrefix__: [Any]!, mainLarge: [String: Any]!) -> UIButton! {
    var min_pF: Float = 3.0
    _ = min_pF
    var listJ: String! = String(cString: [108,111,103,100,98,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
      listJ.append("\(1)")
   }
       var frame_yup: Double = 2.0
      repeat {
         frame_yup /= Swift.max(5, (Double(Int(frame_yup > 273774492.0 || frame_yup < -273774492.0 ? 71.0 : frame_yup))))
         if 1634046.0 == frame_yup {
            break
         }
      } while (1.69 >= frame_yup) && (1634046.0 == frame_yup)
         frame_yup /= Swift.max(Double(2), 1)
      for _ in 0 ..< 2 {
         frame_yup /= Swift.max(4, (Double(Int(frame_yup > 336710933.0 || frame_yup < -336710933.0 ? 26.0 : frame_yup))))
      }
      listJ = "\((2 + Int(min_pF > 220851053.0 || min_pF < -220851053.0 ? 46.0 : min_pF)))"
     var preferredContent: Int = 50
     let controllerEdit: String! = String(cString: [100,105,115,97,108,108,111,119,0], encoding: .utf8)!
     let remainInput: UIImageView! = UIImageView(image:UIImage(named:String(cString: [111,117,116,112,117,116,115,0], encoding: .utf8)!), highlightedImage:UIImage(named:String(cString: [101,108,115,100,101,99,0], encoding: .utf8)!))
     var normalLeading: String! = String(cString: [99,111,100,101,0], encoding: .utf8)!
    var authorizationRevealed:UIButton! = UIButton()
    remainInput.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    remainInput.alpha = 0.1
    remainInput.frame = CGRect(x: 109, y: 216, width: 0, height: 0)
    remainInput.contentMode = .scaleAspectFit
    remainInput.animationRepeatCount = 10
    remainInput.image = UIImage(named:String(cString: [115,116,97,114,0], encoding: .utf8)!)
    
    authorizationRevealed.setBackgroundImage(UIImage(named:String(cString: [101,109,112,116,121,0], encoding: .utf8)!), for: .normal)
    authorizationRevealed.titleLabel?.font = UIFont.systemFont(ofSize:14)
    authorizationRevealed.setImage(UIImage(named:String(cString: [102,111,108,108,111,119,105,110,103,0], encoding: .utf8)!), for: .normal)
    authorizationRevealed.setTitle("", for: .normal)
    authorizationRevealed.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    authorizationRevealed.alpha = 0.3
    authorizationRevealed.frame = CGRect(x: 266, y: 287, width: 0, height: 0)

    
    return authorizationRevealed

}





    private func makeCoinView() -> UIView {

         var rewardsShortly: UIButton! = heightWideScrollPresentationButton(cachePrefix__:[40, 63], mainLarge:[String(cString: [118,112,120,100,101,99,0], encoding: .utf8)!:72])

      if rewardsShortly != nil {
          self.view.addSubview(rewardsShortly)
          let rewardsShortly_tag = rewardsShortly.tag
      }

withUnsafeMutablePointer(to: &rewardsShortly) { pointer in
    
}


       var costq: String! = String(cString: [114,97,116,101,99,116,114,108,0], encoding: .utf8)!
    var dimw: Double = 2.0
   withUnsafeMutablePointer(to: &dimw) { pointer in
    
   }
   if (dimw * Double(costq.count)) <= 1.35 || (dimw * 1.35) <= 4.31 {
      dimw /= Swift.max((Double((String(cString:[112,0], encoding: .utf8)!) == costq ? costq.count : Int(dimw > 260212019.0 || dimw < -260212019.0 ? 82.0 : dimw))), 1)
   }

        if let image = UIImage(named: PSFCompanionFriendI.coinIcon) {
            let postsView = UIImageView(image: image)
            postsView.contentMode = .scaleAspectFit
            return postsView
        }

        let homeLabel = UILabel()
        homeLabel.text = "¥"
        homeLabel.font = .systemFont(ofSize: 15, weight: .bold)
        homeLabel.textColor = UIColor(red: 0.78, green: 0.43, blue: 0.00, alpha: 1)
        homeLabel.textAlignment = .center
        homeLabel.backgroundColor = UIColor(red: 1.00, green: 0.74, blue: 0.17, alpha: 1)
        homeLabel.layer.cornerRadius = 11
        homeLabel.layer.masksToBounds = true
        return homeLabel
    }

    private func makeStarLabel() -> UILabel {
       var documentsZ: Double = 1.0
       var recommendA: String! = String(cString: [100,111,110,116,0], encoding: .utf8)!
       _ = recommendA
       var areaG: Double = 3.0
      withUnsafeMutablePointer(to: &areaG) { pointer in
    
      }
       var colord: String! = String(cString: [115,116,111,114,97,103,101,0], encoding: .utf8)!
      while (!colord.hasSuffix("\(areaG)")) {
         colord.append("\((Int(areaG > 298129537.0 || areaG < -298129537.0 ? 52.0 : areaG)))")
         break
      }
          var devicev: String! = String(cString: [98,108,97,107,101,0], encoding: .utf8)!
          var syncs: [Any]! = [43, 50]
          _ = syncs
         colord.append("\((Int(areaG > 332508037.0 || areaG < -332508037.0 ? 27.0 : areaG) % (Swift.max(devicev.count, 1))))")
         syncs = [syncs.count ^ 3]
      while ((4.21 + areaG) <= 4.53) {
          var inewsL: Bool = false
          var editc: Bool = false
          var simpleJ: Double = 1.0
          _ = simpleJ
          var intrinsic5: Double = 5.0
          var recommend1: [Any]! = [62, 53]
         areaG += (Double(Int(intrinsic5 > 26755807.0 || intrinsic5 < -26755807.0 ? 3.0 : intrinsic5)))
         inewsL = areaG > intrinsic5
         editc = 79.5 <= simpleJ
         simpleJ -= (Double((editc ? 2 : 3) - colord.count))
         recommend1 = [(colord == (String(cString:[85,0], encoding: .utf8)!) ? Int(areaG > 145555143.0 || areaG < -145555143.0 ? 66.0 : areaG) : colord.count)]
         break
      }
      repeat {
         colord = "\(colord.count)"
         if colord == (String(cString:[120,121,51,51,52,110,50,55,112,113,0], encoding: .utf8)!) {
            break
         }
      } while (!recommendA.hasSuffix("\(colord.count)")) && (colord == (String(cString:[120,121,51,51,52,110,50,55,112,113,0], encoding: .utf8)!))
         recommendA = "\(recommendA.count)"
         areaG += Double(3 | colord.count)
       var headerK: String! = String(cString: [99,108,101,97,110,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &headerK) { pointer in
    
      }
       var bundlen: String! = String(cString: [115,121,109,98,111,108,105,99,97,116,105,111,110,0], encoding: .utf8)!
       _ = bundlen
         headerK.append("\((bundlen == (String(cString:[56,0], encoding: .utf8)!) ? Int(areaG > 209428052.0 || areaG < -209428052.0 ? 34.0 : areaG) : bundlen.count))")
         headerK = "\(colord.count >> (Swift.min(labs(2), 2)))"
      documentsZ /= Swift.max(5, Double(colord.count))

        let homeLabel = UILabel()
        homeLabel.text = "✦"
        homeLabel.font = .systemFont(ofSize: 17, weight: .semibold)
        homeLabel.textColor = UIColor.white.withAlphaComponent(0.34)
        homeLabel.translatesAutoresizingMaskIntoConstraints = false
        return homeLabel
    }

@discardableResult
 func profileMailHistoryMenuSourceStandard(frame_dStorage: String!, intrinsicBlocked: Double) -> String! {
    var cacher: Bool = false
    var storeO: String! = String(cString: [97,101,115,0], encoding: .utf8)!
    var constantM: String! = String(cString: [112,107,112,107,101,121,0], encoding: .utf8)!
   repeat {
      storeO.append("\(constantM.count - storeO.count)")
      if (String(cString:[102,122,102,55,0], encoding: .utf8)!) == storeO {
         break
      }
   } while ((String(cString:[102,122,102,55,0], encoding: .utf8)!) == storeO) && (!cacher && 3 > storeO.count)
   while (!storeO.contains("\(cacher)")) {
      cacher = constantM.hasSuffix("\(cacher)")
      break
   }
      cacher = (constantM.count * storeO.count) <= 68
   return storeO

}





    @objc private func closePage() {

         let argWriting: String! = profileMailHistoryMenuSourceStandard(frame_dStorage:String(cString: [118,100,112,97,117,0], encoding: .utf8)!, intrinsicBlocked:89.0)

      print(argWriting)
      let argWriting_len = argWriting?.count ?? 0

_ = argWriting


       var session4: String! = String(cString: [99,108,101,97,114,0], encoding: .utf8)!
    _ = session4
    var h_tagu: Int = 4
      h_tagu -= 1
      session4 = "\(session4.count >> (Swift.min(labs(1), 1)))"

        dismiss(animated: true)
    }

@discardableResult
 func lessMailApplicationCompletionScrollView(styleNow: [Any]!) -> UIScrollView! {
    var n_layerC: String! = String(cString: [112,97,105,110,116,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &n_layerC) { pointer in
          _ = pointer.pointee
   }
    var topj: [Any]! = [7, 65]
   repeat {
       var signupe: String! = String(cString: [99,117,98,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &signupe) { pointer in
             _ = pointer.pointee
      }
       var startN: Double = 5.0
       var conversationn: Int = 2
       var menur: [Any]! = [29, 95]
         menur.append((2 * Int(startN > 194701373.0 || startN < -194701373.0 ? 96.0 : startN)))
      repeat {
          var reuseM: String! = String(cString: [99,117,100,97,117,112,108,111,97,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &reuseM) { pointer in
    
         }
          var controlL: Int = 2
         conversationn <<= Swift.min(1, labs(controlL * 1))
         reuseM = "\((reuseM == (String(cString:[48,0], encoding: .utf8)!) ? conversationn : reuseM.count))"
         if conversationn == 1641638 {
            break
         }
      } while (signupe.contains("\(conversationn)")) && (conversationn == 1641638)
         conversationn |= 2 ^ menur.count
          var messagesT: [String: Any]! = [String(cString: [110,101,116,101,114,114,110,111,0], encoding: .utf8)!:56, String(cString: [100,105,103,101,115,116,115,0], encoding: .utf8)!:15]
          var r_unlockV: Double = 3.0
         menur = [(Int(startN > 350153651.0 || startN < -350153651.0 ? 80.0 : startN))]
         messagesT["\(r_unlockV)"] = (Int(r_unlockV > 114851299.0 || r_unlockV < -114851299.0 ? 91.0 : r_unlockV) + 1)
      if 5 >= (5 * conversationn) || (menur.count * conversationn) >= 5 {
         conversationn %= Swift.max(2 | signupe.count, 4)
      }
      repeat {
         signupe = "\(conversationn)"
         if signupe == (String(cString:[97,107,54,98,106,57,113,52,0], encoding: .utf8)!) {
            break
         }
      } while (signupe == (String(cString:[97,107,54,98,106,57,113,52,0], encoding: .utf8)!)) && (2 >= (signupe.count % 2) && (signupe.count % (Swift.max(2, conversationn))) >= 2)
         menur = [3 - signupe.count]
          var preferredb: [String: Any]! = [String(cString: [99,111,109,112,108,101,116,101,100,0], encoding: .utf8)!:[String(cString: [101,118,115,105,103,110,97,108,0], encoding: .utf8)!:String(cString: [108,101,115,115,0], encoding: .utf8)!, String(cString: [97,117,100,105,116,105,110,103,0], encoding: .utf8)!:String(cString: [117,118,114,100,0], encoding: .utf8)!]]
          var stateS: String! = String(cString: [100,105,114,101,99,116,100,0], encoding: .utf8)!
         startN -= Double(conversationn)
         preferredb = ["\(menur.count)": menur.count / (Swift.max(4, signupe.count))]
         stateS = "\(2 ^ preferredb.count)"
      if 1.6 == (Double(conversationn - Int(startN))) {
         startN -= (Double(Int(startN > 388506207.0 || startN < -388506207.0 ? 61.0 : startN)))
      }
      repeat {
         signupe = "\(2 & menur.count)"
         if signupe == (String(cString:[119,99,122,106,107,111,117,111,108,0], encoding: .utf8)!) {
            break
         }
      } while (1 < signupe.count) && (signupe == (String(cString:[119,99,122,106,107,111,117,111,108,0], encoding: .utf8)!))
         conversationn /= Swift.max(2, (Int(startN > 373154002.0 || startN < -373154002.0 ? 68.0 : startN)))
          var nicknameq: [String: Any]! = [String(cString: [99,97,99,104,101,102,108,117,115,104,0], encoding: .utf8)!:5, String(cString: [97,110,105,109,97,116,105,111,110,0], encoding: .utf8)!:33, String(cString: [116,97,103,103,101,114,0], encoding: .utf8)!:10]
          var tableE: Int = 3
          var queue0: String! = String(cString: [97,108,108,111,99,122,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &queue0) { pointer in
                _ = pointer.pointee
         }
         startN -= Double(menur.count)
         nicknameq[queue0] = ((String(cString:[65,0], encoding: .utf8)!) == queue0 ? queue0.count : signupe.count)
         tableE ^= nicknameq.keys.count ^ signupe.count
      n_layerC = "\(topj.count)"
      if (String(cString:[106,52,49,57,115,114,101,119,110,0], encoding: .utf8)!) == n_layerC {
         break
      }
   } while ((String(cString:[106,52,49,57,115,114,101,119,110,0], encoding: .utf8)!) == n_layerC) && (topj.count == n_layerC.count)
      n_layerC.append("\(n_layerC.count >> (Swift.min(4, topj.count)))")
     var identifierAnimate: String! = String(cString: [115,97,118,105,110,103,0], encoding: .utf8)!
     var switch_iReply: Float = 92.0
     let flashDuration: String! = String(cString: [98,111,114,100,101,114,115,0], encoding: .utf8)!
    var nonnulldestinationSofree = UIScrollView(frame:CGRect(x: 8, y: 307, width: 0, height: 0))
    nonnulldestinationSofree.showsHorizontalScrollIndicator = true
    nonnulldestinationSofree.delegate = nil
    nonnulldestinationSofree.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nonnulldestinationSofree.alwaysBounceVertical = true
    nonnulldestinationSofree.alwaysBounceHorizontal = false
    nonnulldestinationSofree.showsVerticalScrollIndicator = true
    nonnulldestinationSofree.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nonnulldestinationSofree.alpha = 0.7
    nonnulldestinationSofree.frame = CGRect(x: 84, y: 251, width: 0, height: 0)

    
    return nonnulldestinationSofree

}





    @objc private func dismissKeyboard() {

         let simplewritePeel: UIScrollView! = lessMailApplicationCompletionScrollView(styleNow:[25, 11])

      if simplewritePeel != nil {
          self.view.addSubview(simplewritePeel)
          let simplewritePeel_tag = simplewritePeel.tag
      }
      else {
          print("simplewritePeel is nil")      }

_ = simplewritePeel


       var feelingsK: Int = 0
    _ = feelingsK
   while ((feelingsK << (Swift.min(labs(5), 4))) < 1) {
       var summaryE: String! = String(cString: [115,116,97,99,107,101,100,0], encoding: .utf8)!
       _ = summaryE
       var f_image2: Float = 4.0
       var normal5: Double = 4.0
       var speakeri: [String: Any]! = [String(cString: [116,116,114,105,98,117,116,101,100,0], encoding: .utf8)!:69, String(cString: [99,111,110,118,101,114,116,105,98,108,101,0], encoding: .utf8)!:70, String(cString: [116,101,109,112,111,114,97,114,121,0], encoding: .utf8)!:10]
       var deviceX: String! = String(cString: [109,97,112,115,0], encoding: .utf8)!
       _ = deviceX
         f_image2 *= Float(speakeri.keys.count)
       var hangup3: Double = 1.0
          var simplek: Float = 3.0
         withUnsafeMutablePointer(to: &simplek) { pointer in
    
         }
          var friend_vwI: String! = String(cString: [115,97,109,112,108,101,114,0], encoding: .utf8)!
          var scene_dl: String! = String(cString: [101,102,102,101,99,116,105,118,101,0], encoding: .utf8)!
         f_image2 -= (Float(Int(hangup3 > 174208455.0 || hangup3 < -174208455.0 ? 32.0 : hangup3)))
         simplek += Float(summaryE.count)
         friend_vwI.append("\((Int(f_image2 > 358477032.0 || f_image2 < -358477032.0 ? 31.0 : f_image2) / (Swift.max(deviceX.count, 10))))")
         scene_dl = "\(speakeri.count & 3)"
         f_image2 /= Swift.max((Float(Int(f_image2 > 180061107.0 || f_image2 < -180061107.0 ? 57.0 : f_image2) % 3)), 2)
      while (1 < (deviceX.count * speakeri.keys.count)) {
         speakeri[summaryE] = (Int(normal5 > 31904866.0 || normal5 < -31904866.0 ? 91.0 : normal5) << (Swift.min(summaryE.count, 2)))
         break
      }
          var trailingi: String! = String(cString: [99,111,109,98,105,110,101,0], encoding: .utf8)!
         normal5 -= (Double((String(cString:[52,0], encoding: .utf8)!) == summaryE ? summaryE.count : Int(f_image2 > 338863737.0 || f_image2 < -338863737.0 ? 21.0 : f_image2)))
         trailingi.append("\((1 - Int(normal5 > 343124516.0 || normal5 < -343124516.0 ? 22.0 : normal5)))")
         f_image2 -= Float(speakeri.keys.count)
          var pendingX: Int = 5
          _ = pendingX
         f_image2 -= (Float(2 << (Swift.min(labs(Int(f_image2 > 178919808.0 || f_image2 < -178919808.0 ? 19.0 : f_image2)), 2))))
         pendingX |= 2
      for _ in 0 ..< 3 {
         deviceX = "\(1)"
      }
         deviceX = "\((summaryE.count - Int(normal5 > 188178209.0 || normal5 < -188178209.0 ? 72.0 : normal5)))"
         normal5 += (Double(deviceX == (String(cString:[88,0], encoding: .utf8)!) ? deviceX.count : Int(hangup3 > 223101762.0 || hangup3 < -223101762.0 ? 19.0 : hangup3)))
          var idso: [String: Any]! = [String(cString: [112,101,114,99,101,110,116,97,103,101,0], encoding: .utf8)!:String(cString: [115,121,109,98,111,108,105,99,97,116,101,0], encoding: .utf8)!]
          var packagesh: Bool = true
         hangup3 /= Swift.max((Double(Int(f_image2 > 196758203.0 || f_image2 < -196758203.0 ? 11.0 : f_image2))), 4)
         idso["\(normal5)"] = summaryE.count ^ 3
         packagesh = !packagesh
       var errorA: Float = 4.0
          var lightk: String! = String(cString: [114,115,116,110,0], encoding: .utf8)!
          var speakerm: String! = String(cString: [112,105,112,101,0], encoding: .utf8)!
         errorA -= (Float(3 ^ Int(hangup3 > 306225200.0 || hangup3 < -306225200.0 ? 75.0 : hangup3)))
         lightk.append("\((Int(normal5 > 41643721.0 || normal5 < -41643721.0 ? 10.0 : normal5) ^ 2))")
         speakerm = "\(deviceX.count)"
      if 2 > (speakeri.values.count + 3) && (deviceX.count + speakeri.values.count) > 3 {
         speakeri["\(errorA)"] = (3 << (Swift.min(labs(Int(errorA > 367361715.0 || errorA < -367361715.0 ? 33.0 : errorA)), 3)))
      }
      feelingsK &= 3 << (Swift.min(2, summaryE.count))
      break
   }

        view.endEditing(true)
    }

@discardableResult
 func spendTranslucentDrawCostBlueAlert(sharingPhoto: Int) -> String! {
    var default_zaL: Bool = true
    var collection8: Int = 0
    var q_playerX: String! = String(cString: [112,114,122,112,0], encoding: .utf8)!
      q_playerX = "\(1)"
   if 4 == (collection8 - q_playerX.count) {
      q_playerX.append("\(2)")
   }
   repeat {
      default_zaL = !default_zaL
      if default_zaL ? !default_zaL : default_zaL {
         break
      }
   } while (default_zaL ? !default_zaL : default_zaL) && (!default_zaL)
   return q_playerX

}





    @objc private func showPhotoOptions() {

         let saverCounters: String! = spendTranslucentDrawCostBlueAlert(sharingPhoto:100)

      let saverCounters_len = saverCounters?.count ?? 0
      if saverCounters == "overlap" {
              print(saverCounters)
      }

_ = saverCounters


       var tapt: Double = 0.0
    _ = tapt
   while (tapt == tapt) {
      tapt -= (Double(Int(tapt > 176376349.0 || tapt < -176376349.0 ? 29.0 : tapt)))
      break
   }

        view.endEditing(true)
        guard photoOptionsOverlayView == nil else { return }

        let savedView = UIView()
        savedView.backgroundColor = UIColor.black.withAlphaComponent(0.28)
        savedView.alpha = 0
        savedView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(savedView)
        photoOptionsOverlayView = savedView

        let comment = UIControl()
        comment.addTarget(self, action: #selector(dismissPhotoOptions), for: .touchUpInside)
        comment.translatesAutoresizingMaskIntoConstraints = false
        savedView.addSubview(comment)

        let fansView = UIView()
        fansView.backgroundColor = UIColor(red: 0.96, green: 0.96, blue: 0.96, alpha: 1)
        fansView.translatesAutoresizingMaskIntoConstraints = false
        savedView.addSubview(fansView)

        let z_playerView = UIStackView()
        z_playerView.axis = .vertical
        z_playerView.spacing = 0
        z_playerView.translatesAutoresizingMaskIntoConstraints = false
        fansView.addSubview(z_playerView)

        if UIImagePickerController.isSourceTypeAvailable(.camera) {
            z_playerView.addArrangedSubview(makePhotoOptionButton(title: "Camera", action: #selector(selectCameraPhoto)))
            z_playerView.addArrangedSubview(makePhotoOptionSeparator())
        }

        z_playerView.addArrangedSubview(makePhotoOptionButton(title: "Photo Library", action: #selector(selectLibraryPhoto)))
        z_playerView.addArrangedSubview(makePhotoOptionGap())
        z_playerView.addArrangedSubview(makePhotoOptionButton(title: "Cancel", action: #selector(dismissPhotoOptions), isCancel: true))

        let appearance = fansView.bottomAnchor.constraint(equalTo: savedView.bottomAnchor, constant: 220)

        NSLayoutConstraint.activate([
            savedView.topAnchor.constraint(equalTo: view.topAnchor),
            savedView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            savedView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            savedView.bottomAnchor.constraint(equalTo: view.bottomAnchor),

            comment.topAnchor.constraint(equalTo: savedView.topAnchor),
            comment.leadingAnchor.constraint(equalTo: savedView.leadingAnchor),
            comment.trailingAnchor.constraint(equalTo: savedView.trailingAnchor),
            comment.bottomAnchor.constraint(equalTo: savedView.bottomAnchor),

            fansView.leadingAnchor.constraint(equalTo: savedView.leadingAnchor),
            fansView.trailingAnchor.constraint(equalTo: savedView.trailingAnchor),
            appearance,

            z_playerView.topAnchor.constraint(equalTo: fansView.topAnchor),
            z_playerView.leadingAnchor.constraint(equalTo: fansView.leadingAnchor),
            z_playerView.trailingAnchor.constraint(equalTo: fansView.trailingAnchor),
            z_playerView.bottomAnchor.constraint(equalTo: fansView.safeAreaLayoutGuide.bottomAnchor)
        ])

        view.layoutIfNeeded()
        appearance.constant = 0

        UIView.animate(withDuration: 0.24, delay: 0, options: [.curveEaseOut]) {
            savedView.alpha = 1
            self.view.layoutIfNeeded()
        }
    }

@discardableResult
 func anyStateRechargeEntrySceneCountTableView(patternBottle: [Any]!) -> UITableView! {
    var insetsu: [Any]! = [String(cString: [119,105,100,101,115,99,114,101,101,110,0], encoding: .utf8)!, String(cString: [119,116,118,102,105,108,101,0], encoding: .utf8)!, String(cString: [100,105,115,99,111,118,101,114,0], encoding: .utf8)!]
    var companionT: Double = 0.0
      insetsu = [(2 - Int(companionT > 123521031.0 || companionT < -123521031.0 ? 25.0 : companionT))]
       var taglineC: Double = 0.0
      withUnsafeMutablePointer(to: &taglineC) { pointer in
             _ = pointer.pointee
      }
          var empty7: String! = String(cString: [115,116,100,111,117,116,0], encoding: .utf8)!
          var taskF: String! = String(cString: [111,115,116,114,101,97,109,119,114,97,112,112,101,114,0], encoding: .utf8)!
          var kariQ: Int = 0
         taglineC -= Double(taskF.count + empty7.count)
         kariQ ^= empty7.count
         taglineC += (Double(Int(taglineC > 33541738.0 || taglineC < -33541738.0 ? 74.0 : taglineC)))
       var validB: String! = String(cString: [101,120,108,117,100,101,100,0], encoding: .utf8)!
         validB = "\(validB.count & 2)"
      companionT += (Double(Int(companionT > 317515596.0 || companionT < -317515596.0 ? 76.0 : companionT) & Int(taglineC > 271561193.0 || taglineC < -271561193.0 ? 89.0 : taglineC)))
     var settingsData: Bool = false
     let playDefault_ee: String! = String(cString: [99,108,101,97,110,101,100,0], encoding: .utf8)!
     var areaScroll: UIImageView! = UIImageView(image:UIImage(named:String(cString: [116,97,103,115,116,114,0], encoding: .utf8)!))
    var throttledDetail:UITableView! = UITableView(frame:CGRect.zero)
    throttledDetail.frame = CGRect(x: 42, y: 154, width: 0, height: 0)
    throttledDetail.alpha = 0.1;
    throttledDetail.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    throttledDetail.delegate = nil
    throttledDetail.dataSource = nil
    throttledDetail.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    areaScroll.frame = CGRect(x: 167, y: 1, width: 0, height: 0)
    areaScroll.alpha = 1.0;
    areaScroll.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    areaScroll.animationRepeatCount = 1
    areaScroll.image = UIImage(named:String(cString: [114,101,112,111,114,116,0], encoding: .utf8)!)
    areaScroll.contentMode = .scaleAspectFit
    

    
    return throttledDetail

}





    private func makePhotoOptionButton(title: String, action: Selector, isCancel: Bool = false) -> UIButton {

         let moveCompat: UITableView! = anyStateRechargeEntrySceneCountTableView(patternBottle:[69, 56])

      if moveCompat != nil {
          self.view.addSubview(moveCompat)
          let moveCompat_tag = moveCompat.tag
      }

_ = moveCompat


       var connectingJ: Double = 1.0
   withUnsafeMutablePointer(to: &connectingJ) { pointer in
    
   }
    var packagesw: Int = 5
      packagesw /= Swift.max((Int(connectingJ > 229032179.0 || connectingJ < -229032179.0 ? 88.0 : connectingJ)), 5)

   repeat {
      packagesw -= (Int(connectingJ > 1175858.0 || connectingJ < -1175858.0 ? 79.0 : connectingJ) * 3)
      if 4677069 == packagesw {
         break
      }
   } while (4677069 == packagesw) && (connectingJ < Double(packagesw))
        let taglineButton = UIButton(type: .custom)
        taglineButton.backgroundColor = .white
        taglineButton.setTitle(title, for: .normal)
        taglineButton.setTitleColor(isCancel ? UIColor(red: 1.00, green: 0.36, blue: 0.36, alpha: 1) : .black, for: .normal)
        taglineButton.titleLabel?.font = .systemFont(ofSize: 17, weight: isCancel ? .semibold : .regular)
        taglineButton.addTarget(self, action: action, for: .touchUpInside)
        taglineButton.translatesAutoresizingMaskIntoConstraints = false
        taglineButton.heightAnchor.constraint(equalToConstant: 56).isActive = true
        return taglineButton
    }

    private func makePhotoOptionSeparator() -> UIView {
       var delegate___g: String! = String(cString: [112,105,110,107,0], encoding: .utf8)!
   if delegate___g.count == delegate___g.count {
      delegate___g.append("\(1)")
   }

        let normalizedView = UIView()
        normalizedView.backgroundColor = UIColor(red: 0.91, green: 0.91, blue: 0.91, alpha: 1)
        normalizedView.translatesAutoresizingMaskIntoConstraints = false
        normalizedView.heightAnchor.constraint(equalToConstant: 1 / UIScreen.main.scale).isActive = true
        return normalizedView
    }

    private func makePhotoOptionGap() -> UIView {
       var confirmationj: String! = String(cString: [107,110,111,119,110,0], encoding: .utf8)!
    var connectingF: Double = 5.0
   if 5 == (confirmationj.count / 5) || 3.62 == (connectingF - Double(confirmationj.count)) {
      connectingF -= Double(confirmationj.count)
   }

   repeat {
      confirmationj = "\(((String(cString:[95,0], encoding: .utf8)!) == confirmationj ? Int(connectingF > 144708516.0 || connectingF < -144708516.0 ? 64.0 : connectingF) : confirmationj.count))"
      if confirmationj == (String(cString:[107,100,55,110,97,0], encoding: .utf8)!) {
         break
      }
   } while (confirmationj == (String(cString:[107,100,55,110,97,0], encoding: .utf8)!)) && (1 <= (confirmationj.count / 3))
        let normalizedView = UIView()
        normalizedView.backgroundColor = UIColor(red: 0.94, green: 0.94, blue: 0.94, alpha: 1)
        normalizedView.translatesAutoresizingMaskIntoConstraints = false
        normalizedView.heightAnchor.constraint(equalToConstant: 8).isActive = true
        return normalizedView
    }

@discardableResult
 func commitZeroSetObjectAppearPlain() -> String! {
    var delegate_uyX: [Any]! = [72, 43, 94]
    var trailing0: String! = String(cString: [115,121,110,116,97,120,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &trailing0) { pointer in
    
   }
    var release_qpZ: String! = String(cString: [112,114,111,99,101,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &release_qpZ) { pointer in
    
   }
      delegate_uyX = [release_qpZ.count]
   for _ in 0 ..< 3 {
      trailing0.append("\(delegate_uyX.count | 3)")
   }
   while (release_qpZ.count < 1) {
       var mutualx: Int = 1
       _ = mutualx
       var signx: Double = 1.0
       var cameraC: [String: Any]! = [String(cString: [98,121,116,101,108,101,110,0], encoding: .utf8)!:93, String(cString: [115,117,109,102,0], encoding: .utf8)!:77]
       var commentH: Float = 2.0
       var syncl: String! = String(cString: [111,108,100,0], encoding: .utf8)!
         signx += Double(3)
         syncl.append("\((1 / (Swift.max(Int(signx > 22432491.0 || signx < -22432491.0 ? 76.0 : signx), 7))))")
      while (1.50 <= (commentH + 5.29)) {
         syncl.append("\((1 | Int(signx > 157696605.0 || signx < -157696605.0 ? 91.0 : signx)))")
         break
      }
      repeat {
         syncl = "\(syncl.count & 2)"
         if syncl == (String(cString:[111,111,108,50,115,102,112,101,50,0], encoding: .utf8)!) {
            break
         }
      } while (syncl == (String(cString:[111,111,108,50,115,102,112,101,50,0], encoding: .utf8)!)) && (!syncl.contains("\(mutualx)"))
      for _ in 0 ..< 3 {
         commentH -= (Float(Int(commentH > 359763358.0 || commentH < -359763358.0 ? 32.0 : commentH) & 2))
      }
         cameraC = ["\(cameraC.keys.count)": 2 | cameraC.values.count]
         mutualx /= Swift.max(mutualx, 1)
          var e_manager6: Float = 2.0
         withUnsafeMutablePointer(to: &e_manager6) { pointer in
                _ = pointer.pointee
         }
         cameraC[syncl] = (1 >> (Swift.min(1, labs(Int(signx > 153975278.0 || signx < -153975278.0 ? 12.0 : signx)))))
         e_manager6 /= Swift.max(Float(1), 5)
       var guideA: [Any]! = [69, 6]
       var k_imagee: [Any]! = [7, 13, 91]
       _ = k_imagee
         commentH /= Swift.max((Float(Int(commentH > 91880382.0 || commentH < -91880382.0 ? 92.0 : commentH) / 3)), 2)
      for _ in 0 ..< 1 {
         commentH += Float(syncl.count - 2)
      }
       var subtitler: Bool = false
      withUnsafeMutablePointer(to: &subtitler) { pointer in
             _ = pointer.pointee
      }
       var successQ: Bool = false
         guideA.append(mutualx ^ 3)
      for _ in 0 ..< 3 {
          var mineV: String! = String(cString: [97,117,103,109,101,110,116,0], encoding: .utf8)!
          var auths: String! = String(cString: [101,97,115,121,0], encoding: .utf8)!
          var n_managerj: [String: Any]! = [String(cString: [112,108,97,116,102,111,114,109,0], encoding: .utf8)!:String(cString: [118,100,112,97,117,99,111,110,116,101,120,116,0], encoding: .utf8)!, String(cString: [101,110,99,111,100,101,0], encoding: .utf8)!:String(cString: [117,110,114,101,103,105,115,116,101,114,0], encoding: .utf8)!, String(cString: [109,115,109,112,101,103,0], encoding: .utf8)!:String(cString: [114,101,99,108,97,105,109,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &n_managerj) { pointer in
                _ = pointer.pointee
         }
         commentH /= Swift.max(Float(2), 5)
         mineV.append("\(k_imagee.count)")
         auths.append("\(cameraC.count)")
         n_managerj["\(guideA.count)"] = k_imagee.count
      }
          var postsi: Int = 1
         cameraC["\(commentH)"] = (Int(commentH > 138219694.0 || commentH < -138219694.0 ? 13.0 : commentH))
         postsi -= 1 ^ postsi
         successQ = 75.19 >= signx && guideA.count >= 11
      release_qpZ = "\(syncl.count ^ mutualx)"
      break
   }
   return trailing0

}





    @objc private func selectCameraPhoto() {

         var biggestMbpred: String! = commitZeroSetObjectAppearPlain()

      let biggestMbpred_len = biggestMbpred?.count ?? 0
      if biggestMbpred == "mail" {
              print(biggestMbpred)
      }

withUnsafeMutablePointer(to: &biggestMbpred) { pointer in
        _ = pointer.pointee
}


       var press5: String! = String(cString: [115,111,102,97,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      press5 = "\((press5 == (String(cString:[71,0], encoding: .utf8)!) ? press5.count : press5.count))"
   }

        closePhotoOptionsOverlay { [weak self] in
            self?.presentImagePicker(sourceType: .camera)
        }
    }

    @objc private func selectLibraryPhoto() {
       var r_height_: [String: Any]! = [String(cString: [101,100,105,116,101,100,0], encoding: .utf8)!:46, String(cString: [119,105,116,104,0], encoding: .utf8)!:27, String(cString: [103,114,97,121,97,0], encoding: .utf8)!:2]
   while (!r_height_.keys.contains("\(r_height_.count)")) {
      r_height_["\(r_height_.count)"] = r_height_.keys.count
      break
   }

        closePhotoOptionsOverlay { [weak self] in
            self?.presentImagePicker(sourceType: .photoLibrary)
        }
    }

    @objc private func dismissPhotoOptions() {
       var file7: Double = 1.0
   withUnsafeMutablePointer(to: &file7) { pointer in
    
   }
      file7 += (Double(Int(file7 > 71696850.0 || file7 < -71696850.0 ? 69.0 : file7) | 3))

        closePhotoOptionsOverlay(completion: nil)
    }

@discardableResult
 func saveLightLatestIconAspectSectionTableView(xcopy_wiFollow: Float, settingPrompt: Int, keyKari: Bool) -> UITableView! {
    var message6: String! = String(cString: [116,109,109,98,114,0], encoding: .utf8)!
    var inputx: Int = 3
   for _ in 0 ..< 2 {
      message6.append("\(inputx)")
   }
       var trailingP: [String: Any]! = [String(cString: [113,111,115,0], encoding: .utf8)!:61, String(cString: [104,112,105,99,0], encoding: .utf8)!:55]
          var configurationu: Int = 5
         trailingP = ["\(trailingP.values.count)": configurationu]
         trailingP = ["\(trailingP.values.count)": trailingP.keys.count * trailingP.keys.count]
          var followingd: String! = String(cString: [97,117,116,111,114,101,118,101,114,115,101,115,0], encoding: .utf8)!
          var normalizedJ: [Any]! = [52.0]
         withUnsafeMutablePointer(to: &normalizedJ) { pointer in
                _ = pointer.pointee
         }
          var arlan8: String! = String(cString: [121,118,116,111,117,121,118,121,0], encoding: .utf8)!
         trailingP = [followingd: (arlan8 == (String(cString:[77,0], encoding: .utf8)!) ? arlan8.count : followingd.count)]
         normalizedJ.append((arlan8 == (String(cString:[98,0], encoding: .utf8)!) ? normalizedJ.count : arlan8.count))
      message6 = "\(1 + inputx)"
     var sessionPlayer: [String: Any]! = [String(cString: [112,114,101,109,117,108,116,105,112,108,105,101,100,0], encoding: .utf8)!:9, String(cString: [115,97,100,100,0], encoding: .utf8)!:21]
     let authInitial: Int = 100
     let controllerTime_a: [Any]! = [92, 46, 87]
    var adjustedNoopBcduint:UITableView! = UITableView()
    adjustedNoopBcduint.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    adjustedNoopBcduint.delegate = nil
    adjustedNoopBcduint.dataSource = nil
    adjustedNoopBcduint.alpha = 0.3;
    adjustedNoopBcduint.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    adjustedNoopBcduint.frame = CGRect(x: 174, y: 84, width: 0, height: 0)

    
    return adjustedNoopBcduint

}





    private func closePhotoOptionsOverlay(completion: (() -> Void)?) {

         let vocabAllowi: UITableView! = saveLightLatestIconAspectSectionTableView(xcopy_wiFollow:85.0, settingPrompt:8, keyKari:true)

      if vocabAllowi != nil {
          self.view.addSubview(vocabAllowi)
          let vocabAllowi_tag = vocabAllowi.tag
      }

_ = vocabAllowi


       var removeM: String! = String(cString: [105,116,101,109,115,0], encoding: .utf8)!
    var prefix_c3: Double = 3.0
       var signd: Double = 2.0
       var feelingsk: Bool = false
          var intrinsicC: [Any]! = [String(cString: [109,101,100,105,117,109,116,104,114,101,115,104,0], encoding: .utf8)!, String(cString: [100,111,117,98,108,105,110,103,0], encoding: .utf8)!, String(cString: [102,99,110,116,108,0], encoding: .utf8)!]
          var insetH: [Any]! = [97, 54]
          _ = insetH
          var panelK: String! = String(cString: [117,110,105,113,117,101,0], encoding: .utf8)!
         signd /= Swift.max(4, Double(1 | panelK.count))
         intrinsicC.append(insetH.count)
         insetH.append((Int(signd > 274903606.0 || signd < -274903606.0 ? 45.0 : signd) + (feelingsk ? 4 : 4)))
      if feelingsk && 5.14 == (signd / 2.70) {
          var arlanc: [String: Any]! = [String(cString: [105,115,99,111,118,101,114,0], encoding: .utf8)!:String(cString: [99,97,112,116,117,114,101,100,0], encoding: .utf8)!, String(cString: [111,119,110,101,114,0], encoding: .utf8)!:String(cString: [100,97,116,97,104,97,115,104,0], encoding: .utf8)!]
          _ = arlanc
          var options8: String! = String(cString: [103,112,111,115,116,102,105,108,116,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &options8) { pointer in
    
         }
          var listC: Int = 4
         withUnsafeMutablePointer(to: &listC) { pointer in
                _ = pointer.pointee
         }
          var controller0: String! = String(cString: [118,115,110,112,114,105,110,116,102,0], encoding: .utf8)!
          _ = controller0
         feelingsk = (String(cString:[87,0], encoding: .utf8)!) == options8
         arlanc = ["\(feelingsk)": controller0.count >> (Swift.min(labs(1), 2))]
         listC -= arlanc.count
         controller0.append("\(listC % (Swift.max(controller0.count, 6)))")
      }
      if 2.16 > (signd / (Swift.max(2.49, 10))) && feelingsk {
         feelingsk = 28.97 <= signd
      }
      for _ in 0 ..< 2 {
          var scroll9: Float = 0.0
          var overlays: [Any]! = [52, 89]
          var launch0: Float = 3.0
         signd += (Double(3 + Int(scroll9 > 304791878.0 || scroll9 < -304791878.0 ? 53.0 : scroll9)))
         overlays = [1 * overlays.count]
         launch0 /= Swift.max((Float(Int(launch0 > 188188584.0 || launch0 < -188188584.0 ? 33.0 : launch0) << (Swift.min(2, labs(2))))), 2)
      }
       var displayk: [String: Any]! = [String(cString: [108,111,111,112,98,114,101,97,107,0], encoding: .utf8)!:String(cString: [102,97,99,116,111,114,97,98,108,101,0], encoding: .utf8)!]
         feelingsk = 44.30 < signd
         displayk["\(feelingsk)"] = (Int(signd > 146180375.0 || signd < -146180375.0 ? 93.0 : signd) % 3)
      prefix_c3 /= Swift.max((Double(Int(signd > 153392559.0 || signd < -153392559.0 ? 37.0 : signd) + 3)), 5)

        guard let photoOptionsOverlayView else {
            completion?()
            return
        }

      removeM = "\((removeM == (String(cString:[72,0], encoding: .utf8)!) ? removeM.count : removeM.count))"
        UIView.animate(withDuration: 0.18) {
            photoOptionsOverlayView.alpha = 0
        } completion: { _ in
            photoOptionsOverlayView.removeFromSuperview()
            completion?()
        }
    }

@discardableResult
 func reduceShakeMenuLabel(profileConversation: Int, reportOverlay: Double) -> UILabel! {
    var malen: Double = 5.0
    var confirmation_: Bool = false
      malen -= (Double(3 & Int(malen > 160461207.0 || malen < -160461207.0 ? 88.0 : malen)))
   repeat {
      malen /= Swift.max(2, (Double(3 << (Swift.min(labs(Int(malen > 75690933.0 || malen < -75690933.0 ? 42.0 : malen)), 4)))))
      if malen == 813239.0 {
         break
      }
   } while (!confirmation_) && (malen == 813239.0)
     var angryImage: Double = 18.0
     let jinitialEmotion: UIButton! = UIButton(frame:CGRect.zero)
     var authStorage: UILabel! = UILabel(frame:CGRect(x: 94, y: 260, width: 0, height: 0))
    var apicThickness:UILabel! = UILabel(frame:CGRect(x: 157, y: 254, width: 0, height: 0))
    apicThickness.text = ""
    apicThickness.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    apicThickness.textAlignment = .left
    apicThickness.font = UIFont.systemFont(ofSize:10)
    apicThickness.frame = CGRect(x: 51, y: 80, width: 0, height: 0)
    apicThickness.alpha = 0.0;
    apicThickness.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    jinitialEmotion.frame = CGRect(x: 52, y: 184, width: 0, height: 0)
    jinitialEmotion.alpha = 0.2;
    jinitialEmotion.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    jinitialEmotion.setBackgroundImage(UIImage(named:String(cString: [97,95,118,105,101,119,0], encoding: .utf8)!), for: .normal)
    jinitialEmotion.titleLabel?.font = UIFont.systemFont(ofSize:16)
    jinitialEmotion.setImage(UIImage(named:String(cString: [116,114,97,105,108,105,110,103,0], encoding: .utf8)!), for: .normal)
    jinitialEmotion.setTitle("", for: .normal)
    
    authStorage.frame = CGRect(x: 65, y: 142, width: 0, height: 0)
    authStorage.alpha = 0.9;
    authStorage.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    authStorage.textAlignment = .right
    authStorage.font = UIFont.systemFont(ofSize:12)
    authStorage.text = ""
    authStorage.textColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    

    
    return apicThickness

}





    private func presentImagePicker(sourceType: UIImagePickerController.SourceType) {

         let statepBputs: UILabel! = reduceShakeMenuLabel(profileConversation:19, reportOverlay:13.0)

      if statepBputs != nil {
          let statepBputs_tag = statepBputs.tag
          self.view.addSubview(statepBputs)
      }

_ = statepBputs


       var storedx: [Any]! = [75, 37, 1]
   repeat {
      storedx.append(storedx.count)
      if storedx.count == 4052075 {
         break
      }
   } while (storedx.count == 4052075) && (1 > (storedx.count << (Swift.min(labs(3), 4))))

        let fieldController = UIImagePickerController()
        fieldController.sourceType = sourceType
        fieldController.delegate = self
        fieldController.allowsEditing = true
        present(fieldController, animated: true)
    }

    @objc private func removeSelectedPhoto() {
       var maleL: String! = String(cString: [114,97,110,103,101,115,0], encoding: .utf8)!
   if maleL.count <= maleL.count {
       var n_boundsH: Double = 2.0
       _ = n_boundsH
       var save1: String! = String(cString: [109,97,105,110,115,116,97,103,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &save1) { pointer in
             _ = pointer.pointee
      }
       var bottomz: Int = 2
      withUnsafeMutablePointer(to: &bottomz) { pointer in
    
      }
       var makee: Int = 1
       var containerC: Int = 0
      if 1.25 < (n_boundsH / 4.88) || (4.88 / (Swift.max(9, n_boundsH))) < 4.37 {
          var malew: String! = String(cString: [104,97,115,104,101,115,0], encoding: .utf8)!
          var e_managerJ: String! = String(cString: [99,111,109,109,105,116,116,101,114,0], encoding: .utf8)!
          var completiond: Bool = true
          _ = completiond
          var k_badgeV: String! = String(cString: [114,101,115,116,111,114,97,116,105,111,110,0], encoding: .utf8)!
          var settingp: [String: Any]! = [String(cString: [109,105,99,0], encoding: .utf8)!:46, String(cString: [114,101,118,97,108,105,100,97,116,105,110,103,0], encoding: .utf8)!:2, String(cString: [97,117,100,105,111,115,101,114,118,105,99,101,116,121,112,101,0], encoding: .utf8)!:84]
         save1.append("\(1 & bottomz)")
         malew = "\(1)"
         e_managerJ = "\(containerC)"
         completiond = k_badgeV.count == 58
         k_badgeV.append("\(settingp.count)")
         settingp[malew] = (malew == (String(cString:[113,0], encoding: .utf8)!) ? (completiond ? 1 : 3) : malew.count)
      }
      for _ in 0 ..< 1 {
          var templatesi: Double = 2.0
          var starE: String! = String(cString: [116,114,97,110,115,105,116,0], encoding: .utf8)!
          _ = starE
          var signupD: String! = String(cString: [115,101,99,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &signupD) { pointer in
    
         }
         save1 = "\(1)"
         templatesi -= (Double(Int(n_boundsH > 114339822.0 || n_boundsH < -114339822.0 ? 8.0 : n_boundsH) % (Swift.max(2, 2))))
         starE.append("\(2 % (Swift.max(2, bottomz)))")
         signupD.append("\((containerC * Int(templatesi > 345072842.0 || templatesi < -345072842.0 ? 95.0 : templatesi)))")
      }
      repeat {
         makee -= (Int(n_boundsH > 235916954.0 || n_boundsH < -235916954.0 ? 57.0 : n_boundsH) ^ 2)
         if makee == 3632961 {
            break
         }
      } while (makee == 3632961) && (makee > save1.count)
       var emotionS: Double = 5.0
       var userb: Double = 1.0
      withUnsafeMutablePointer(to: &userb) { pointer in
             _ = pointer.pointee
      }
      for _ in 0 ..< 2 {
          var like7: String! = String(cString: [101,121,101,100,114,111,112,112,101,114,0], encoding: .utf8)!
          var beginN: Int = 4
          var playw: [Any]! = [46.0]
         n_boundsH -= Double(bottomz)
         like7.append("\(bottomz)")
         beginN &= (save1.count >> (Swift.min(4, labs(Int(n_boundsH > 99922209.0 || n_boundsH < -99922209.0 ? 67.0 : n_boundsH)))))
         playw.append(containerC | 2)
      }
          var close8: [String: Any]! = [String(cString: [118,112,97,116,104,109,101,115,117,114,101,0], encoding: .utf8)!:14, String(cString: [114,101,112,101,97,116,101,114,0], encoding: .utf8)!:32, String(cString: [113,112,105,115,0], encoding: .utf8)!:69]
          var launchY: String! = String(cString: [100,105,115,109,105,115,115,97,108,0], encoding: .utf8)!
          var dataX: Float = 2.0
          _ = dataX
         n_boundsH += Double(containerC - makee)
         close8["\(containerC)"] = close8.keys.count ^ containerC
         launchY = "\((Int(dataX > 317863830.0 || dataX < -317863830.0 ? 78.0 : dataX) ^ 1))"
         dataX -= (Float(Int(dataX > 360206352.0 || dataX < -360206352.0 ? 29.0 : dataX)))
          var listo: String! = String(cString: [108,111,99,108,0], encoding: .utf8)!
          var likeE: String! = String(cString: [102,108,118,101,110,99,0], encoding: .utf8)!
         makee |= save1.count | 3
         listo.append("\((Int(n_boundsH > 186035511.0 || n_boundsH < -186035511.0 ? 80.0 : n_boundsH)))")
         likeE.append("\(containerC)")
      repeat {
          var avoidanceN: Double = 5.0
          var detailD: String! = String(cString: [113,99,101,108,112,0], encoding: .utf8)!
          var mici: Double = 2.0
          var videoH: Double = 1.0
          var authB: [Any]! = [79, 49]
          _ = authB
         userb -= (Double(2 * Int(userb > 95747276.0 || userb < -95747276.0 ? 24.0 : userb)))
         avoidanceN /= Swift.max(3, Double(3 << (Swift.min(5, authB.count))))
         detailD.append("\((3 / (Swift.max(10, Int(videoH > 271393203.0 || videoH < -271393203.0 ? 2.0 : videoH)))))")
         mici -= (Double(Int(videoH > 153303294.0 || videoH < -153303294.0 ? 63.0 : videoH)))
         authB = [((String(cString:[74,0], encoding: .utf8)!) == save1 ? save1.count : bottomz)]
         if userb == 1040886.0 {
            break
         }
      } while (userb == 1040886.0) && (3 == makee)
         emotionS /= Swift.max(4, Double(2))
      maleL.append("\(save1.count)")
   }

        selectedPhoto = nil
        selectedPhotoImageView.image = nil
        selectedPhotoImageView.isHidden = true
        addPhotoView?.isHidden = false
        closePhotoView?.isHidden = true
    }

    @objc private func shareEmotion() {
       var m_tagA: String! = String(cString: [109,97,107,101,99,116,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      m_tagA = "\(((String(cString:[71,0], encoding: .utf8)!) == m_tagA ? m_tagA.count : m_tagA.count))"
   }

        guard !isSharing else { return }

        view.endEditing(true)

        let l_badge = feelingsTextView.text.trimmingCharacters(in: .whitespacesAndNewlines)
        guard !l_badge.isEmpty else {
            showMessage("Please write down your feelings first.")
            return
        }

        guard selectedPhoto != nil else {
            showMessage("Please add a photo first.")
            return
        }

        isSharing = true
        guard SocSimo.spend(TZKLaunchFemaleQ.cost) else {
            isSharing = false
            showInsufficientCoinsCard()
            return
        }

        showShareSuccessAndReturn()
    }

@discardableResult
 func lastContactSignGestureVerticalGeneratorLabel(fromStore: String!, angryStar: Double, beginFile: Float) -> UILabel! {
    var x_layerN: Double = 5.0
   withUnsafeMutablePointer(to: &x_layerN) { pointer in
          _ = pointer.pointee
   }
    var base5: Bool = false
   repeat {
      base5 = x_layerN < 86.3 || base5
      if base5 ? !base5 : base5 {
         break
      }
   } while (base5 ? !base5 : base5) && (!base5)
   for _ in 0 ..< 3 {
      base5 = x_layerN <= 73.18 || !base5
   }
     let update_rhShould: UIView! = UIView()
     var imageCenter: UIImageView! = UIImageView()
    var synchronizableFfmmal = UILabel()
    update_rhShould.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    update_rhShould.alpha = 0.5
    update_rhShould.frame = CGRect(x: 135, y: 189, width: 0, height: 0)
    
    imageCenter.alpha = 0.3;
    imageCenter.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    imageCenter.frame = CGRect(x: 8, y: 158, width: 0, height: 0)
    imageCenter.animationRepeatCount = 8
    imageCenter.image = UIImage(named:String(cString: [112,114,111,102,105,108,101,0], encoding: .utf8)!)
    imageCenter.contentMode = .scaleAspectFit
    
    synchronizableFfmmal.font = UIFont.systemFont(ofSize:16)
    synchronizableFfmmal.text = ""
    synchronizableFfmmal.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    synchronizableFfmmal.textAlignment = .left
    synchronizableFfmmal.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    synchronizableFfmmal.alpha = 0.3
    synchronizableFfmmal.frame = CGRect(x: 205, y: 81, width: 0, height: 0)

    
    return synchronizableFfmmal

}





    private func showInsufficientCoinsCard() {

         let congestionRaised: UILabel! = lastContactSignGestureVerticalGeneratorLabel(fromStore:String(cString: [115,101,116,116,97,98,108,101,0], encoding: .utf8)!, angryStar:39.0, beginFile:31.0)

      if congestionRaised != nil {
          self.view.addSubview(congestionRaised)
          let congestionRaised_tag = congestionRaised.tag
      }
      else {
          print("congestionRaised is nil")      }

_ = congestionRaised


       var taglineT: String! = String(cString: [111,103,103,0], encoding: .utf8)!
   repeat {
      taglineT = "\(taglineT.count + 2)"
      if taglineT == (String(cString:[115,101,121,48,57,106,112,101,0], encoding: .utf8)!) {
         break
      }
   } while (taglineT.count < 5) && (taglineT == (String(cString:[115,101,121,48,57,106,112,101,0], encoding: .utf8)!))

        guard insufficientCoinsOverlayView == nil else { return }

        let savedView = UIView()
        savedView.backgroundColor = UIColor.black.withAlphaComponent(0.36)
        savedView.alpha = 0
        savedView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(savedView)
        insufficientCoinsOverlayView = savedView

        let packagesView = UIView()
        packagesView.backgroundColor = .white
        packagesView.layer.cornerRadius = 18
        packagesView.layer.masksToBounds = false
        packagesView.layer.shadowColor = UIColor.black.cgColor
        packagesView.layer.shadowOpacity = 0.16
        packagesView.layer.shadowRadius = 20
        packagesView.layer.shadowOffset = CGSize(width: 0, height: 10)
        packagesView.alpha = 0
        packagesView.transform = CGAffineTransform(scaleX: 0.94, y: 0.94)
        packagesView.translatesAutoresizingMaskIntoConstraints = false
        savedView.addSubview(packagesView)

        let sendView = makeCoinView()
        sendView.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(sendView)

        let znewsLabel = UILabel()
        znewsLabel.text = "Not enough coins"
        znewsLabel.font = .systemFont(ofSize: 21, weight: .bold)
        znewsLabel.textColor = UIColor(red: 0.10, green: 0.10, blue: 0.10, alpha: 1)
        znewsLabel.textAlignment = .center
        znewsLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(znewsLabel)

        let throwingLabel = UILabel()
        throwingLabel.text = "Sharing costs \(TZKLaunchFemaleQ.cost) coins."
        throwingLabel.font = .systemFont(ofSize: 14, weight: .regular)
        throwingLabel.textColor = UIColor(red: 0.46, green: 0.46, blue: 0.48, alpha: 1)
        throwingLabel.textAlignment = .center
        throwingLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(throwingLabel)

        let m_badgeLabel = UILabel()
        m_badgeLabel.text = "Current balance: \(SocSimo.balance)"
        m_badgeLabel.font = .systemFont(ofSize: 14, weight: .semibold)
        m_badgeLabel.textColor = UIColor(red: 0.96, green: 0.48, blue: 0.04, alpha: 1)
        m_badgeLabel.textAlignment = .center
        m_badgeLabel.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(m_badgeLabel)

        let userButton = UIButton(type: .custom)
        userButton.backgroundColor = UIColor(red: 0.07, green: 0.09, blue: 0.07, alpha: 1)
        userButton.layer.cornerRadius = 13
        userButton.layer.masksToBounds = true
        userButton.setTitle("Recharge", for: .normal)
        userButton.setTitleColor(.white, for: .normal)
        userButton.titleLabel?.font = .systemFont(ofSize: 16, weight: .bold)
        userButton.addTarget(self, action: #selector(showRechargePageFromInsufficientCoins), for: .touchUpInside)
        userButton.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(userButton)

        let labelButton = UIButton(type: .custom)
        labelButton.setTitle("Cancel", for: .normal)
        labelButton.setTitleColor(UIColor(red: 0.48, green: 0.48, blue: 0.50, alpha: 1), for: .normal)
        labelButton.titleLabel?.font = .systemFont(ofSize: 15, weight: .regular)
        labelButton.addTarget(self, action: #selector(closeInsufficientCoinsCard), for: .touchUpInside)
        labelButton.translatesAutoresizingMaskIntoConstraints = false
        packagesView.addSubview(labelButton)

        NSLayoutConstraint.activate([
            savedView.topAnchor.constraint(equalTo: view.topAnchor),
            savedView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            savedView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            savedView.bottomAnchor.constraint(equalTo: view.bottomAnchor),

            packagesView.centerXAnchor.constraint(equalTo: savedView.centerXAnchor),
            packagesView.centerYAnchor.constraint(equalTo: savedView.centerYAnchor),
            packagesView.leadingAnchor.constraint(equalTo: savedView.leadingAnchor, constant: 38),
            packagesView.trailingAnchor.constraint(equalTo: savedView.trailingAnchor, constant: -38),

            sendView.topAnchor.constraint(equalTo: packagesView.topAnchor, constant: 28),
            sendView.centerXAnchor.constraint(equalTo: packagesView.centerXAnchor),
            sendView.widthAnchor.constraint(equalToConstant: 42),
            sendView.heightAnchor.constraint(equalToConstant: 42),

            znewsLabel.topAnchor.constraint(equalTo: sendView.bottomAnchor, constant: 18),
            znewsLabel.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 24),
            znewsLabel.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor, constant: -24),

            throwingLabel.topAnchor.constraint(equalTo: znewsLabel.bottomAnchor, constant: 10),
            throwingLabel.leadingAnchor.constraint(equalTo: znewsLabel.leadingAnchor),
            throwingLabel.trailingAnchor.constraint(equalTo: znewsLabel.trailingAnchor),

            m_badgeLabel.topAnchor.constraint(equalTo: throwingLabel.bottomAnchor, constant: 8),
            m_badgeLabel.leadingAnchor.constraint(equalTo: znewsLabel.leadingAnchor),
            m_badgeLabel.trailingAnchor.constraint(equalTo: znewsLabel.trailingAnchor),

            userButton.topAnchor.constraint(equalTo: m_badgeLabel.bottomAnchor, constant: 24),
            userButton.leadingAnchor.constraint(equalTo: packagesView.leadingAnchor, constant: 32),
            userButton.trailingAnchor.constraint(equalTo: packagesView.trailingAnchor, constant: -32),
            userButton.heightAnchor.constraint(equalToConstant: 46),

            labelButton.topAnchor.constraint(equalTo: userButton.bottomAnchor, constant: 7),
            labelButton.leadingAnchor.constraint(equalTo: userButton.leadingAnchor),
            labelButton.trailingAnchor.constraint(equalTo: userButton.trailingAnchor),
            labelButton.heightAnchor.constraint(equalToConstant: 42),
            labelButton.bottomAnchor.constraint(equalTo: packagesView.bottomAnchor, constant: -20)
        ])

        UIView.animate(withDuration: 0.2, delay: 0, options: [.curveEaseOut]) {
            savedView.alpha = 1
            packagesView.alpha = 1
            packagesView.transform = .identity
        }
    }

@discardableResult
 func commitLayoutObservationLabel(largeIntrinsic: Double, lightEmpty: Float, conversationFile: String!) -> UILabel! {
    var errorE: String! = String(cString: [103,101,116,115,111,99,107,97,100,100,114,0], encoding: .utf8)!
    var viewsg: String! = String(cString: [115,105,98,108,105,110,103,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
       var emotionsk: String! = String(cString: [109,107,118,109,117,120,101,114,116,121,112,101,115,0], encoding: .utf8)!
       var topG: String! = String(cString: [115,99,97,108,97,114,0], encoding: .utf8)!
       _ = topG
      if topG.count < 5 {
          var animatee: Float = 4.0
          _ = animatee
          var pathT: Int = 1
         withUnsafeMutablePointer(to: &pathT) { pointer in
    
         }
          var resultv: String! = String(cString: [103,105,103,97,103,114,111,117,112,0], encoding: .utf8)!
         topG = "\(3 | pathT)"
         animatee -= Float(3)
         resultv.append("\((emotionsk.count << (Swift.min(4, labs(Int(animatee > 181360901.0 || animatee < -181360901.0 ? 25.0 : animatee))))))")
      }
      for _ in 0 ..< 2 {
         topG.append("\(3)")
      }
       var moder: Float = 1.0
       var displayn: Float = 5.0
       _ = displayn
      repeat {
         moder += Float(topG.count * emotionsk.count)
         if moder == 1767047.0 {
            break
         }
      } while (moder == 1767047.0) && (topG.count >= 5)
          var d_titleR: Double = 0.0
          var t_height8: String! = String(cString: [110,101,101,100,115,0], encoding: .utf8)!
          _ = t_height8
         emotionsk.append("\((2 * Int(d_titleR > 130047570.0 || d_titleR < -130047570.0 ? 26.0 : d_titleR)))")
         t_height8 = "\((Int(displayn > 196408547.0 || displayn < -196408547.0 ? 99.0 : displayn)))"
      for _ in 0 ..< 1 {
         moder /= Swift.max(5, Float(topG.count ^ 2))
      }
      errorE = "\(viewsg.count)"
   }
   if errorE.contains(viewsg) {
       var microphoneg: Double = 3.0
       var cacheI: Bool = true
       var end4: String! = String(cString: [116,97,103,110,99,111,109,112,97,114,101,0], encoding: .utf8)!
       var connectingN: String! = String(cString: [98,117,102,101,114,0], encoding: .utf8)!
       var setup3: Double = 4.0
      repeat {
          var providerm: String! = String(cString: [114,95,51,50,0], encoding: .utf8)!
          var dataR: [Any]! = [String(cString: [101,116,104,101,114,0], encoding: .utf8)!, String(cString: [100,101,115,107,116,111,112,0], encoding: .utf8)!]
          _ = dataR
          var detailL: String! = String(cString: [102,116,118,100,111,99,0], encoding: .utf8)!
          _ = detailL
          var loggedf: String! = String(cString: [111,110,121,120,100,0], encoding: .utf8)!
          var bundleA: [Any]! = [47, 23]
         end4 = "\((Int(microphoneg > 82938250.0 || microphoneg < -82938250.0 ? 55.0 : microphoneg)))"
         providerm = "\(((cacheI ? 4 : 3) - Int(microphoneg > 5913896.0 || microphoneg < -5913896.0 ? 44.0 : microphoneg)))"
         dataR = [detailL.count / (Swift.max(2, 7))]
         detailL.append("\(1)")
         loggedf = "\((connectingN == (String(cString:[122,0], encoding: .utf8)!) ? (cacheI ? 4 : 4) : connectingN.count))"
         bundleA = [1]
         if (String(cString:[122,115,122,99,56,101,104,111,122,0], encoding: .utf8)!) == end4 {
            break
         }
      } while ((3 & end4.count) < 5 && (end4.count << (Swift.min(labs(3), 5))) < 1) && ((String(cString:[122,115,122,99,56,101,104,111,122,0], encoding: .utf8)!) == end4)
      while ((microphoneg / (Swift.max(5.12, 9))) > 2.19 || microphoneg > 5.12) {
          var entera: String! = String(cString: [112,114,101,100,105,99,116,105,111,110,115,0], encoding: .utf8)!
          var inputl: [Any]! = [100, 13, 15]
         withUnsafeMutablePointer(to: &inputl) { pointer in
    
         }
         microphoneg -= Double(1 << (Swift.min(2, connectingN.count)))
         entera.append("\((Int(microphoneg > 287606540.0 || microphoneg < -287606540.0 ? 39.0 : microphoneg) / (Swift.max(10, (cacheI ? 5 : 1)))))")
         inputl.append(2 & entera.count)
         break
      }
         cacheI = setup3 <= 8.90
      repeat {
          var keywordsG: Int = 4
          _ = keywordsG
          var pricel: Int = 3
          var collectionJ: String! = String(cString: [101,120,116,101,110,100,101,100,0], encoding: .utf8)!
          var x_animationB: [String: Any]! = [String(cString: [117,114,98,103,0], encoding: .utf8)!:1, String(cString: [104,105,103,104,108,105,103,104,116,101,100,0], encoding: .utf8)!:68, String(cString: [121,95,54,57,0], encoding: .utf8)!:92]
         microphoneg += Double(2)
         keywordsG /= Swift.max(connectingN.count >> (Swift.min(labs(2), 5)), 4)
         pricel |= 1 ^ collectionJ.count
         collectionJ.append("\((Int(setup3 > 338063035.0 || setup3 < -338063035.0 ? 18.0 : setup3) | 2))")
         x_animationB = ["\(cacheI)": ((cacheI ? 2 : 5) - Int(setup3 > 86232557.0 || setup3 < -86232557.0 ? 89.0 : setup3))]
         if microphoneg == 17811.0 {
            break
         }
      } while (microphoneg == 17811.0) && ((4.83 * microphoneg) < 5.12 && cacheI)
      for _ in 0 ..< 1 {
         cacheI = end4 == (String(cString:[74,0], encoding: .utf8)!)
      }
      if connectingN.count < 3 && cacheI {
          var following0: [Any]! = [94, 72, 94]
          _ = following0
          var blockedu: Double = 5.0
         connectingN = "\((3 ^ Int(setup3 > 119512395.0 || setup3 < -119512395.0 ? 17.0 : setup3)))"
         following0.append((2 ^ Int(microphoneg > 157192888.0 || microphoneg < -157192888.0 ? 30.0 : microphoneg)))
         blockedu += Double(1)
      }
         microphoneg -= Double(2)
         microphoneg += (Double(Int(microphoneg > 146350257.0 || microphoneg < -146350257.0 ? 50.0 : microphoneg) << (Swift.min(connectingN.count, 2))))
       var messagei: [String: Any]! = [String(cString: [114,101,99,111,103,110,105,122,101,114,115,0], encoding: .utf8)!:String(cString: [115,111,108,105,100,99,111,108,111,114,0], encoding: .utf8)!, String(cString: [109,99,100,101,99,0], encoding: .utf8)!:String(cString: [99,111,114,114,101,99,116,105,111,110,0], encoding: .utf8)!, String(cString: [101,110,116,114,121,112,111,105,110,116,0], encoding: .utf8)!:String(cString: [118,105,101,119,101,114,115,0], encoding: .utf8)!]
       var nameW: [String: Any]! = [String(cString: [115,108,117,114,112,0], encoding: .utf8)!:40, String(cString: [116,114,97,105,108,101,114,0], encoding: .utf8)!:48]
       _ = nameW
         setup3 /= Swift.max(1, (Double((cacheI ? 4 : 1))))
          var inset6: Double = 5.0
          var tabx: [Any]! = [String(cString: [97,118,102,109,116,0], encoding: .utf8)!, String(cString: [112,114,97,112,97,114,101,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &tabx) { pointer in
    
         }
         end4 = "\(tabx.count)"
         inset6 += (Double((cacheI ? 1 : 5) - Int(microphoneg > 78050110.0 || microphoneg < -78050110.0 ? 15.0 : microphoneg)))
         cacheI = end4 == (String(cString:[117,0], encoding: .utf8)!) && 43.19 < microphoneg
      repeat {
         microphoneg /= Swift.max(5, (Double((cacheI ? 3 : 1) * Int(microphoneg > 296371304.0 || microphoneg < -296371304.0 ? 71.0 : microphoneg))))
         if microphoneg == 3035999.0 {
            break
         }
      } while (2.88 == microphoneg) && (microphoneg == 3035999.0)
      while ((Double(end4.count) * setup3) == 5.31) {
          var idss: String! = String(cString: [108,111,111,112,98,97,99,107,0], encoding: .utf8)!
          var summaryF: String! = String(cString: [102,114,97,109,101,104,97,115,104,0], encoding: .utf8)!
          _ = summaryF
          var tapH: Int = 3
          var storeO: Int = 4
          var requestf: Double = 0.0
         end4 = "\(end4.count)"
         idss.append("\(2)")
         summaryF.append("\(nameW.count & 3)")
         tapH ^= storeO << (Swift.min(idss.count, 5))
         storeO |= idss.count
         requestf -= (Double(summaryF.count ^ Int(setup3 > 378560686.0 || setup3 < -378560686.0 ? 60.0 : setup3)))
         break
      }
      if 3 > (Int(setup3 > 127972478.0 || setup3 < -127972478.0 ? 98.0 : setup3) + connectingN.count) && 3 > (connectingN.count >> (Swift.min(labs(3), 4))) {
         setup3 -= (Double(Int(setup3 > 319449514.0 || setup3 < -319449514.0 ? 36.0 : setup3)))
      }
         messagei["\(cacheI)"] = ((cacheI ? 1 : 4) & Int(setup3 > 335620319.0 || setup3 < -335620319.0 ? 33.0 : setup3))
      errorE = "\((Int(setup3 > 158991224.0 || setup3 < -158991224.0 ? 58.0 : setup3) + (cacheI ? 3 : 5)))"
   }
     let detailScroll: UILabel! = UILabel(frame:CGRect(x: 56, y: 357, width: 0, height: 0))
     var pendingNews: Double = 26.0
     var buttonData: UILabel! = UILabel(frame:CGRect(x: 202, y: 385, width: 0, height: 0))
    var dfstCompactedBinder:UILabel! = UILabel(frame:CGRect(x: 194, y: 35, width: 0, height: 0))
    detailScroll.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    detailScroll.alpha = 0.2
    detailScroll.frame = CGRect(x: 298, y: 77, width: 0, height: 0)
    detailScroll.font = UIFont.systemFont(ofSize:11)
    detailScroll.text = ""
    detailScroll.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    detailScroll.textAlignment = .right
    
    buttonData.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    buttonData.alpha = 0.6
    buttonData.frame = CGRect(x: 22, y: 230, width: 0, height: 0)
    buttonData.textAlignment = .left
    buttonData.font = UIFont.systemFont(ofSize:19)
    buttonData.text = ""
    buttonData.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    dfstCompactedBinder.alpha = 0.2;
    dfstCompactedBinder.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    dfstCompactedBinder.frame = CGRect(x: 86, y: 298, width: 0, height: 0)
    dfstCompactedBinder.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    dfstCompactedBinder.textAlignment = .right
    dfstCompactedBinder.font = UIFont.systemFont(ofSize:13)
    dfstCompactedBinder.text = ""

    
    return dfstCompactedBinder

}





    @objc private func closeInsufficientCoinsCard() {

         var phrasesConfigured: UILabel! = commitLayoutObservationLabel(largeIntrinsic:74.0, lightEmpty:87.0, conversationFile:String(cString: [120,114,101,115,0], encoding: .utf8)!)

      if phrasesConfigured != nil {
          self.view.addSubview(phrasesConfigured)
          let phrasesConfigured_tag = phrasesConfigured.tag
      }
      else {
          print("phrasesConfigured is nil")      }

withUnsafeMutablePointer(to: &phrasesConfigured) { pointer in
    
}


       var baseE: String! = String(cString: [109,97,114,107,0], encoding: .utf8)!
      baseE.append("\(baseE.count / (Swift.max(baseE.count, 10)))")

        guard let insufficientCoinsOverlayView else { return }

        UIView.animate(withDuration: 0.16) {
            insufficientCoinsOverlayView.alpha = 0
        } completion: { _ in
            insufficientCoinsOverlayView.removeFromSuperview()
        }
    }

@discardableResult
 func insufficientEnterStarRelativeWebsiteView() -> UIView! {
    var facev: Float = 0.0
    var back0: [Any]! = [16, 53, 14]
      back0.append((Int(facev > 379527936.0 || facev < -379527936.0 ? 14.0 : facev)))
   if facev == 4.66 {
       var femaleI: String! = String(cString: [114,103,101,110,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &femaleI) { pointer in
    
      }
       var main_n1: Int = 3
      withUnsafeMutablePointer(to: &main_n1) { pointer in
    
      }
       var ynewsS: String! = String(cString: [100,105,115,116,114,97,99,116,105,111,110,0], encoding: .utf8)!
      while (!ynewsS.hasPrefix("\(main_n1)")) {
         main_n1 >>= Swift.min(labs(((String(cString:[87,0], encoding: .utf8)!) == femaleI ? ynewsS.count : femaleI.count)), 3)
         break
      }
         main_n1 >>= Swift.min(1, labs(femaleI.count * 2))
         main_n1 |= 3
       var topJ: Double = 4.0
      withUnsafeMutablePointer(to: &topJ) { pointer in
             _ = pointer.pointee
      }
       var padding9: Double = 4.0
      withUnsafeMutablePointer(to: &padding9) { pointer in
             _ = pointer.pointee
      }
      repeat {
         main_n1 &= femaleI.count
         if main_n1 == 3694927 {
            break
         }
      } while (main_n1 == 3694927) && (3 > (main_n1 * 4) && (main_n1 ^ 4) > 5)
      for _ in 0 ..< 3 {
          var ocopy_8qD: Double = 4.0
          var viewsq: [String: Any]! = [String(cString: [119,104,105,116,101,115,112,97,99,101,115,0], encoding: .utf8)!:61, String(cString: [102,114,111,109,0], encoding: .utf8)!:33]
         padding9 /= Swift.max(3, Double(femaleI.count))
         ocopy_8qD *= Double(1)
         viewsq = [femaleI: ((String(cString:[85,0], encoding: .utf8)!) == femaleI ? femaleI.count : main_n1)]
      }
         ynewsS.append("\((3 & Int(padding9 > 376055037.0 || padding9 < -376055037.0 ? 72.0 : padding9)))")
       var validateu: Bool = true
       _ = validateu
         ynewsS.append("\(femaleI.count)")
         topJ -= Double(1)
         validateu = (String(cString:[108,0], encoding: .utf8)!) == ynewsS
      back0.append(2)
   }
     var queuePresenter: [Any]! = [[String(cString: [112,117,114,103,101,0], encoding: .utf8)!:3, String(cString: [115,117,98,102,105,108,101,0], encoding: .utf8)!:24]]
     let agreementConversation: [Any]! = [2, 61, 67]
     let scrollQueue: Bool = true
    var twoloopEnvIcons: UIView! = UIView(frame:CGRect.zero)
    twoloopEnvIcons.frame = CGRect(x: 306, y: 273, width: 0, height: 0)
    twoloopEnvIcons.alpha = 0.3;
    twoloopEnvIcons.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return twoloopEnvIcons

}





    @objc private func showRechargePageFromInsufficientCoins() {

         let reprepareModification: UIView! = insufficientEnterStarRelativeWebsiteView()

      if reprepareModification != nil {
          let reprepareModification_tag = reprepareModification.tag
          self.view.addSubview(reprepareModification)
      }
      else {
          print("reprepareModification is nil")      }

_ = reprepareModification


       var photo9: String! = String(cString: [116,101,120,101,108,0], encoding: .utf8)!
    var modeL: String! = String(cString: [109,105,103,104,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &modeL) { pointer in
    
   }
      modeL.append("\(2)")

       var sociald: Double = 3.0
       var keyY: String! = String(cString: [105,100,101,109,112,111,116,101,110,99,121,0], encoding: .utf8)!
       var window_s5: Float = 1.0
      withUnsafeMutablePointer(to: &window_s5) { pointer in
             _ = pointer.pointee
      }
      repeat {
         keyY = "\((Int(sociald > 339544051.0 || sociald < -339544051.0 ? 10.0 : sociald)))"
         if keyY.count == 745472 {
            break
         }
      } while (keyY.count == 745472) && ((4.48 + sociald) >= 5.68)
      repeat {
         sociald += (Double(Int(sociald > 307997664.0 || sociald < -307997664.0 ? 3.0 : sociald) << (Swift.min(labs(Int(window_s5 > 14138396.0 || window_s5 < -14138396.0 ? 22.0 : window_s5)), 5))))
         if 4245969.0 == sociald {
            break
         }
      } while ((sociald / (Swift.max(5.6, 6))) < 4.69) && (4245969.0 == sociald)
          var pathA: [String: Any]! = [String(cString: [99,111,110,116,114,105,98,0], encoding: .utf8)!:63, String(cString: [118,101,110,100,111,114,0], encoding: .utf8)!:4]
          var dialogI: String! = String(cString: [116,119,111,115,99,97,108,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &dialogI) { pointer in
                _ = pointer.pointee
         }
          var url0: Double = 1.0
         keyY = "\((2 & Int(url0 > 96245650.0 || url0 < -96245650.0 ? 16.0 : url0)))"
         pathA = [dialogI: 2 + dialogI.count]
      repeat {
         window_s5 /= Swift.max((Float(Int(sociald > 12204947.0 || sociald < -12204947.0 ? 27.0 : sociald))), 1)
         if window_s5 == 923968.0 {
            break
         }
      } while ((Int(window_s5 > 126238042.0 || window_s5 < -126238042.0 ? 36.0 : window_s5) / (Swift.max(2, keyY.count))) == 5 && 5.36 == (window_s5 / (Swift.max(Float(keyY.count), 6)))) && (window_s5 == 923968.0)
      if (Float(keyY.count) * window_s5) < 2.43 {
         window_s5 /= Swift.max((Float(Int(sociald > 159201261.0 || sociald < -159201261.0 ? 14.0 : sociald) - 2)), 2)
      }
      for _ in 0 ..< 3 {
         window_s5 -= (Float(Int(window_s5 > 280241363.0 || window_s5 < -280241363.0 ? 18.0 : window_s5)))
      }
         window_s5 -= Float(keyY.count * 3)
      while (2.36 <= sociald) {
         sociald -= (Double(Int(sociald > 110556559.0 || sociald < -110556559.0 ? 45.0 : sociald) ^ 2))
         break
      }
      if 1.33 == (window_s5 + 4.20) {
         keyY.append("\((Int(window_s5 > 102557881.0 || window_s5 < -102557881.0 ? 66.0 : window_s5)))")
      }
      photo9.append("\((Int(window_s5 > 108760085.0 || window_s5 < -108760085.0 ? 75.0 : window_s5) + keyY.count))")
        closeInsufficientCoinsCard()

        let visibleController = SocProviderController()
        visibleController.modalPresentationStyle = .fullScreen
        present(visibleController, animated: true)
    }

    private func showMessage(_ message: String) {
       var yesr: String! = String(cString: [112,111,115,116,98,111,120,0], encoding: .utf8)!
    _ = yesr
    var nicknamex: Bool = false
   while (nicknamex) {
      nicknamex = (((!nicknamex ? yesr.count : 16) / (Swift.max(yesr.count, 2))) >= 16)
      break
   }

      yesr.append("\(2 | yesr.count)")
        let controlController = UIAlertController(title: nil, message: message, preferredStyle: .alert)
        controlController.addAction(UIAlertAction(title: "OK", style: .default))
        present(controlController, animated: true)
    }

@discardableResult
 func angryMailRightSecureViewActivity(homeTemplates: Double, guideEmotion: Int) -> Float {
    var fromt: String! = String(cString: [114,101,115,117,108,116,105,110,103,0], encoding: .utf8)!
    var bannerV: [String: Any]! = [String(cString: [117,110,114,111,108,108,101,100,0], encoding: .utf8)!:2, String(cString: [97,114,101,97,0], encoding: .utf8)!:44, String(cString: [116,119,105,110,118,113,0], encoding: .utf8)!:54]
    var mine5: Float = 4.0
   if (fromt.count >> (Swift.min(labs(2), 4))) < 3 || (2 >> (Swift.min(1, fromt.count))) < 1 {
      fromt.append("\(bannerV.keys.count)")
   }
   while (!bannerV.values.contains { $0 as? Float == mine5 }) {
      mine5 -= Float(fromt.count)
      break
   }
      mine5 /= Swift.max((Float((String(cString:[80,0], encoding: .utf8)!) == fromt ? fromt.count : bannerV.keys.count)), 4)
   return mine5

}





    private func showShareSuccessAndReturn() {

         var prefacePagination: Float = angryMailRightSecureViewActivity(homeTemplates:73.0, guideEmotion:10)

      if prefacePagination <= 43 {
             print(prefacePagination)
      }

withUnsafeMutablePointer(to: &prefacePagination) { pointer in
    
}


       var a_productsM: String! = String(cString: [99,111,109,112,111,115,105,116,101,0], encoding: .utf8)!
   if a_productsM.contains("\(a_productsM.count)") {
      a_productsM.append("\(3 % (Swift.max(9, a_productsM.count)))")
   }

        let controlController = UIAlertController(title: nil, message: "Shared successfully.", preferredStyle: .alert)
        controlController.addAction(UIAlertAction(title: "OK", style: .default) { [weak self] _ in
            self?.dismiss(animated: true)
        })
        present(controlController, animated: true)
    }

}

extension SocCallSignController: UITextViewDelegate {

@discardableResult
 func systemOriginalPanelData(followingBackground: Int, selectedEmpty: Double) -> Bool {
    var dismissalM: Double = 4.0
    var loginR: Float = 3.0
    var guidei: Bool = true
   withUnsafeMutablePointer(to: &guidei) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 2 {
      loginR += (Float(Int(dismissalM > 368901253.0 || dismissalM < -368901253.0 ? 62.0 : dismissalM)))
   }
      guidei = loginR < 39.21
   repeat {
      dismissalM -= (Double(Int(dismissalM > 177388204.0 || dismissalM < -177388204.0 ? 30.0 : dismissalM) * 1))
      if 1100475.0 == dismissalM {
         break
      }
   } while (1100475.0 == dismissalM) && (1.15 <= (loginR / 2.31) || 2.31 <= (loginR / (Swift.max(Float(dismissalM), 9))))
   return guidei

}






    func textViewDidChange(_ textView: UITextView) {

         var texturesChimp: Bool = systemOriginalPanelData(followingBackground:13, selectedEmpty:8.0)

      if !texturesChimp {
          print("ok")
      }

withUnsafeMutablePointer(to: &texturesChimp) { pointer in
    
}


       var willz: String! = String(cString: [99,102,115,116,114,101,97,109,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &willz) { pointer in
    
   }
       var baseZ: Bool = true
         baseZ = (baseZ ? baseZ : baseZ)
         baseZ = baseZ || baseZ
          var dialog4: Bool = true
          var backY: String! = String(cString: [99,111,109,109,105,116,0], encoding: .utf8)!
         baseZ = !dialog4
         backY.append("\(((baseZ ? 4 : 1)))")
      willz.append("\(willz.count)")

        placeholderLabel.isHidden = !textView.text.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty
    }

}

extension SocCallSignController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {

@discardableResult
 func validateGuideMine(gradientCopy_z: [Any]!, followingContact: Int) -> Double {
    var r_unlockF: Double = 1.0
   withUnsafeMutablePointer(to: &r_unlockF) { pointer in
    
   }
    var tab0: Float = 3.0
    var modeA: Double = 4.0
      modeA += (Double(Int(tab0 > 350314736.0 || tab0 < -350314736.0 ? 35.0 : tab0)))
   for _ in 0 ..< 2 {
      tab0 -= (Float(Int(tab0 > 324915583.0 || tab0 < -324915583.0 ? 11.0 : tab0) * 2))
   }
      modeA /= Swift.max(3, Double(3))
      r_unlockF -= (Double(Int(r_unlockF > 138423732.0 || r_unlockF < -138423732.0 ? 45.0 : r_unlockF)))
   return r_unlockF

}






    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {

         var gamesRetrieved: Double = validateGuideMine(gradientCopy_z:[UILabel()], followingContact:86)

      print(gamesRetrieved)

withUnsafeMutablePointer(to: &gamesRetrieved) { pointer in
        _ = pointer.pointee
}


       var resultp: Int = 4
   withUnsafeMutablePointer(to: &resultp) { pointer in
    
   }
      resultp -= resultp

        picker.dismiss(animated: true)
    }


    func imagePickerController(
        _ picker: UIImagePickerController,
        didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]
    ) {
       var settingF: Double = 3.0
   while ((Double(2 / (Swift.max(7, Int(settingF))))) < 5.91) {
      settingF += (Double(Int(settingF > 290820532.0 || settingF < -290820532.0 ? 21.0 : settingF)))
      break
   }

        let endImage = info[.editedImage] as? UIImage ?? info[.originalImage] as? UIImage
        selectedPhoto = endImage
        selectedPhotoImageView.image = endImage
        selectedPhotoImageView.isHidden = endImage == nil
        addPhotoView?.isHidden = endImage != nil
        closePhotoView?.isHidden = endImage == nil
        picker.dismiss(animated: true)
    }

}
