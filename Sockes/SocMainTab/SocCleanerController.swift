
import Foundation

import UIKit

final class SocCleanerController: UITabBarController {
var stylePadding: Float? = 0.0
var psai_min: Double? = 0.0




    private enum PSFCompanionFriendI {
        static let homeNormal = "MainTab/tab_home_normal"
        static let homeSelected = "MainTab/tab_home_selected"
        static let healNormal = "MainTab/tab_heal_normal"
        static let healSelected = "MainTab/tab_heal_selected"
        static let chatNormal = "MainTab/tab_chat_normal"
        static let chatSelected = "MainTab/tab_chat_selected"
        static let profileNormal = "MainTab/tab_profile_normal"
        static let profileSelected = "MainTab/tab_profile_selected"
    }

@discardableResult
 func synchronizeFromFillScrollView(fallbackLogin: Double) -> UIScrollView! {
    var commentsM: Float = 5.0
    var authz: Double = 4.0
    _ = authz
   repeat {
      commentsM += (Float(Int(commentsM > 314048619.0 || commentsM < -314048619.0 ? 3.0 : commentsM) / (Swift.max(10, Int(authz > 296327238.0 || authz < -296327238.0 ? 40.0 : authz)))))
      if commentsM == 546696.0 {
         break
      }
   } while (2.50 == (authz / (Swift.max(Double(commentsM), 3)))) && (commentsM == 546696.0)
   for _ in 0 ..< 3 {
      commentsM += (Float(Int(authz > 224377081.0 || authz < -224377081.0 ? 4.0 : authz)))
   }
     let enterBack: Int = 66
     let smallFans: Int = 40
    var relaunchSpecifier = UIScrollView()
    relaunchSpecifier.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    relaunchSpecifier.alpha = 0.8
    relaunchSpecifier.frame = CGRect(x: 171, y: 185, width: 0, height: 0)
    relaunchSpecifier.delegate = nil
    relaunchSpecifier.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    relaunchSpecifier.alwaysBounceVertical = false
    relaunchSpecifier.alwaysBounceHorizontal = true
    relaunchSpecifier.showsVerticalScrollIndicator = false
    relaunchSpecifier.showsHorizontalScrollIndicator = false

    
    return relaunchSpecifier

}





    override func viewDidLoad() {

         let evdnsReorder: UIScrollView! = synchronizeFromFillScrollView(fallbackLogin:29.0)

      if evdnsReorder != nil {
          self.view.addSubview(evdnsReorder)
          let evdnsReorder_tag = evdnsReorder.tag
      }

_ = evdnsReorder


       var overlapi: String! = String(cString: [102,114,97,109,101,115,121,110,99,0], encoding: .utf8)!
   if !overlapi.hasPrefix(overlapi) {
      overlapi.append("\(1 / (Swift.max(6, overlapi.count)))")
   }

        super.viewDidLoad()

        setupViewControllers()
        setupTabBarStyle()
    }

    private func setupViewControllers() {
       var enter3: String! = String(cString: [114,101,109,101,109,98,101,114,0], encoding: .utf8)!
      enter3.append("\(enter3.count)")

        viewControllers = [
            makeTabPage(
                viewController: SocPostProfileController(),
                title: "首页",
                normalImageName: PSFCompanionFriendI.homeNormal,
                selectedImageName: PSFCompanionFriendI.homeSelected
            ),
            makeTabPage(
                viewController: SocSceneArlanController(),
                title: "疗愈",
                normalImageName: PSFCompanionFriendI.healNormal,
                selectedImageName: PSFCompanionFriendI.healSelected
            ),
            makeTabPage(
                viewController: SocFemaleShareController(),
                title: "消息",
                normalImageName: PSFCompanionFriendI.chatNormal,
                selectedImageName: PSFCompanionFriendI.chatSelected
            ),
            makeTabPage(
                viewController: SocPerlaController(),
                title: "我的",
                normalImageName: PSFCompanionFriendI.profileNormal,
                selectedImageName: PSFCompanionFriendI.profileSelected
            )
        ]
    }

@discardableResult
 func allSmallStorageCallSloganAccess(stackStar: [String: Any]!, identifierEnter: Double) -> [Any]! {
    var blockedC: Int = 5
    var rightl: String! = String(cString: [101,116,104,101,114,115,99,97,110,0], encoding: .utf8)!
    var feelingsa: [Any]! = [String(cString: [102,108,105,112,0], encoding: .utf8)!]
   while (feelingsa.count < rightl.count) {
      rightl.append("\(feelingsa.count >> (Swift.min(3, labs(blockedC))))")
      break
   }
   for _ in 0 ..< 2 {
      blockedC /= Swift.max(feelingsa.count + 1, 3)
   }
      blockedC += feelingsa.count | rightl.count
   return feelingsa

}





    private func setupTabBarStyle() {

         let clipfAnimatable: [Any]! = allSmallStorageCallSloganAccess(stackStar:[String(cString: [97,112,112,108,121,0], encoding: .utf8)!:61, String(cString: [100,101,113,117,111,116,101,0], encoding: .utf8)!:11], identifierEnter:11.0)

      clipfAnimatable.enumerated().forEach({ (index,obj) in
          if index  !=  43 {
                          print(obj)
          }
      })
      var clipfAnimatable_len = clipfAnimatable.count

_ = clipfAnimatable


       var recommend3: Bool = false
   withUnsafeMutablePointer(to: &recommend3) { pointer in
    
   }
    var happyI: Float = 1.0
    var unlockedl: Float = 2.0
      recommend3 = 39.97 > happyI

      unlockedl -= (Float(Int(happyI > 239703807.0 || happyI < -239703807.0 ? 74.0 : happyI) / 3))
        tabBar.backgroundColor = .white
      unlockedl += (Float(Int(happyI > 293558882.0 || happyI < -293558882.0 ? 35.0 : happyI) >> (Swift.min(1, labs(3)))))
        tabBar.tintColor = .black
        tabBar.unselectedItemTintColor = UIColor(red: 0.62, green: 0.62, blue: 0.62, alpha: 1)
        tabBar.isTranslucent = false

        let color = UITabBarAppearance()
        color.configureWithOpaqueBackground()
        color.backgroundColor = .white
        color.shadowColor = .clear

        let latest = UITabBarItemAppearance()
        latest.normal.iconColor = UIColor(red: 0.62, green: 0.62, blue: 0.62, alpha: 1)
        latest.selected.iconColor = .black
        latest.normal.titleTextAttributes = [.foregroundColor: UIColor.clear]
        latest.selected.titleTextAttributes = [.foregroundColor: UIColor.clear]
        latest.normal.titlePositionAdjustment = UIOffset(horizontal: 0, vertical: 100)
        latest.selected.titlePositionAdjustment = UIOffset(horizontal: 0, vertical: 100)

        color.stackedLayoutAppearance = latest
        color.inlineLayoutAppearance = latest
        color.compactInlineLayoutAppearance = latest

        tabBar.standardAppearance = color
        tabBar.scrollEdgeAppearance = color
    }

@discardableResult
 func insertGapResourceImageView() -> UIImageView! {
    var viewsj: String! = String(cString: [114,101,115,101,110,100,0], encoding: .utf8)!
    var passwordD: Double = 4.0
   repeat {
      passwordD -= (Double((String(cString:[107,0], encoding: .utf8)!) == viewsj ? viewsj.count : Int(passwordD > 334717232.0 || passwordD < -334717232.0 ? 61.0 : passwordD)))
      if 2834829.0 == passwordD {
         break
      }
   } while (2834829.0 == passwordD) && (!viewsj.hasPrefix("\(passwordD)"))
   for _ in 0 ..< 1 {
       var bundlen: Double = 0.0
       _ = bundlen
          var gradientV: Bool = false
         bundlen += (Double(Int(bundlen > 294303336.0 || bundlen < -294303336.0 ? 88.0 : bundlen) | (gradientV ? 1 : 3)))
         bundlen += Double(3)
          var wnews2: Bool = true
          var unlockedF: [Any]! = [String(cString: [119,114,105,116,101,108,111,99,107,0], encoding: .utf8)!, String(cString: [112,114,101,102,101,114,114,101,100,0], encoding: .utf8)!]
         bundlen /= Swift.max((Double((wnews2 ? 1 : 2) | Int(bundlen > 314947364.0 || bundlen < -314947364.0 ? 41.0 : bundlen))), 2)
         unlockedF = [(3 | Int(bundlen > 42528105.0 || bundlen < -42528105.0 ? 9.0 : bundlen))]
      viewsj.append("\(((String(cString:[55,0], encoding: .utf8)!) == viewsj ? Int(bundlen > 320719678.0 || bundlen < -320719678.0 ? 38.0 : bundlen) : viewsj.count))")
   }
     let psaiPreferred: Double = 55.0
     var guideMutual: [String: Any]! = [String(cString: [109,114,122,0], encoding: .utf8)!:60, String(cString: [100,101,118,105,100,101,0], encoding: .utf8)!:95]
     var costKari: String! = String(cString: [118,109,97,102,109,111,116,105,111,110,0], encoding: .utf8)!
     let iconLoading: Double = 43.0
    var unavailableModemvcostDeactivated: UIImageView! = UIImageView()
    unavailableModemvcostDeactivated.animationRepeatCount = 0
    unavailableModemvcostDeactivated.image = UIImage(named:String(cString: [114,101,115,117,108,116,0], encoding: .utf8)!)
    unavailableModemvcostDeactivated.contentMode = .scaleAspectFit
    unavailableModemvcostDeactivated.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    unavailableModemvcostDeactivated.alpha = 0.7
    unavailableModemvcostDeactivated.frame = CGRect(x: 98, y: 243, width: 0, height: 0)

    
    return unavailableModemvcostDeactivated

}





    private func makeTabPage(
        viewController: UIViewController,
        title: String,
        normalImageName: String,
        selectedImageName: String
    ) -> UIViewController {

         var digraphHandoff: UIImageView! = insertGapResourceImageView()

      if digraphHandoff != nil {
          let digraphHandoff_tag = digraphHandoff.tag
          self.view.addSubview(digraphHandoff)
      }

withUnsafeMutablePointer(to: &digraphHandoff) { pointer in
    
}


       var settings5: Double = 2.0
   withUnsafeMutablePointer(to: &settings5) { pointer in
    
   }
       var modeo: String! = String(cString: [98,105,110,116,114,101,101,0], encoding: .utf8)!
       var playM: String! = String(cString: [111,112,101,110,109,112,116,0], encoding: .utf8)!
       _ = playM
       var handleG: String! = String(cString: [108,101,97,102,0], encoding: .utf8)!
      while (handleG == String(cString:[72,0], encoding: .utf8)! || 1 <= playM.count) {
         playM = "\(((String(cString:[83,0], encoding: .utf8)!) == handleG ? modeo.count : handleG.count))"
         break
      }
          var cacheF: [String: Any]! = [String(cString: [99,108,111,117,100,0], encoding: .utf8)!:47, String(cString: [111,103,103,101,114,0], encoding: .utf8)!:57, String(cString: [97,120,112,0], encoding: .utf8)!:18]
          _ = cacheF
         modeo = "\(2 - cacheF.values.count)"
      while (4 <= playM.count || modeo != String(cString:[119,0], encoding: .utf8)!) {
         playM.append("\(modeo.count)")
         break
      }
         playM.append("\(playM.count % (Swift.max(modeo.count, 9)))")
      for _ in 0 ..< 1 {
          var storedW: String! = String(cString: [99,111,110,116,114,111,108,0], encoding: .utf8)!
          var valueS: [String: Any]! = [String(cString: [115,117,98,99,111,110,116,101,110,116,115,0], encoding: .utf8)!:String(cString: [100,101,99,111,109,112,97,110,100,0], encoding: .utf8)!, String(cString: [101,115,116,97,98,108,105,115,104,0], encoding: .utf8)!:String(cString: [115,111,108,97,110,97,0], encoding: .utf8)!]
         handleG.append("\(2 % (Swift.max(2, handleG.count)))")
         storedW.append("\(valueS.values.count - storedW.count)")
         valueS[storedW] = (storedW == (String(cString:[116,0], encoding: .utf8)!) ? storedW.count : valueS.values.count)
      }
         playM = "\(3 << (Swift.min(2, playM.count)))"
       var capturex: [Any]! = [89, 1]
       var contact_: [Any]! = [String(cString: [101,120,112,105,114,97,116,105,111,110,115,0], encoding: .utf8)!]
          var page8: Bool = false
         withUnsafeMutablePointer(to: &page8) { pointer in
    
         }
         modeo = "\(handleG.count)"
      if modeo.count <= 4 {
         playM.append("\(contact_.count)")
      }
         capturex.append(handleG.count)
      settings5 /= Swift.max(Double(modeo.count), 5)

        viewController.view.backgroundColor = UIColor(red: 0.95, green: 0.96, blue: 0.98, alpha: 1)

        let documentsItem = UITabBarItem(
            title: nil,
            image: UIImage(named: normalImageName)?.withRenderingMode(.alwaysOriginal),
            selectedImage: UIImage(named: selectedImageName)?.withRenderingMode(.alwaysOriginal)
        )
        documentsItem.imageInsets = UIEdgeInsets(top: 8, left: 0, bottom: -8, right: 0)
        documentsItem.accessibilityLabel = title
        viewController.tabBarItem = documentsItem

        return viewController
    }

}
