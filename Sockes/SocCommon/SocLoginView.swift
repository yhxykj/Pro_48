
import Foundation

import UIKit

final class SocLoginView: UIView {
private var cameraIdx: Int? = 0
private var is_Overlay: Bool? = false
var enbaleRaw: Bool? = false




    private enum PSFCompanionFriendI {
        static let noDataIcon = "Common/empty_no_data_icon"
    }

    private let imageView = UIImageView(image: UIImage(named: PSFCompanionFriendI.noDataIcon))

    override init(frame: CGRect) {
        super.init(frame: frame)

        setupContent()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

@discardableResult
 func purchaseIntoResumeRectActivityImageView(dataSession: [Any]!) -> UIImageView! {
    var dismissL: Double = 0.0
    var areas: Double = 2.0
   while ((dismissL - 4.27) == 5.83) {
      dismissL -= Double(2)
      break
   }
   if 5.45 <= dismissL {
       var itemsY: String! = String(cString: [112,98,108,111,99,107,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &itemsY) { pointer in
    
      }
      while (4 <= itemsY.count) {
         itemsY.append("\(2)")
         break
      }
         itemsY.append("\(itemsY.count + 1)")
      repeat {
         itemsY.append("\(((String(cString:[101,0], encoding: .utf8)!) == itemsY ? itemsY.count : itemsY.count))")
         if (String(cString:[120,117,99,0], encoding: .utf8)!) == itemsY {
            break
         }
      } while ((String(cString:[120,117,99,0], encoding: .utf8)!) == itemsY) && (!itemsY.contains("\(itemsY.count)"))
      areas /= Swift.max((Double(Int(areas > 220571380.0 || areas < -220571380.0 ? 25.0 : areas) % 1)), 1)
   }
     let playUnlocked: [String: Any]! = [String(cString: [112,104,111,110,101,115,0], encoding: .utf8)!:false]
     let maleTemplate_bo: String! = String(cString: [116,101,114,109,105,110,97,116,101,0], encoding: .utf8)!
     let durationMessage: Int = 91
    var simplesignalSquare: UIImageView! = UIImageView(frame:CGRect.zero)
    simplesignalSquare.alpha = 0.7;
    simplesignalSquare.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    simplesignalSquare.frame = CGRect(x: 69, y: 283, width: 0, height: 0)
    simplesignalSquare.image = UIImage(named:String(cString: [110,105,99,107,110,97,109,101,0], encoding: .utf8)!)
    simplesignalSquare.contentMode = .scaleAspectFit
    simplesignalSquare.animationRepeatCount = 8

    
    return simplesignalSquare

}





    private func setupContent() {

         let asmdefsSuppress: UIImageView! = purchaseIntoResumeRectActivityImageView(dataSession:[40, 0])

      if asmdefsSuppress != nil {
          self.addSubview(asmdefsSuppress)
          let asmdefsSuppress_tag = asmdefsSuppress.tag
      }

_ = asmdefsSuppress


       var urlM: Double = 2.0
    _ = urlM
   for _ in 0 ..< 2 {
       var comment8: String! = String(cString: [102,105,101,108,100,115,0], encoding: .utf8)!
          var presenterF: String! = String(cString: [113,115,118,100,101,105,110,116,0], encoding: .utf8)!
          var insufficientK: String! = String(cString: [99,111,100,101,99,0], encoding: .utf8)!
         comment8.append("\(presenterF.count)")
         insufficientK = "\(1)"
      if comment8 != comment8 {
          var replyk: String! = String(cString: [114,101,108,105,97,98,108,101,0], encoding: .utf8)!
          var d_titleE: Double = 2.0
          var updated5: String! = String(cString: [112,114,101,115,115,101,100,0], encoding: .utf8)!
          var yinitial3: Bool = true
          _ = yinitial3
         comment8.append("\(replyk.count)")
         d_titleE -= (Double(updated5.count << (Swift.min(1, labs(Int(d_titleE > 158285177.0 || d_titleE < -158285177.0 ? 47.0 : d_titleE))))))
         updated5 = "\((Int(d_titleE > 2559307.0 || d_titleE < -2559307.0 ? 75.0 : d_titleE)))"
         yinitial3 = ((Int(d_titleE > 260588698.0 || d_titleE < -260588698.0 ? 61.0 : d_titleE)) == comment8.count)
      }
       var row2: Double = 1.0
       _ = row2
         row2 += (Double(3 ^ Int(row2 > 86742334.0 || row2 < -86742334.0 ? 32.0 : row2)))
      urlM /= Swift.max(1, (Double(comment8.count - Int(urlM > 46265557.0 || urlM < -46265557.0 ? 12.0 : urlM))))
   }

        backgroundColor = .clear
        isHidden = true

        imageView.contentMode = .scaleAspectFit
        imageView.translatesAutoresizingMaskIntoConstraints = false
        addSubview(imageView)

        NSLayoutConstraint.activate([
            imageView.centerXAnchor.constraint(equalTo: centerXAnchor),
            imageView.centerYAnchor.constraint(equalTo: centerYAnchor),
            imageView.widthAnchor.constraint(equalToConstant: 118),
            imageView.heightAnchor.constraint(equalToConstant: 160)
        ])
    }

}
