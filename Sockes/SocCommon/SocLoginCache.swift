
import UIKit

import Foundation

enum SocLoginCache {

    private enum Key {
        static let isLoggedIn = "auth.isLoggedIn"
        static let currentEmail = "auth.currentEmail"
    }

    static var isLoggedIn: Bool {
       var z_width4: Int = 1
    var authn: String! = String(cString: [115,116,114,99,109,112,0], encoding: .utf8)!
    var destinationE: [Any]! = [68, 14]
    _ = destinationE
   while ((3 >> (Swift.min(3, labs(z_width4)))) >= 3) {
      authn.append("\(destinationE.count)")
      break
   }
      destinationE = [z_width4]

            return UserDefaults.standard.bool(forKey: Key.isLoggedIn)
    }

    static var currentEmail: String? {
       var syncM: [Any]! = [String(cString: [117,114,97,110,100,111,109,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &syncM) { pointer in
    
   }
   if syncM.count > 1 {
       var itemsF: String! = String(cString: [102,112,109,98,0], encoding: .utf8)!
       _ = itemsF
       var friend_o8: String! = String(cString: [101,108,97,115,116,105,99,0], encoding: .utf8)!
       var switch_hK: Bool = true
       var local_xA: String! = String(cString: [114,101,97,108,108,121,0], encoding: .utf8)!
         local_xA = "\((itemsF == (String(cString:[110,0], encoding: .utf8)!) ? itemsF.count : (switch_hK ? 1 : 1)))"
         switch_hK = local_xA == (String(cString:[82,0], encoding: .utf8)!) && 50 >= itemsF.count
      if switch_hK {
          var destinationa: [String: Any]! = [String(cString: [112,114,101,115,101,110,116,97,116,105,111,110,115,0], encoding: .utf8)!:36, String(cString: [100,117,97,108,105,110,112,117,116,0], encoding: .utf8)!:92, String(cString: [112,97,114,101,110,116,97,103,101,0], encoding: .utf8)!:57]
          var postst: [Any]! = [95, 44, 18]
         itemsF = "\(3)"
         destinationa = ["\(destinationa.values.count)": itemsF.count]
         postst = [local_xA.count]
      }
      while (friend_o8.contains("\(switch_hK)")) {
          var constantO: String! = String(cString: [115,112,105,108,108,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &constantO) { pointer in
                _ = pointer.pointee
         }
          var xnewsW: String! = String(cString: [104,97,115,104,105,110,105,116,0], encoding: .utf8)!
          var visiblei: String! = String(cString: [115,101,114,105,102,0], encoding: .utf8)!
          var startv: String! = String(cString: [119,105,100,101,102,101,108,101,109,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &startv) { pointer in
    
         }
         switch_hK = !switch_hK
         constantO.append("\(1 << (Swift.min(2, xnewsW.count)))")
         xnewsW = "\(itemsF.count)"
         visiblei.append("\(visiblei.count - friend_o8.count)")
         startv = "\(friend_o8.count)"
         break
      }
      for _ in 0 ..< 1 {
          var alertY: String! = String(cString: [97,109,114,110,98,0], encoding: .utf8)!
         itemsF = "\((friend_o8.count | (switch_hK ? 1 : 3)))"
         alertY = "\(2)"
      }
         switch_hK = itemsF.count == friend_o8.count
       var progressj: Float = 0.0
       var modeM: Float = 5.0
         friend_o8.append("\(((String(cString:[108,0], encoding: .utf8)!) == itemsF ? (switch_hK ? 2 : 5) : itemsF.count))")
         local_xA = "\((Int(modeM > 293403138.0 || modeM < -293403138.0 ? 86.0 : modeM)))"
          var datab: [Any]! = [19, 15, 6]
         itemsF = "\(itemsF.count)"
         datab.append(((String(cString:[100,0], encoding: .utf8)!) == local_xA ? local_xA.count : friend_o8.count))
          var offsetL: [Any]! = [26, 78]
          var infog: Double = 0.0
          var bottleK: Double = 2.0
         withUnsafeMutablePointer(to: &bottleK) { pointer in
                _ = pointer.pointee
         }
         modeM += Float(itemsF.count / 3)
         offsetL = [(Int(progressj > 156987997.0 || progressj < -156987997.0 ? 91.0 : progressj) * 2)]
         infog /= Swift.max(5, Double(friend_o8.count >> (Swift.min(itemsF.count, 1))))
         bottleK -= (Double(Int(progressj > 238306280.0 || progressj < -238306280.0 ? 21.0 : progressj) + (switch_hK ? 5 : 5)))
         switch_hK = !switch_hK
      syncM.append(1)
   }

            return UserDefaults.standard.string(forKey: Key.currentEmail)
    }

    static func markLoggedIn(email: String) {
       var styleq: String! = String(cString: [116,111,111,108,98,111,120,0], encoding: .utf8)!
   repeat {
       var friend_ni6: [String: Any]! = [String(cString: [105,110,115,116,97,108,108,105,110,103,0], encoding: .utf8)!:String(cString: [99,111,110,116,111,108,108,101,114,0], encoding: .utf8)!]
       _ = friend_ni6
      for _ in 0 ..< 2 {
          var followingt: Double = 5.0
         withUnsafeMutablePointer(to: &followingt) { pointer in
    
         }
          var d_managerF: Double = 2.0
          var nowt: [Any]! = [String(cString: [98,108,117,114,108,101,115,115,0], encoding: .utf8)!, String(cString: [104,111,115,116,115,0], encoding: .utf8)!, String(cString: [114,101,115,116,114,97,105,110,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &nowt) { pointer in
    
         }
         friend_ni6 = ["\(nowt.count)": 1]
         followingt -= Double(2)
         d_managerF += (Double(Int(followingt > 389434295.0 || followingt < -389434295.0 ? 76.0 : followingt) << (Swift.min(2, labs(2)))))
      }
      repeat {
         friend_ni6 = ["\(friend_ni6.count)": friend_ni6.count]
         if friend_ni6.count == 2506047 {
            break
         }
      } while (friend_ni6.count == 2506047) && (2 > (4 | friend_ni6.values.count))
      for _ in 0 ..< 3 {
         friend_ni6 = ["\(friend_ni6.keys.count)": friend_ni6.values.count * friend_ni6.keys.count]
      }
      styleq.append("\(friend_ni6.keys.count & styleq.count)")
      if styleq == (String(cString:[113,110,51,54,0], encoding: .utf8)!) {
         break
      }
   } while (styleq == (String(cString:[113,110,51,54,0], encoding: .utf8)!)) && (styleq.count >= styleq.count)

        let result = email.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        UserDefaults.standard.set(true, forKey: Key.isLoggedIn)
        UserDefaults.standard.set(result, forKey: Key.currentEmail)
    }

    static func markLoggedOut() {
       var smallZ: String! = String(cString: [112,97,103,101,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
       var register_inD: String! = String(cString: [99,111,110,115,116,114,97,105,110,116,0], encoding: .utf8)!
       var seedp: [Any]! = [String(cString: [109,111,115,116,0], encoding: .utf8)!, String(cString: [102,102,116,103,0], encoding: .utf8)!]
       _ = seedp
       var endd: Bool = true
      for _ in 0 ..< 3 {
         seedp = [seedp.count - register_inD.count]
      }
         seedp = [((String(cString:[97,0], encoding: .utf8)!) == register_inD ? register_inD.count : seedp.count)]
      while (register_inD.count >= 3) {
          var friend__P: String! = String(cString: [112,114,111,98,101,114,0], encoding: .utf8)!
          var clearR: String! = String(cString: [100,106,112,101,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &clearR) { pointer in
                _ = pointer.pointee
         }
          var scene_mb: [String: Any]! = [String(cString: [112,101,117,107,101,114,0], encoding: .utf8)!:String(cString: [99,111,108,117,109,115,0], encoding: .utf8)!, String(cString: [120,105,110,99,0], encoding: .utf8)!:String(cString: [101,120,112,111,114,116,101,100,0], encoding: .utf8)!, String(cString: [101,120,116,114,99,0], encoding: .utf8)!:String(cString: [112,104,105,0], encoding: .utf8)!]
          var deviceV: Float = 3.0
         register_inD.append("\((3 | Int(deviceV > 350618760.0 || deviceV < -350618760.0 ? 84.0 : deviceV)))")
         friend__P.append("\(register_inD.count)")
         clearR.append("\(1)")
         scene_mb = [register_inD: 1]
         break
      }
          var gcopy_nU: Int = 4
          _ = gcopy_nU
         seedp.append((register_inD.count % (Swift.max(8, (endd ? 5 : 4)))))
         gcopy_nU -= ((endd ? 1 : 5) % (Swift.max(gcopy_nU, 6)))
      for _ in 0 ..< 3 {
         endd = !endd
      }
         register_inD.append("\(2 + seedp.count)")
      for _ in 0 ..< 1 {
         endd = register_inD.count >= 39
      }
       var social_: String! = String(cString: [121,117,121,118,116,111,121,117,118,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &social_) { pointer in
    
      }
       var rightF: String! = String(cString: [109,105,110,117,116,101,115,0], encoding: .utf8)!
         social_ = "\(3)"
         rightF = "\(2)"
      smallZ = "\(((endd ? 5 : 3)))"
   }

        UserDefaults.standard.set(false, forKey: Key.isLoggedIn)
        UserDefaults.standard.removeObject(forKey: Key.currentEmail)
    }

}
