
import Foundation

import UIKit

final class SocWilliamsCell: UITableViewCell {
var editSize: Float? = 0.0
var placeholderMutualValue_str: String!



    static let reuseIdentifier = "SocWilliamsCell"

    private let avatarImageView = UIImageView()
    private let avatarLabel = UILabel()
    private let bubbleLabel = SocMessageLabel()
    private var leadingConstraints: [NSLayoutConstraint] = []
    private var trailingConstraints: [NSLayoutConstraint] = []
    private var bubbleWidthConstraint: NSLayoutConstraint?

    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        super.init(style: style, reuseIdentifier: reuseIdentifier)

        setupCell()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

@discardableResult
 func leftGapMicrophoneNavigationComponentMapImageView(menuRelease_sx: Float, rowPosts: String!, delete_9eLoading: String!) -> UIImageView! {
    var bubbleA: String! = String(cString: [114,101,97,115,115,101,109,98,108,101,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &bubbleA) { pointer in
          _ = pointer.pointee
   }
    var cacher: Float = 4.0
   while (bubbleA.count <= 4) {
       var constraint3: String! = String(cString: [99,111,100,101,98,111,111,107,0], encoding: .utf8)!
       var commentsg: Double = 2.0
       _ = commentsg
       var microphoneW: Float = 2.0
       var storeG: String! = String(cString: [114,101,110,100,101,114,97,98,108,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &storeG) { pointer in
             _ = pointer.pointee
      }
       var constraintso: [Any]! = [66, 14]
      while (!storeG.hasPrefix("\(constraintso.count)")) {
         storeG = "\((2 << (Swift.min(labs(Int(microphoneW > 202801102.0 || microphoneW < -202801102.0 ? 50.0 : microphoneW)), 5))))"
         break
      }
      while ((Int(microphoneW > 183098529.0 || microphoneW < -183098529.0 ? 39.0 : microphoneW)) >= storeG.count) {
         microphoneW -= (Float(Int(commentsg > 240272999.0 || commentsg < -240272999.0 ? 25.0 : commentsg) / (Swift.max(7, Int(microphoneW > 117401062.0 || microphoneW < -117401062.0 ? 75.0 : microphoneW)))))
         break
      }
      repeat {
         microphoneW -= (Float((String(cString:[84,0], encoding: .utf8)!) == storeG ? Int(commentsg > 172097888.0 || commentsg < -172097888.0 ? 13.0 : commentsg) : storeG.count))
         if microphoneW == 1700324.0 {
            break
         }
      } while (3.65 < microphoneW) && (microphoneW == 1700324.0)
         microphoneW /= Swift.max(1, (Float((String(cString:[116,0], encoding: .utf8)!) == storeG ? storeG.count : Int(commentsg > 58135280.0 || commentsg < -58135280.0 ? 35.0 : commentsg))))
      for _ in 0 ..< 3 {
          var register_mK: String! = String(cString: [109,105,103,114,97,116,105,110,103,0], encoding: .utf8)!
         microphoneW -= Float(constraintso.count)
         register_mK.append("\((Int(commentsg > 393034027.0 || commentsg < -393034027.0 ? 33.0 : commentsg) & 3))")
      }
      repeat {
         storeG.append("\(3)")
         if 3099757 == storeG.count {
            break
         }
      } while (5 < (storeG.count & constraintso.count) && (storeG.count & 5) < 5) && (3099757 == storeG.count)
         microphoneW /= Swift.max(Float(constraintso.count), 1)
      repeat {
          var unlockedO: String! = String(cString: [115,99,101,110,97,114,105,111,0], encoding: .utf8)!
          var cameram: String! = String(cString: [117,110,105,115,119,97,112,0], encoding: .utf8)!
          _ = cameram
          var syncw: Float = 2.0
          var williamsw: String! = String(cString: [99,101,110,116,101,114,105,110,103,0], encoding: .utf8)!
         commentsg += (Double((String(cString:[111,0], encoding: .utf8)!) == cameram ? cameram.count : Int(microphoneW > 214532500.0 || microphoneW < -214532500.0 ? 35.0 : microphoneW)))
         unlockedO = "\(3 >> (Swift.min(3, constraint3.count)))"
         syncw /= Swift.max((Float(Int(commentsg > 62822828.0 || commentsg < -62822828.0 ? 17.0 : commentsg))), 3)
         williamsw = "\(cameram.count - 2)"
         if commentsg == 468234.0 {
            break
         }
      } while (commentsg == 468234.0) && (2.85 < (3.53 - commentsg))
       var companionO: String! = String(cString: [105,110,116,101,114,112,111,108,97,116,111,114,0], encoding: .utf8)!
       _ = companionO
         constraint3.append("\(1 >> (Swift.min(1, constraintso.count)))")
         commentsg /= Swift.max(5, Double(1 / (Swift.max(9, constraintso.count))))
      for _ in 0 ..< 1 {
         commentsg -= Double(constraintso.count)
      }
      if companionO.count > 2 {
         companionO = "\(3 * companionO.count)"
      }
          var socialI: Float = 4.0
          _ = socialI
         commentsg /= Swift.max((Double(Int(commentsg > 167704840.0 || commentsg < -167704840.0 ? 96.0 : commentsg) >> (Swift.min(constraintso.count, 4)))), 5)
         socialI += Float(1)
         constraint3 = "\(constraint3.count)"
      cacher /= Swift.max((Float(Int(cacher > 106654804.0 || cacher < -106654804.0 ? 4.0 : cacher))), 4)
      break
   }
     var hangupRetry: UIView! = UIView(frame:CGRect(x: 27, y: 394, width: 0, height: 0))
     let nowReply: UIImageView! = UIImageView(image:UIImage(named:String(cString: [113,105,110,100,101,120,0], encoding: .utf8)!))
    var xferElevated = UIImageView()
    xferElevated.frame = CGRect(x: 271, y: 172, width: 0, height: 0)
    xferElevated.alpha = 0.3;
    xferElevated.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    xferElevated.animationRepeatCount = 3
    xferElevated.image = UIImage(named:String(cString: [104,97,110,103,117,112,0], encoding: .utf8)!)
    xferElevated.contentMode = .scaleAspectFit
    hangupRetry.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    hangupRetry.alpha = 0.7
    hangupRetry.frame = CGRect(x: 32, y: 184, width: 0, height: 0)
    
    nowReply.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nowReply.alpha = 0.4
    nowReply.frame = CGRect(x: 38, y: 239, width: 0, height: 0)
    nowReply.contentMode = .scaleAspectFit
    nowReply.animationRepeatCount = 2
    nowReply.image = UIImage(named:String(cString: [111,112,116,105,111,110,115,0], encoding: .utf8)!)
    

    
    return xferElevated

}






    private func configureAvatar(isMine: Bool) {

         var strsepEncodemv: UIImageView! = leftGapMicrophoneNavigationComponentMapImageView(menuRelease_sx:23.0, rowPosts:String(cString: [118,101,114,105,102,121,0], encoding: .utf8)!, delete_9eLoading:String(cString: [99,111,110,118,101,110,105,101,110,99,101,0], encoding: .utf8)!)

      if strsepEncodemv != nil {
          let strsepEncodemv_tag = strsepEncodemv.tag
          self.addSubview(strsepEncodemv)
      }

withUnsafeMutablePointer(to: &strsepEncodemv) { pointer in
        _ = pointer.pointee
}


       var durationp: Double = 0.0
   withUnsafeMutablePointer(to: &durationp) { pointer in
          _ = pointer.pointee
   }
   repeat {
      durationp += (Double(Int(durationp > 179160367.0 || durationp < -179160367.0 ? 27.0 : durationp) + 3))
      if 818211.0 == durationp {
         break
      }
   } while ((1.17 + durationp) > 3.1 && 5.11 > (durationp - 1.17)) && (818211.0 == durationp)

        let agreementImage = isMine
            ? SocEmptyMenu.avatarImage
            : UIImage(named: "EmotionSync/PostAvatars/emotion_post_avatar_kari")

        avatarImageView.image = agreementImage
        avatarImageView.backgroundColor = isMine
            ? UIColor(red: 1.00, green: 0.91, blue: 0.68, alpha: 1)
            : UIColor(red: 0.78, green: 0.88, blue: 1.00, alpha: 1)
        avatarLabel.text = isMine ? SocEmptyMenu.avatarInitial() : "L"
        avatarLabel.isHidden = agreementImage != nil
    }


    private func setupCell() {
       var summariesi: Bool = true
    _ = summariesi
    var spendJ: String! = String(cString: [106,95,55,56,0], encoding: .utf8)!
   if spendJ.hasSuffix("\(summariesi)") {
      summariesi = !summariesi
   }

      spendJ.append("\(((summariesi ? 3 : 5)))")
        backgroundColor = .clear
        contentView.backgroundColor = .clear
        selectionStyle = .none

        avatarImageView.contentMode = .scaleAspectFill
        avatarImageView.layer.cornerRadius = 4
        avatarImageView.clipsToBounds = true
        avatarImageView.translatesAutoresizingMaskIntoConstraints = false
        contentView.addSubview(avatarImageView)

        avatarLabel.font = .systemFont(ofSize: 12, weight: .bold)
        avatarLabel.textColor = .white
        avatarLabel.textAlignment = .center
        avatarLabel.translatesAutoresizingMaskIntoConstraints = false
        avatarImageView.addSubview(avatarLabel)

        bubbleLabel.numberOfLines = 0
        bubbleLabel.font = .systemFont(ofSize: 17, weight: .regular)
        bubbleLabel.textColor = UIColor(red: 0.20, green: 0.20, blue: 0.20, alpha: 1)
        bubbleLabel.layer.cornerRadius = 12
        bubbleLabel.layer.masksToBounds = true
        bubbleLabel.translatesAutoresizingMaskIntoConstraints = false
        contentView.addSubview(bubbleLabel)
        bubbleWidthConstraint = bubbleLabel.widthAnchor.constraint(equalToConstant: 256)
        bubbleWidthConstraint?.isActive = true

        leadingConstraints = [
            avatarImageView.leadingAnchor.constraint(equalTo: contentView.leadingAnchor, constant: 17),
            bubbleLabel.leadingAnchor.constraint(equalTo: avatarImageView.trailingAnchor, constant: 15)
        ]

        trailingConstraints = [
            avatarImageView.trailingAnchor.constraint(equalTo: contentView.trailingAnchor, constant: -17),
            bubbleLabel.trailingAnchor.constraint(equalTo: avatarImageView.leadingAnchor, constant: -15)
        ]

        NSLayoutConstraint.activate([
            avatarImageView.topAnchor.constraint(equalTo: contentView.topAnchor, constant: 5),
            avatarImageView.widthAnchor.constraint(equalToConstant: 28),
            avatarImageView.heightAnchor.constraint(equalToConstant: 28),

            avatarLabel.centerXAnchor.constraint(equalTo: avatarImageView.centerXAnchor),
            avatarLabel.centerYAnchor.constraint(equalTo: avatarImageView.centerYAnchor),

            bubbleLabel.topAnchor.constraint(equalTo: contentView.topAnchor, constant: 5),
            bubbleLabel.bottomAnchor.constraint(equalTo: contentView.bottomAnchor, constant: -40)
        ])
    }

@discardableResult
 func handleArrayShadowQueueCenterLabel(bottlePresentation: Double, visibleName: Double) -> UILabel! {
    var bundleC: String! = String(cString: [104,101,120,116,105,108,101,0], encoding: .utf8)!
    var handleM: Double = 1.0
   withUnsafeMutablePointer(to: &handleM) { pointer in
          _ = pointer.pointee
   }
      handleM -= Double(3)
       var q_layerM: [String: Any]! = [String(cString: [122,114,101,111,114,100,101,114,0], encoding: .utf8)!:81, String(cString: [105,118,97,114,115,0], encoding: .utf8)!:15]
      withUnsafeMutablePointer(to: &q_layerM) { pointer in
    
      }
      if (2 << (Swift.min(4, q_layerM.count))) <= 1 {
         q_layerM["\(q_layerM.count)"] = 3
      }
         q_layerM["\(q_layerM.keys.count)"] = q_layerM.values.count / 1
         q_layerM["\(q_layerM.count)"] = 2 << (Swift.min(3, q_layerM.keys.count))
      bundleC.append("\(q_layerM.count / (Swift.max(bundleC.count, 2)))")
     let editChat: UIButton! = UIButton()
     var unlockUrl: UIButton! = UIButton(frame:CGRect(x: 286, y: 444, width: 0, height: 0))
     let largeItems: Double = 95.0
    var initackCornerPrimer = UILabel(frame:CGRect.zero)
    editChat.alpha = 0.0;
    editChat.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    editChat.frame = CGRect(x: 18, y: 281, width: 0, height: 0)
    editChat.setImage(UIImage(named:String(cString: [115,116,97,114,116,0], encoding: .utf8)!), for: .normal)
    editChat.setTitle("", for: .normal)
    editChat.setBackgroundImage(UIImage(named:String(cString: [109,105,110,95,100,0], encoding: .utf8)!), for: .normal)
    editChat.titleLabel?.font = UIFont.systemFont(ofSize:13)
    
    unlockUrl.frame = CGRect(x: 262, y: 255, width: 0, height: 0)
    unlockUrl.alpha = 0.2;
    unlockUrl.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    unlockUrl.titleLabel?.font = UIFont.systemFont(ofSize:17)
    unlockUrl.setImage(UIImage(named:String(cString: [99,111,110,116,101,110,116,0], encoding: .utf8)!), for: .normal)
    unlockUrl.setTitle("", for: .normal)
    unlockUrl.setBackgroundImage(UIImage(named:String(cString: [115,99,101,110,101,95,104,0], encoding: .utf8)!), for: .normal)
    
    initackCornerPrimer.frame = CGRect(x: 10, y: 197, width: 0, height: 0)
    initackCornerPrimer.alpha = 0.0;
    initackCornerPrimer.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    initackCornerPrimer.textAlignment = .left
    initackCornerPrimer.font = UIFont.systemFont(ofSize:16)
    initackCornerPrimer.text = ""
    initackCornerPrimer.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return initackCornerPrimer

}






    func configure(with message: SocBottleSocial) {

         var gdataStoped: UILabel! = handleArrayShadowQueueCenterLabel(bottlePresentation:14.0, visibleName:84.0)

      if gdataStoped != nil {
          self.addSubview(gdataStoped)
          let gdataStoped_tag = gdataStoped.tag
      }

withUnsafeMutablePointer(to: &gdataStoped) { pointer in
    
}


       var modityU: [String: Any]! = [String(cString: [116,103,101,116,0], encoding: .utf8)!:String(cString: [114,101,100,105,114,101,99,116,105,111,110,97,108,0], encoding: .utf8)!, String(cString: [115,121,110,99,104,114,111,110,105,122,101,114,0], encoding: .utf8)!:String(cString: [105,112,109,111,118,105,101,0], encoding: .utf8)!]
   repeat {
      modityU["\(modityU.count)"] = modityU.count + 3
      if modityU.count == 552959 {
         break
      }
   } while (modityU.count == 552959) && ((modityU.count / 1) == 4 && (modityU.values.count / 1) == 2)

        bubbleLabel.text = message.text
        bubbleLabel.backgroundColor = message.isMine
            ? UIColor(red: 0.73, green: 0.84, blue: 1.00, alpha: 1)
            : .white
        configureAvatar(isMine: message.isMine)
        bubbleWidthConstraint?.constant = message.isMine ? 252 : 256

        NSLayoutConstraint.deactivate(leadingConstraints + trailingConstraints)
        NSLayoutConstraint.activate(message.isMine ? trailingConstraints : leadingConstraints)
    }

}

private final class SocMessageLabel: UILabel {
private var register_tc: Bool? = false
private var angryManagerBaseArray: [Any]!




    var textInsets = UIEdgeInsets(top: 11, left: 15, bottom: 11, right: 15)

@discardableResult
 func anyVerificationCalculationDomainHighInitial(viewAuth: Bool, listenerRemember: Int, layoutContent: Float) -> [Any]! {
    var privacyG: String! = String(cString: [100,101,108,97,121,115,0], encoding: .utf8)!
    var patternu: String! = String(cString: [97,118,101,114,97,103,101,105,105,114,100,0], encoding: .utf8)!
    var duration2: [Any]! = [22, 26, 29]
   withUnsafeMutablePointer(to: &duration2) { pointer in
          _ = pointer.pointee
   }
   repeat {
       var barQ: Bool = true
      withUnsafeMutablePointer(to: &barQ) { pointer in
             _ = pointer.pointee
      }
      if barQ {
         barQ = !barQ
      }
         barQ = !barQ
      for _ in 0 ..< 1 {
         barQ = !barQ
      }
      patternu.append("\(privacyG.count & patternu.count)")
      if 969985 == patternu.count {
         break
      }
   } while (969985 == patternu.count) && (3 >= patternu.count)
      patternu.append("\(privacyG.count)")
   if 5 == (privacyG.count / (Swift.max(2, duration2.count))) && (privacyG.count / (Swift.max(5, 8))) == 2 {
       var rememberb: String! = String(cString: [115,105,103,110,114,97,110,100,0], encoding: .utf8)!
       var sessionR: Double = 5.0
      withUnsafeMutablePointer(to: &sessionR) { pointer in
             _ = pointer.pointee
      }
          var sad6: Bool = false
          _ = sad6
         rememberb = "\(((sad6 ? 4 : 1)))"
          var launchC: Bool = false
         sessionR /= Swift.max((Double((launchC ? 5 : 3) / (Swift.max(Int(sessionR > 29430529.0 || sessionR < -29430529.0 ? 37.0 : sessionR), 10)))), 2)
      privacyG.append("\(duration2.count + rememberb.count)")
   }
   return duration2

}






    override func drawText(in rect: CGRect) {

         let moveProject: [Any]! = anyVerificationCalculationDomainHighInitial(viewAuth:true, listenerRemember:25, layoutContent:81.0)

      let moveProject_len = moveProject.count
      moveProject.enumerated().forEach({ (index,obj) in
          if index  ==  79 {
                          print(obj)
          }
      })

_ = moveProject


       var hangup4: Float = 0.0
    _ = hangup4
       var intrinsicc: [Any]! = [91, 47]
       var conversationM: String! = String(cString: [99,108,105,101,110,116,0], encoding: .utf8)!
         intrinsicc = [conversationM.count | intrinsicc.count]
         intrinsicc.append(conversationM.count % 3)
      repeat {
         conversationM.append("\(intrinsicc.count)")
         if conversationM == (String(cString:[103,55,52,106,95,114,111,119,0], encoding: .utf8)!) {
            break
         }
      } while (4 < (1 / (Swift.max(4, intrinsicc.count))) && (intrinsicc.count / (Swift.max(8, conversationM.count))) < 1) && (conversationM == (String(cString:[103,55,52,106,95,114,111,119,0], encoding: .utf8)!))
      if (conversationM.count >> (Swift.min(labs(4), 1))) >= 5 {
          var loginT: Bool = true
          var options6: Double = 0.0
          var resignV: Double = 1.0
          var colorV: Double = 5.0
          var d_heightr: Bool = false
         intrinsicc = [2]
         loginT = colorV == resignV
         options6 += (Double((String(cString:[54,0], encoding: .utf8)!) == conversationM ? conversationM.count : (d_heightr ? 3 : 4)))
         resignV -= (Double((loginT ? 2 : 3) / (Swift.max(Int(options6 > 225109907.0 || options6 < -225109907.0 ? 62.0 : options6), 4))))
         colorV -= (Double(Int(colorV > 350694781.0 || colorV < -350694781.0 ? 77.0 : colorV) >> (Swift.min(1, labs(3)))))
         d_heightr = loginT
      }
       var emotionT: Int = 5
       _ = emotionT
          var feelings8: String! = String(cString: [99,100,102,116,0], encoding: .utf8)!
          var happy6: String! = String(cString: [115,115,108,0], encoding: .utf8)!
         conversationM = "\(conversationM.count)"
         feelings8.append("\(conversationM.count / (Swift.max(2, 9)))")
         happy6.append("\(3)")
         emotionT %= Swift.max(4, conversationM.count)
      hangup4 -= Float(conversationM.count << (Swift.min(4, intrinsicc.count)))

        super.drawText(in: rect.inset(by: textInsets))
    }

    override var intrinsicContentSize: CGSize {
       var listenq: Double = 1.0
   withUnsafeMutablePointer(to: &listenq) { pointer in
          _ = pointer.pointee
   }
       var signP: Int = 0
       var constrainta: String! = String(cString: [99,111,109,112,108,101,116,105,111,110,115,0], encoding: .utf8)!
      while (3 == (5 & signP) && (constrainta.count & 5) == 1) {
         signP -= 1 / (Swift.max(8, constrainta.count))
         break
      }
         constrainta = "\(3 * constrainta.count)"
         constrainta = "\(constrainta.count)"
      while (constrainta.count > signP) {
         signP += 1
         break
      }
         constrainta.append("\(signP)")
       var urlT: String! = String(cString: [101,110,117,109,101,114,97,116,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &urlT) { pointer in
             _ = pointer.pointee
      }
         urlT.append("\(1 << (Swift.min(4, constrainta.count)))")
      listenq += Double(constrainta.count)

        let size = super.intrinsicContentSize
        return CGSize(
            width: size.width + textInsets.left + textInsets.right,
            height: size.height + textInsets.top + textInsets.bottom
        )
    }


    override func textRect(
        forBounds bounds: CGRect,
        limitedToNumberOfLines numberOfLines: Int
    ) -> CGRect {
       var n_managerc: Double = 1.0
    var packagesW: Int = 2
   while (Int(n_managerc) >= packagesW) {
      packagesW ^= (packagesW >> (Swift.min(labs(Int(n_managerc > 225786455.0 || n_managerc < -225786455.0 ? 59.0 : n_managerc)), 4)))
      break
   }

       var o_boundsw: Int = 0
       var videop: [String: Any]! = [String(cString: [115,116,114,117,99,116,115,0], encoding: .utf8)!:51, String(cString: [110,111,100,111,119,110,0], encoding: .utf8)!:54]
      withUnsafeMutablePointer(to: &videop) { pointer in
             _ = pointer.pointee
      }
          var button2: Float = 2.0
          var communityD: Double = 2.0
          _ = communityD
         videop["\(o_boundsw)"] = videop.values.count | o_boundsw
         button2 += (Float(Int(communityD > 38066142.0 || communityD < -38066142.0 ? 50.0 : communityD) - videop.keys.count))
         communityD /= Swift.max(1, Double(1))
       var dataa: Bool = true
       _ = dataa
         dataa = !dataa || o_boundsw < 77
       var p_products9: String! = String(cString: [102,114,101,101,0], encoding: .utf8)!
       var shouldP: String! = String(cString: [98,105,116,118,101,99,0], encoding: .utf8)!
      if dataa {
         dataa = shouldP.count > 44
      }
          var screenF: String! = String(cString: [97,100,114,109,0], encoding: .utf8)!
         shouldP = "\(1 % (Swift.max(9, videop.count)))"
         screenF.append("\((o_boundsw + (dataa ? 1 : 1)))")
         p_products9.append("\(videop.keys.count * o_boundsw)")
      packagesW &= (packagesW % (Swift.max(Int(n_managerc > 152029075.0 || n_managerc < -152029075.0 ? 89.0 : n_managerc), 6)))
        let duration = bounds.inset(by: textInsets)
        let change = super.textRect(forBounds: duration, limitedToNumberOfLines: numberOfLines)
        return change.inset(
            by: UIEdgeInsets(
                top: -textInsets.top,
                left: -textInsets.left,
                bottom: -textInsets.bottom,
                right: -textInsets.right
            )
        )
    }

}
