
import Foundation

import AVFoundation
import UIKit

final class SocVideoMutualController: UIViewController {
var constraints_margin: Float? = 0.0
private var menuAuth_list: [Any]!




    private enum PSFCompanionFriendI {
        static let backIcon = "Common/common_back_icon"
        static let topBackground = "EmotionSync/ShareEmotions/share_emotions_background"
        static let avatarRing = "Message/VideoCall/video_call_avatar_ring"
        static let micSelected = "Message/VideoCall/video_call_mic_selected"
        static let micNormal = "Message/VideoCall/video_call_mic_normal"
        static let speakerSelected = "Message/VideoCall/video_call_speaker_selected"
        static let speakerNormal = "Message/VideoCall/video_call_speaker_normal"
        static let hangupButton = "Message/VideoCall/video_call_hangup_button"
    }

    private let friend: SocEmotion
    private let captureQueue = DispatchQueue(label: "com.sockes.messageVideoCall.capture")
    private var captureSession: AVCaptureSession?
    private var hasCheckedMutualFollow = false

    init(friend: SocEmotion) {
        self.friend = friend
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

@discardableResult
 func rawEmotionLeftDomainImage() -> [Any]! {
    var storagej: String! = String(cString: [111,111,117,114,97,0], encoding: .utf8)!
    _ = storagej
    var caches8: Bool = false
    var templatesC: [Any]! = [78, 78, 84]
   withUnsafeMutablePointer(to: &templatesC) { pointer in
    
   }
   repeat {
      caches8 = templatesC.count < 58 && storagej == (String(cString:[100,0], encoding: .utf8)!)
      if caches8 ? !caches8 : caches8 {
         break
      }
   } while (5 > templatesC.count) && (caches8 ? !caches8 : caches8)
      caches8 = (templatesC.contains { $0 as? Bool == caches8 })
   repeat {
       var williamsJ: String! = String(cString: [107,101,121,102,114,97,109,101,0], encoding: .utf8)!
       var key8: String! = String(cString: [99,111,110,116,114,105,98,117,116,105,110,103,0], encoding: .utf8)!
      repeat {
         key8.append("\(key8.count >> (Swift.min(5, williamsJ.count)))")
         if 3776334 == key8.count {
            break
         }
      } while (3776334 == key8.count) && (key8.count >= 3)
         williamsJ = "\(2)"
          var fnewsB: String! = String(cString: [108,97,117,110,99,104,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &fnewsB) { pointer in
    
         }
         key8 = "\(key8.count + 2)"
         fnewsB = "\(williamsJ.count)"
       var leadingH: Bool = true
      while (williamsJ.count == 1) {
          var itemR: Double = 0.0
         key8.append("\(1 << (Swift.min(2, williamsJ.count)))")
         itemR += Double(williamsJ.count)
         break
      }
         williamsJ = "\(1)"
      storagej = "\(templatesC.count & williamsJ.count)"
      if (String(cString:[107,111,100,54,108,101,122,118,50,103,0], encoding: .utf8)!) == storagej {
         break
      }
   } while (caches8) && ((String(cString:[107,111,100,54,108,101,122,118,50,103,0], encoding: .utf8)!) == storagej)
   return templatesC

}





    override func viewDidLoad() {

         var vmatrixStsc: [Any]! = rawEmotionLeftDomainImage()

      let vmatrixStsc_len = vmatrixStsc.count
      vmatrixStsc.enumerated().forEach({ (index,obj) in
          if index  <=  86 {
                          print(obj)
          }
      })

withUnsafeMutablePointer(to: &vmatrixStsc) { pointer in
        _ = pointer.pointee
}


       var loadingJ: [Any]! = [51, 48]
    _ = loadingJ
   for _ in 0 ..< 2 {
      loadingJ.append(loadingJ.count)
   }

        super.viewDidLoad()

        setupContent()
    }

@discardableResult
 func contentTranslucentAxisScrollView(signupStored: [Any]!, sourceNames: Bool) -> UIScrollView! {
    var pendingb: Bool = true
    var mailm: [String: Any]! = [String(cString: [104,97,108,102,100,0], encoding: .utf8)!:37, String(cString: [109,97,120,105,109,117,109,0], encoding: .utf8)!:41, String(cString: [98,108,101,101,100,0], encoding: .utf8)!:60]
   while (1 >= (mailm.keys.count - 5)) {
       var class_8m: String! = String(cString: [97,108,105,103,110,105,110,103,0], encoding: .utf8)!
       var appearancex: Float = 3.0
       _ = appearancex
       var deletedO: Float = 4.0
      withUnsafeMutablePointer(to: &deletedO) { pointer in
             _ = pointer.pointee
      }
       var bottleu: String! = String(cString: [97,114,105,116,104,0], encoding: .utf8)!
       var p_count9: Int = 4
      withUnsafeMutablePointer(to: &p_count9) { pointer in
    
      }
      if (p_count9 % (Swift.max(class_8m.count, 3))) <= 2 || 4 <= (p_count9 % 2) {
          var privacys: String! = String(cString: [109,97,116,116,101,100,0], encoding: .utf8)!
          var attributesB: String! = String(cString: [100,101,116,97,105,108,0], encoding: .utf8)!
          var f_bounds7: Double = 0.0
         p_count9 -= 3
         privacys.append("\(class_8m.count % 1)")
         attributesB.append("\(attributesB.count)")
         f_bounds7 += (Double(Int(f_bounds7 > 214265556.0 || f_bounds7 < -214265556.0 ? 90.0 : f_bounds7) >> (Swift.min(attributesB.count, 1))))
      }
         bottleu = "\(p_count9)"
      while (2.3 == (appearancex + Float(class_8m.count))) {
         appearancex += Float(class_8m.count % (Swift.max(3, p_count9)))
         break
      }
         p_count9 -= (class_8m == (String(cString:[116,0], encoding: .utf8)!) ? class_8m.count : Int(deletedO > 364147723.0 || deletedO < -364147723.0 ? 94.0 : deletedO))
      while (4.43 > (deletedO - 3.57)) {
         bottleu.append("\(bottleu.count ^ 3)")
         break
      }
          var bannerE: Int = 0
          var leadingL: String! = String(cString: [110,111,116,99,104,0], encoding: .utf8)!
          var z_productsV: Double = 0.0
         deletedO /= Swift.max(Float(bottleu.count), 5)
         bannerE -= (Int(appearancex > 77667333.0 || appearancex < -77667333.0 ? 9.0 : appearancex))
         leadingL = "\((1 % (Swift.max(4, Int(z_productsV > 96157479.0 || z_productsV < -96157479.0 ? 66.0 : z_productsV)))))"
         z_productsV -= (Double(Int(appearancex > 95815831.0 || appearancex < -95815831.0 ? 27.0 : appearancex)))
          var friend_qi: Float = 5.0
          _ = friend_qi
         deletedO /= Swift.max(1, (Float(Int(friend_qi > 377908430.0 || friend_qi < -377908430.0 ? 19.0 : friend_qi) ^ p_count9)))
      for _ in 0 ..< 1 {
          var arlanN: String! = String(cString: [100,105,115,112,97,116,99,104,105,110,103,0], encoding: .utf8)!
          var pressd: Double = 1.0
          var product4: String! = String(cString: [114,101,97,100,121,0], encoding: .utf8)!
          _ = product4
         appearancex += (Float(Int(pressd > 287960750.0 || pressd < -287960750.0 ? 29.0 : pressd)))
         arlanN.append("\(product4.count)")
         product4.append("\(3 - p_count9)")
      }
       var emptyk: Double = 3.0
       var current6: Double = 5.0
         emptyk /= Swift.max((Double(Int(deletedO > 306017889.0 || deletedO < -306017889.0 ? 82.0 : deletedO) + Int(appearancex > 279300800.0 || appearancex < -279300800.0 ? 31.0 : appearancex))), 5)
      for _ in 0 ..< 2 {
          var package6: String! = String(cString: [102,105,110,103,101,114,112,114,105,110,116,0], encoding: .utf8)!
         deletedO *= (Float(Int(emptyk > 274893051.0 || emptyk < -274893051.0 ? 84.0 : emptyk)))
         package6.append("\(package6.count)")
      }
         bottleu.append("\((Int(appearancex > 274229162.0 || appearancex < -274229162.0 ? 1.0 : appearancex) * 2))")
      if 2.36 < (emptyk / (Swift.max(5.80, 6))) && (emptyk / 5.80) < 4.62 {
         emptyk += (Double(class_8m == (String(cString:[78,0], encoding: .utf8)!) ? class_8m.count : Int(deletedO > 64969790.0 || deletedO < -64969790.0 ? 45.0 : deletedO)))
      }
      for _ in 0 ..< 2 {
         appearancex /= Swift.max(1, (Float(bottleu.count - Int(emptyk > 223649073.0 || emptyk < -223649073.0 ? 83.0 : emptyk))))
      }
         current6 += Double(2)
      pendingb = bottleu == (String(cString:[101,0], encoding: .utf8)!)
      break
   }
   repeat {
       var baseZ: Double = 5.0
      withUnsafeMutablePointer(to: &baseZ) { pointer in
    
      }
       var followingV: Float = 0.0
      withUnsafeMutablePointer(to: &followingV) { pointer in
             _ = pointer.pointee
      }
       var applicationt: String! = String(cString: [115,101,103,109,101,110,116,0], encoding: .utf8)!
       var k_widthU: Double = 3.0
       var malez: String! = String(cString: [103,114,97,110,117,108,97,114,105,116,121,0], encoding: .utf8)!
      repeat {
          var buildc: String! = String(cString: [115,116,97,109,112,115,0], encoding: .utf8)!
          var backq: Int = 4
          _ = backq
          var containerY: Bool = true
          var reuseX: Double = 5.0
         withUnsafeMutablePointer(to: &reuseX) { pointer in
                _ = pointer.pointee
         }
          var pricec: [Any]! = [36, 67, 12]
          _ = pricec
         baseZ /= Swift.max(4, (Double(Int(reuseX > 369319174.0 || reuseX < -369319174.0 ? 41.0 : reuseX))))
         buildc = "\((2 / (Swift.max(Int(baseZ > 257022095.0 || baseZ < -257022095.0 ? 86.0 : baseZ), 8))))"
         backq += backq
         containerY = 94.39 <= baseZ
         pricec.append(1 ^ applicationt.count)
         if baseZ == 129647.0 {
            break
         }
      } while (baseZ == 129647.0) && (2.73 == (Double(followingV / (Swift.max(10, Float(2))))))
      if 4.3 <= (followingV * Float(applicationt.count)) {
         applicationt.append("\((Int(k_widthU > 126787770.0 || k_widthU < -126787770.0 ? 41.0 : k_widthU)))")
      }
      for _ in 0 ..< 1 {
         applicationt = "\(2)"
      }
          var stackh: String! = String(cString: [115,110,100,105,111,0], encoding: .utf8)!
          var baseD: Bool = true
         applicationt.append("\(((String(cString:[118,0], encoding: .utf8)!) == stackh ? Int(k_widthU > 110094832.0 || k_widthU < -110094832.0 ? 37.0 : k_widthU) : stackh.count))")
         baseD = !baseD
      repeat {
          var scene_nz: String! = String(cString: [115,105,122,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &scene_nz) { pointer in
    
         }
          var layoutO: Int = 2
         applicationt.append("\((Int(baseZ > 78445560.0 || baseZ < -78445560.0 ? 39.0 : baseZ) * scene_nz.count))")
         layoutO -= 1
         if applicationt.count == 1629045 {
            break
         }
      } while (2 == (5 * applicationt.count)) && (applicationt.count == 1629045)
         followingV -= Float(malez.count)
          var avatarG: String! = String(cString: [112,101,101,114,99,111,110,110,101,99,116,105,111,110,105,110,116,101,114,102,97,99,101,0], encoding: .utf8)!
          _ = avatarG
          var sheetL: Float = 3.0
         baseZ += (Double(Int(k_widthU > 117703366.0 || k_widthU < -117703366.0 ? 16.0 : k_widthU) * Int(baseZ > 56485689.0 || baseZ < -56485689.0 ? 25.0 : baseZ)))
         avatarG = "\(((String(cString:[66,0], encoding: .utf8)!) == avatarG ? Int(sheetL > 127635342.0 || sheetL < -127635342.0 ? 98.0 : sheetL) : avatarG.count))"
         sheetL -= (Float(malez == (String(cString:[98,0], encoding: .utf8)!) ? Int(k_widthU > 211521924.0 || k_widthU < -211521924.0 ? 23.0 : k_widthU) : malez.count))
         followingV += Float(applicationt.count % 2)
       var leftP: String! = String(cString: [112,114,101,115,101,110,116,101,114,0], encoding: .utf8)!
      if 2.7 < (3.87 * followingV) {
         followingV += Float(applicationt.count)
      }
       var displayh: String! = String(cString: [103,97,109,109,97,102,105,108,116,101,114,0], encoding: .utf8)!
       var unlocked0: String! = String(cString: [113,117,97,108,105,116,121,0], encoding: .utf8)!
         k_widthU -= Double(3)
       var postsr: Bool = false
       var state1: Bool = true
       _ = state1
       var updatedX: [String: Any]! = [String(cString: [105,102,97,115,116,0], encoding: .utf8)!:40, String(cString: [109,97,100,101,0], encoding: .utf8)!:84, String(cString: [110,111,109,105,110,97,116,111,114,115,0], encoding: .utf8)!:25]
         updatedX = ["\(updatedX.values.count)": (Int(followingV > 47265785.0 || followingV < -47265785.0 ? 44.0 : followingV))]
         leftP.append("\(((state1 ? 1 : 4) << (Swift.min(2, labs(2)))))")
         displayh = "\(((String(cString:[104,0], encoding: .utf8)!) == malez ? (state1 ? 5 : 3) : malez.count))"
         unlocked0.append("\(((state1 ? 2 : 1)))")
         postsr = !state1
      mailm["\(k_widthU)"] = (Int(baseZ > 255789948.0 || baseZ < -255789948.0 ? 94.0 : baseZ) | 1)
      if 3117082 == mailm.count {
         break
      }
   } while (3117082 == mailm.count) && (2 == mailm.count)
     var checkTable: UIImageView! = UIImageView(frame:CGRect(x: 58, y: 386, width: 0, height: 0))
     let moodTest: [Any]! = [52, 38, 60]
     let durationOverlay: UIView! = UIView(frame:CGRect.zero)
    var latencyInlink = UIScrollView(frame:CGRect(x: 279, y: 269, width: 0, height: 0))
    latencyInlink.alwaysBounceHorizontal = false
    latencyInlink.showsVerticalScrollIndicator = true
    latencyInlink.showsHorizontalScrollIndicator = false
    latencyInlink.delegate = nil
    latencyInlink.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    latencyInlink.alwaysBounceVertical = true
    latencyInlink.alpha = 0.1;
    latencyInlink.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    latencyInlink.frame = CGRect(x: 256, y: 278, width: 0, height: 0)
    checkTable.frame = CGRect(x: 92, y: 235, width: 0, height: 0)
    checkTable.alpha = 0.7;
    checkTable.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    checkTable.animationRepeatCount = 3
    checkTable.image = UIImage(named:String(cString: [108,111,97,100,0], encoding: .utf8)!)
    checkTable.contentMode = .scaleAspectFit
    
    latencyInlink.addSubview(checkTable)
    durationOverlay.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    durationOverlay.alpha = 0.7
    durationOverlay.frame = CGRect(x: 222, y: 316, width: 0, height: 0)
    
    latencyInlink.addSubview(durationOverlay)

    
    return latencyInlink

}





    override func viewDidAppear(_ animated: Bool) {

         let libcodecFtvdoc: UIScrollView! = contentTranslucentAxisScrollView(signupStored:[UILabel()], sourceNames:true)

      if libcodecFtvdoc != nil {
          let libcodecFtvdoc_tag = libcodecFtvdoc.tag
          self.view.addSubview(libcodecFtvdoc)
      }

_ = libcodecFtvdoc


       var primary9: String! = String(cString: [112,114,105,110,116,102,0], encoding: .utf8)!
    var signo: Int = 3
   repeat {
      primary9 = "\((primary9 == (String(cString:[65,0], encoding: .utf8)!) ? primary9.count : signo))"
      if 1318830 == primary9.count {
         break
      }
   } while (1318830 == primary9.count) && (primary9.hasPrefix("\(signo)"))

   repeat {
      primary9.append("\(2 ^ signo)")
      if primary9.count == 1919969 {
         break
      }
   } while (primary9.count == 1919969) && ((primary9.count * 1) <= 4 || (signo * 1) <= 3)
        super.viewDidAppear(animated)

        checkMutualFollowIfNeeded()
    }

    deinit {
        stopCaptureSession()
    }

@discardableResult
 func stopResultPageContinuation(signHandle: Double, priceArlan: Int, badgeCleaner: Double) -> Double {
    var state4: String! = String(cString: [116,109,112,0], encoding: .utf8)!
    var updatedW: [Any]! = [90, 69, 100]
    var rightk: Double = 3.0
       var sendF: String! = String(cString: [114,97,110,103,101,0], encoding: .utf8)!
       _ = sendF
          var settingC: [Any]! = [57.0]
          var listv: Float = 4.0
          var keyboardr: Float = 2.0
         sendF = "\(settingC.count << (Swift.min(labs(2), 4)))"
         listv /= Swift.max(Float(1), 1)
         keyboardr += Float(1)
         sendF.append("\(sendF.count ^ 3)")
      if sendF != sendF {
         sendF = "\(sendF.count)"
      }
      rightk -= (Double((String(cString:[70,0], encoding: .utf8)!) == sendF ? Int(rightk > 345925383.0 || rightk < -345925383.0 ? 72.0 : rightk) : sendF.count))
      updatedW = [updatedW.count]
   repeat {
       var controlr: String! = String(cString: [105,112,99,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &controlr) { pointer in
             _ = pointer.pointee
      }
       var moodR: String! = String(cString: [104,97,100,0], encoding: .utf8)!
       var local__r: Double = 1.0
       var userh: [Any]! = [64, 68]
          var savedE: String! = String(cString: [102,105,110,100,101,114,0], encoding: .utf8)!
          var emptyh: [Any]! = [53, 21, 25]
         moodR = "\(controlr.count)"
         savedE.append("\(controlr.count << (Swift.min(labs(2), 5)))")
         emptyh.append((Int(local__r > 378345061.0 || local__r < -378345061.0 ? 82.0 : local__r) / 1))
      while (moodR.contains(controlr)) {
          var itemsV: String! = String(cString: [115,105,102,116,0], encoding: .utf8)!
          _ = itemsV
          var contacte: [Any]! = [5, 21]
          _ = contacte
          var endP: String! = String(cString: [99,111,117,110,116,100,111,119,110,0], encoding: .utf8)!
          var commentg: Int = 3
         controlr.append("\(userh.count)")
         itemsV.append("\(3 + userh.count)")
         contacte.append(userh.count & 1)
         endP = "\(controlr.count >> (Swift.min(moodR.count, 3)))"
         commentg >>= Swift.min(2, labs(moodR.count | 1))
         break
      }
      for _ in 0 ..< 1 {
         local__r += (Double(moodR == (String(cString:[87,0], encoding: .utf8)!) ? Int(local__r > 185988593.0 || local__r < -185988593.0 ? 87.0 : local__r) : moodR.count))
      }
       var currentC: Int = 0
         moodR = "\(((String(cString:[121,0], encoding: .utf8)!) == moodR ? moodR.count : userh.count))"
          var perlaH: String! = String(cString: [110,111,110,110,101,103,0], encoding: .utf8)!
          var t_playerB: String! = String(cString: [103,108,111,98,97,108,108,121,0], encoding: .utf8)!
          _ = t_playerB
          var markD: [Any]! = [70, 25, 52]
         userh = [(controlr == (String(cString:[79,0], encoding: .utf8)!) ? perlaH.count : controlr.count)]
         t_playerB = "\(3 << (Swift.min(2, labs(currentC))))"
         markD = [1]
      if currentC == controlr.count {
         currentC %= Swift.max(3, controlr.count - 2)
      }
         currentC += (Int(local__r > 127982096.0 || local__r < -127982096.0 ? 99.0 : local__r) / 2)
      for _ in 0 ..< 3 {
          var info6: [String: Any]! = [String(cString: [100,101,110,111,114,109,97,108,0], encoding: .utf8)!:String(cString: [111,114,100,101,114,115,0], encoding: .utf8)!, String(cString: [109,101,109,0], encoding: .utf8)!:String(cString: [99,108,111,115,101,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &info6) { pointer in
                _ = pointer.pointee
         }
          var anxiousH: Double = 0.0
         withUnsafeMutablePointer(to: &anxiousH) { pointer in
                _ = pointer.pointee
         }
         controlr.append("\(3 | moodR.count)")
         info6 = [moodR: currentC + 2]
         anxiousH -= Double(userh.count)
      }
         currentC &= 2
      if (Int(local__r > 72519894.0 || local__r < -72519894.0 ? 96.0 : local__r) * controlr.count) > 2 {
         controlr.append("\(moodR.count & 3)")
      }
      if currentC < 1 {
         currentC &= userh.count
      }
      rightk += (Double(moodR.count | Int(rightk > 319025244.0 || rightk < -319025244.0 ? 52.0 : rightk)))
      if rightk == 3480672.0 {
         break
      }
   } while (rightk == 3480672.0) && (4 > updatedW.count)
   if 5 <= state4.count {
      state4 = "\(2)"
   }
   return rightk

}





    private func setupContent() {

         let audioconvertProperty: Double = stopResultPageContinuation(signHandle:22.0, priceArlan:95, badgeCleaner:0.0)

      print(audioconvertProperty)

_ = audioconvertProperty


       var authj: Bool = false
   withUnsafeMutablePointer(to: &authj) { pointer in
          _ = pointer.pointee
   }
       var messageN: Double = 4.0
      if (2.11 * messageN) >= 5.67 || (messageN * 2.11) >= 3.65 {
         messageN -= Double(1)
      }
      while (messageN < messageN) {
          var removeh: Float = 2.0
          var speaker3: [Any]! = [String(cString: [102,105,110,100,0], encoding: .utf8)!, String(cString: [115,107,105,112,112,97,98,108,101,0], encoding: .utf8)!]
          var overlapy: String! = String(cString: [99,114,111,115,115,104,97,105,114,115,0], encoding: .utf8)!
          var launchq: String! = String(cString: [115,117,98,102,114,97,109,101,115,0], encoding: .utf8)!
         messageN += Double(2 << (Swift.min(4, speaker3.count)))
         removeh -= Float(launchq.count & 1)
         overlapy = "\(1)"
         launchq.append("\(((String(cString:[90,0], encoding: .utf8)!) == overlapy ? overlapy.count : launchq.count))")
         break
      }
          var rememberd: Int = 0
         withUnsafeMutablePointer(to: &rememberd) { pointer in
                _ = pointer.pointee
         }
          var recommendO: String! = String(cString: [115,99,97,108,101,102,97,99,116,111,114,115,0], encoding: .utf8)!
          var delegate_9j4: Double = 5.0
          _ = delegate_9j4
         messageN += (Double(recommendO == (String(cString:[104,0], encoding: .utf8)!) ? recommendO.count : Int(messageN > 273242884.0 || messageN < -273242884.0 ? 13.0 : messageN)))
         rememberd -= (Int(delegate_9j4 > 337079731.0 || delegate_9j4 < -337079731.0 ? 80.0 : delegate_9j4) % 2)
         delegate_9j4 += (Double(Int(messageN > 163166814.0 || messageN < -163166814.0 ? 96.0 : messageN) / 2))
      authj = !authj

        view.backgroundColor = UIColor(red: 0.96, green: 0.96, blue: 0.97, alpha: 1)

        let inputView = UIImageView(image: UIImage(named: PSFCompanionFriendI.topBackground))
        inputView.contentMode = .scaleToFill
        inputView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(inputView)

        let insufficientButton = UIButton(type: .custom)
        insufficientButton.setImage(UIImage(named: PSFCompanionFriendI.backIcon), for: .normal)
        insufficientButton.imageView?.contentMode = .scaleAspectFit
        insufficientButton.addTarget(self, action: #selector(closePage), for: .touchUpInside)
        insufficientButton.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(insufficientButton)

        let delete_rLabel = UILabel()
        delete_rLabel.text = friend.name
        delete_rLabel.font = .systemFont(ofSize: 23, weight: .regular)
        delete_rLabel.textColor = .black
        delete_rLabel.textAlignment = .center
        delete_rLabel.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(delete_rLabel)

        let cacheView = UIView()
        cacheView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(cacheView)

        let sloganLabel = UILabel()
        sloganLabel.text = "Connecting..."
        sloganLabel.font = .systemFont(ofSize: 16, weight: .regular)
        sloganLabel.textColor = UIColor(red: 0.48, green: 0.48, blue: 0.49, alpha: 1)
        sloganLabel.textAlignment = .center
        sloganLabel.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(sloganLabel)

        let friend_lmView = UIImageView(image: UIImage(named: PSFCompanionFriendI.avatarRing))
        friend_lmView.contentMode = .scaleAspectFit
        friend_lmView.translatesAutoresizingMaskIntoConstraints = false
        cacheView.addSubview(friend_lmView)

        let pickerView = UIView()
        pickerView.backgroundColor = friend.avatarColor
        pickerView.layer.cornerRadius = 72
        pickerView.layer.masksToBounds = true
        pickerView.translatesAutoresizingMaskIntoConstraints = false
        cacheView.addSubview(pickerView)

        let nameLabelA = UILabel()
        nameLabelA.text = String(friend.name.prefix(1))
        nameLabelA.font = .systemFont(ofSize: 52, weight: .semibold)
        nameLabelA.textColor = .white
        nameLabelA.textAlignment = .center
        nameLabelA.translatesAutoresizingMaskIntoConstraints = false
        pickerView.addSubview(nameLabelA)

        let m_viewButton = makeToggleButton(normalImageName: PSFCompanionFriendI.speakerNormal, selectedImageName: PSFCompanionFriendI.speakerSelected)
        m_viewButton.isSelected = true
        m_viewButton.addTarget(self, action: #selector(toggleCallButton(_:)), for: .touchUpInside)
        view.addSubview(m_viewButton)

        let bottleButton = makeControlButton(imageName: PSFCompanionFriendI.hangupButton)
        bottleButton.addTarget(self, action: #selector(closePage), for: .touchUpInside)
        view.addSubview(bottleButton)
        startHangupShake(on: bottleButton)

        let emailButton = makeToggleButton(normalImageName: PSFCompanionFriendI.micNormal, selectedImageName: PSFCompanionFriendI.micSelected)
        emailButton.isSelected = true
        emailButton.addTarget(self, action: #selector(toggleCallButton(_:)), for: .touchUpInside)
        view.addSubview(emailButton)

        NSLayoutConstraint.activate([
            inputView.topAnchor.constraint(equalTo: view.topAnchor),
            inputView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            inputView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            inputView.heightAnchor.constraint(equalToConstant: 180),

            insufficientButton.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 13),
            insufficientButton.topAnchor.constraint(equalTo: view.safeAreaLayoutGuide.topAnchor, constant: 3),
            insufficientButton.widthAnchor.constraint(equalToConstant: 44),
            insufficientButton.heightAnchor.constraint(equalToConstant: 44),

            delete_rLabel.centerXAnchor.constraint(equalTo: view.centerXAnchor),
            delete_rLabel.centerYAnchor.constraint(equalTo: insufficientButton.centerYAnchor),

            cacheView.centerXAnchor.constraint(equalTo: view.centerXAnchor),
            cacheView.topAnchor.constraint(equalTo: view.topAnchor, constant: 210),
            cacheView.widthAnchor.constraint(equalToConstant: 234.79),
            cacheView.heightAnchor.constraint(equalToConstant: 207.24),

            friend_lmView.centerXAnchor.constraint(equalTo: cacheView.centerXAnchor),
            friend_lmView.centerYAnchor.constraint(equalTo: cacheView.centerYAnchor),
            friend_lmView.widthAnchor.constraint(equalToConstant: 234.79),
            friend_lmView.heightAnchor.constraint(equalToConstant: 207.24),

            pickerView.leadingAnchor.constraint(equalTo: cacheView.leadingAnchor, constant: 29),
            pickerView.topAnchor.constraint(equalTo: cacheView.topAnchor, constant: 29),
            pickerView.widthAnchor.constraint(equalToConstant: 141.94),
            pickerView.heightAnchor.constraint(equalToConstant: 141.94),

            nameLabelA.centerXAnchor.constraint(equalTo: pickerView.centerXAnchor),
            nameLabelA.centerYAnchor.constraint(equalTo: pickerView.centerYAnchor),

            sloganLabel.centerXAnchor.constraint(equalTo: view.centerXAnchor),
            sloganLabel.topAnchor.constraint(equalTo: cacheView.bottomAnchor, constant: 24),

            m_viewButton.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 50),
            m_viewButton.topAnchor.constraint(equalTo: view.topAnchor, constant: 570),
            m_viewButton.widthAnchor.constraint(equalToConstant: 48),
            m_viewButton.heightAnchor.constraint(equalToConstant: 48),

            bottleButton.centerXAnchor.constraint(equalTo: view.centerXAnchor),
            bottleButton.centerYAnchor.constraint(equalTo: m_viewButton.centerYAnchor),
            bottleButton.widthAnchor.constraint(equalToConstant: 48),
            bottleButton.heightAnchor.constraint(equalToConstant: 48),

            emailButton.trailingAnchor.constraint(equalTo: view.trailingAnchor, constant: -50),
            emailButton.centerYAnchor.constraint(equalTo: m_viewButton.centerYAnchor),
            emailButton.widthAnchor.constraint(equalToConstant: 48),
            emailButton.heightAnchor.constraint(equalToConstant: 48)
        ])
    }

@discardableResult
 func backgroundEmotionBackMapObservationView(boundsAction: String!, taskNickname: Double) -> UIView! {
    var headerK: [String: Any]! = [String(cString: [104,97,100,100,0], encoding: .utf8)!:5]
    var delegate_jp: String! = String(cString: [118,116,101,110,99,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      headerK["\(delegate_jp)"] = headerK.values.count >> (Swift.min(labs(1), 3))
   }
       var followingn: String! = String(cString: [102,114,97,103,109,101,110,116,0], encoding: .utf8)!
       var messagesd: Float = 1.0
         messagesd /= Swift.max(4, (Float((String(cString:[116,0], encoding: .utf8)!) == followingn ? Int(messagesd > 99056801.0 || messagesd < -99056801.0 ? 47.0 : messagesd) : followingn.count)))
       var urlW: String! = String(cString: [102,108,101,120,0], encoding: .utf8)!
       var presentM: String! = String(cString: [117,116,109,111,115,116,0], encoding: .utf8)!
       _ = presentM
         urlW.append("\((urlW == (String(cString:[56,0], encoding: .utf8)!) ? urlW.count : Int(messagesd > 341071450.0 || messagesd < -341071450.0 ? 42.0 : messagesd)))")
         followingn.append("\(presentM.count % (Swift.max(3, followingn.count)))")
         messagesd += (Float(presentM == (String(cString:[108,0], encoding: .utf8)!) ? presentM.count : followingn.count))
       var delegate_vop: Int = 2
         delegate_vop %= Swift.max((Int(messagesd > 261049614.0 || messagesd < -261049614.0 ? 89.0 : messagesd)), 1)
      headerK = [delegate_jp: (followingn == (String(cString:[74,0], encoding: .utf8)!) ? followingn.count : delegate_jp.count)]
     var mainAngry: Double = 32.0
     var usersPerla: Bool = true
    var snapshotsPenalty = UIView()
    snapshotsPenalty.frame = CGRect(x: 66, y: 185, width: 0, height: 0)
    snapshotsPenalty.alpha = 0.0;
    snapshotsPenalty.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return snapshotsPenalty

}





    private func makeControlButton(imageName: String) -> UIButton {

         let publisherColons: UIView! = backgroundEmotionBackMapObservationView(boundsAction:String(cString: [97,99,107,115,0], encoding: .utf8)!, taskNickname:99.0)

      if publisherColons != nil {
          self.view.addSubview(publisherColons)
          let publisherColons_tag = publisherColons.tag
      }
      else {
          print("publisherColons is nil")      }

_ = publisherColons


       var appearancek: String! = String(cString: [98,102,114,97,99,116,105,111,110,0], encoding: .utf8)!
    _ = appearancek
    var settings0: Float = 2.0
   while (3.46 >= (settings0 / (Swift.max(3, Float(appearancek.count)))) || 3.46 >= (settings0 / (Swift.max(Float(appearancek.count), 1)))) {
       var delete_5_: String! = String(cString: [116,119,111,102,105,115,104,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &delete_5_) { pointer in
             _ = pointer.pointee
      }
       var profileX: Bool = true
      withUnsafeMutablePointer(to: &profileX) { pointer in
             _ = pointer.pointee
      }
      while (profileX || delete_5_.count < 5) {
         profileX = delete_5_.count >= 41 && profileX
         break
      }
      if delete_5_.count > 1 && profileX {
         delete_5_ = "\(delete_5_.count)"
      }
      repeat {
         profileX = delete_5_.count == 32
         if profileX ? !profileX : profileX {
            break
         }
      } while (profileX ? !profileX : profileX) && (4 >= delete_5_.count)
      while (profileX) {
          var productF: Float = 5.0
          var presentation4: String! = String(cString: [115,105,110,101,119,105,110,0], encoding: .utf8)!
          var file4: String! = String(cString: [112,97,114,115,101,100,0], encoding: .utf8)!
          _ = file4
          var size_l_: [String: Any]! = [String(cString: [101,120,99,108,117,100,105,110,103,0], encoding: .utf8)!:47, String(cString: [109,101,116,97,0], encoding: .utf8)!:72]
          var indexJ: String! = String(cString: [114,101,116,114,97,110,115,109,105,116,0], encoding: .utf8)!
         profileX = indexJ.count < 80
         productF *= Float(indexJ.count)
         presentation4.append("\(3)")
         file4.append("\(1)")
         size_l_ = [file4: file4.count << (Swift.min(presentation4.count, 3))]
         break
      }
       var dialogE: Double = 2.0
      withUnsafeMutablePointer(to: &dialogE) { pointer in
             _ = pointer.pointee
      }
       var providerf: Double = 0.0
      withUnsafeMutablePointer(to: &providerf) { pointer in
    
      }
         profileX = !delete_5_.hasSuffix("\(profileX)")
         dialogE += Double(delete_5_.count + 1)
         providerf /= Swift.max((Double(delete_5_.count - Int(dialogE > 173105306.0 || dialogE < -173105306.0 ? 83.0 : dialogE))), 5)
      appearancek.append("\(((String(cString:[75,0], encoding: .utf8)!) == appearancek ? Int(settings0 > 243087110.0 || settings0 < -243087110.0 ? 89.0 : settings0) : appearancek.count))")
      break
   }

      appearancek = "\(((String(cString:[113,0], encoding: .utf8)!) == appearancek ? appearancek.count : Int(settings0 > 12588079.0 || settings0 < -12588079.0 ? 83.0 : settings0)))"
        let taglineButton = UIButton(type: .custom)
        taglineButton.setImage(UIImage(named: imageName), for: .normal)
        taglineButton.imageView?.contentMode = .scaleAspectFit
        taglineButton.translatesAutoresizingMaskIntoConstraints = false
        return taglineButton
    }

@discardableResult
 func gradientCubicCurrentInteractionLabelLarge() -> Float {
    var bottlew: Bool = true
    _ = bottlew
    var coins2: Bool = false
    var modityd: Float = 0.0
       var sharingu: Bool = true
       var saved1: Int = 2
       var product7: Bool = false
      for _ in 0 ..< 1 {
         product7 = !product7
      }
       var authr: Bool = true
       var privacyc: Bool = false
       _ = privacyc
      repeat {
          var storeT: String! = String(cString: [115,99,114,101,101,110,115,104,97,114,101,0], encoding: .utf8)!
          var n_badgeC: String! = String(cString: [115,101,101,107,0], encoding: .utf8)!
          var inputd: String! = String(cString: [113,117,101,115,116,105,111,110,0], encoding: .utf8)!
          var w_boundsr: String! = String(cString: [99,111,108,108,97,116,105,110,103,0], encoding: .utf8)!
          var rechargeU: [String: Any]! = [String(cString: [112,114,111,100,117,99,116,102,0], encoding: .utf8)!:27, String(cString: [117,112,103,114,97,100,101,0], encoding: .utf8)!:25, String(cString: [109,101,115,104,101,115,0], encoding: .utf8)!:25]
         privacyc = product7
         storeT.append("\(((sharingu ? 5 : 1)))")
         n_badgeC.append("\(1)")
         inputd = "\(rechargeU.values.count & inputd.count)"
         w_boundsr = "\(w_boundsr.count)"
         rechargeU = [n_badgeC: n_badgeC.count >> (Swift.min(labs(1), 1))]
         if privacyc ? !privacyc : privacyc {
            break
         }
      } while (privacyc ? !privacyc : privacyc) && (!sharingu)
         authr = sharingu
      for _ in 0 ..< 2 {
          var w_productsJ: Int = 0
          var prefix_a6: Double = 5.0
          var nicknameL: Int = 5
          _ = nicknameL
          var emotionsi: String! = String(cString: [122,112,98,105,113,117,97,100,115,0], encoding: .utf8)!
          var checkedv: [Any]! = [String(cString: [115,116,114,108,99,112,121,0], encoding: .utf8)!, String(cString: [100,99,116,120,100,99,0], encoding: .utf8)!, String(cString: [100,114,97,103,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &checkedv) { pointer in
    
         }
         product7 = 64 == nicknameL && sharingu
         w_productsJ %= Swift.max((Int(prefix_a6 > 258853215.0 || prefix_a6 < -258853215.0 ? 96.0 : prefix_a6) | 2), 2)
         prefix_a6 -= (Double(1 >> (Swift.min(labs((authr ? 4 : 4)), 4))))
         emotionsi = "\(((privacyc ? 3 : 2) - Int(prefix_a6 > 142409530.0 || prefix_a6 < -142409530.0 ? 9.0 : prefix_a6)))"
         checkedv.append(nicknameL)
      }
       var rowJ: Float = 2.0
       var errort: Float = 5.0
      withUnsafeMutablePointer(to: &errort) { pointer in
    
      }
      for _ in 0 ..< 3 {
          var scrolly: String! = String(cString: [100,97,116,97,99,101,110,116,101,114,0], encoding: .utf8)!
          var storeX: Float = 1.0
          var fansa: String! = String(cString: [100,101,108,97,117,110,97,121,0], encoding: .utf8)!
          var mineU: Int = 4
          var row4: String! = String(cString: [108,105,98,112,104,111,110,101,110,117,109,98,101,114,0], encoding: .utf8)!
          _ = row4
         privacyc = scrolly.count <= 28 && 28 <= row4.count
         storeX += (Float(1 * Int(errort > 247449506.0 || errort < -247449506.0 ? 82.0 : errort)))
         fansa = "\(((String(cString:[108,0], encoding: .utf8)!) == fansa ? Int(storeX > 71394503.0 || storeX < -71394503.0 ? 27.0 : storeX) : fansa.count))"
         mineU >>= Swift.min(labs(2 & scrolly.count), 4)
      }
         saved1 += 2
      for _ in 0 ..< 3 {
          var replyC: String! = String(cString: [100,114,111,112,111,102,102,0], encoding: .utf8)!
          var labelf: Int = 5
          var listY: Bool = true
          var keywordsn: Float = 0.0
         withUnsafeMutablePointer(to: &keywordsn) { pointer in
                _ = pointer.pointee
         }
          var s_layera: String! = String(cString: [112,114,105,110,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &s_layera) { pointer in
                _ = pointer.pointee
         }
         rowJ -= Float(saved1 ^ 2)
         replyC = "\(labelf - 2)"
         labelf >>= Swift.min(5, labs((Int(keywordsn > 32081193.0 || keywordsn < -32081193.0 ? 57.0 : keywordsn) >> (Swift.min(1, labs((sharingu ? 5 : 2)))))))
         listY = !product7
         keywordsn -= (Float(Int(errort > 385182367.0 || errort < -385182367.0 ? 89.0 : errort) / (Swift.max(2, (authr ? 3 : 2)))))
         s_layera.append("\((Int(errort > 265783559.0 || errort < -265783559.0 ? 94.0 : errort) % (Swift.max(5, (product7 ? 5 : 2)))))")
      }
      coins2 = !coins2
   repeat {
      modityd += (Float(3 % (Swift.max(Int(modityd > 240652002.0 || modityd < -240652002.0 ? 57.0 : modityd), 8))))
      if modityd == 2586429.0 {
         break
      }
   } while (modityd == 2586429.0) && (coins2)
       var friend__u: [Any]! = [64, 67, 6]
       var trailingq: Double = 0.0
       var pauseN: String! = String(cString: [104,101,105,103,104,116,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
         pauseN = "\((pauseN == (String(cString:[112,0], encoding: .utf8)!) ? pauseN.count : Int(trailingq > 24535398.0 || trailingq < -24535398.0 ? 67.0 : trailingq)))"
      }
      for _ in 0 ..< 2 {
         friend__u.append((2 * Int(trailingq > 377080642.0 || trailingq < -377080642.0 ? 93.0 : trailingq)))
      }
      repeat {
         trailingq -= Double(friend__u.count)
         if trailingq == 4833758.0 {
            break
         }
      } while (trailingq == 4833758.0) && (2.53 == (trailingq / (Swift.max(Double(friend__u.count), 7))))
         friend__u = [1]
      repeat {
         trailingq -= Double(friend__u.count)
         if 4191620.0 == trailingq {
            break
         }
      } while (1.30 == (trailingq + Double(pauseN.count))) && (4191620.0 == trailingq)
      for _ in 0 ..< 2 {
          var closeS: String! = String(cString: [112,114,111,98,108,101,109,115,0], encoding: .utf8)!
          var press5: Double = 3.0
          var userT: Double = 1.0
         withUnsafeMutablePointer(to: &userT) { pointer in
    
         }
          var happyh: String! = String(cString: [99,98,99,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &happyh) { pointer in
                _ = pointer.pointee
         }
          var size_6T: Int = 0
          _ = size_6T
         trailingq += (Double(happyh == (String(cString:[67,0], encoding: .utf8)!) ? happyh.count : Int(userT > 107179981.0 || userT < -107179981.0 ? 42.0 : userT)))
         closeS.append("\((Int(userT > 163225343.0 || userT < -163225343.0 ? 96.0 : userT) / 3))")
         press5 += (Double(3 << (Swift.min(labs(Int(press5 > 209230658.0 || press5 < -209230658.0 ? 75.0 : press5)), 2))))
         size_6T >>= Swift.min(5, labs(happyh.count % (Swift.max(9, friend__u.count))))
      }
      for _ in 0 ..< 2 {
         pauseN = "\(1 / (Swift.max(9, pauseN.count)))"
      }
          var insetsI: [String: Any]! = [String(cString: [114,101,99,105,112,105,101,110,116,115,0], encoding: .utf8)!:24, String(cString: [105,110,108,105,110,107,0], encoding: .utf8)!:13, String(cString: [101,120,116,115,107,0], encoding: .utf8)!:24]
         withUnsafeMutablePointer(to: &insetsI) { pointer in
    
         }
          var insufficientV: Double = 1.0
         withUnsafeMutablePointer(to: &insufficientV) { pointer in
                _ = pointer.pointee
         }
         trailingq /= Swift.max(Double(friend__u.count), 3)
         insetsI["\(insufficientV)"] = 1
         insufficientV += (Double(Int(trailingq > 340081400.0 || trailingq < -340081400.0 ? 10.0 : trailingq)))
         friend__u = [(Int(trailingq > 159340480.0 || trailingq < -159340480.0 ? 72.0 : trailingq))]
      coins2 = !bottlew
   return modityd

}





    private func requestCallPermissionsAndStartSession() {

         let finalizerDisc: Float = gradientCubicCurrentInteractionLabelLarge()

      if finalizerDisc != 32 {
             print(finalizerDisc)
      }

_ = finalizerDisc


       var stateu: Float = 4.0
    _ = stateu
      stateu /= Swift.max((Float(1 * Int(stateu > 22681714.0 || stateu < -22681714.0 ? 81.0 : stateu))), 5)

        AVCaptureDevice.requestAccess(for: .video) { [weak self] videoGranted in
            AVCaptureDevice.requestAccess(for: .audio) { [weak self] audioGranted in
                guard let self, videoGranted, audioGranted else { return }

                self.captureQueue.async {
                    self.configureCaptureSession()
                }
            }
        }
    }

    private func configureCaptureSession() {
       var gestureH: String! = String(cString: [98,97,115,101,0], encoding: .utf8)!
       var signZ: Float = 1.0
       var flash_: Double = 0.0
       var infoQ: [Any]! = [60.0]
         signZ -= Float(2)
      for _ in 0 ..< 3 {
          var arlanJ: Float = 0.0
          _ = arlanJ
          var switch_icx: [String: Any]! = [String(cString: [102,109,116,99,111,110,118,101,114,116,0], encoding: .utf8)!:27, String(cString: [99,111,110,102,105,114,109,0], encoding: .utf8)!:91]
         signZ += (Float(Int(flash_ > 126601037.0 || flash_ < -126601037.0 ? 36.0 : flash_) ^ 1))
         arlanJ += (Float(infoQ.count << (Swift.min(3, labs(Int(flash_ > 10689580.0 || flash_ < -10689580.0 ? 57.0 : flash_))))))
         switch_icx["\(arlanJ)"] = (Int(signZ > 148140197.0 || signZ < -148140197.0 ? 67.0 : signZ))
      }
         flash_ -= Double(3 % (Swift.max(1, infoQ.count)))
      for _ in 0 ..< 3 {
         infoQ = [1]
      }
      repeat {
         flash_ -= (Double(Int(flash_ > 69692423.0 || flash_ < -69692423.0 ? 64.0 : flash_) - Int(signZ > 194780243.0 || signZ < -194780243.0 ? 20.0 : signZ)))
         if 1370528.0 == flash_ {
            break
         }
      } while (1370528.0 == flash_) && (5.45 < (Float(flash_) * signZ) && (Double(signZ) * flash_) < 5.45)
          var nickname3: Bool = false
          var endM: Int = 3
         flash_ /= Swift.max((Double(endM - (nickname3 ? 2 : 2))), 4)
          var rightW: String! = String(cString: [111,112,97,113,117,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &rightW) { pointer in
    
         }
         signZ += Float(rightW.count * 1)
       var nameZ: [Any]! = [76, 76, 35]
       var constraints9: [Any]! = [String(cString: [115,109,97,108,108,101,115,116,0], encoding: .utf8)!, String(cString: [97,110,103,117,108,97,114,0], encoding: .utf8)!]
       _ = constraints9
      for _ in 0 ..< 3 {
          var healingx: String! = String(cString: [101,120,116,114,97,112,111,108,97,116,111,114,0], encoding: .utf8)!
          var syncY: Int = 5
          var yesT: Double = 4.0
          var return_rvf: String! = String(cString: [103,112,116,111,112,116,115,0], encoding: .utf8)!
          var shares: String! = String(cString: [103,101,110,99,98,0], encoding: .utf8)!
          _ = shares
         flash_ += (Double(healingx == (String(cString:[89,0], encoding: .utf8)!) ? constraints9.count : healingx.count))
         syncY -= (healingx == (String(cString:[115,0], encoding: .utf8)!) ? healingx.count : infoQ.count)
         yesT += Double(nameZ.count)
         return_rvf.append("\(nameZ.count)")
         shares.append("\((Int(signZ > 367488657.0 || signZ < -367488657.0 ? 96.0 : signZ)))")
      }
      gestureH = "\((Int(signZ > 280730258.0 || signZ < -280730258.0 ? 76.0 : signZ) + 2))"

        let cancel = AVCaptureSession()
        cancel.beginConfiguration()
        cancel.sessionPreset = .medium

        if
            let hero = AVCaptureDevice.default(.builtInWideAngleCamera, for: .video, position: .front),
            let chat = try? AVCaptureDeviceInput(device: hero),
            cancel.canAddInput(chat) {
            cancel.addInput(chat)
        }

        if
            let setting = AVCaptureDevice.default(for: .audio),
            let w_count = try? AVCaptureDeviceInput(device: setting),
            cancel.canAddInput(w_count) {
            cancel.addInput(w_count)
        }

        cancel.commitConfiguration()
        captureSession = cancel
        cancel.startRunning()
    }

@discardableResult
 func subtitleWideLeadingDisappearValue() -> String! {
    var tagss: String! = String(cString: [116,114,97,110,115,102,101,114,114,105,110,103,0], encoding: .utf8)!
    var j_widtho: String! = String(cString: [108,112,99,108,115,112,0], encoding: .utf8)!
    var sendL: String! = String(cString: [112,114,107,0], encoding: .utf8)!
      tagss.append("\(2 ^ sendL.count)")
      j_widtho = "\(tagss.count)"
   return tagss

}





    private func stopCaptureSession() {

         let branchEvaluated: String! = subtitleWideLeadingDisappearValue()

      if branchEvaluated == "social" {
              print(branchEvaluated)
      }
      let branchEvaluated_len = branchEvaluated?.count ?? 0

_ = branchEvaluated


       var addn: Double = 2.0
      addn /= Swift.max((Double(Int(addn > 358507782.0 || addn < -358507782.0 ? 11.0 : addn))), 5)

        captureQueue.async { [captureSession] in
            captureSession?.stopRunning()
        }
    }

@discardableResult
 func performRelativeReuseBannerFilterButton() -> UIButton! {
    var seedf: Bool = false
    _ = seedf
    var loginy: Double = 0.0
    _ = loginy
       var stackc: String! = String(cString: [100,105,97,103,110,111,115,116,105,99,0], encoding: .utf8)!
       var launchc: String! = String(cString: [101,115,116,105,109,97,116,105,110,103,0], encoding: .utf8)!
          var tapD: String! = String(cString: [97,112,112,108,105,101,114,0], encoding: .utf8)!
          var authJ: String! = String(cString: [115,116,97,110,100,97,108,111,110,101,0], encoding: .utf8)!
          _ = authJ
          var padding9: Double = 0.0
          _ = padding9
         launchc = "\(launchc.count % 1)"
         tapD = "\((tapD == (String(cString:[84,0], encoding: .utf8)!) ? tapD.count : authJ.count))"
         authJ.append("\(1 * authJ.count)")
         padding9 /= Swift.max(Double(stackc.count), 5)
      repeat {
          var headerh: Double = 2.0
          var rowv: Bool = true
          var testE: String! = String(cString: [97,108,105,118,101,0], encoding: .utf8)!
          var release_utv: String! = String(cString: [114,116,109,112,101,0], encoding: .utf8)!
          var screenb: String! = String(cString: [112,108,97,121,101,114,0], encoding: .utf8)!
         launchc = "\(release_utv.count)"
         headerh += (Double(Int(headerh > 229155357.0 || headerh < -229155357.0 ? 52.0 : headerh)))
         testE = "\(2)"
         screenb = "\(release_utv.count)"
         if 265034 == launchc.count {
            break
         }
      } while (265034 == launchc.count) && (stackc != String(cString:[90,0], encoding: .utf8)!)
         stackc = "\(1 * stackc.count)"
      if stackc.contains(launchc) {
          var indicatorh: Double = 0.0
          var indexq: String! = String(cString: [115,105,103,105,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &indexq) { pointer in
                _ = pointer.pointee
         }
          var storeds: String! = String(cString: [100,105,115,112,111,115,101,100,0], encoding: .utf8)!
         launchc.append("\((stackc == (String(cString:[87,0], encoding: .utf8)!) ? stackc.count : Int(indicatorh > 117489542.0 || indicatorh < -117489542.0 ? 39.0 : indicatorh)))")
         indexq = "\(launchc.count / (Swift.max(indexq.count, 8)))"
         storeds.append("\(2 * launchc.count)")
      }
      repeat {
         stackc.append("\(launchc.count >> (Swift.min(1, stackc.count)))")
         if (String(cString:[119,120,101,0], encoding: .utf8)!) == stackc {
            break
         }
      } while (launchc == stackc) && ((String(cString:[119,120,101,0], encoding: .utf8)!) == stackc)
         launchc.append("\(stackc.count)")
      loginy -= Double(1 - stackc.count)
   while (!seedf) {
      loginy /= Swift.max(3, (Double(Int(loginy > 204290795.0 || loginy < -204290795.0 ? 67.0 : loginy) + 3)))
      break
   }
     let errorComments: String! = String(cString: [108,105,116,101,114,97,108,0], encoding: .utf8)!
     var companionsMood: Float = 14.0
     let heightUpdated: Double = 87.0
     let dataDetail: Float = 49.0
    var abiAvframeDownload = UIButton(frame:CGRect.zero)
    abiAvframeDownload.setImage(UIImage(named:String(cString: [115,99,101,110,101,95,101,0], encoding: .utf8)!), for: .normal)
    abiAvframeDownload.setTitle("", for: .normal)
    abiAvframeDownload.setBackgroundImage(UIImage(named:String(cString: [112,95,117,110,108,111,99,107,0], encoding: .utf8)!), for: .normal)
    abiAvframeDownload.titleLabel?.font = UIFont.systemFont(ofSize:13)
    abiAvframeDownload.frame = CGRect(x: 273, y: 131, width: 0, height: 0)
    abiAvframeDownload.alpha = 0.3;
    abiAvframeDownload.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return abiAvframeDownload

}





    private func checkMutualFollowIfNeeded() {

         var centerVal: UIButton! = performRelativeReuseBannerFilterButton()

      if centerVal != nil {
          let centerVal_tag = centerVal.tag
          self.view.addSubview(centerVal)
      }
      else {
          print("centerVal is nil")      }

withUnsafeMutablePointer(to: &centerVal) { pointer in
    
}


       var template_gr: String! = String(cString: [119,97,108,108,0], encoding: .utf8)!
    var text8: [Any]! = [34, 17, 78]
      text8.append(2 & template_gr.count)

        guard !hasCheckedMutualFollow else { return }

      template_gr = "\(template_gr.count << (Swift.min(labs(3), 3)))"
        hasCheckedMutualFollow = true
        guard !SocPackageShare.isMutualFollow(name: friend.name) else {
            requestCallPermissionsAndStartSession()
            return
        }

        stopCaptureSession()
        showMutualFollowCard(
            message: "You can start a video call after you both follow each other."
        ) { [weak self] in
            self?.dismiss(animated: true)
        }
    }

@discardableResult
 func prepareValidSetupAdjustmentDuration(detailFile: Float, listenerPath: Double) -> Bool {
    var angryj: Float = 0.0
    var startt: [String: Any]! = [String(cString: [103,101,116,111,112,116,0], encoding: .utf8)!:87, String(cString: [100,118,100,97,116,97,0], encoding: .utf8)!:94]
    var scene_cX: Bool = false
   withUnsafeMutablePointer(to: &scene_cX) { pointer in
    
   }
   while ((Float(startt.count) + angryj) < 3.3) {
       var paddingz: Int = 3
       var gradient_: String! = String(cString: [117,110,119,105,110,100,0], encoding: .utf8)!
       var agreementI: [String: Any]! = [String(cString: [98,105,110,0], encoding: .utf8)!:43, String(cString: [116,121,112,101,0], encoding: .utf8)!:69, String(cString: [98,108,105,116,0], encoding: .utf8)!:84]
      withUnsafeMutablePointer(to: &agreementI) { pointer in
             _ = pointer.pointee
      }
       var saveA: String! = String(cString: [109,101,109,115,101,116,0], encoding: .utf8)!
       _ = saveA
       var simou: [Any]! = [36, 25]
      withUnsafeMutablePointer(to: &simou) { pointer in
    
      }
         saveA = "\(agreementI.keys.count)"
      while (3 >= (gradient_.count - 1) || 3 >= (1 - paddingz)) {
          var conversationV: Float = 4.0
          var itemF: String! = String(cString: [100,115,104,111,119,0], encoding: .utf8)!
          var u_heightd: [String: Any]! = [String(cString: [119,114,97,112,112,101,100,0], encoding: .utf8)!:String(cString: [115,111,99,107,101,116,118,97,114,0], encoding: .utf8)!]
          var largeU: String! = String(cString: [108,111,111,112,105,110,103,0], encoding: .utf8)!
         gradient_ = "\(gradient_.count)"
         conversationV += Float(gradient_.count)
         itemF.append("\(1 << (Swift.min(5, largeU.count)))")
         u_heightd = ["\(u_heightd.count)": (largeU == (String(cString:[54,0], encoding: .utf8)!) ? largeU.count : u_heightd.count)]
         break
      }
      if !simou.contains { $0 as? Int == paddingz } {
          var itemu: String! = String(cString: [105,110,112,99,98,0], encoding: .utf8)!
          var signA: Float = 1.0
          var durationn: Bool = true
          var layoutT: String! = String(cString: [114,101,113,117,97,110,116,0], encoding: .utf8)!
         simou = [(3 << (Swift.min(labs(Int(signA > 31408051.0 || signA < -31408051.0 ? 40.0 : signA)), 2)))]
         itemu = "\((simou.count % (Swift.max(5, (durationn ? 2 : 5)))))"
         durationn = (layoutT.count >> (Swift.min(3, agreementI.values.count))) < 1
         layoutT = "\(saveA.count ^ agreementI.keys.count)"
      }
      for _ in 0 ..< 1 {
         gradient_.append("\(gradient_.count ^ agreementI.values.count)")
      }
         agreementI["\(paddingz)"] = paddingz
         agreementI = [gradient_: saveA.count]
          var tagline9: Float = 4.0
         simou.append(2 + paddingz)
         tagline9 -= Float(paddingz % 3)
         gradient_ = "\(agreementI.count)"
         gradient_.append("\(gradient_.count)")
      while ((3 - agreementI.count) < 1) {
         simou = [(saveA == (String(cString:[97,0], encoding: .utf8)!) ? agreementI.count : saveA.count)]
         break
      }
      for _ in 0 ..< 2 {
         agreementI[saveA] = saveA.count
      }
          var socialy: Double = 3.0
         paddingz |= gradient_.count * agreementI.values.count
         socialy /= Swift.max(Double(saveA.count ^ paddingz), 3)
          var collectionE: String! = String(cString: [115,99,97,108,101,114,0], encoding: .utf8)!
          var leftE: Int = 4
         agreementI[gradient_] = gradient_.count / 2
         collectionE = "\(agreementI.values.count % (Swift.max(gradient_.count, 5)))"
         leftE |= paddingz + 2
         paddingz /= Swift.max(simou.count ^ gradient_.count, 1)
      for _ in 0 ..< 3 {
         paddingz -= paddingz
      }
      startt[saveA] = 2 >> (Swift.min(5, simou.count))
      break
   }
       var loadingE: [Any]! = [28, 11]
       var sharingj: Bool = false
       var emotiona: [Any]! = [14.0]
       _ = emotiona
       var n_centerw: Double = 5.0
       var williamsh: Int = 5
       var appearance2: Int = 3
      repeat {
          var labelZ: String! = String(cString: [99,114,111,108,108,0], encoding: .utf8)!
         williamsh %= Swift.max(appearance2 % (Swift.max(labelZ.count, 5)), 5)
         if 916058 == williamsh {
            break
         }
      } while (916058 == williamsh) && (4 == (appearance2 / 3))
      for _ in 0 ..< 2 {
         emotiona = [appearance2 + 3]
      }
      for _ in 0 ..< 3 {
         emotiona.append(3 & loadingE.count)
      }
         n_centerw -= Double(emotiona.count)
      scene_cX = !scene_cX
      sharingj = startt["\(angryj)"] == nil
      startt = ["\(startt.values.count)": 1]
   return scene_cX

}





    private func startHangupShake(on button: UIButton) {

         var fielKalman: Bool = prepareValidSetupAdjustmentDuration(detailFile:17.0, listenerPath:76.0)

      if !fielKalman {
          print("ok")
      }

withUnsafeMutablePointer(to: &fielKalman) { pointer in
        _ = pointer.pointee
}


       var reuser: Double = 3.0
    _ = reuser
   for _ in 0 ..< 1 {
      reuser /= Swift.max(Double(2), 5)
   }

        let appearance = CAKeyframeAnimation(keyPath: "transform.rotation.z")
        appearance.values = [-0.08, 0.08, -0.06, 0.06, 0]
        appearance.duration = 0.36
        appearance.repeatCount = .infinity
        appearance.isRemovedOnCompletion = false
        button.layer.add(appearance, forKey: "hangupShake")
    }

    private func makeToggleButton(normalImageName: String, selectedImageName: String) -> UIButton {
       var min_34: Double = 4.0
      min_34 -= (Double(Int(min_34 > 126595749.0 || min_34 < -126595749.0 ? 89.0 : min_34) & Int(min_34 > 312543978.0 || min_34 < -312543978.0 ? 13.0 : min_34)))

        let taglineButton = UIButton(type: .custom)
        taglineButton.setImage(UIImage(named: normalImageName), for: .normal)
        taglineButton.setImage(UIImage(named: selectedImageName), for: .selected)
        taglineButton.imageView?.contentMode = .scaleAspectFit
        taglineButton.translatesAutoresizingMaskIntoConstraints = false
        return taglineButton
    }

@discardableResult
 func rawAxisCaptureAspectNoneView(hasGesture: String!, privacySession: Int, animationSync: Bool) -> UIView! {
    var observerY: Float = 5.0
    var healZ: Int = 4
    _ = healZ
   while ((healZ % 2) <= 4) {
      healZ += (3 % (Swift.max(4, Int(observerY > 117390795.0 || observerY < -117390795.0 ? 55.0 : observerY))))
      break
   }
     let summarySaved: Bool = false
     var sceneScreen: Float = 41.0
     var productsClass_qy: UILabel! = UILabel()
    var emmsVarsPretwiddle: UIView! = UIView(frame:CGRect.zero)
    emmsVarsPretwiddle.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    emmsVarsPretwiddle.alpha = 1.0
    emmsVarsPretwiddle.frame = CGRect(x: 62, y: 210, width: 0, height: 0)
    productsClass_qy.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    productsClass_qy.alpha = 0.3
    productsClass_qy.frame = CGRect(x: 2, y: 180, width: 0, height: 0)
    productsClass_qy.text = ""
    productsClass_qy.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    productsClass_qy.textAlignment = .right
    productsClass_qy.font = UIFont.systemFont(ofSize:17)
    
    emmsVarsPretwiddle.addSubview(productsClass_qy)

    
    return emmsVarsPretwiddle

}





    private func makeSmallFaceView(text: String) -> UIView {

         var cyuvNtfs: UIView! = rawAxisCaptureAspectNoneView(hasGesture:String(cString: [100,112,114,105,110,116,0], encoding: .utf8)!, privacySession:16, animationSync:true)

      if cyuvNtfs != nil {
          self.view.addSubview(cyuvNtfs)
          let cyuvNtfs_tag = cyuvNtfs.tag
      }

withUnsafeMutablePointer(to: &cyuvNtfs) { pointer in
        _ = pointer.pointee
}


       var mail4: String! = String(cString: [105,110,118,111,99,97,116,105,111,110,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &mail4) { pointer in
          _ = pointer.pointee
   }
      mail4 = "\(mail4.count / 2)"

        let priceView = UIView()
        priceView.backgroundColor = UIColor(red: 0.77, green: 0.95, blue: 0.98, alpha: 1)
        priceView.layer.cornerRadius = 14
        priceView.layer.shadowColor = UIColor(red: 0.42, green: 0.64, blue: 0.88, alpha: 0.35).cgColor
        priceView.layer.shadowOpacity = 1
        priceView.layer.shadowRadius = 5
        priceView.layer.shadowOffset = CGSize(width: 0, height: 2)
        priceView.translatesAutoresizingMaskIntoConstraints = false

        let homeLabel = UILabel()
        homeLabel.text = text
        homeLabel.font = .systemFont(ofSize: 16, weight: .semibold)
        homeLabel.textColor = UIColor(red: 0.12, green: 0.17, blue: 0.18, alpha: 1)
        homeLabel.textAlignment = .center
        homeLabel.translatesAutoresizingMaskIntoConstraints = false
        priceView.addSubview(homeLabel)

        NSLayoutConstraint.activate([
            homeLabel.centerXAnchor.constraint(equalTo: priceView.centerXAnchor),
            homeLabel.centerYAnchor.constraint(equalTo: priceView.centerYAnchor)
        ])

        return priceView
    }

@discardableResult
 func lastConstraintIntrinsicFactorChat(heightPerla: String!) -> String! {
    var local_2nV: String! = String(cString: [102,102,109,97,116,104,0], encoding: .utf8)!
    var viewsK: Float = 4.0
    var base_: String! = String(cString: [103,114,101,121,0], encoding: .utf8)!
      base_.append("\(3)")
      base_ = "\(base_.count | 1)"
   if (Int(viewsK > 148983588.0 || viewsK < -148983588.0 ? 61.0 : viewsK) / (Swift.max(local_2nV.count, 6))) < 2 {
      local_2nV.append("\((Int(viewsK > 309856811.0 || viewsK < -309856811.0 ? 27.0 : viewsK) / (Swift.max(1, 6))))")
   }
   return local_2nV

}





    @objc private func closePage() {

         var thrmatButterfly: String! = lastConstraintIntrinsicFactorChat(heightPerla:String(cString: [119,111,114,100,108,101,110,0], encoding: .utf8)!)

      let thrmatButterfly_len = thrmatButterfly?.count ?? 0
      if thrmatButterfly == "info" {
              print(thrmatButterfly)
      }

withUnsafeMutablePointer(to: &thrmatButterfly) { pointer in
    
}


       var tagline_: Int = 2
    var passwordO: Double = 5.0
   withUnsafeMutablePointer(to: &passwordO) { pointer in
    
   }
       var replyO: Double = 1.0
       var headerK: String! = String(cString: [115,108,111,119,0], encoding: .utf8)!
       var controly: Bool = false
      if (replyO / 2.66) < 3.24 {
          var unlockedF: Double = 3.0
         headerK = "\((3 % (Swift.max(Int(replyO > 148165342.0 || replyO < -148165342.0 ? 82.0 : replyO), 5))))"
         unlockedF -= (Double((controly ? 1 : 1) - Int(unlockedF > 130060887.0 || unlockedF < -130060887.0 ? 80.0 : unlockedF)))
      }
      if 4.24 < (replyO - 5.65) {
         replyO += Double(2 + headerK.count)
      }
          var avatarp: [Any]! = [86, 84, 35]
          var label0: String! = String(cString: [112,108,97,99,101,104,111,108,100,101,114,0], encoding: .utf8)!
          var emotion9: String! = String(cString: [99,97,110,100,105,100,97,116,101,0], encoding: .utf8)!
         controly = 57 > headerK.count
         avatarp = [headerK.count]
         label0 = "\((label0.count << (Swift.min(1, labs(Int(replyO > 56796844.0 || replyO < -56796844.0 ? 84.0 : replyO))))))"
         emotion9.append("\((label0 == (String(cString:[48,0], encoding: .utf8)!) ? label0.count : Int(replyO > 135932182.0 || replyO < -135932182.0 ? 48.0 : replyO)))")
         replyO += (Double(headerK == (String(cString:[80,0], encoding: .utf8)!) ? headerK.count : Int(replyO > 207857047.0 || replyO < -207857047.0 ? 76.0 : replyO)))
          var w_titleQ: Int = 2
          var dialog8: Float = 0.0
          _ = dialog8
         headerK.append("\(1)")
         w_titleQ -= (Int(dialog8 > 346453819.0 || dialog8 < -346453819.0 ? 21.0 : dialog8))
         dialog8 /= Swift.max(4, Float(w_titleQ))
      repeat {
          var u_productsX: Float = 4.0
          _ = u_productsX
          var trailing6: Double = 3.0
         withUnsafeMutablePointer(to: &trailing6) { pointer in
    
         }
          var select6: String! = String(cString: [105,110,100,101,120,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &select6) { pointer in
                _ = pointer.pointee
         }
         controly = Double(select6.count) == trailing6
         u_productsX /= Swift.max(1, (Float(2 - Int(trailing6 > 249768146.0 || trailing6 < -249768146.0 ? 47.0 : trailing6))))
         if controly ? !controly : controly {
            break
         }
      } while (controly ? !controly : controly) && ((replyO + 4.67) == 5.11)
      repeat {
          var loadingX: Int = 0
          _ = loadingX
         replyO -= (Double(Int(replyO > 22134713.0 || replyO < -22134713.0 ? 4.0 : replyO)))
         loadingX |= ((String(cString:[53,0], encoding: .utf8)!) == headerK ? (controly ? 2 : 1) : headerK.count)
         if replyO == 3854164.0 {
            break
         }
      } while (replyO == 3854164.0) && (headerK.hasSuffix("\(replyO)"))
      for _ in 0 ..< 1 {
         headerK.append("\(2)")
      }
         controly = 35 < headerK.count || 50.48 < replyO
      tagline_ |= headerK.count
      tagline_ %= Swift.max((2 | Int(passwordO > 330301956.0 || passwordO < -330301956.0 ? 67.0 : passwordO)), 2)

       var delegate_pw: Int = 5
      withUnsafeMutablePointer(to: &delegate_pw) { pointer in
             _ = pointer.pointee
      }
       var register_36: Double = 3.0
       _ = register_36
         delegate_pw ^= (Int(register_36 > 277465491.0 || register_36 < -277465491.0 ? 6.0 : register_36))
      if 5.88 > (3.75 - register_36) && (delegate_pw % 4) > 2 {
         register_36 -= (Double(Int(register_36 > 357541690.0 || register_36 < -357541690.0 ? 53.0 : register_36)))
      }
      while (1.49 <= (register_36 * Double(delegate_pw)) || (register_36 * Double(delegate_pw)) <= 1.49) {
         register_36 += (Double(Int(register_36 > 326120137.0 || register_36 < -326120137.0 ? 22.0 : register_36) + delegate_pw))
         break
      }
          var o_playerL: Bool = true
          var converted5: String! = String(cString: [98,121,116,101,115,0], encoding: .utf8)!
         register_36 -= (Double(Int(register_36 > 99537914.0 || register_36 < -99537914.0 ? 90.0 : register_36) >> (Swift.min(5, labs((o_playerL ? 4 : 4))))))
         converted5 = "\((2 * (o_playerL ? 5 : 5)))"
      while (3 == (4 & delegate_pw) && 3.22 == (Double(delegate_pw) - register_36)) {
         register_36 -= Double(delegate_pw)
         break
      }
         register_36 /= Swift.max(4, Double(2))
      passwordO /= Swift.max((Double(Int(register_36 > 4274527.0 || register_36 < -4274527.0 ? 14.0 : register_36) | Int(passwordO > 28226070.0 || passwordO < -28226070.0 ? 44.0 : passwordO))), 4)
        stopCaptureSession()
        dismiss(animated: true)
    }

    @objc private func toggleCallButton(_ sender: UIButton) {
       var baseY: [String: Any]! = [String(cString: [101,114,114,0], encoding: .utf8)!:13]
    var control1: Double = 1.0
   if baseY.values.count > (Int(control1 > 156277010.0 || control1 < -156277010.0 ? 55.0 : control1)) {
      control1 += Double(1)
   }
   repeat {
      baseY["\(control1)"] = 3 >> (Swift.min(3, baseY.keys.count))
      if baseY.count == 1559469 {
         break
      }
   } while (baseY.count == 1559469) && ((baseY.count * 4) >= 5 || (4 | baseY.count) >= 3)

        sender.isSelected.toggle()
    }

}
